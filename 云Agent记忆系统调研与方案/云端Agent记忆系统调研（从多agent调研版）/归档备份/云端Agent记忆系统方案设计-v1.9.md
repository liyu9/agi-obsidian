# 云端 Agent 记忆系统方案设计

## 0. 阅读说明

本文档为《云端 Agent 记忆系统》的**方案设计**部分，基于调研文档 5.6 选型结论（**OpenViking** 为云端 Agent 记忆系统主选型）展开。

**选型结论**：以「知识库」为中心抽象、OpenViking 为底座，叠加多实例物理隔离与 2 层知识库授权中心，实现跨 Agent 记忆复用、部门/个人/会话三级隔离与企业级合规治理（选型过程详见[《云端Agent记忆系统调研报告-v1.9》](云端Agent记忆系统调研报告-v1.9.md)5.6）。

本文档各章节安排：

- 第 1 章：**业务侧方案设计**（1.0 方案概览 + 1.1 产品蓝图 + 1.2 方案架构 + 1.3 方案级风险）
- 第 2 章：**接入与记忆模型**（2.0 关键数据流 + 2.1 接入通道 + 2.2-2.5 记忆模型）
- 第 3 章：**产品框架**（3.0 业务架构视图 + 3.1 功能形态定义 + 3.2 一期功能架构）
- 第 4 章：关键风险与建议
- 调研部分（业务背景、用户、问题、记忆系统能力框架、候选产品调研与选型）详见[《云端Agent记忆系统调研报告-v1.9》](云端Agent记忆系统调研报告-v1.9.md)

***

## 1. 业务侧方案设计

### 1.0 方案概览

基于 《调研文档》5.6 选型结论，OpenViking 作为主选型。本章按"概览 → 蓝图 → 架构 → 风险"四段展开：1.0 给出核心概念速查，1.1 给出一/二期业务需求与开发功能蓝图，1.2 给出完整架构设计（三层架构图见 1.2.1、分期规划见 1.2.0），1.3 给出方案级风险。接入与记忆模型见第 2 章。

#### 1.0.1 核心概念速查

阅读本章架构图与目录表前，先掌握以下 6 个核心概念（架构图中 `(官方)` / `(企业侧扩展)` 标注依据此划分）：

- **官方原生**：OpenViking 开源产品自带的能力（4 命名空间 / 7 Hook / 9 MCP / L0-L2 分层 / RAGFS 存储引擎 / 多租户 account-user 模型等），开箱即用
- **企业侧扩展**：OpenViking 官方不提供、由本方案自建补齐的能力（② 层知识库授权中心 / 多实例物理隔离 / 跨知识库联邦查询 / 180 天冷存 / KMS 审计等），用于满足企业级多 Agent 治理需求
- **② 层知识库授权中心**：本方案核心扩展组件，做 Agent×知识库 授权矩阵（哪个 Agent 能访问哪个知识库）+ 身份鉴权 + 路由 + 审计，Agent 不直连知识库、所有请求经此层
- **多实例物理隔离**：部门/公司等高敏知识库用独立 OpenViking 实例部署（物理分隔），比官方 account 级逻辑隔离更强
- **跨知识库联邦查询**：② 层并行 fan-out 多个知识库实例检索再合并排序，让一次请求触达多个库
- **知识库（KB）**：独立于 Agent 存在的记忆载体，按归属/用途分类（个人偏好/技能/项目上下文/部门SOP/公司政策/会话归档），Agent 被授权访问、不拥有

### 1.1 产品蓝图

本章核心：把业务需求落成"开发功能清单 + 验收标准"。本节按"场景满足度 → 一期蓝图 → 二期蓝图"三步展开，回答"每期做什么、为什么做、做成什么样算交付"。完整技术架构见 1.2，接入与记忆模型见第 2 章，方案级风险见 1.3。

#### 1.1.1 业务场景与分期满足度

**对齐 《调研文档》2 三类用户诉求**：本方案围绕三类核心用户的诉求展开，每类诉求对应方案中的具体能力承接：

| 用户类型                     | 核心诉求                      | 本方案如何承接                                | 详见                    |
| ------------------------ | ------------------------- | -------------------------------------- | --------------------- |
| A · Agent 使用方（个人/知识库维护方） | 让 Agent 记住我的偏好与技能，跨会话不重讲  | 六类知识库承载个人偏好/技能与部门 SOP，Agent 被授权访问      | 1.2.3                |
| B · 公司/部门（管理视角）          | 管控本部门知识库被哪些 Agent 访问、审计合规 | ②层授权矩阵（RBAC+ACL）+ 知识库归属分级隔离            | 1.2.1 / 1.2.3       |
| C · 云端 Agent 团队（建设方）     | 三层架构清晰、接入方式标准、风险可控        | 三层架构（①应用层/②授权中心/③实例池）+ 三通道接入 + 方案级风险清单 | 1.2.1 / 2.0 / 第 2 章 |

**业务场景与分期满足度**

本方案按两期演进，对齐 《调研文档》1 痛点与 《调研文档》3 五道业务题 Q1-Q5。下表按"基础能力 → 治理能力 → 多 Agent 协作"三类列出 8 个核心场景的分期满足度——一期解决基础能力与基础治理（S1-S5），二期补全多 Agent 协作能力（S6-S8）并强化治理（S4）：

| #                    | 业务场景（对应业务题）                                | 一期     | 二期     | 分期价值说明                                  |
| -------------------- | ------------------------------------------ | ------ | ------ | --------------------------------------- |
| **基础能力**             | <br />                                     | <br /> | <br /> | <br />                                  |
| S1                   | 单 Agent 跨会话记忆——同一用户明天再来不用重讲（Q2）            | ✅      | ✅      | 一期即跑通"1 Agent↔1 库"的记忆沉淀与召回              |
| S2                   | 个人偏好/技能持久化——记住用户习惯、习得操作（Q2）                | ✅      | ✅      | Hooks 自动 capture + Memory Extraction 落地 |
| S3                   | 会话归档与冷存——长对话压缩归档、180 天迁 TOS（Q2）            | ✅      | ✅      | 单实例内 commit→archive→TOS 链路完整            |
| **治理能力**             | <br />                                     | <br /> | <br /> | <br />                                  |
| S4                   | 接入鉴权与审计——谁访问了什么（Q4 治理）                     | ⚠️ 部分  | ✅      | 一期②层简化版有鉴权+审计，**无授权矩阵**；二期补全 RBAC+ACL   |
| S5                   | 多 Agent 并存各自隔离——CC/OpenClaw 互不串数据（Q3）      | ✅ 天然   | ✅      | 一期靠各 Agent 独占实例**硬分隔**；二期升级为策略层隔离       |
| **多 Agent 协作（二期专属）** | <br />                                     | <br /> | <br /> | <br />                                  |
| S6                   | 一 Agent 访问多库——一次任务同时调取个人库+部门库+公司库（Q1/Q2）   | ✗      | ✅      | 一期 1 Agent 只绑 1 库；二期补跨库联邦 fan-out       |
| S7                   | 多 Agent 共用一库——公司政策库全公司共享、部门 SOP 库部门内共享（Q1） | ✗      | ✅      | 一期知识库与 Agent 1-to-1 绑定；二期补知识库独立化        |
| S8                   | 新增 Agent 零改造接入——Cursor/Trae/自建即插即用（Q4）     | ✗      | ✅      | 一期每接一个 Agent 要独立部署实例；二期补授权矩阵配置即接入       |

> **一期边界**：S1-S3（基础能力）+ S4 部分 + S5（天然）共 5 个场景跑通，验证单 Agent 记忆链路可行性。**二期补全**：S6-S8 三个多 Agent 协作场景 + S4 授权矩阵 + S5 策略层升级，面向 2000 人多 Agent 公司的真实治理需求。以下 1.1.2 / 1.1.3 分别给出两期的需求 → 开发功能 → 验收标准蓝图。

#### 1.1.2 一期蓝图：单 Agent 记忆链路跑通

**一期目标**：用最简形态（1 实例 ↔ 1 Agent ↔ 1 库，多 Agent 并存各 1-to-1）跑通单 Agent 的 capture / recall / 归档全链路，验证 OpenViking 核心能力在公司场景下的可行性，同时为二期②层网关入口预埋。

**一期需求 → 开发功能 → 验收清单**：

| 业务需求（对应 1.1.1 场景） | 开发功能                                                                       | 技术归属         | 优先级 | 验收标准                                    |
| ------------------ | -------------------------------------------------------------------------- | ------------ | --- | --------------------------------------- |
| 单 Agent 跨会话记忆      | ②层简化版网关：协议适配 + mTLS/JWT 身份鉴权 + 1-to-1 路由 + 审计 + 观测埋点                       | 企业侧扩展        | P0  | 同一用户隔天再发起会话，能自动 recall 上次偏好/决策案例        |
| 个人偏好/技能持久化         | Hooks auto-capture（Stop Hook）+ Memory Extraction（LLM 5 类分类）+ Embedding 向量化 | 官方原生         | P0  | 用户偏好 7 天后仍能命中召回（命中率基线见 4.2 第1条 PoC 验证） |
| 会话归档与冷存            | 单实例内 commit（≥20000 token）→ archive → 180 天迁 TOS 链路                         | 官方原生 + 企业侧策略 | P1  | 180 天前的会话能从 TOS 恢复且按 URI 检索             |
| 多 Agent 物理隔离       | 每 Agent 独占 1 个 OpenViking 实例（CC/OpenClaw/Hermes 各自部署）                      | 企业侧扩展        | P0  | CC 与 OpenClaw 实例间零数据互通，审计日志可区分来源        |
| 接入鉴权与审计（基础）        | ②层简化版的身份鉴权 + 审计日志（谁访问了什么）                                                  | 企业侧扩展        | P0  | 每条访问有 Agent 身份 + 时间戳 + 目标实例记录           |

**一期不做的**（明确边界）：

- ✗ 授权矩阵（1-to-1 天然隔离，无需求）
- ✗ 跨库联邦查询（单实例无此需求）
- ✗ 多实例路由（每 Agent 直连自己的实例）
- ✗ 知识库独立化（一期知识库与 Agent 1-to-1 绑定）

> 一期技术形态详见 1.2.0 分期规划表、1.2.1 一期架构图。

#### 1.1.3 二期蓝图：多 Agent 多库统一治理

**二期目标**：面向 2000 人多 Agent 公司的真实场景，把知识库独立化（独立于 Agent 存在、可被多 Agent 共享），实现"一个 Agent 用多库、多个 Agent 用一库、多类型知识库统一治理与分享、新增 Agent 零改造接入"。二期②层网关补全授权矩阵与跨库联邦，Agent 侧接入契约不变、零改造演进。

**业务背景**：2000 人规模互联网公司统一部署多类 AI Agent，知识库类型多（个人偏好/技能、项目上下文、部门 SOP、公司政策、会话归档等 6 类，见 1.2.3），且知识库天然需要被分享与复用。一期"1 实例↔1 Agent↔1 库"解决了单 Agent 记忆链路跑通的基础问题，但无法支撑以下真实业务需求，二期承接这些需求的落地（二期技术形态见 1.2.0 分期规划）。

**二期需求 → 开发功能 → 验收清单**：

| 业务需求                    | 开发功能                                   | 技术归属  | 依赖              | 验收标准                                              |
| ----------------------- | -------------------------------------- | ----- | --------------- | ------------------------------------------------- |
| 需求一 · 一个 Agent 需要多个知识库源 | ②层跨库联邦 fan-out（多实例并行检索 + 合并排序）         | 企业侧扩展 | 授权矩阵            | 一次请求并行检索个人库+部门库+公司库，结果按相关度+知识库优先级合并去重             |
| 需求二 · 多个 Agent 共用一个知识库源 | 知识库独立化（独立创建/归属/授权登记）+ 同一库被多 Agent 授权访问 | 企业侧扩展 | 授权矩阵            | 公司政策库只存一份、全公司所有 Agent 只读共享，一处更新所有授权 Agent 即时生效    |
| 需求三 · 知识库类型多需统一治理与隔离    | ②层 RBAC + ACL 授权矩阵 + 多实例路由 + 审计 + 成本归因 | 企业侧扩展 | KMS / audit log | 部门管理者可管控本部门库的 Agent 访问范围；token 成本可精确分摊到 Agent×知识库 |
| 需求四 · 新增 Agent 零改造接入    | ②层授权矩阵配置即可接入（解耦 Agent 与知识库）            | 企业侧扩展 | 授权矩阵            | 新增 Agent 仅在矩阵配置一行授权，不动任何知识库数据，零改造上线               |

> 综上：一期验证记忆链路可行性（单 Agent 跑通 capture/recall/归档），二期面向 2000 人多 Agent 公司的真实场景——一个 Agent 用多库、多个 Agent 用一库、多类型知识库统一治理与分享，是知识库独立化、跨 Agent 协作记忆复用的二期目标。

### 1.2 方案架构

#### 1.2.0 设计理念：以知识库为中心 + 企业侧扩展

OpenViking 官方提供通用记忆能力，业务上有 4 类需求（部门级物理隔离 / Agent×知识库 授权矩阵 / 跨知识库联邦查询 / 归档冷存 180 天迁 TOS）官方不解决或解决不足，需企业侧扩展（4 类需求与官方能力边界的详细对照见文末附录 A）。

> 以下架构图中，`(官方)` 标注为 OpenViking 原生能力，`(企业侧扩展)` 标注为本报告设计的企业侧治理逻辑。

**分期规划**

本方案按两期演进，②层网关入口一期即埋好，二期补全授权矩阵与联邦，Agent 侧接入契约不变、二期零改造：

| 维度   | 一期（公司云 Agent 平台现状）                         | 二期（目标态）                               |
| ---- | ------------------------------------------ | ------------------------------------- |
| 形态   | 1 实例 ↔ 1 Agent ↔ 1 库（多 Agent 并存，各自 1-to-1） | 1 Agent ↔ N 库（跨库联邦）                   |
| 实例   | 每 Agent 独占 1 个 OV 实例                       | 多实例池（个人共享 + 部门独立 + 公司只读）              |
| 授权矩阵 | 无（1-to-1 天然隔离）                             | 完整 Agent×库 矩阵（RBAC+ACL）               |
| 实例池  | 无（各 Agent 直连自己的实例）                         | 多实例池 + 路由 + 跨库联邦 fan-out              |
| ②层   | 简化：协议适配 + 身份鉴权 + 路由 + 审计 + 观测（矩阵/联邦缺席）     | 完整：协议适配 + 矩阵 + 鉴权 + 路由 + 联邦 + 审计 + 观测 |
| 目标   | 各 Agent 跑通自身记忆链路，天然隔离                      | 跨 Agent 共享、跨库联邦、统一治理、成本分摊             |

> 二期的业务价值（一个 Agent 用多库 / 多个 Agent 用一库 / 多类型库治理 / 零改造接入）见 1.1.3。

**演进原则**：②层网关入口一期即部署（简化版），二期补全授权矩阵 + 联邦 + 多实例路由；Agent 侧接入契约不变，二期零改造演进。

#### 1.2.1 系统架构图（分期）

本方案按两期演进（分期规划见 1.2.0），以下分别给出一期与二期架构图。一期验证单 Agent 记忆链路可行性，二期面向 2000 人多 Agent 真实场景补全授权矩阵与跨库联邦。

**一期架构图**（1 实例 ↔ 1 Agent ↔ 1 库 · 多 Agent 并存各 1-to-1）

```
┌─────────────────────────────────────────────────────────────────┐
│  ① Agent 应用层（多 Agent 并存 · 各 1-to-1 绑定）                │
│     Claude Code │ OpenClaw │ Hermes ...  每个 Agent 独占 1 实例  │
│     Agent 是被授权的访问者，不拥有知识库                        │
└─────────────────────────────┬───────────────────────────────────┘
                              │ 入站消息携带 Agent 身份（mTLS/JWT）
                              │（接入方式：Agent 用 Hooks/MCP/SDK 之一
                              │  与本层通信，见图下详细说明，非数据流下一站）
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ② 知识库授权中心（简化版 · 网关入口已埋）                       │
│  作用：Agent 不直连知识库，所有请求经此层归一+鉴权后转发         │
│  ┌─────────────┬──────────────┬─────────────┬──────────────┐    │
│  │ 协议适配    │ Agent 身份   │ 路由         │ 观测埋点     │    │
│  │ 多 Agent    │ 鉴权         │ 1-to-1 直连  │ OTel + Prom  │    │
│  │ 消息归一    │ mTLS / JWT   │ 本 Agent 绑  │ +token 成本  │    │
│  │ → OV HTTP   │              │ 定的实例     │              │    │
│  └─────────────┴──────────────┴─────────────┴──────────────┘    │
│  + 审计日志                                                      │
│  ✗ 授权矩阵（1-to-1 天然隔离，无需求）                           │
│  ✗ 联邦查询 / 多实例路由（单实例无此需求）                       │
└─────────────────────────────┬───────────────────────────────────┘
                              │ 路由到本 Agent 绑定的唯一实例
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ③ 单知识库实例（每 Agent 1 个 OpenViking 实例）                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ viking://user/{uid}/memories/（preferences/events/       │   │
│  │  cases/memories）+ viking://user/{uid}/skills/           │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ 实例内部（官方六层架构）：                                │   │
│  │  ├ openviking-server（治理+抽取+检索+存储）             │   │
│  │  ├ web-studio（Web 控制台）                              │   │
│  │  └ ov CLI（运维）                                        │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  横切支撑层（基础设施与外部依赖 · 非数据流下游 · 不参与层级编号） │
│   为 ②③ 提供横切支撑（旁路调用，非流水线下一站）：               │
│   观测：Prometheus + Grafana    密钥：KMS                       │
│   存储：TOS（冷存）             审计：audit log                  │
│   模型：LLM + Embedding + VLM + rerank                          │
└─────────────────────────────────────────────────────────────────┘
```

> 接入方式（Hooks/MCP/SDK 三通道职责 + 7 Hook 生命周期时间轴）见下方二期架构图后的详细说明。

**二期架构图**（1 Agent ↔ N 库 · 多实例池 + 跨库联邦 · 目标态）

```
┌─────────────────────────────────────────────────────────────────┐
│  ① Agent 应用层（多 Agent 共享同一套授权矩阵）                   │
│     已有：Claude Code │ OpenClaw │ Hermes                         │
│     外部 MCP 兼容：Cursor / Trae 等                               │
│     二期自建 Agent（复用同一套授权矩阵）                          │
│     Agent 是被授权的访问者，不拥有知识库                          │
└─────────────────────────────┬───────────────────────────────────┘
                              │ 入站消息携带 Agent 身份（mTLS/JWT）
                              │（接入方式：Agent 用 Hooks/MCP/SDK 之一
                              │  与本层通信，见图下详细说明，非数据流下一站）
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ② 知识库授权中心 + 平台路由层（完整版 · 企业侧扩展核心）        │
│  作用：在 Agent 与知识库实例池之间做协议归一+授权决策与路由——    │
│        Agent 不直连知识库，所有请求经此层归一+鉴权后转发，确保    │
│        Agent 只能访问被授权的知识库，且每次访问可审计。           │
│  ┌──────────┬──────────┬───────────┬─────────┬─────────┬──────┐ │
│  │ 协议适配 │ 身份鉴权 │ 知识库授权 │ 路由    │ 联邦查询│ 观测 │ │
│  │ 多 Agent │ mTLS/JWT │ 矩阵      │ 按授权  │ fan-out │ OTel │ │
│  │ 消息归一 │ 不可伪造 │ RBAC+ACL  │ 路由到  │ 多实例  │ +Prom│ │
│  │ →OV HTTP │          │ Agent×库  │ 目标库  │ 并行检索│+token│ │
│  │          │          │ 允许/不允许│        │ +合并   │      │ │
│  └──────────┴──────────┴───────────┴─────────┴─────────┴──────┘ │
│  + 审计日志                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 授权矩阵示例：Agent × 知识库 → 允许 / 不允许              │   │
│  │  Claude Code → [张三偏好库 ✓, 研发SOP库 ✓, 公司政策库 ✓] │   │
│  │  OpenClaw   → [张三偏好库 ✗, 研发SOP库 ✓, 公司政策库 ✓] │   │
│  │  注：公司政策库为只读属性（库级），非 Agent 权限区分      │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────┘
                              │ 按授权决策路由到目标知识库实例
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ③ 多知识库实例池（官方 · 附件3 第 17-27 行 六层）               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 个人知识库 共享实例（逻辑隔离 · namespace）               │   │
│  │  ├ viking://user/{uid}/memories/ + skills/               │   │
│  │  └ viking://session/{sid}/archive/（会话归档）            │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ 部门知识库 独立实例（物理隔离 · 按部门或敏感度分组）       │   │
│  │  ├ 研发部 OV 实例：viking://resources/team/rd/           │   │
│  │  ├ 设计部 OV 实例：viking://resources/team/design/       │   │
│  │  └ ...（N 个部门，按敏感度合并为若干实例）                │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ 公司政策知识库 独立只读实例                                │   │
│  │  └ viking://resources/public/（HR/合规/通用知识·全只读）  │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ 每个实例内部（官方六层架构）：                             │   │
│  │  ├ openviking-server（治理+抽取+检索+存储）              │   │
│  │  │  ├ Memory Extraction（LLM，按 MemoryTypeRegistry 5 类）│   │
│  │  │  ├ Working Memory v2 Compression（WM 7 sections）     │   │
│  │  │  ├ L0 .abstract.md / L1 .overview.md / L2 原文        │   │
│  │  │  ├ flat_hybrid + doubao-seed-rerank 两阶段检索        │   │
│  │  │  └ RAGFS（Rust）+ VectorDB 底座                       │   │
│  │  ├ web-studio（Web 控制台 · Playground + 检索轨迹可视化）  │   │
│  │  └ ov CLI（Rust 命令行 · ls/tree/read/search 运维）       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  横切支撑层（基础设施与外部依赖 · 非数据流下游 · 不参与层级编号） │
│   为 ②③ 提供横切支撑（旁路调用，非流水线下一站）：               │
│   观测：Prometheus + Grafana（旁路抓取 ②③ 指标·多知识库聚合）   │
│   密钥：KMS（被 ②③ 调用·企业侧扩展）                            │
│   存储：TOS（被 ③ 调用·180 天冷存）                             │
│   审计：audit log（被 ② 写入·企业侧扩展）                       │
│   模型：LLM(extraction/compression) + Embedding + VLM + rerank  │
│        （被 ③ 调用·官方外部服务依赖）                            │
└─────────────────────────────────────────────────────────────────┘
```

> 接入方式（Hooks/MCP/SDK 三通道职责 + 7 Hook 生命周期时间轴 + Agent 接入决策树 + ②层观测埋点独特价值）见 2.1 接入通道。

#### 1.2.2 层职责说明

数据流主干为 ①→②→③。接入方式（Hooks/MCP/SDK）是 Agent 出站时的协议选择，Agent 按能力择一、非依次经过，不单独成层（详见 2.1 接入通道）。横切支撑层为旁路依赖，不参与层级编号。

| 层                 | 承担职责                                                                                                                                           | 关键组件                                                          | 归属         |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ---------- |
| ① Agent 应用层       | 消费记忆能力，产生对话 / 任务 / 事实；Agent 无库归属，被授权访问知识库；接入方式为 Hooks/MCP/SDK 择一（见 1.2.1 接入方式详细说明）                                                            | Claude Code、OpenClaw、Hermes、Cursor/Trae（外部 MCP 兼容）、二期自建 Agent | 官方接入对象     |
| ② 知识库授权中心 + 平台路由层 | **协议适配**（多 Agent 消息归一→OV HTTP）+ Agent 身份鉴权（mTLS/JWT）+ 知识库授权矩阵（RBAC + ACL，Agent×知识库 允许/不允许）+ 按授权路由到目标知识库实例 + 跨库联邦查询 + 审计日志 + 观测埋点转发（含 token 成本） | 知识库授权中心（协议适配 + RBAC + ACL + 联邦 + 审计）                          | 企业侧扩展      |
| ③ 多知识库实例池         | 个人知识库共享实例（namespace 逻辑隔离）+ 部门知识库独立实例（物理隔离）+ 公司政策知识库只读实例；每实例含 openviking-server（治理+抽取+检索+存储）+ web-studio + ov CLI + RAGFS                       | 多个 OpenViking 实例 + RAGFS + flat\_hybrid + web-studio + ov CLI | 官方         |
| 横切支撑层（不编号）        | **非数据流下游**，为 ②③ 提供横切支撑：多知识库聚合观测、密钥管理、对象存储（冷存）、合规审计、模型层（LLM+Embedding+VLM+rerank）                                                               | Prometheus / Grafana / KMS / TOS / audit log / 豆包模型           | 官方 + 企业侧扩展 |

**②层分期差异**：一期②层为简化版（协议适配 + 鉴权 + 1-to-1 路由 + 审计 + 观测，无授权矩阵/联邦/多实例路由）；二期补全授权矩阵 + 联邦 + 多实例路由。

> Agent 接入方式决策树（CC/OpenClaw/Hermes/Cursor/Trae/LangChain/自建 各走哪条通道）见 2.1 接入通道。

#### 1.2.3 知识库归属与授权模型

**知识库独立于 Agent 存在**：知识库独立于 Agent 存在，**支持单独创建**（如法务部门单独创建并维护法务知识库），也支持**在创建 Agent 时选择/创建知识库**（为 Agent 绑定其可访问的库）。Agent 不拥有知识库，只通过 ② 层授权矩阵被授权访问；同一知识库可被多个 Agent 访问，同一 Agent 可访问多个知识库。知识库的创建、归属、授权均经 ② 层知识库授权中心登记与治理。

**知识库类型与授权关系**：知识库按归属分为个人/项目组/部门/公司四类，类型多样、归属与敏感度各异（个人偏好低敏、部门 SOP 中敏、公司政策高敏只读）。② 层按 Agent × 知识库 做授权矩阵（RBAC + ACL），同一知识库可被多个 Agent 授权访问、同一 Agent 可访问多个知识库。具体的归属类型、命名空间、典型承载内容示例与授权矩阵样例见下。接入通道与 4 条数据流的端到端实证见第 2 章。

**知识库归属类型与授权矩阵样例**

二期多 Agent 场景下，知识库按归属分为以下几类（典型分类，非穷举；2000 人公司实际类型更杂，按业务需要增列）：

| 知识库类型      | 归属方    | OpenViking 命名空间                   | 记忆类型（官方分类）                              | 典型承载内容（示例）                 | 实例形态               | 数量级（参考） |
| ---------- | ------ | --------------------------------- | --------------------------------------- | -------------------------- | ------------------ | ------- |
| 个人偏好知识库    | 个人     | `viking://user/{uid}/memories/`   | preferences / events / cases / memories | 用户偏好、稳定事实、决策案例（示例）         | 共享实例（namespace 隔离） | N（按用户数） |
| 个人技能知识库    | 个人     | `viking://user/{uid}/skills/`     | skills                                  | 用户习得的操作方式、任务经验（示例）         | 共享实例（namespace 隔离） | N（按用户数） |
| 项目上下文知识库   | 个人/项目组 | `viking://resources/{project}/`   | 多模态资源 / 知识规则                            | 项目结构、代码风格、协作规范（示例）         | 按项目独立或共享           | 按项目数    |
| 部门 SOP 知识库 | 部门     | `viking://resources/team/{dept}/` | 多模态资源 / skills                          | 部门规范、流程、最佳实践（示例）           | 独立实例（物理隔离）         | N（按部门数） |
| 公司政策知识库    | 公司     | `viking://resources/public/`      | 多模态资源 / 知识规则                            | HR 制度、合规规范、通用知识（示例）        | 独立只读实例             | 1 个     |
| 会话归档知识库    | 个人（临时） | `viking://session/{sid}/archive/` | archive（对话档案）                           | 单次对话上下文（20000 token 切分，示例） | 共享实例               | 按活跃会话   |

**Agent × 知识库 授权矩阵示例**（② 层知识库授权中心强制）：

| Agent \ 知识库 | 张三·偏好库 | 张三·技能库 | 研发部·SOP库 | 公司·政策库 | 张三·项目A库 |
| ----------- | ------ | ------ | -------- | ------ | ------- |
| Claude Code | ✓      | ✓      | ✓        | ✓      | ✓       |
| OpenClaw    | ✗      | ✗      | ✓        | ✓      | ✗       |
| Hermes      | ✗      | ✗      | ✗        | ✓      | ✗       |
| 二期自建 Agent  | 按需配置   | 按需配置   | 按需配置     | ✓      | 按需配置    |

### 1.3 方案级风险（4 条）

以下 4 条为本方案（选 OpenViking + 多实例架构）的方案级风险；记忆系统通用风险见 4.1。

1. **单产品绑定** — 若 OpenViking 项目停滞或不满足新需求，迁移成本高
   - **缓解**：接受单栈绑定风险。PoC 阶段充分验证 8 项核心能力 + 3 维 RAG 底座 + 5 道业务题覆盖度，确保上线前识别能力缺口。不预留 Adapter 抽象层（避免过度设计）。若未来确需迁移，按当时实际需求评估迁移成本（重评估触发条件见 4.2 第3条）。
2. **企业合规责任由部署侧承担** — OpenViking 不提供第三方 SaaS 合规证书；由于是自托管部署，数据不出企业，合规能力由公司内部审计 / 等保 / 数据驻留策略保证（**这是私有化部署的常规做法，不构成产品缺陷**）
3. **被动抽取质量需持续调优** — OpenViking 官方抽取管线（`compression.ov_wm_v2` + Memory Extraction，附件3 第 297-308 行）依赖 LLM 与 prompt
   - **缓解**：建立 golden set 抽取效果基线，定期回归；PoC 阶段用真实数据校准；利用 web-studio 检索轨迹可视化定位抽取偏差
4. **多知识库联邦查询延迟** — 本方案多实例物理隔离部署下，Agent 跨知识库召回需平台层 fan-out 多个知识库实例并行检索再合并排序，增加召回 P95 延迟
   - **缓解**：并行 fan-out（最多 5 个知识库实例，`KB_FEDERATION_MAX_KB`）+ 8000ms 超时（`KB_FEDERATION_TIMEOUT_MS`）；高频知识库结果本地缓存（授权矩阵缓存 `KB_AUTH_MATRIX_CACHE_TTL=300s`）；Hooks 自动召回（路径 3）只查个人知识库共享实例（单实例无联邦），仅 MCP search（路径 4）才触发跨知识库联邦

## 2. 接入与记忆模型

本章把 1.2 架构里的"接入通道"与"记忆模型"单独成章——2.0 给出 4 条关键数据流端到端实证，2.1 回答"Agent 用什么协议通信"，2.2-2.5 回答"记忆如何记入、存储、召回"。通道机制只在 2.1 定义一次，2.3/2.5 直接引用。

### 2.0 关键数据流（4 条路径）

> 4 条路径对应接入方式的两条通道（Plugins/Hooks + MCP）× 两个方向（写入 + 召回），召回路径（3/4）落地 《调研文档》4.4.4 记忆召回能力。Hooks 通道自动触发，MCP 通道模型按需触发。
> **协议适配前置**：4 条路径入②层时均先经协议适配子模块归一消息格式（Hooks JSON / MCP 调用 → OV HTTP 语义），再进入身份鉴权；以下 ★ 标注聚焦鉴权+授权+路由环节。

**4 条路径总览**（接入通道 × 方向）：

| <br />          | 写入                          | 召回                                     |
| --------------- | --------------------------- | -------------------------------------- |
| **Hooks（自动触发）** | 路径 1 · Stop Hook 自动 Capture | 路径 3 · UserPromptSubmit Hook 自动 Recall |
| **MCP（模型按需）**   | 路径 2 · MCP store 显式写入       | 路径 4 · MCP search 跨库联邦检索               |

> **4 条路径对应关系**：路径 1/3 走 Hooks 通道（自动），路径 2/4 走 MCP 通道（按需）；写入走路径 1/2，召回走路径 3/4。Hooks 与 MCP 互补：Hooks 保证记忆能力不依赖 LLM 意愿，MCP 提供精细化操作能力。
>
> **本方案授权贯穿**：4 条路径均经过 ② 层知识库授权中心校验——写入路径（1/2）校验写权限后路由到目标知识库实例；召回路径（3/4）校验读权限后路由/联邦查询授权知识库实例。Agent 无法越界访问未授权知识库。

> Claude Code × OpenViking 端到端接入实证（hooks 生命周期/写入抽取/召回注入/观测埋点全链路）见 [附件3-智能体集成OpenViking实证.md](附件3-智能体集成OpenViking实证.md)。

4 条路径的完整流程见 2.3（写入：路径 1/2）与 2.5（召回：路径 3/4）。下面回答两个核心问题：**Agent 用什么协议与记忆系统通信**（2.1 接入通道），**记忆如何记入、存储、召回**（2.2-2.5 记忆模型）。

### 2.1 接入通道

接入方式是 Agent 出站时用什么协议与本系统通信的选择，和数据流主干（①→②→③）是两件事：同一请求按 Agent 框架能力择一走 Hooks / MCP / SDK 通道，而非依次经过。接入方式不是数据流的"第二层"，而是 ① Agent 出站时的协议选择维度（官方 · 附件3 第 60-110 行）。

**三通道职责**

| 通道            | 驱动方式        | 覆盖能力                                                              | 适用 Agent                                                           |
| ------------- | ----------- | ----------------------------------------------------------------- | ------------------------------------------------------------------ |
| Plugins·Hooks | 生命周期驱动·自动触发 | 7 Hook 覆盖读/写/归档/隔离四类                                              | Claude Code 等生命周期型 Agent                                           |
| MCP           | 模型意图驱动·按需调用 | 9 工具：search/read/list/store/add\_resource/grep/glob/forget/health | MCP 兼容 Agent（Cursor/Trae/OpenClaw 等）                               |
| SDK/HTTP      | 通用编程接入      | Python/Go/TS SDK + HTTP                                           | 无 MCP/Hook 能力的框架（LangChain/二期自建/特殊集成）；已有 Agent（CC/OpenClaw 等）走左两通道 |

**7 Hook 生命周期时间轴**（官方 · 附件3 第 75-83 行 · Plugins 通道自动触发）：

```
SessionStart ─► UserPromptSubmit(读) ─► Stop(写)
     │            │                       │
     │            └─► [模型可选调 MCP 9 工具]
     ▼                                    ▼
PreCompact(强制归档) ─► SessionEnd(收尾)  SubagentStart(隔离)
                                          └─► SubagentStop
                                               (子 Agent 归档)
```

> Hooks 与 MCP 互补：Hooks 保证记忆能力不依赖 LLM 意愿（自动 capture/recall），MCP 提供精细化操作能力（模型按需 store/search）。两者均经 ② 层协议适配归一为 OV HTTP 语义后鉴权路由。

**Agent 接入决策树**：

- **Claude Code / 同类生命周期型 Agent** → Plugins(Hooks) + MCP 双通道（自动召回 + 模型按需检索）
- **OpenClaw / Hermes** → 按其框架能力选择 MCP 通道或 SDK 通道（待 PoC 确认）
- **Cursor / Trae / 其他 MCP 兼容 Agent** → MCP 通道（9 工具）
- **LangChain / LangGraph / 二期自建 Agent** → Python/Go SDK + HTTP（无 MCP/Hook 能力时的通用接入；二期自建 Agent 上线后零改造复用已建的授权矩阵与多实例池）
- **新增 Agent 零改造接入**：只需在知识库授权中心配置 Agent×知识库授权矩阵，不动任何知识库数据（本方案解耦优势）

**② 层观测埋点独特价值**：② 层是所有 Agent 请求的唯一全量拦截点，在此埋点可捕获 100% 的 Agent×知识库访问（若埋点放各知识库实例内部，只能看到单实例视角，无法聚合）。具体价值：① 跨知识库归因——一次 Agent 请求可能触达多个知识库（联邦查询），只有 ② 层能记录"这次请求触达了哪些库"；② token 成本分摊——② 层知道 Agent 身份 + 目标知识库，能把 LLM/Embedding token 成本精确归因到"哪个 Agent 访问哪个知识库花了多少"，支撑按部门/Agent 的成本分摊。

### 2.2 记忆类型与分类

记忆系统的本体是 OpenViking 官方定义的记忆分类，记入/存储/召回都围绕这些类型展开（官方 · 附件3 第 323-333 行）：

| 记忆类型   | 语义   | 承载内容                  | 落地命名空间                                  |
| -------- | ---- | --------------------- | ---------------------------------------- |
| preferences | 用户偏好  | 稳定偏好（编码习惯、语言、风格）        | `viking://user/{uid}/memories/preferences/` |
| events      | 事件    | 稳定事实、决策案例、关键事件          | `viking://user/{uid}/memories/events/`      |
| cases       | 案例    | 决策案例、问题解决经验             | `viking://user/{uid}/memories/cases/`       |
| memories    | 通用记忆  | 其他值得沉淀的稳定信息             | `viking://user/{uid}/memories/memories/`    |
| skills      | 技能    | 用户习得的操作方式、任务经验          | `viking://user/{uid}/skills/`               |
| archive     | 会话归档  | 单次对话上下文（20000 token 切分） | `viking://session/{sid}/archive/`           |
| resources   | 多模态资源 | 项目结构、部门 SOP、公司政策、知识规则 | `viking://resources/{project、team、public}/` |

> 记忆类型与知识库归属的映射见 1.2.3 知识库归属与授权模型——个人库承载 preferences/events/cases/memories/skills，部门库/公司库承载 resources，会话归档库承载 archive。

### 2.3 记忆记入模型

本节回答"什么场景、什么时机、用什么方式把记忆**记入**系统"。通道机制（Hooks/MCP 触发时机）引用 2.1，本节聚焦记入业务逻辑（门禁规则、抽取算法、落地命名空间）。

**场景 × 时机 × 记入方式框架表**：

| 通道（引用 2.1） | 场景         | 时机           | 记入方式                                                                               | LLM 介入                          | 落地命名空间                                      |
| ----------- | ---------- | ------------ | ---------------------------------------------------------------------------------- | ------------------------------- | ------------------------------------------- |
| Hooks（自动）   | 每轮对话结束     | Stop Hook 触发 | auto-capture.mjs 解析 transcript → shouldCapture 门禁 → buildParts → Memory Extraction | LLM 5 类分类 + WM v2 7 sections 压缩 | `viking://user/{uid}/memories/` 或 `skills/` |
| MCP（显式）     | 模型判断"值得沉淀" | MCP store 调用 | SDK 补 ID/元数据 → 服务端 LLM extraction + Embedding → 生成 L0/L1                           | LLM extraction + Embedding 向量化  | 指定知识库（含 resources）                          |

**自动记入流程**（Hooks 通道 · 对应 2.0 路径 1）：

```
Stop Hook 触发（每轮对话结束）
   │
   ▼
auto-capture.mjs 读 transcript_path → 解析 turn → 增量过滤（capturedTurnCount，避免重复 capture）
   │
   ▼
sanitize：剥离 <openviking-context> / <system-reminder> 等注入块（防污染回环）
   │
   ▼
shouldCapture 门禁（核心过滤规则）：
   - 长度门禁：低于阈值的不记入
   - 命令/问句模式：纯命令或纯问句不记入
   - 符号/关键词模式：无实质内容的不记入
   │
   ▼
buildParts：text→text part，tool_use/tool_result→tool part（output ≤2000 字截断）
   │
   ▼
★ ②层鉴权路由：Agent 身份鉴权（mTLS/JWT）→ 确认写入权限 → 路由到目标知识库实例
   │
   ▼
addMessage → POST 到目标实例的 OV Session（cc-<sha256(cc_session_id)>）→ 服务端处理（见 2.4 存储模型）
   │
   ▼
落地到 viking://user/{uid}/memories/{preferences|events|cases|...}/ 或 viking://user/{uid}/skills/
```

**显式记入流程**（MCP 通道 · 对应 2.0 路径 2）：

```
模型判断"这一条值得沉淀" → 调 MCP store 工具
   │
   ▼
★ 知识库选择 + 授权校验：Agent 指定目标知识库 → ②层校验写入权限（RBAC + ACL）
   │  ─ 无写入权限 → 拒绝 + 审计日志
   ▼
SDK 层补 ID + 元数据拼路径（企业侧扩展）
   │
   ▼
②层路由 → 目标知识库实例的 OpenViking HTTP/SDK → 服务端处理（见 2.4 存储模型）
```

> Hooks 自动记入覆盖绝大多数场景（无需 LLM 意愿判断），MCP 显式记入用于模型主动沉淀的精细化场景。两者均经 ② 层鉴权后进入服务端存储流程。

### 2.4 记忆存储模型

本节回答"记入后如何分层、压缩、归档存储"。对应 2.0 路径 1/2 中"服务端处理"段的内部逻辑。

**L0/L1/L2 三层结构**（官方 · 附件3 第 337-343 行）：

| 层级 | 文件形态           | 内容              | 加载策略                       |
| -- | -------------- | --------------- | -------------------------- |
| L0 | `.abstract.md` | LLM 生成的摘要（最精炼）  | 召回时优先加载，省 token            |
| L1 | `.overview.md` | LLM 生成的概览（中等粒度） | L0 空时 fallback L1          |
| L2 | 原文             | 完整原始内容          | 相关度高 + 预算充足时才加载（单条截 500 字） |

**Working Memory v2 Compression**（官方 · 附件3 第 297-308 行）：

- 触发：pending token ≥ 20000（COMMIT\_TOKEN\_THRESHOLD）时触发 commit
- 压缩方式：LLM Function Calling，按 WM 7 sections 结构化压缩
- 配合 Memory Extraction：LLM 按 MemoryTypeRegistry 5 类分类（preferences/events/cases/memories/skills）
- 输出：生成 L0 abstract + L1 overview + Embedding 向量化 → 落地到对应命名空间

**commit / archive 阈值与冷存**：

| 阶段      | 触发条件                   | 动作                                                          | 归属    |
| ------- | ---------------------- | ----------------------------------------------------------- | ----- |
| commit  | pending token ≥ 20000  | 触发 WM v2 压缩 + Memory Extraction + Embedding → 生成 L0/L1 → 落地 | 官方    |
| archive | 会话结束 / PreCompact Hook | session 内容打包归档到 `viking://session/{sid}/archive/`           | 官方    |
| 冷存      | 180 天未访问               | 迁移到 TOS 对象存储（按 URI 检索可恢复）                                   | 企业侧策略 |

> 官方 session archive 是会话级归档，企业侧 180 天迁 TOS 是冷存策略——两者不冲突，archive 是"会话结束归档"，180 天规则是"长期未访问冷存"。

### 2.5 记忆召回模型

本节回答"需要时怎么把记忆召回注入 Agent 上下文"。通道机制（Hooks/MCP 触发时机）引用 2.1，本节聚焦召回算法（排序、过滤、预算、降级）。

**自动召回 + 显式召回对比表**：

| 通道（引用 2.1） | 触发                       | 召回范围                                                                 | 排序与过滤                                                                                                               | 预算与降级                                                                                |
| ----------- | ------------------------ | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Hooks（自动）   | UserPromptSubmit Hook    | 仅 `viking://user/memories` + `skills`（官方安全边界，resources 不参与，防跨命名空间泄露） | base score + leafBoost + eventBoost + prefBoost + lexicalOverlap → score≥0.35（SCORE\_THRESHOLD）→ 去重 → recallLimit=6 | 2000 token 内（RECALL\_TOKEN\_BUDGET）注完整内容，超出降级为「URI + 相关度%」；注入 `<openviking-context>` |
| MCP（显式）     | 模型判断"6 条不够" → MCP search | 可覆盖 `viking://resources`（自动召回不搜的范围），跨库联邦 fan-out 多实例并行检索             | 各实例两阶段检索（flat\_hybrid dense+sparse+int8 量化 → doubao-seed-rerank 精排）→ 多库结果按相关度+知识库优先级合并 → 去重 → Top-K                 | 返回 L0/L1 摘要 + 按需 MCP read 加载 L2 详情                                                   |

**自动召回流程**（Hooks 通道 · 对应 2.0 路径 3）：

```
UserPromptSubmit Hook 触发（用户每次提交 prompt）
   │
   ▼
auto-recall.mjs 通过 stdin 拿到 prompt / session_id / cwd
   │
   ├─ 命中 bypass / query<3 字符 / server offline → 直接 approve（跳过）
   ▼
★ ②层鉴权路由：Agent 身份鉴权 → 获取授权知识库列表 → 路由到目标实例
   │
   ▼
多路检索：POST /api/v1/search/find（发往授权的个人知识库共享实例）
   │
   ▼
★ 自动召回边界（官方安全边界，附件3 第 345-350 行）：
   只搜 viking://user/memories + viking://user/skills
   viking://resources 不参与自动召回（防跨命名空间泄露）
   │
   ▼
排序：base score + leafBoost + eventBoost + prefBoost + lexicalOverlap
   │
   ▼
score ≥ 0.35 过滤（SCORE_THRESHOLD）+ 去重 + 取前 recallLimit=6
   │
   ▼
内容解析：优先 L0 abstract，abstract 空时 fallback L1 overview，
        相关度高 + 预算充足时才拉 L2 全文（单条截 500 字 RECALL_MAX_CONTENT_CHARS）
   │
   ▼
预算：2000 token 内（RECALL_TOKEN_BUDGET）注入完整内容，
     超出降级为「URI + 相关度%」
   │
   ▼
输出 <openviking-context> 注入块 → 追加到用户 Prompt 前
   │
   ▼
SDK 层按授权矩阵过滤（企业侧扩展 · 知识库授权中心已校验）
   │
   ▼
SDK 发 OTel Trace + Prometheus 指标（命中率 / 延迟 / llm_token_usage / embedding_token_usage）
```

**显式召回流程**（MCP 通道 · 对应 2.0 路径 4）：

```
模型判断"自动注入的 6 条不够" → 调 MCP search 工具（可指定 scope + 知识库）
   │
   ▼
★ ②层鉴权：Agent 身份鉴权 → 获取授权知识库列表 → 确认目标知识库读权限
   │
   ▼
★ 跨知识库联邦查询：②层 fan-out 到多个授权知识库实例并行检索
   │  ├ 个人偏好知识库共享实例（viking://user/）
   │  ├ 部门 SOP 知识库独立实例（viking://resources/team/{dept}/）
   │  └ 公司政策知识库只读实例（viking://resources/public/）
   ▼
各知识库实例执行两阶段检索
   │
   ├─ 阶段 1：flat_hybrid（dense + sparse + int8 量化）向量召回
   └─ 阶段 2：doubao-seed-rerank 精排 + 阈值过滤
   │
   ▼
★ 可覆盖 viking://resources（自动召回不搜的范围），由模型显式指定 scope
   │
   ▼
合并排序：多知识库结果按相关度 + 知识库优先级合并 → 去重 → 取 Top-K
   │
   ▼
返回 L0/L1 摘要 + 必要时按 URI 调 MCP read 加载 L2 详情
```

> **自动召回 vs 显式召回的边界**：自动召回（Hooks）保证每轮对话都有基础记忆注入（6 条内、2000 token 预算），不依赖 LLM 意愿；显式召回（MCP）让模型按需深挖（跨库联邦、可覆盖 resources）。两者互补，共同构成完整的记忆召回能力。

## 3. 产品框架

第 1 章给出方案设计，第 2 章给出接入与记忆模型，本章把 1.1.2 一期蓝图翻译为产品视角的「功能架构」——哪些功能模块、每个模块什么形态（后端服务/API · Agent 侧集成 · 需开发页面）、一期要开发哪些页面。竞品控制台功能借鉴（Coze 知识库管理 / Dify Retrieval Testing / OpenViking web-studio 检索轨迹 / Mem0 记忆浏览 / Letta ADE）详见附件2，本方案「授权矩阵配置页」为特色（竞品无）。

### 3.0 业务架构视图

1.2.1 给出的是**系统架构**（Agent→授权中心→实例池），本节给出**业务架构**——知识从哪来、怎么分层、向量知识库与记忆系统什么关系。两个视图互补：一个讲系统怎么部署接入，一个讲业务上数据怎么流动。

#### 3.0.1 知识分层架构图

```
┌─────────────────────────────────────────────────────────────────┐
│  数据源层（知识从哪来）                                          │
│  ┌─────────────────────────┬───────────────────────────────────┐│
│  │ A · 企业传统知识库（已有） │ B · Agent 中（运行中沉淀）         ││
│  │ wiki / 在线文档 /        │ B1 项目上下文与代码资源            ││
│  │ 智能表格 /               │ B2 会话归档转存                    ││
│  │ 静态文档(md/txt/pdf...) │ B3 Agent 生成的文档/SOP            ││
│  │ （静态权威文档）          │ B4 Agent 执行过程可沉淀数据        ││
│  └───────────┬─────────────┴───────────────┬───────────────────┘│
│              │ ① 批量导入/API 同步           │ ② Hooks capture /   │
│              │   （《调研文档》4.4.2 批量导入）          │    MCP store /      │
│              │                              │    归档转存          │
│              ▼                              ▼                    │
├─────────────────────────────────────────────────────────────────┤
│  产品层（两类知识载体 · 各自独立承载 · 互不读取对方内容）        │
│  ┌─────────────────────────────────┬───────────────────────────┐│
│  │ L1 · 向量知识库                 │ L2 · 记忆系统              ││
│  │ 让 AI"懂"企业资料               │ 让 AI"认得"用户            ││
│  │ · 数据：静态文档型               │ · 数据：动态事件/画像型     ││
│  │ · 承载：部门SOP/公司政策/        │ · 承载：偏好/事件/案例/     ││
│  │   项目文档/法规规范              │   技能/稳定事实/会话归档    ││
│  │ · 命名空间：viking://resources/ │ · 命名空间：               ││
│  │ · 抽取：切片(Chunking)           │   viking://user/+session/  ││
│  │ · 检索单位：文档切片             │ · 抽取：LLM 5类分类+WM压缩  ││
│  │ · 召回：仅 MCP 显式检索(路径4)   │ · 召回：自动(路径3)+MCP(路径4)│
│  └────────────┬────────────────────┴────────────┬──────────────┘│
│               │                                  │               │
│      ┌────────┘                                  └────────┐      │
│      │ L1 即 RAG 的直接应用                       │ L2 复用 RAG 检索能力      │
│      │ (文档→切片→向量化→检索)                    │ (记忆内容向量化存储+语义召回)│
│      ▼                                           ▼               │
├─────────────────────────────────────────────────────────────────┤
│  底座层（RAG 检索能力 · L1 与 L2 共用同一套）                   │
│  ┌──────────┬───────────┬───────────────┬────────────────────┐ │
│  │ 向量化   │ 向量索引   │ 混合检索       │ Rerank 重排        │ │
│  │ Embedding│ VectorDB  │ 稠密+稀疏+元数据│ doubao-seed-rerank│ │
│  └──────────┴───────────┴───────────────┴────────────────────┘ │
│  OpenViking 统一引擎：喂文档=知识库(L1)、喂对话=记忆(L2)         │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.0.2 数据源说明

| 数据源                  | 来源                                       | 入库方式                                     | 落地产品层            | 命名空间                              |
| -------------------- | ---------------------------------------- | ---------------------------------------- | ---------------- | --------------------------------- |
| A · 企业传统知识库          | wiki / 在线文档 / 智能表格 / 静态文档(md/txt/pdf...) | 批量导入 / API 同步 / 增量监听                     | L1 向量知识库         | `viking://resources/`             |
| B1 · 项目上下文与代码资源      | Agent 工作中访问/产生的项目结构、代码风格、协作规范            | Hooks capture / MCP add\_resource / 批量导入 | L1 向量知识库         | `viking://resources/{project}/`   |
| B2 · 会话归档转存          | Agent 会话结束归档的 archive                    | commit/archive 阈值触发 → 转存                 | L1（转存后）/ L2（归档时） | `viking://session/{sid}/archive/` |
| B3 · Agent 生成的文档/SOP | Agent 生成的文档、SOP 草稿、任务产物                  | MCP store / 审核后入库                        | L1 向量知识库         | `viking://resources/`             |
| B4 · Agent 执行过程可沉淀数据 | Agent 对话中的偏好/事件/案例/技能                    | Hooks Stop capture + LLM 抽取              | L2 记忆系统          | `viking://user/{uid}/`            |

#### 3.0.3 向量知识库 vs 记忆系统

| 对比点  | L1 向量知识库                       | L2 记忆系统                       |
| ---- | ------------------------------ | ----------------------------- |
| 核心问题 | AI 懂不懂企业资料                     | AI 认不认得用户                     |
| 数据来源 | 企业传统知识库(A) + Agent 中(B1/B2/B3) | Agent(B4)                     |
| 数据性质 | 静态文档型                          | 动态事件/画像型                      |
| 抽取方式 | 切片(按文档结构)                      | LLM 5类分类 + WM v2 压缩           |
| 检索单位 | 文档切片                           | 事件/画像/偏好                      |
| 命名空间 | `viking://resources/`          | `viking://user/` + `session/` |
| 召回方式 | 仅 MCP 显式检索(路径4)，不参与自动召回        | 自动召回(路径3) + MCP 显式检索(路径4)     |

#### 3.0.4 L2 与向量知识库的关系

L2 记忆系统与向量知识库（L1）的关系需从两个维度区分，避免混淆：

| 关系维度                                 | 是否成立  | 说明                                                                                                                                     |
| ------------------------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------- |
| L2 读取 L1 的业务知识内容（如部门 SOP）            | ✗ 不成立 | 按 2.0 路径3，自动召回只搜 `viking://user/memories` + `user/skills`，**不搜** **`viking://resources/`**（防跨命名空间泄露）。L1 的业务知识只由 Agent 通过 MCP 路径4 显式检索 |
| L2 复用 L1 的 RAG 检索能力（向量化+混合检索+Rerank） | ✓ 成立  | L2 的记忆内容（偏好/事件/案例）同样经 Embedding 向量化存储、用 flat\_hybrid + rerank 两阶段检索召回——"记忆系统复用 RAG 检索能力"即此意                                            |

一句话：**L2 不读 L1 的业务文档，但复用 L1 的 RAG 检索基础设施。**

#### 3.0.5 会话归档（archive）的双重角色

archive 在本架构中具有双重角色，需单独说明：

- **作为 L2 记忆系统的归档层**：会话结束/PreCompact 时触发 archive，落到 `viking://session/{sid}/archive/`，是记忆系统的冷数据层（对应 《调研文档》4.1 归档记忆）。
- **作为 L1 向量知识库的数据源**：archive 经沉淀/转存后，可提取有价值的项目上下文、决策案例等入库 `viking://resources/`，服务于向量知识库检索。
- **边界**：archive 原始归属 L2（记忆系统），转存后的衍生内容归属 L1（向量知识库）。180 天后迁 TOS 冷存（企业侧策略，1.1.2）。

#### 3.0.6 OpenViking 统一引擎

- OpenViking 官方定位 "Context Database for AI Agents"：**喂文档=知识库(L1)、喂对话=记忆(L2)**，L1 与 L2 由同一 OpenViking 实例统一承载。
- 产品概念上分层（L1 管"懂不懂资料"、L2 管"认不认得你"），实现上共用同一套 RAGFS + VectorDB 底座，靠 `viking://` 命名空间区分（`resources/` vs `user/`+`session/`）。
- 这也解释了为什么记忆系统能"复用 RAG 能力"——同一引擎、同一套向量检索链路（flat\_hybrid + rerank 两阶段检索，2.5）。

> 系统架构（Agent→授权中心→实例池）见 1.2.1；记忆类型与命名空间映射见 2.2；批量导入入口见 《调研文档》4.4.2；知识库归属类型见 1.2.3。

### 3.1 功能形态定义

| 形态            | 定义                                  | 典型例子                                        |
| ------------- | ----------------------------------- | ------------------------------------------- |
| **后端服务/API**  | 平台侧运行、无 UI、被 Agent 或控制台调用           | 协议适配、鉴权、路由、抽取、审计、埋点                         |
| **Agent 侧集成** | 跑在 Agent 端的 Hooks/MCP/SDK 代码，无独立 UI | auto-capture.mjs、auto-recall.mjs、MCP 工具、SDK |
| **需开发页面**     | 管理控制台 Web 页面                        | 知识库管理页、Agent 管理页、检索调试面板                     |

> OpenViking web-studio（Playground + 检索轨迹可视化）+ ov CLI 为官方原生、按实例免费提供；本方案「需开发页面」指 web-studio 不覆盖的平台级管理页面。

### 3.2 一期功能架构

基于 1.1.2 一期蓝图（1 实例↔1 Agent↔1 库，跑通单 Agent capture/recall/归档全链路），一期功能架构按 6 个功能模块展开：

| 功能模块          | 功能                                      | 形态        | 归属     |
| ------------- | --------------------------------------- | --------- | ------ |
| ① 接入网关（②层简化版） | 协议适配（Hooks/MCP/SDK→OV HTTP）             | 后端服务/API  | 企业侧    |
| <br />        | mTLS/JWT 身份鉴权                           | 后端服务/API  | 企业侧    |
| <br />        | 1-to-1 路由（Agent→绑定实例）                   | 后端服务/API  | 企业侧    |
| <br />        | 审计日志写入                                  | 后端服务/API  | 企业侧    |
| <br />        | 观测埋点（OTel + Prom）                       | 后端服务/API  | 企业侧    |
| ② 记忆写入        | Hooks auto-capture.mjs（Stop Hook）       | Agent 侧集成 | 官方+企业侧 |
| <br />        | Memory Extraction（LLM 5 类分类）            | 后端服务/API  | 官方     |
| <br />        | WM v2 Compression + Embedding           | 后端服务/API  | 官方     |
| ③ 记忆召回        | Hooks auto-recall.mjs（UserPromptSubmit） | Agent 侧集成 | 官方+企业侧 |
| <br />        | 两阶段检索（flat\_hybrid + rerank）            | 后端服务/API  | 官方     |
| ④ 存储与归档       | L0/L1/L2 落地 + 按需加载                      | 后端服务/API  | 官方     |
| <br />        | commit/archive 阈值触发                     | 后端服务/API  | 官方     |
| <br />        | 180 天冷存迁 TOS                            | 后端服务/API  | 企业侧    |
| ⑤ 多实例隔离       | 每 Agent 独占 1 个 OV 实例部署                  | 后端服务/API  | 企业侧    |
| ⑥ 管理控制台       | Agent 注册与接入配置                           | 需开发页面     | 企业侧    |
| <br />        | 知识库基础管理（创建/列表/配置）                       | 需开发页面     | 企业侧    |
| <br />        | 检索调试面板（一期借 web-studio）                  | 需开发页面     | 官方     |
| <br />        | 知识库内容浏览（一期借 web-studio）                 | 需开发页面     | 官方     |

> **一期需开发页面 4 个**：其中检索调试面板与内容浏览一期直接复用 OpenViking web-studio（官方按实例提供），仅 Agent 管理页与知识库基础管理页需自研。

> **一期不做的页面**（明确边界，均为二期补全，见 1.1.3）：授权矩阵配置页、审计查询页、可观测大盘、生命周期管理页、多实例池管理页、成本分摊大盘。

## 4. 关键风险与建议

### 4.1 关键风险

> 方案级风险（OpenViking 选型 / 本方案架构）见 1.3；以下为记忆系统通用风险。

| #  | 风险             | 影响            | 缓解                                                                                                                |
| -- | -------------- | ------------- | ----------------------------------------------------------------------------------------------------------------- |
| R1 | 记忆污染（错误偏好被强化）  | AI 行为偏离       | 借鉴 Letta 的召回-证据引用做法（archival passage + sleep-time 整合，每次召回要求 LLM 显式引用来源），做记忆污染防护                                   |
| R2 | 2000 人规模的成本失控  | 算力/存储账单爆      | 引入"记忆生命周期"策略：180 天未访问自动归档到 TOS 对象存储（知识库级归档，对齐 1.2.3）；并按官方默认关键参数（commit=20000 / recallLimit=6）预算 LLM+Embedding 成本 |
| R3 | 敏感数据泄露（跨部门/跨人） | 合规事故          | 本方案知识库级 ACL 服务端强制（知识库授权中心 RBAC + ACL），Agent 无法越界访问未授权知识库；部门知识库物理隔离（独立实例）                                          |
| R4 | 删除权不彻底         | GDPR/个保法违规    | 选型时优先支持"级联删除 + 被引用清理"的产品                                                                                          |
| R5 | Agent 框架绑定     | 未来 Agent 接入成本 | 优先使用 MCP 协议或 HTTP API，避免 SDK 锁定                                                                                   |

### 4.1.1 文档使用注意（非技术风险）

- **竞品功能参考被误读为"完整候选"**：Coze / VikingDB / Dify 已从 《调研文档》5.6.1 的候选池移出，仅在附件2 承载功能借鉴。选型评审时请复核，不要把附件2 三款商业产品误当作可选项。

### 4.2 后续建议

1. **下一步**：先在研发部小范围 PoC（建议 2 周内），验证"个人偏好命中率"和"部门 SOP 召回质量"
2. **关注 2026 H2 / 2027 新动向**：
   - OpenViking 的多 Agent 协议层演进
   - OpenViking 官方抽取管线（SessionCompressorV2）的持续优化
   - OpenViking 生态工具链与 Agent 生态（Claude Code / OpenClaw / Hermes 等）的集成成熟度
3. **持续观察竞品功能参考三者向"记忆系统级"抽象的演进**（关联 1.3 风险1 单产品绑定）（Coze / VikingDB / Dify）；**触发重评估条件**：Coze 补齐部门级 ACL 或 VikingDB 提供长期记忆 SaaS 或 Dify 官方深化 Mem0 集成，任一发生即重开选型
4. **暂不评估**：商业 SaaS（如 Zep、ChatGPT Memory）的私有化部署能力——若公有云合规可接受，可作为对标基线

***

> 引用来源与竞品功能参考详情见 [附件2-引用来源与竞品参考.md](附件2-引用来源与竞品参考.md)。

***

## 附录 A：企业侧扩展需求详述

**为什么需要"企业侧扩展"？** OpenViking 是面向个人开发者 / 小团队的通用开源产品，官方只提供必要且通用的能力（4 个顶级命名空间、7 Hook、9 MCP 工具、LLM+Embedding+VLM 三分工）。业务上有 4 类需求是 OpenViking 官方不解决或解决不足的：

一是**部门级隔离**（设计部/研发部 SOP 不串数据）——OpenViking 有 account 级逻辑隔离（Concepts 11），但本方案用独立实例做多实例物理隔离，隔离强度高于官方逻辑隔离。

二是**Agent×知识库 授权矩阵**（哪个 Agent 能访问哪个知识库）——OpenViking 官方 account/user 模型表达不了跨租户 Agent 授权关系，需企业侧扩展 ② 层授权中心。

三是**跨知识库联邦查询**（一次请求触达多个知识库）——OpenViking 官方是单实例单 account 视角，无联邦能力，需企业侧扩展 ② 层 fan-out。

四是**归档冷存 180 天迁 TOS**——OpenViking 官方支持 resources 挂 S3，但没有"180 天规则"这一企业侧策略。

> 以上 4 类企业需求对应的核心概念（官方原生 / 企业侧扩展 / ②层知识库授权中心 / 多实例物理隔离 / 跨知识库联邦查询 / 知识库 KB）定义见 1.0.1 核心概念速查。

## 附录 B：设计理念：分界与承接

- **官方能力**：解决"记忆怎么读、怎么写、怎么召回、怎么分层、怎么被 LLM 结构化压缩"+ account/user/peer 多租户隔离 + ROOT/ADMIN/USER 角色权限——**覆盖记忆读写/召回/分层/压缩/多租户隔离**。
- **企业侧扩展**：解决"哪个 Agent 能访问哪个知识库、跨知识库联邦查询、部门级物理隔离、审计怎么落"——**覆盖 Agent×知识库 授权/联邦查询/物理隔离/审计**（官方 account/user 模型表达不了这种跨租户 Agent 授权关系）。
- **两者接口**：企业侧扩展**不改 OpenViking 源码**，通过 ② 层知识库授权中心（网关模式）实现——
  0\. **协议适配**（入站首步）：多 Agent 消息格式异构（Hooks transcript JSON / MCP 工具调用 / SDK 编程对象），② 层入站时先归一为 OpenViking 的 `viking://` + REST 语义（addMessage/search/find + commit/archive），再进入鉴权；
  1. **身份鉴权 + 授权决策**：归一后验 mTLS/JWT 令牌 → 查 Agent×知识库 授权矩阵（允许/不允许）→ 放行或拒绝；
  2. **路由转发 + 联邦查询**：放行后路由到目标知识库实例；跨库召回时 ② 层 fan-out 多实例并行检索再合并排序。
- **好处**：OpenViking 升级 → ② 层授权中心无需重写（仅靠 HTTP 网关转发对接）；合规责任边界清晰（与 1.3 风险 2 一致）。
