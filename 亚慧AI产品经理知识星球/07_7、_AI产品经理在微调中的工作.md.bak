# 7、 AI产品经理在微调中的工作
随着大模型逐渐融入企业场景，AI 产品经理不仅需要理解大模型的能力边界，更要在大模型能力不足时，协助推动微调工作，确保产品能在专属场景下更稳定、精准地输出

# # 7.1 为什么需要微调（正确思考+行动）
AI 产品经理必须明确，**微调并不是“高配方案”，而是企业落地 LLM 应用的常规操作**，原因有3个：

````JSON
（1）保障安全与合规性
 使用企业专属数据进行微调，更易满足行业合规（如金融/医疗/政务等），减少敏感数据泄露风险。

（2）适配真实业务场景，提高稳定性与准确率
 通用模型“什么都懂一点”，但企业真正需要的是“对自身业务特别熟”。微调后的模型在私有业务数据集上，准确率普遍能提升约 10%～30%。

（3）从长期视角看，更经济高效
 私有化部署 + 微调后的模型，推理成本更可控，输出更稳定，长期更具 ROI。
````

# # 7.2 明确微调数据特征
AI 产品经理在微调中承担的第一职责：定义数据的结构和目标

| 类型 | 用途 | 目标能力 |
| --- | --- | --- |
| 思考类数据 | 强化推理与流程理解 | 提升 Agent 的“决策能力” |
| 执行类数据 | 强化函数调用的准确性 | 提升 Agent 的“行动能力” |

（1）**思考力数据：遵循企业流程、具备多轮推理结构（ReAct）**（2）**执行力数据：训练 LLM 生成正确函数调用语句（Function Calling）**````JSON
【提升思考能力】
[
  {
    "role": "user",
    "content": "空调突然不制冷，是不是坏了？"
  },
  {
    "role": "assistant",
    "content": "Thought: 我需要确认用户是否已切换至制冷模式，并排查过滤网是否堵塞。"
  },
  {
    "role": "user",
    "content": "Observation: 用户表示制冷模式已设置，且过滤网清洁无堵塞。"
  },
  {
    "role": "assistant",
    "content": "Thought: 初步判断无法远程解决，建议安排上门服务。"
  }
]

【提升调用工具的能力】：微调数据就是“步骤类自然语言指令”与“函数调用方法”
{
  "input": {
    "action": "请安排师傅明天下午 3 点去北京朝阳维修空调不制冷的问题。",
    "function_list": [
      {
        "name": "CreateRepairTicket",
        "parameters": {
          "device": "string",
          "issue": "string",
          "time": "string",
          "address": "string"
        }
      }
    ]
  },
  "output": "CreateRepairTicket{\"device\": \"空调\", \"issue\": \"不制冷\", \"time\": \"明天下午 3 点\", \"address\": \"北京朝阳\"}"
}
````

# # 7.3 准备数据**1、准备提升思考力的数据准备步骤****（1）梳理业务流程**：覆盖所有分支路径，包括主流程、异常流、兜底流程。**（2）与运营/业务共创 few-shot**：对每类路径写出示范样本。**（3）使用 LLM 自动扩展**：输入 few-shot 模板，批量生成同类推理数据。**（4）控制数据结构**：统一 Thought → Action → Observation 的链式结构。**（5）设计 Bad Case**：人为制造 Observation 错误、Action 冲突，提升鲁棒性。

2、在准备数据的过程中，还需要注意以下几点来**提升专属场景中的准确率**：

|**维度**|**说明**|
| --- | --- |
|**覆盖全面**| 包含主流程、异常流程、兜底机制 |
|**样本数量充足**| 初始准备建议 1000+，逐步扩展到 3000+ |
|**强化鲁棒性**| 制造 Observation 模糊、字段缺失、意图转移等情况 |
|**目标模型合理**| 如任务涉及强推理能力，建议 70B 参数级模型起步 |

3、**准备提升行动力的数据准备步骤：****（1）准备 API Schema 列表**：例如空调工单系统的 `CreateTicket` , `SearchTechnician` , `SendMessage` 等。**（2）借助 LLM 生成样例数据**：为每个字段生成典型值，如“address”生成城市名，“issue”生成常见问题。**（3）排列组合生成 API 请求语句**：例如 100 个字段组合 → 1000 条 API request。**（4）再通过 LLM 生成配套自然语言指令**：构建“Action –> API”的映射训练对。

# # 7.4 评估模型
（1）评估思考力模型：3看1测（见2.1）

（2）评估行动力模型：AST（Abstract Syntax Tree）抽象语法树（基础了解即可，见下方）

在评估Agent的行动力模型时，AST（抽象语法树）被用作一种关键技术，它在软件工程和代码分析中扮演着重要角色。以下是AST在评估Agent行动力模型中的一些应用和重要性：

-**代码理解与故障定位**：

AST被用于提取代码中的函数和类，以及全局代码更改的位置。在评估Agent的行动力模型时，AST帮助确定Agent对代码的理解和故障定位能力。通过将补丁映射到仓库的当前版本，并利用AST提取对应于修改位置的函数和类，可以评估Agent在代码块、函数和文件级别的定位准确率。

-**自动化程序改进任务**：

在自动化程序改进任务中，AST用于分析代码块级别的修改，包括函数/类的修改以及全局代码更改的位置。这种分析有助于评估Agent在自动化软件改进方面的能力，特别是在解决真实GitHub issue的能力。

-**评估Agent的性能**：

AST作为一个工具，可以帮助评估Agent在软件工程任务中的有效性，尤其是在执行复杂任务时。通过对比不同模型在代码块、函数和文件级别的定位准确率，可以确定Agent在故障定位方面的能力，这对于成功解决问题至关重要。

-**代码生成和程序修复**：

在端到端地解决现实问题时，Agent需要具备包括存储库理解、故障定位、代码生成和程序修复等多种功能。AST在这些功能中起到了桥梁作用，帮助Agent理解和操作代码结构，从而实现这些复杂的任务。

-**技术挑战与未来研究方向**：

尽管AST在评估Agent行动力模型中发挥了重要作用，但故障定位仍然是自动化问题解决中的一大挑战。未来的研究可以在此基础上进一步提升故障定位技术，比如通过增强代码理解机制或采用更复杂的静态和动态分析方法，来提升模型在实际开发场景中的表现

# # 7.5 如何生成Function Calling微调数据？
大语言模型在函数调用中的关键能力

在function calling微调过程中，模型需要掌握以下**4个关键能力**：

-**准确的API选择**： 识别并选择最合适的API执行用户的查询 。要求模型具备强大的理解能力，能够分析用户的自然语言意图，并从多个可选的API中匹配到最相关的一个或多个。
-**精确的参数映射**：**将用户的自然语言输入映射到所选API的参数中**。这一过程要求模型 能够处理复杂的嵌套结构、依赖关系等复杂的API调用场景 。
-**遵循API格式**： 生成的函数调用必须严格遵循API的预定义格式和语法要求 ，确保函数调用可以被正确执行。
-**推理和一致性**：在复杂的决策过程中， 模型需要能够推理，评估用户的意图和工具的要求，并确保参数选择的准确性和一致性 。

# # # 7.5.1**如何提升API选择的准确度？**1、**理解用户意图：抓住“要做什么”**模型要具备将自然语言转化为结构化操作意图的能力。以一句看似“闲聊”的请求为例， 用户输入：

````Bash
用户：我想订一个离我最近的意大利餐厅。
````

模型应自动推理出这句话包含如下意图：

（1）动作类型：查找

（2）查询对象：意大利餐厅

（3）限制条件：当前定位附近

（4）对应工具：**餐厅查询类 API**此类“意图识别能力”是 Function Calling 和 Agent 系统中的第一道门槛，准确率直接影响后续 API 选择与参数填充。

2、**选择合适的 API：抓住“怎么做”**当模型成功识别了操作意图后，下一步是：**从多个可用 API 中挑出“最合适的那个”**。

````JSON
用户意图：“查找附近餐厅”
````

此时模型可能面临多个可用选项：

`Google Maps API` ：返回餐厅定位、地图路径等

`Yelp API` ：返回餐厅评分、推荐、用户评论**判断标准**：

如果用户侧重“附近” → 倾向使用 Google Maps

如果用户更在意“好不好吃” → 倾向使用 Yelp

此过程涉及对 API 的功能描述进行匹配和筛选，是 Function Routing（函数路由）中的核心任务。

3、**构造合成数据：系统训练 API 选择策略：**为进一步提升模型的 API 选择能力，可以借助**合成数据集进行微调或 Few-Shot 提示优化。示例：**| 场景 | 用户意图 | 应调用的 API |
| --- | --- | --- |
| 用户问：“今天天气怎么样？” | 查询天气 | WeatherAPI.get_current_weather |
| 用户问：“怎么从家到公司最快？” | 查询路线 | MapAPI.route_planner |
| 用户问：“这附近有评分高的意大利餐厅吗？” | 餐厅查找 + 推荐 | YelpAPI.recommend_restaurant |
| 用户说：“我想叫一辆车去机场” | 叫车服务 | UberAPI.create_ride_request |

# # # 7.5.2**如何实现精确的参数映射？**在大语言模型（LLM）驱动的智能应用中，**正确调用 API 不仅取决于函数选择是否准确，更关键的是参数是否“对齐”**。实现从自然语言到结构化参数的精准映射，是 Function Calling 系统落地的关键能力之一。

1、**简单参数映射：直接提取用户关键词：**在大多数基础场景中，**用户表达的意图参数是明确且平铺直叙的**，可以通过语义解析或命名实体识别（NER）直接提取对应字段。

````Bash
用户：搜索一下北京的咖啡馆。
映射后的API调用：searchRestaurants(location="北京", type="咖啡馆")
````

````JSON
searchRestaurants({
  "location": "北京",
  "type": "咖啡馆"
})
````

2、**复杂结构映射：多字段+逻辑判断+嵌套参数**现实业务中，用户往往一次性表达多个条件，而**API 调用格式可能是结构化嵌套的或包含逻辑判断的字段**，此时就需要更复杂的“语义-结构对齐”。

````Bash
用户：找一家北京的意大利餐馆，评分至少4星，价格适中。
映射后的API调用：searchRestaurants(location="北京", type="意大利餐馆", rating>=4, price_range="适中")
````

````JSON
searchRestaurants({
  "location": "北京",
  "type": "意大利餐馆",
  "rating": { "gte": 4 },
  "price_range": "适中"
})
````**3、利用合成数据训练复杂参数映射能力****为让模型在复杂场景中也能稳定输出高质量 API 调用，推荐使用合成 API 和合成训练数据来进行训练或 Few-shot 强化。**示例：**示例：**通过多种合成场景，模型能够学习如何处理不同类型的参数映射。

````TypeScript
function: searchRestaurants
parameters: {
  "location": string,
  "type": string,
  "rating": { "gte": number },
  "price_range": enum["低", "适中", "高"]
}
````

# # # 7.5.3 如何确保生成数据严格符合 API Schema？
在基于大语言模型构建具备调用工具能力的系统中，**函数调用的结构正确性（Schema一致性）是保障功能可用的关键前提**。无论是 Few-shot Prompt 编排，还是微调数据准备，都必须让模型“理解并遵守”预定义的 Schema。

示例：Schema：

````Bash
{
  "location": "string",
  "type": "string",
  "rating": "float",
  "price_range": "string"
}
````

这意味着模型生成的调用必须满足以下规范：

````Bash
searchRestaurants(
  location="北京",
  type="意大利餐馆",
  rating=4.5,
  price_range="适中"
)
````

# # # 7.5.4 如何确保用户对话与函数调用的一致性？
在 Function Calling 或 Agent 系统中，实现**自然语言对话 → 函数调用（API Request）的闭环，关键在于保证模型生成的函数调用准确表达用户的真实意图**1、**用户意图和API调用的匹配：**在自然语言交互中，用户的请求通常不以“结构化字段”的方式表达，而模型必须在理解这些意图后生成参数齐全、语义正确的 API 调用。

示例：

````Bash
用户：我想查找最近的电影院。

映射后的API调用：
searchTheaters({
  "location": "当前",
  "distance": "最近"
})
````

2、**使用对话数据进行微调：****为了增强模型对用户语境的理解与结构化表达能力，引入真实对话数据进行微调是提升效果的关键策略。对话数据是训练function calling模型的重要资源。****示例训练样本：**````SQL
{
  "messages": [
    {
      "role": "user",
      "content": "我想看场电影，有哪些电影院离我近？"
    },
    {
      "role": "assistant",
      "content": "searchTheaters({ \"location\": \"当前位置\", \"distance\": \"最近\" })"
    }
  ]
}
````

# # 7.6 举例
# # # 1、以电商客服对话举例
建议从【现有的客服聊天历史日志】中抽取对话构造微调数据。但原始日志存在格式不统一、信息杂乱、数据敏感等问题，需要经过清洗和标注。

构建数据集的步骤和预处理过程如下：

（1）**日志筛选与抽取：——AI产品经理主导设计筛选标准，协调数据工程筛选逻辑**````JSON
从客服系统的聊天记录中筛选规则抽取有价值的对话
筛选标准包括:
a）对话完整(包含问题和解决方案的全流程，如起因-应对-结果）
b）主题明确(聚焦电商客服常见场景)，剔除闲聊或无法识别意图的对话，聚焦典型客服场景
c）多意图类型均衡：确保留下的对话涵盖多种问题类型(物流、订单、支付、退换货等)
````

（2）对话分段与整理：——**AI产品经理确认切分是否影响意图完整性**````JSON
将长对话根据主题或情境进行切分为多条单轮/多轮任务导向型对话，使每条数据集中在一个用户意图上（如先咨询物流、后退货→拆成两条）。
每段聚焦单一意图：从开始到结束保持对话连续
去除与当前问题无关的聊天
例如：有些长聊天可能包含多个独立问题，建议拆分成独立对话数据。保证每条数据自成一体，不依赖日志中更早的内容(除非作为context字段提供)。
````

（3）敏感信息脱敏（必须要做）：——**AI产品经理主导脱敏规范定义**````JSON
替换或移除对话中的任何个人隐私和敏感信息，必须清洗:姓名、手机号、身份证号、详细地址、订单编号中的个人信息片段、支付交易号等。

姓名用[姓名〕替代，如“张三”>“[姓名]”
电话/手机号用[电话]替代
地址用[地址]或模糊处理(如只保留省市)
身份证等证件号用[证件号]
银行卡号用[卡号]等。
替换要注意不破坏对话结构，例如用户询问“我单号是12345”，订单号一般非敏感可保留，但若单号格式中包含手机号则要处理。确保所有个人信息均已处理，以免违规和干扰模型泛化
````

（4）格式规范与归一化：**——AI产品经理指导规范处理规则**````JSON
对日志中的语言和格式进行规范化处理。
a）客服和用户在实际聊天中可能存在错别字、俚语大小写混用、表情符号、无意义的语气词等。适度清理这些信息:
b）纠正明显的错别字和拼写错误，但保留常见口语用词(保持对话自然)。
c）统一标点和数字格式，例如将全角标点替换为半角，中文数字根据习惯统一为阿拉伯数字或中文数字，时间格式统一(如“下午五点”改为“17:00”)
d）处理表情符号/颜文字：若对语义影响不大，可移除或用文本描述替代 (例如“[笑脸]”)
````

（5）对话标注：清洗后的对话基础上，增加必要的标签信息（重要）：——**AI产品经理主导设计标签体系，协调标注；主导设计情绪/意图标签集**````JSON
a）意图/类别标注：希望模型区分不同问题类型，为对话整体打上类别标签(如“物流查询”、“退货请求”、“催促发货”、“咨询商品信息”等)。通过在数据集中均衡各类别数量，避免模型偏向高频意图。可配合大模型先自动弱标注，再人工校验提高效率

b）情绪标签标注：对每一轮用户发言，标记用户的情绪状态。
例如用户语气激动，则标为“愤怒”或“焦急”;语气平静正常则标为“中性”;表示感谢称赞则标为“满意/高兴”等。情绪标签的定义需要结合电商客服场景常见的用户情绪范围进行。

c）流程阶段标注(可选)：对于流程引导类对话、可在内部标注对话所处的流程步骤，例如“已确认订单号”、“用
户已提供退货原因”、“等待用户上传凭证”等。
更多用于分析和确保对话完整性，在训练数据中可不直接呈现给模型，但在组织语料时可以帮助检查是否流程步骤齐全。
````

标签举例：

| 标签类别 | 标签项（示例） |
| --- | --- |
| 意图标签 | 咨询商品、查询物流、申请退货、修改地址、退款催办、优惠咨询、投诉等 |
| 情绪标签 | 中性、疑问、焦急、生气、满意、冷漠等 |
| 流程阶段标签 | 提供订单号、描述问题、上传凭证、平台审核、客服处理、等待用户响应等 |

（6）数据验证：**——AI产品经理主导制定质检标准**````JSON
清洗和标注完成后，全面抽样审核脱敏完整性、标签一致性、语义通顺性、对话轮次是否乱序：
a）角色标识正确无误(没有混淆用户和客服的语句)
b）对话轮次顺序正确，敏感信息确实脱敏
c）每条对话能够读通、
d）标签信息与内容匹配(情绪标签不要标错)
必要时请人工复核高优先级数据，确保语料质量过关。
````

经过上述抽取和预处理，历史日志将转化为一致、高质量的对话数据。**脱敏和情绪标注是关键，使得数据既合规又富含可训练信号**。处理后的数据将为后续模型微调提供可靠基础。

# # 7.7 Function Calling不做微调会导致的常见错误
在搭建智能Agent时，一个现实问题是：**当我们还没有做SFT或RL微调的阶段，如何让模型准确调用Function？**很多人一开始就陷入提示词设计，**但其实更关键的第一步，是先明确“调用行为的定位逻辑”——也就是说，面对一个用户请求，模型究竟是选择调用哪个函数、用什么参数、以及是否调用，都依赖于它对“问题意图”的判断能力**。

假设：“智能办公助手”，它内置了6个基础工具组件，如：

````JSON
{
  "tools": [
    {
      "name": "get_weather",
      "description": "获取指定城市的天气信息",
      "params": {
        "city": "string",
        "unit": "string"
      }
    },
    {
      "name": "search_web",
      "description": "进行网页搜索",
      "params": {
        "query": "string",
        "num_results": "integer"
      }
    },
    {
      "name": "send_email",
      "description": "发送电子邮件",
      "params": {
        "to": "string",
        "subject": "string",
        "body": "string"
      }
    },
    {
      "name": "get_calendar",
      "description": "查看用户的日历日程",
      "params": {
        "date": "string",
        "user_id": "string"
      }
    },
    {
      "name": "create_reminder",
      "description": "创建一个提醒事项",
      "params": {
        "message": "string",
        "remind_time": "string"
      }
    }，
    {
      "name": "book_meeting",
      "description": "预定会议室",
      "params": {
        "room": "string",
      }
    }
  ]
}
````

未微调模型的典型表现：**1、参数格式理解错误：**用户发出请求：

“请帮我发封邮件给张经理，内容是会议改到下午三点，主题写‘会议调整通知’”

未微调模型的错误输出：

````JSON
{
  "function_call": {
    "name": "send_email",
    "arguments": {
      "recipient": "张经理",     // ❌ 错误字段，应为 "to"，且需要标准邮箱地址
      "title": "会议调整通知",     // ❌ 错误字段，应为 "subject"
      "text": "会议改到下午三点", // ❌ 错误字段，应为 "body"
      "cc": "自己"               // ❌ 多余字段，该接口不支持 cc
    }
  }
}
````

错误说明：

字段名不匹配（如 `"title"` 应该是 `"subject"`）

值不符合预期格式（“张经理” 应该是 `"zhang@example.com"`）

多余参数（如 `"cc"` 在 schema 中未定义）这会直接导致 API 调用失败或 Function dispatch 路由错误。

正确输出：

````JSON
{
  "function_call": {
    "name": "send_email",
    "arguments": {
      "to": "zhang@example.com",
      "subject": "会议调整通知",
      "body": "会议改到下午三点"
    }
  }
}
````**2、函数名选择错误：**用户请求：

“帮我设置个提醒，下午两点给我发邮件通知我去参加部门会议”

未微调模型的错误输出：

````JSON
{
  "function_call": {
    "name": "send_email",  // ❌ 错误1：选择了“发邮件”工具，跳过了“提醒”步骤
    "arguments": {
      "to": "user@example.com",
      "subject": "部门会议提醒",
      "body": "别忘了下午两点开会"
    }
  }
}
````

````JSON
{
  "function_call": {
    "name": "email_reminder",  // ❌ 错误2：调用了不存在的函数名
    "arguments": {
      "message": "部门会议提醒",
      "time": "14:00"
    }
  }
}
````

错误说明：

理解偏差，调用了“发送邮件”而非“创建提醒”

凭空构造不存在的函数名（如 `email_reminder`）：Function Calling 不微调时，模型易“凭空幻想”出函数名

正确输出：

````JSON
{
  "function_call": {
    "name": "create_reminder",
    "arguments": {
      "message": "下午两点参加部门会议",
      "remind_time": "14:00"
    }
  }
}
````**3、冗余调用：**用户请求：

“请安排一下明天下午四点给我发封邮件提醒：开客户回顾会，别忘了！”

未微调模型的错误输出：

````JSON
// 第一次调用：创建提醒（✅ 合理）
{
  "function_call": {
    "name": "create_reminder",
    "arguments": {
      "message": "开客户回顾会",
      "remind_time": "16:00"
    }
  }
}
// 第二次调用：重复提醒（❌ 完全相同）
{
  "function_call": {
    "name": "create_reminder",
    "arguments": {
      "message": "开客户回顾会",
      "remind_time": "16:00"
    }
  }
}
// 第三次调用：尝试发邮件（✅ 合理）
{
  "function_call": {
    "name": "send_email",
    "arguments": {
      "to": "me@example.com",
      "subject": "客户回顾会提醒",
      "body": "别忘了明天下午四点开客户回顾会"
    }
  }
}
// 第四次调用：再次发送完全相同邮件（❌ 冗余）
{
  "function_call": {
    "name": "send_email",
    "arguments": {
      "to": "me@example.com",
      "subject": "客户回顾会提醒",
      "body": "别忘了明天下午四点开客户回顾会"
    }
  }
}
````

错误说明：

模型没有记住“调用过的函数”，重复发起同样的请求；

每一步看似都符合意图，但整体执行链出现了重复创建 + 重复通知；

正确输出：

````JSON
// 创建提醒 + 发送一封邮件，二者分别执行一次即可
[
  {
    "function_call": {
      "name": "create_reminder",
      "arguments": {
        "message": "开客户回顾会",
        "remind_time": "16:00"
      }
    }
  },
  {
    "function_call": {
      "name": "send_email",
      "arguments": {
        "to": "me@example.com",
        "subject": "客户回顾会提醒",
        "body": "别忘了明天下午四点开客户回顾会"
      }
    }
  }
]
````**4、不恰当的调用时机**（1）过早调用（抢跑执行）

用户请求：

“我在考虑要不要给老板发个邮件解释昨天的项目延期”

````JSON
{
  "function_call": {
    "name": "send_email",
    "arguments": {
      "to": "boss@example.com",
      "subject": "项目延期说明",
      "body": "关于昨天的延期，我做出如下说明……"
    }
  }
}
````

（2）该调用的时候不调用（关键时刻掉链子）

用户请求：

“明天下午三点有时间吗？我想安排一个客户会议”

````JSON
模型回复："好的，我记下来了，但具体安排你再告诉我吧。"
````

（3）无需调用但调用了（多度反馈，幻觉）

用户请求：

“你好，辛苦啦~”

````JSON
{
  "function_call": {
    "name": "translate_text",
    "arguments": {
      "text": "你好，辛苦啦~",
      "target_language": "English"
    }
  }
}
````**5、复杂场景下的连锁反应**多任务、多意图的复杂指令中，未经微调的模型往往会出现“工具用错”、“参数填错”、“顺序混乱”、“功能漏调”等一系列连锁反应，根本原因在于模型缺乏对整个任务结构的统筹理解和步骤规划。这种问题，必须通过结构微调、多步规划机制才能有效解决

用户请求：

“下周我有一个客户接待任务，帮我安排：1）查一下下周北京的天气；2）预订会议室；3）提醒我当天早上九点前准备资料；4）发封欢迎邮件给客户。”

未微调模型的错误输出：

````JSON
// 1️⃣ 工具选择错误：天气信息错用翻译工具
{
  "function_call": {
    "name": "translate_text",
    "arguments": {
      "text": "Beijing weather next week",
      "target_language": "Chinese"
    }
  }
}
// 2️⃣ 参数格式混乱：日期不标准
{
  "function_call": {
    "name": "get_weather",
    "arguments": {
      "city": "北京",
      "date": "下周",  // ❌ 非标准日期格式
      "lang": "中文"   // ❌ 多余字段
    }
  }
}
// 3️⃣ 重复无效操作：会议室预订两次
{
  "function_call": {
    "name": "book_meeting",
    "arguments": {
      "room": "会议室A",
      "time": "2024-12-21 10:00"
    }
  }
}
{
  "function_call": {
    "name": "book_meeting",  // ❌ 重复调用
    "arguments": {
      "room": "会议室A",
      "time": "2024-12-21 10:00"
    }
  }
}
// 4️⃣ 时机错误：提醒被安排在模糊时间点
{
  "function_call": {
    "name": "create_reminder",
    "arguments": {
      "message": "准备客户资料",
      "remind_time": "早上"  // ❌ 非标准时间格式，工具无法解析
    }
  }
}
// 5️⃣ 关键遗漏：完全漏掉发送邮件这一步（send_email 未调用）
````

正确的响应链是：

````JSON
[
  {
    "function_call": {
      "name": "get_weather",
      "arguments": {
        "city": "Beijing",
        "date": "2024-12-21"
      }
    }
  },
  {
    "function_call": {
      "name": "book_meeting",
      "arguments": {
        "room": "会议室A",
        "time": "2024-12-21 10:00"
      }
    }
  },
  {
    "function_call": {
      "name": "create_reminder",
      "arguments": {
        "message": "准备客户接待资料",
        "remind_time": "2024-12-21T08:30:00"
      }
    }
  },
  {
    "function_call": {
      "name": "send_email",
      "arguments": {
        "to": "client@example.com",
        "subject": "欢迎来访",
        "body": "期待您下周莅临公司，祝旅途愉快！"
      }
    }
  }
]
````

6、性能数据比对与问题分析：

![image](images/img_0433.png)

在没有针对 Function Calling 进行微调的情况下，大模型在执行多工具任务时，往往会出现调用错工具、参数混乱、遗漏步骤等问题。核心原因可以归结为以下4个方面：**1、缺乏结构化训练数据**大多数基础模型并未系统接受过如何构造 Function Calling 请求的训练，缺少对工具调用格式、顺序、约束条件等的理解。**2、上下文感知能力不足**模型无法准确判断当前对话是否需要触发工具调用，或者是继续语言对话。这种语境判断不清，容易导致误调用或漏调用。**3、多步骤执行缺乏规划策略**面对多指令场景，模型常常无法制定清晰的执行计划。它不知道哪些工具该先用、哪些可以合并、哪些是冗余，从而出现混乱的执行链条。**4、对格式过于敏感**模型对 JSON、参数名、类型等格式极度敏感，稍有格式错漏就会导致调用失败，尤其是未经结构化格式对齐训练的模型更容易“出格”。

````