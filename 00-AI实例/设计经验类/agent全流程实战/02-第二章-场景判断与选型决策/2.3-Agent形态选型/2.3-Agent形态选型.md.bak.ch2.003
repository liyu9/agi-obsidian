> **来源标注**
> - 素材源：
>   - `12-学习笔记\13-极客时间-企业级多智能体设计实战\03-原文+图片\L01-拨开迷雾：AI 应用开发的四种架构范式.md`
>   - `12-学习笔记\13-极客时间-企业级多智能体设计实战\03-原文+图片\L02-解构智能体：Agent的解剖学与ReAct范式.md`
>   - `12-学习笔记\13-极客时间-企业级多智能体设计实战\03-原文+图片\L04-架构师的决断：AI 应用开发选型工具.md`
>   - `12-学习笔记\01-极客时间-成为AGI产品经理\02-第二章-AI实战\10-提升可靠性-选择合适的Agent的设计模式.md`
>   - `13-PM-Agent全流程实战课程库\01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）\1.1-四种基础形态\08-L01-AI应用开发的四大范式.md`
>   - `13-PM-Agent全流程实战课程库\01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）\1.1-四种基础形态\08-L02-解构智能体-Agent解剖学与ReAct范式.md`
> - 整理时间：2026-06-23
> - 整理目的：第二章第 3 节"四种基础形态与场景的匹配"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 2.3 Agent 形态选型：把业务熵映射到正确的架构形态

## 一、形态选型是 PM 的核心决策

L01 把"AI 应用开发的四种架构范式"作为整套课程的认知原点——一个 PM 要想在 AI 时代做出正确的选型决策，必须先建立一套统一的架构词汇表。在 AI 圈"概念满天飞、极度混乱"的今天，同一个词在不同人嘴里意思可能完全不一样。有人认为只有具备自主决策能力的才叫 Agent，有人却把只要多调了几次大模型的工作流也叫 Agent。

讲师给出的解药是用"自主性"和"复杂度"两条轴，把所有 AI 应用形态统一收纳。本节的核心目标是：PM 在面对一个候选场景时，能在 10 分钟内回答"这个场景应该用哪种形态"。

## 二、自主性三档分级（Copilot / Assist / Automated）

L01 在讲完四种范式之前，先用一个自主性三档分级作为"连续光谱"来描述 AI 与人的协作关系。这三档分级是后续选型的关键基础设施。

**Copilot（副驾驶）**：AI 在人旁边提供建议、起草内容，但最终动作必须由人触发。典型场景：写代码时的 Tab 补全、起草邮件时的智能补全。

**Assist（辅助）**：AI 主动完成部分步骤，但仍需要人在关键节点确认。典型场景：智能客服在用户问题超出知识库时主动转人工。

**Automated（自动化）**：AI 自主完成全流程，人只在异常或审计节点介入。典型场景：定时批量处理、智能体自主完成跨境数据迁移。

讲师在 L01 范式三（Single Agent）的描述中提到，AI 在"五脏俱全"状态下会自主进行规划、调用工具、生成结果——这本身就是 Automated 档的形态。PM 在做选型时，第一步要判断的就是这个业务场景在"自主性光谱"上落在哪一档。如果业务方明确要求"AI 永远不能直接给用户答案，必须有人审核"——这是 Copilot 档；如果业务方说"AI 可以直接发邮件，但每周需要人复盘一次"——这是 Automated 档。

## 三、形态四分类（对话 / 工作流 / 自主 / Multi-Agent）

讲师在 L01 中把"AI 应用开发的四种架构范式"作为统一的分类框架。PM 可以用这套范式来描述所有候选场景。这四种范式按"自主性"和"复杂度"由低到高排列。

### 3.1 范式一：对话型（Prompt Engineering）

核心理念：一切交给模型。在这个范式下，所有的处理逻辑都在一个大模型（LLM）的黑盒里一次性完成。开发者要做的核心工作，就是去构建并优化给到模型的那一长串 Message List。

输入构成通常包括静态文本（如手动粘贴的竞品数据）、思维链 CoT（如生成报告的步骤指导），以及任务预期（目标、风格样例等）。

局限性：第一，容易超出上下文限制——虽然现在模型号称支持 100K 甚至更大的上下文，但实战中，当文本量超过 3-5 万字后，模型的指令遵循能力和逻辑提取能力会大幅下降。第二，对模型要求极高，效果不稳定——你把所有的压力都给到了模型的大脑，模型一换或者稍有波动，你的提示词可能就要推翻重调。

### 3.2 范式二：工作流型（Workflow / Chain）

核心理念：预先编排，稳定执行。这是目前工业界落地最多、大家最熟悉的一种范式。它的本质是 Code-Driven（程序员决定下一步）。

开发者通过硬编码（Hard-coded Path）预先设计好整个处理路径，在其中的某些节点调用大模型的能力。例如：爬虫抓取网页（代码节点）→ Python 清洗 HTML（代码节点）→ LLM 提取观点和信息（分析节点）→ LLM 生成报告（总结节点）→ LLM 检查报告并改进（审核节点）→ 写入 PDF（代码节点）。

优势：极其稳定。只要边界清晰，这种方式能解决 80%-90% 的工程落地问题。局限性：一旦遇到步骤不确定、异常分支极多的复杂场景（比如排查一个未知的线上系统 Bug），工作流的枚举成本就会变得不可接受。

### 3.3 范式三：自主型（Single Agent）

核心理念：模型决定下一步（Model-Driven）。这就超越了传统程序员的掌控范畴。在这个范式下，模型成为了真正的"大脑"。你只需要给它任务目标、工具箱（Tools）和记忆（Memory），它会基于反馈回路自主进行规划（Planning）。

痛点：单体 Agent 在执行复杂长链路任务时，每一次搜索和调用结果都会堆积在上下文中，导致上下文迅速膨胀，最终引发模型幻觉或执行崩溃。

L02（Agent 的解剖学与 ReAct 范式）对这一形态做了进一步展开。L02 指出 Single Agent 的核心是 ReAct 范式——Reasoning + Acting 循环。模型每一步先推理（决定下一步做什么），再行动（调用工具），再观察（评估结果），循环到目标完成。这种范式是处理高过程熵业务的关键设计。

### 3.4 范式四：Multi-Agent（组织的力量）

核心理念：用组织的力量给模型"减负"。L01 特别强调一个容易踩的坑：在一条工作流里用了三个不同的 Prompt 节点，不叫 Multi-Agent。真正的 Multi-Agent 系统，其每一个节点都必须是一个具备自主决策能力的 Single Agent。

为什么要有 Multi-Agent？因为我们要通过组织形态来实现两大核心目的：

第一，上下文隔离与动态交互（Context Isolation & Interaction）：把每个步骤做到极致。原本单体 Agent 会把所有搜索和分析的历史塞满脑子；而在 MAS 中，Role A（研究员）只负责搜索数据并传递；Role B（分析师）拿到精简后的数据进行分析；Role C（审核员）负责质检，不合格则回退/拒绝（Kickback / Reject）。子任务的隔离大幅缩短了单个 Agent 的上下文长度。

第二，工具的专注度：研究员拥有搜索工具，分析师拥有数据分析工具。就像企业里不同岗位的员工各司其职，这就避免了模型在海量工具库中选错工具的风险。

## 四、能力三分类（检索 / 执行 / 决策）

《成为 AGI 产品经理》10 课（提升可靠性：选择合适的 Agent 的设计模式）对 Agent 的能力做了更细的拆分——记忆、工具使用、规划、反思能力是 Agent 的四大能力。其中"工具使用"对应"执行能力"、"规划 + 反思"对应"决策能力"、"记忆"为前两者提供支撑。

把 L01 的形态四分类和 L10 的能力四维度结合，可以提炼出 PM 在做形态选型时关心的能力三分类：

1. 检索能力：AI 能从企业知识库、外部数据源中"找到"信息。基础能力，几乎所有形态都需要。
2. 执行能力：AI 能调用工具、写入系统、操作外部世界。是 Workflow → Single Agent 的分水岭。
3. 决策能力：AI 能在多个备选路径中做选择。是 Single Agent → Multi-Agent 的分水岭。

L10 中特别强调：Agent 的"规划能力"是把上述三大能力组织成"目标→分解→执行→反思"闭环的关键。一个 Agent 如果没有规划能力，就只是工具的简单堆叠；如果有规划能力，工具和检索才能被合理调度。

## 五、场景 → 形态选型矩阵

把"业务熵分布"（2.2 节）、"自主性档位"、"能力需求"放在一起，可以得到 PM 可直接使用的选型决策表：

| 业务场景示例 | 业务熵分布 | 自主性档位 | 能力需求 | 推荐形态 | 选型理由 |
|------------|-----------|-----------|---------|---------|----------|
| 标准合同信息抽取 | 三维低 | Copilot | 检索 | Prompt Engineering | 输入确定、过程确定、输出标准，模型一次性输出即可 |
| 月度财务月报生成 | 输入中、过程低、结果中 | Assist | 检索 + 执行 | Workflow | 流程固定但需调用多个数据源，编排稳定 |
| 客服工单分类与转人工 | 输入高、过程中、结果低 | Assist | 检索 + 决策 | Single Agent | 用户输入开放，需要模型判断走哪条处理路径 |
| 跨境数据迁移任务 | 三维高 | Automated | 检索 + 执行 + 决策 | Multi-Agent | 任务可拆分为多个子任务，需并行处理 + 上下文隔离 |
| 未知 Bug 排查 | 过程极高、结果模糊 | Automated | 检索 + 执行 + 决策 | Multi-Agent | 执行路径完全未知，需要多角色协作（信息收集、根因分析、修复方案、验证） |

讲师在 L04 中用一句话总结选型原则：**"不要靠直觉去猜，要去精准度量（Don't Guess, Measure）。"**PM 在做形态选型时，应该先画出业务的熵分布图，再去对应形态能力，而不是看哪个形态火就用哪个。

## 六、形态选型的常见误区

讲师在 L01、L04、L34 中反复强调几个误区：

**误区一：追求"最先进"**。L04 比喻：面对一个工程需求，你究竟是用一把精美的美工刀去建大楼，还是雇佣一支专业的施工队去修一扇小窗户？选型错位的代价远高于选型保守。Multi-Agent 不是"更高级"的 Workflow，是为不同业务设计的不同形态。

**误区二：用 Prompt 解决 Multi-Agent 的问题**。典型表现：试图用一个超长 Prompt 让模型完成"搜索 + 分析 + 审核 + 写报告"全流程。讲师在 L01 中明确指出，3-5 万字以上的输入，模型的指令遵循能力会大幅下降。

**误区三：用 Workflow 应对高过程熵业务**。典型表现：试图为"未知 Bug 排查"枚举出所有可能的执行路径。L01 指出，这种情况下工作流的枚举成本会变得不可接受。

**误区四：把"用了三个 Prompt 节点"当 Multi-Agent**。L01 明确说，在一条工作流里用了三个不同的 Prompt 节点，不叫 Multi-Agent。真正的 Multi-Agent 系统，其每一个节点都必须是一个具备自主决策能力的 Single Agent。

## 七、章节内引用素材

- `12-学习笔记\13-极客时间-企业级多智能体设计实战\03-原文+图片\L01-拨开迷雾：AI 应用开发的四种架构范式.md`（主素材，四种范式 + 自主性三档）
- `12-学习笔记\13-极客时间-企业级多智能体设计实战\03-原文+图片\L02-解构智能体：Agent的解剖学与ReAct范式.md`（ReAct 范式如何对应高过程熵）
- `12-学习笔记\13-极客时间-企业级多智能体设计实战\03-原文+图片\L04-架构师的决断：AI 应用开发选型工具.md`（C.U.P. 选型铁律 + 误区）
- `12-学习笔记\01-极客时间-成为AGI产品经理\02-第二章-AI实战\10-提升可靠性-选择合适的Agent的设计模式.md`（Agent 四大能力：记忆、工具使用、规划、反思）
- `13-PM-Agent全流程实战课程库\01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）\1.1-四种基础形态\08-L01-AI应用开发的四大范式.md`（范式一）
- `13-PM-Agent全流程实战课程库\01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）\1.1-四种基础形态\08-L02-解构智能体-Agent解剖学与ReAct范式.md`（范式三 Agent 解剖学）

---

**小结**：形态选型的核心，是**用业务熵分布、自主性档位、能力需求三维度同时约束，匹配到对话/工作流/自主/Multi-Agent 四种基础形态**。PM 不应追求"最先进"，而应追求"熵-架构匹配"——这是 L01 和 L04 反复强调的铁律。能力三分类（检索/执行/决策）是选型时另一把尺：需要决策能力却只用检索能力的形态，必然失败。
