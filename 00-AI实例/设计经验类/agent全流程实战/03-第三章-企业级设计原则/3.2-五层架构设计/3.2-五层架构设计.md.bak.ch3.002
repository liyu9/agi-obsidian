# 3.2 五层架构设计：企业级 Agent 的工程骨架

> **来源标注**
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L05-工程全景图：构建企业级多智能体系统的"施工蓝图".md
> - 整理时间：2026-06-23
> - 整理目的：第三章第 2 节"五层架构设计"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

## 一、为什么需要"五层"而不是"一个 Agent"

PM 写 PRD 时最常犯的错误，是把"Agent"画成一个圆圈，然后把所有功能都塞进这个圆圈里。圆圈越大越乱，最后谁也说不清边界。

企业级 Agent 的正确画法是**分层**：每一层只做一件事，对外暴露明确的接口，对内隐藏实现细节。L05 提出的工程全景图，正是基于这一思想把企业级 Agent 系统拆成五层。下面分别讲每一层的职责、关键模块和常见踩坑点。

## 二、五层架构总览（ASCII 视图）

```
+-----------------------------------------------------------------+
|  L1 接入层  Web / IM (飞书/企微/Slack) / API / 语音 / 移动端     |
+-----------------------------------------------------------------+
                              |
                              v
+-----------------------------------------------------------------+
|  L2 编排层  Orchestrator / Workflow Engine / Multi-Agent 协调    |
|            任务路由 / 计划与反思 / 人机协同节点 / Hook 钩子      |
+-----------------------------------------------------------------+
                              |
                              v
+-----------------------------------------------------------------+
|  L3 能力层  Prompt 模板 / Skills / Tools / 子 Agent / MCP 协议   |
|            工具注册中心 / 能力版本管理 / 调用配额                  |
+-----------------------------------------------------------------+
                              |
                              v
+-----------------------------------------------------------------+
|  L4 数据层  RAG / 知识库 / 长期记忆 / 短期上下文 / 向量库         |
|            文档解析 / 切片策略 / 召回重排 / 记忆生命周期         |
+-----------------------------------------------------------------+
                              |
                              v
+-----------------------------------------------------------------+
|  L5 基础设施层  模型网关 / 算力调度 / 监控告警 / 安全审计         |
|                密钥托管 / 流量控制 / 成本围栏 / 多租户路由       |
+-----------------------------------------------------------------+
```

每层都是单向依赖：上层调用下层，下层不感知上层。这种"漏斗形"的依赖关系是工程可维护性的关键——任何一层升级或替换，都不会让其他层崩溃。

## 三、逐层职责与关键模块

### L1 接入层：把 Agent 接到人

接入层解决"用户从哪儿发起请求"的问题。表面看是 UI，实际上是**多种渠道的协议适配**：
- **Web / 移动端**：H5、原生 App、小程序，统一通过 WebSocket 或 SSE 流式返回。
- **IM 平台**：飞书、企微、钉钉、Slack 各自的消息卡片、机器人回调规范不同，需要做适配器。
- **API 入口**：给业务系统调用的 OpenAPI，要做认证、限流、计量。
- **语音 / 电话**：ASR + TTS 链路，对延迟敏感。

接入层的关键模块：
- **统一会话管理**：用户在 Web 上开了会话，在飞书里继续，必须能串起来。
- **流式响应**：LLM 是 token-by-token 输出的，UI 层要支持流式渲染。
- **多模态输入**：用户可能发图片、文件、语音，接入层要识别并路由到 L3 能力层的对应处理工具。

### L2 编排层：Agent 的"大脑皮层"

编排层是企业级 Agent 区别于"聊天机器人 + 工具调用"的关键。它决定**这一次请求要走哪些步骤、调用哪些能力、何时停下来问人**。
- **Orchestrator（主 Agent）**：拿到用户意图后，拆解任务、派发子任务。
- **Workflow Engine**：当流程是确定性的（审批、ETL、报表生成），用工作流引擎跑而不是让模型自己规划。
- **Multi-Agent 协调**：多个子 Agent 协作时的消息总线、状态共享、冲突解决。
- **Hook 钩子**：在关键节点（如每次工具调用前后、每次 LLM 推理前后）插入可观测、可干预的钩子。
- **人机协同节点**：高风险操作前插入"待人工确认"环节。

编排层的关键设计原则：
- **可中断**：长任务要能取消、暂停、恢复。
- **可重试**：失败的任务能重试，且重试不重复扣款。
- **可回放**：基于 Trace 重放整次任务。

### L3 能力层：Agent 的"手脚"

能力层是 Agent 真正"做事"的地方。L05 把它定义为可被编排层调用的最小单元集合：
- **Prompt 模板**：经过版本管理、AB 测试、效果评估的提示词。
- **Skills**：Anthropic 提出的"技能包"概念，把多个工具+提示词+资源打包成一个可复用能力。
- **Tools**：原子工具，对应一个具体动作（查订单、发邮件、查 CRM）。
- **子 Agent**：复杂任务可以嵌套子 Agent 处理，再把结果汇回主 Agent。
- **MCP 协议**：让工具定义标准化、跨平台复用。

能力层最常见的踩坑是**工具泛滥**：团队兴致勃勃地接了几百个工具，结果模型选择困难、调用准确率骤降。正确做法是建立"工具准入评审"——新增工具必须有明确使用场景和调用示例，并定期下线低频工具。

### L4 数据层：Agent 的"记忆"

数据层回答"Agent 怎么知道公司的事、怎么记得住用户"：
- **RAG / 知识库**：从企业文档（PDF、Wiki、Confluence、飞书文档）解析、向量化、检索、重排。
- **长期记忆**：跨会话的用户偏好、历史决策、行为模式。
- **短期上下文**：当前会话的对话历史、工具调用结果、临时变量。
- **向量库**：Milvus、Qdrant、Pinecone 等，存储 embedding 并提供相似度检索。

数据层的关键设计：
- **切片策略**：文档怎么切直接决定召回质量。切太细噪声多，切太粗相关性差。
- **召回 + 重排**：先用向量检索 Top-K，再用 Reranker 模型精排。
- **记忆生命周期**：哪些记忆该保留、哪些该过期、用户能否主动删除。
- **数据新鲜度**：知识库更新后，索引要能及时刷新。

### L5 基础设施层：Agent 的"水电煤"

这一层是所有上层能力的"底座"，包括：
- **模型网关**：统一接入多家模型（OpenAI / Claude / 通义 / 文心 / 自研），按场景路由、做 fallback。
- **算力调度**：高峰期扩容、低峰期缩容，控制成本。
- **监控告警**：成功率、延迟、Token 消耗、成本超阈值告警。
- **安全审计**：所有工具调用、所有数据访问都有日志。
- **密钥托管**：API Key、数据库密码走 KMS，运行时按需取。
- **流量控制**：限流、防刷、IP 黑名单。
- **多租户路由**：根据租户 ID 路由到对应配置、对应模型、对应知识库。

基础设施层是 PM 最容易忽视的一层——它不直接出功能，但**决定了系统能不能稳定跑一年**。

## 四、PM 在五层架构中的角色

PM 不需要写每一层的代码，但必须：
1. **能在 PRD 中画出五层视图**，并明确每层的关键能力来自哪个供应商、哪段自研代码。
2. **能在评审会上被问"延迟从哪里来"时指出层**，而不是甩锅给"AI 慢"。
3. **能在排期时区分"接入层改动 vs 能力层新增"**，评估对上下游的影响。
4. **能在合规审计时回答每一层的数据流向和留痕策略**。

## 五、五层架构的常见误区

- **"我们只要 L2 + L3"**：跳过 L5 基础设施层直接上线，第一周就会被运维打爆。
- **"L4 数据层就是接个向量库"**：RAG 的真正难点在切片、重排、评估，不是装个 Milvus 就完事。
- **"L2 编排层最核心，其他都是辅助"**：每一层都有自己的核心算法问题（数据层是检索、接入层是协议、基础设施层是稳定性），不能只押注 L2。
- **"五层是死的"**：在不同业务场景下，某两层可能合并（如小型 SaaS 合并 L4+L5），但**漏斗形依赖关系不能破**。

## 六、本节小结

五层架构是企业级 Agent 工程的"通用语"。PM 掌握这套语言后，可以在评审、立项、对外汇报、跨团队协作中保持信息密度一致——所有人说的"编排层"都是同一个意思。L05 的施工蓝图正是这套语言的源头材料。

## 七、本地素材引用

- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L05-工程全景图：构建企业级多智能体系统的"施工蓝图".md（五层架构的本源）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L16-Skills生态：让Agent接入大量工具.md（能力层 Skills 概念来源）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L14-MCP协议：标准化定义工具接口.md（能力层协议基础）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L21-搜索驱动的记忆系统：企业级海量记忆管理方案.md（数据层记忆方案）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md（编排层 Orchestrator 范式）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L30-可观测性：Hook 骨架 + Langfuse 全链路追踪.md（编排层 Hook 机制 + 基础设施层可观测）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L32-企业安全性：沙箱、权限网关与身份认证.md（基础设施层安全模块）
