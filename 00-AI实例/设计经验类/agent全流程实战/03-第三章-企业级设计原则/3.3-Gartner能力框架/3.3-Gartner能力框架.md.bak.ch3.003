# 3.3 Gartner 能力（Competency）框架：用"能力目录"治理企业 Agent

> **来源标注**
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L05-工程全景图：构建企业级多智能体系统的"施工蓝图".md
> - 整理时间：2026-06-23
> - 整理目的：第三章第 3 节"Gartner 能力框架"
> - 说明：本节为"叙述+本地索引"形态，整合 L05 的能力目录理念 + Gartner 公开资料中的 Competency 框架思想，不复制原文。

---

## 一、从"做一个 Agent"到"建一份能力目录"

PM 在企业里推动 Agent 项目时，最常听到的反馈是"业务部门想做一个 Agent"。当业务部门足够多、产品线足够广时，企业内部会出现几十甚至上百个 Agent，名字各异、形态各异，能力大量重叠、数据重复对接、维护成本失控。

Gartner 在 Agent 治理领域提出过"Competency（能力）"的核心思想：**不要按"产品"或"项目"来组织 Agent，而要按"能力"来组织。** 能力是企业里可被多个 Agent 复用的最小可治理单元，它有清晰的定义、负责人、版本、SLA。多个 Agent 是多个能力的组合，而不是相反。

这条思路与本地 L05 提到的"能力层 + 工具注册中心 + Skills 生态"高度吻合——L05 用工程语言讲了同一件事：让能力可被注册、可被检索、可被复用。

## 二、Competency 框架的核心理念

### 1. 以"能力"为资产，而非以 Agent 为资产

传统思路：
- "我们做了一个客服 Agent" → 这个 Agent 是一坨耦合代码，能力分散在它内部。
- 另一个团队想用"查询订单"能力，必须拷贝代码或重新对接。

Competency 思路：
- 能力"查询订单"是独立资产，有 Owner、有 API、有文档、有 SLA。
- 客服 Agent、销售 Agent、财务 Agent 都可以调用它。
- 能力升级一次，所有 Agent 受益。

### 2. 能力的三个属性

一个标准的企业能力（Competency）至少包含三个属性：
- **定义（What）**：这个能力做什么、用什么输入输出、有哪些边界。
- **治理（Who / How）**：谁负责、谁审批变更、出了故障谁兜底、版本号是多少。
- **度量（How Good）**：调用成功率、延迟、用户满意度、成本。

PM 评审一个新能力时，必须能回答这三个属性，缺一不可。

### 3. 能力目录（Competency Catalog）

能力目录是所有能力的索引和注册中心，类比"企业内部的能力商店"。它至少包括：
- **能力元数据**：名称、描述、版本、Owner、所属业务域。
- **接口契约**：输入输出 Schema、调用方式（同步 / 异步 / 流式）。
- **依赖关系**：这个能力依赖哪些其他能力、哪些数据、哪些模型。
- **质量数据**：近 30 天成功率、平均延迟、平均成本。
- **变更历史**：每次升级的 changelog、回滚策略。

PM 推动 Agent 项目时，第一步不是写 Agent PRD，而是**在能力目录里检索：这件事要的能力有没有现成的**。

## 三、企业级能力目录的建设路径

### 阶段 1：能力盘点

把企业内已经存在的能力（API、模型、工具、数据集）梳理一遍，按"输入-输出-语义"分类：
- 业务类：下单、查订单、开票、退款。
- 知识类：合同检索、政策问答、规章制度。
- 系统类：调用 ERP、调用 CRM、发邮件、生成报表。
- AI 类：文本摘要、图像识别、向量检索、内容审核。

盘点输出是一张能力清单 + 责任矩阵，PM 拿这张表去和业务方对齐"哪些能力你们可以复用"。

### 阶段 2：能力标准化

对每个能力做"接口标准化"：
- 统一鉴权方式（OAuth2 / API Key / 内部 mTLS）。
- 统一错误码（成功 / 鉴权失败 / 限流 / 业务错误 / 系统错误）。
- 统一限流策略（每个能力有 QPS 上限、租户配额）。
- 统一日志格式（Trace ID、租户 ID、调用来源、结果状态）。

标准化后，能力才能"即插即用"地被 Agent 调用。

### 阶段 3：能力注册

把标准化后的能力注册到能力目录：
- 提供 API 描述（OpenAPI / JSON Schema）。
- 提供调用示例（curl / SDK）。
- 提供场景说明（典型用例 + 不适用场景）。
- 提供 Owner 联系方式和升级流程。

### 阶段 4：能力治理

能力上线后不是"放出去就完事"，需要持续治理：
- **准入评审**：新增能力必须过 PM + 架构师 + 安全 + 财务的联合评审。
- **定期巡检**：低频能力下线，重复能力合并，僵尸能力清理。
- **成本核算**：每个能力的 Token / 算力 / 存储成本定期复盘。
- **质量回溯**：失败案例和异常调用进入复盘库。

## 四、用能力治理代替"做一个 Agent"

传统 Agent 项目的失败模式是"一个需求 → 一个新 Agent → 重复造轮子"。能力治理的核心是反过来：
- 新需求到来时，**先查能力目录**。
- 能复用的能力直接调用，不要新建。
- 必须新建的能力，定义为新的"能力"而不是"Agent"。
- 多个能力组合成一个"场景化 Agent"。

这样做的好处：
- **降低重复建设**：N 个 Agent 共享 M 个能力，N+M 远小于 N*N。
- **统一质量基线**：能力的 SLA 一旦建立，所有调用方受益。
- **便于合规审计**：所有调用都流经能力目录，审计一目了然。
- **便于成本核算**：按能力维度算成本比按 Agent 维度算更准确。

## 五、PM 在能力治理中的具体动作

PM 不是能力治理的旁观者，而是核心推动者：
1. **立项前查目录**：每个新 Agent 立项，先在能力目录里找现有能力。
2. **PRD 写明能力依赖**：明确列出"本 Agent 依赖能力 A/B/C，新增能力 D"。
3. **推动能力 Owner 对接**：和能力的 Owner 协商 SLA、调用配额、升级节奏。
4. **参加能力评审会**：PM 是评审委员之一，评估"这个能力值不值得做、谁该做、什么时候做"。
5. **建立能力使用度量**：跟踪哪些能力被高频使用、哪些是僵尸能力。

## 六、能力治理与五层架构的关系

能力治理横切在五层架构之上，但**主要落在 L3 能力层**：
- L3 能力层是能力的"实现"层。
- 能力目录是能力的"治理"层。
- L2 编排层通过能力目录查询并组合能力。
- L5 基础设施层提供能力的鉴权、限流、计量能力。

两者不是替代关系，而是"实现 + 治理"的协同。

## 七、本节小结

Gartner 的 Competency 框架 + L05 的能力层思想，共同指向一个企业级 Agent 治理的核心范式：**用能力目录代替 Agent 清单，用能力治理代替 Agent 治理**。PM 掌握这个范式后，能从"做一个 Agent"的产品思维升级到"运营一份能力资产"的平台思维——这正是从单点项目走向企业级 AI 平台的关键一跃。

## 八、本地素材引用

- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L05-工程全景图：构建企业级多智能体系统的"施工蓝图".md（能力层 + 工具注册中心的工程描述）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L12-工具设计哲学：从 API 到 Agent-Native 的范式跃迁.md（能力设计哲学）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L13-自定义工具封装：构建 Tools 的五步标准 SOP.md（能力标准化 SOP）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L16-Skills生态：让Agent接入大量工具.md（Skills 即能力的打包形式）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L14-MCP协议：标准化定义工具接口.md（能力接口标准化协议）
- 12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L04-架构师的决断：AI 应用开发选型工具.md（能力落地时的技术选型参考）
