> **来源标注**
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L34-需求边界：如何使用 AI 适用性评估表识别高 ROI 场景？.md
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L36-评测体系：让AI质量可量化.md
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L37-持续集成：让 AI 应用稳定演进.md
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L38-线上监控：知道你的 AI 到底跑得怎么样.md
> - 素材源：12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L39-数据飞轮：用线上数据持续迭代 AI Agent.md
> - 素材源：12-学习笔记/01-极客时间-成为AGI产品经理/04-第三章-商业化之路/14-重塑产品研发流程：AI产品的PRD怎么写，产品、研发、算法合作的新范式.md
> - 整理时间：2026-06-23
> - 整理目的：第六章第 2 节"PM 持续闭环"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 6.2 PM 持续闭环：从"上线"到"长出来"

## 一、为什么 AI 产品需要"持续闭环"

传统产品的上线是一个"句号"——产品交付、用户使用、运营维护，业务指标靠运营和迭代逐步优化。AI 产品不同，AI 的效果在很大程度上是"长出来"的：

- 同样的模型 + 同样的 prompt + 不同的种子数据，产出效果差异巨大；
- 业务知识、术语、用户习惯需要持续灌入 RAG 和微调数据集；
- 模型版本升级、政策合规要求变化、用户期望值漂移，都会让"已经上线"的 Agent 静悄悄地退化。

因此，PM 在 AI 产品中的角色从"产品交付者"转变为"质量培育者"。本节按"需求边界 → 原型 → 评测 → CI → 监控 → 数据飞轮"六步建立 PM 的持续闭环。

## 二、第一步：需求边界——用 AI 适用性评估表识别高 ROI 场景

### 2.1 不是所有需求都适合用 AI 做

L34 提出了一个关键问题：在业务需求池里，哪些值得用 AI 来做？回答这个问题的工具是"AI 适用性评估表"。评估表通常包含五个维度：

- **任务频率**：高/中/低。优先选高频任务摊薄 AI 的边际成本；
- **任务结构化程度**：高/中/低。结构化任务（合同要素抽取、工单分类）AI 表现稳定，开放任务（开放式咨询）波动大；
- **容错空间**：高/中/低。容错空间小的任务（医疗、法律）需要更高的准确率门槛；
- **数据可得性**：是否已有可用的标注数据、知识库、案例库；
- **ROI 上限**：任务完成后可量化的业务价值（如减少 X 人天/降低 X%错误率）。

### 2.2 评估结果四象限

把五维评分做加权汇总后，需求会落到四个象限：

- **优先做**：高频 + 高结构化 + 高容错 + 高 ROI——典型如"客服工单分类"；
- **可以做**：中频 + 中结构化 + 中容错 + 中 ROI——需要 PoC 验证；
- **谨慎做**：低频 + 低结构化 + 低容错 + 低 ROI——AI 优势不明显；
- **不做**：业务强相关且错误代价极高的领域，留给人类专家。

PM 用这张表在需求评审会上快速对齐"做什么 / 不做什么"，避免团队陷入"为了 AI 而 AI"的内卷。

### 2.3 从需求边界到产品边界

识别出"高 ROI 场景"后，还需要把范围圈定到"产品边界"——哪些交给 AI、哪些交给人类、哪些交给 AI+人类协作。三种典型模式：

- **AI 主导 + 人类兜底**：适用于低风险高频任务；
- **人类主导 + AI 辅助**：适用于高风险低频任务；
- **AI 主导 + 人类审核**：适用于中等风险的中频任务，是企业场景最常见的形态。

## 三、第二步：AI 产品 PRD 怎么写——产品、研发、算法合作的新范式

### 3.1 传统 PRD 在 AI 场景下的失效

传统 PRD 的核心假设是"需求确定、方案确定"，开发过程是"实现"。但 AI 产品的需求本身就包含不确定性——同一个问题可以用 RAG、微调、Agent、工具调用等多种技术路径解决，效果差异显著。沿用传统 PRD 会出现：

- 方案章节过早锁定技术栈，导致后续无法灵活调整；
- 验收标准用"功能是否实现"而非"质量是否达标"；
- 缺少对 Prompt、知识库、评测集的明确要求。

### 3.2 AI 产品 PRD 的结构

素材源中"成为 AGI 产品经理"课程的"AI 产品 PRD"提出了一种新的 PRD 结构：

1. **业务背景与价值**：和传统 PRD 一样；
2. **场景与边界**：明确"AI 做什么 / 不做什么"——直接对接 6.2 节的"产品边界"；
3. **用户旅程与协作模式**：明确 AI 与人类/系统的协作节点；
4. **输入输出契约**：包括输入数据格式、输出格式、长度约束、合规约束；
5. **质量验收标准**：用可量化的指标替代功能验收；
6. **数据与知识来源**：列出 RAG 知识库、Few-shot 示例、领域微调数据的来源与更新机制；
7. **Prompt / 工具说明**：给出初版 Prompt 模板和工具调用清单（可后续迭代）；
8. **风险与降级**：幻觉、偏见、越权的应对策略；
9. **评测与监控**：明确评测集规模、监控指标、上线门槛；
10. **迭代计划**：明确后续数据飞轮的节奏。

### 3.3 产品-研发-算法三方协作的新范式

传统协作是"产品提需求 → 研发做实现 → 算法提供底层能力"。AI 产品的协作必须改为"三方位一体的迭代小组"：

- **产品**：定义场景边界、用户体验、验收指标；
- **研发**：负责 Agent 框架、工具实现、可观测性、CI/CD；
- **算法**：负责 Prompt 调优、RAG 召回、模型选型、评测集建设。

三方的协作以"评测集"为公共语言——所有迭代都基于同一份评测集的效果变化来决策，避免"产品说体验好、算法说指标好、研发说跑得通"的割裂。

## 四、第三步：评测体系——让 AI 质量可量化

### 4.1 评测的四个层次

L36 把 AI 评测分为四个层次：

- **基础正确率**：单轮、单任务的"对/错"判断；
- **任务完成率**：多轮、多步骤的"端到端"完成度；
- **人类偏好对齐**：用人类标注或偏好模型评估"是否像人话"；
- **业务指标关联**：AI 质量指标和最终业务指标（留存、转化、效率）的相关度。

只有当四个层次都被覆盖时，PM 才能真正回答"我的 Agent 到底好不好"。

### 4.2 评测集的构建

评测集不是"测试用例"那么简单，它是一份带标注的"任务样本库"：

- **覆盖度**：覆盖主要业务场景、长尾场景、风险场景；
- **标注质量**：标注必须由业务专家完成，且有交叉校验；
- **更新机制**：评测集必须随业务演进持续更新，否则会"模型升级了但评测集还在评估老问题"；
- **可复用性**：评测集按"场景 × 难度 × 风险"分类，方便针对不同 Agent 复用。

### 4.3 离线评测与在线评测的差异

- **离线评测**：在 CI 流水线中跑，回归性强，但容易"过度拟合评测集"；
- **在线评测**：在生产流量中抽样评估，反映真实情况，但反馈周期长。

两者必须配合使用：离线评测做"上线前把关"，在线评测做"上线后真实情况"。

## 五、第四步：持续集成——让 AI 应用稳定演进

### 5.1 AI 应用的 CI 流水线

L37 提出 AI 应用的 CI 不只是"代码编译 + 单元测试"，必须包括：

- **Prompt 版本管理**：每次 Prompt 改动都生成版本号，对应评测结果；
- **评测集回归**：每次 PR 必须跑评测集，指标下降 X% 阻断合并；
- **数据快照**：RAG 知识库、Few-shot 示例集必须用版本化的快照；
- **模型版本锁定**：主模型、备用模型、降级模型都明确版本号；
- **成本基线**：CI 中嵌入成本估算，超出预算的 PR 必须优化。

### 5.2 灰度发布

AI 应用不能一次性全量发布，必须灰度：

- **流量切分**：按 1% → 5% → 20% → 50% → 100% 渐进放量；
- **金丝雀对比**：新旧版本并行跑同一批真实请求，对比质量指标；
- **回滚机制**：任意指标恶化立即回滚，不依赖人工决策；
- **影子模式**：新版本只"看"不"答"，用真实流量评估但不影响用户。

## 六、第五步：线上监控——知道你的 AI 到底跑得怎么样

### 5.1 监控指标的三层结构

L38 把监控指标分为三层：

- **系统层**：延迟、错误率、Token 消耗、模型可用性——由可观测性平台提供；
- **质量层**：任务完成率、一次解决率、人工接管率、幻觉率——由在线评测系统提供；
- **业务层**：用户留存、转化率、客服满意度、运营效率——由业务系统提供。

三层指标必须打通关联，PM 才能回答"系统变慢 → 是否影响业务"这类问题。

### 5.2 异常告警策略

不是所有指标变化都要告警，告警必须分级：

- **P0（立即响应）**：业务指标（用户大量投诉、退订激增）恶化；
- **P1（小时内响应）**：质量指标（一次解决率、人接管率）恶化超过阈值；
- **P2（当天处理）**：系统指标（延迟、错误率）异常趋势；
- **P3（周级复盘）**：成本指标缓慢上升。

## 七、第六步：数据飞轮——用线上数据持续迭代 AI Agent

### 7.1 数据飞轮的本质

L39 提出"数据飞轮"是 AI 产品持续进化的核心机制：

1. 线上真实用户数据流入；
2. 人工或半自动标注后回流到评测集/训练集；
3. 评测集/训练集驱动 Prompt、RAG、模型的下一轮迭代；
4. 新版本上线后产生新的线上数据，循环往复。

数据飞轮一旦转起来，Agent 的"日久弥新"就有了机制保障。

### 7.2 数据飞轮的组件

完整的数据飞轮需要以下组件：

- **数据采集**：所有用户交互、模型输入输出、人工接管记录都被完整留痕（注意合规）；
- **数据标注**：标注团队对负向样本（badcase）做归因标注；
- **数据回流**：标注后的数据进入 RAG 知识库、Few-shot 库、评测集；
- **效果回归**：基于回流数据跑评测集，对比新旧版本；
- **灰度验证**：新版本先小流量验证，再全量发布。

### 7.3 数据飞轮的治理

数据飞轮不是"采集越多越好"——必须有治理：

- **隐私合规**：用户数据脱敏、用户授权、敏感字段不出库；
- **标注质量**：标注员培训、交叉校验、标注一致性监控；
- **数据分布监控**：避免训练集/评测集与生产数据分布漂移；
- **闭环时效**：badcase 从出现到回流必须有时效目标（如 48 小时）。

## 八、PM 在六步闭环中的具体动作清单

| 闭环步骤 | PM 的关键动作 | 协作方 |
|---|---|---|
| 需求边界 | 维护 AI 适用性评估表，主导需求评审 | 业务、研发、算法 |
| 原型 PRD | 撰写 AI 产品 PRD，明确质量验收标准 | 研发、算法 |
| 评测体系 | 主导评测集建设、定义质量指标 | 算法、研发 |
| 持续集成 | 定义灰度策略、把关上线门槛 | 研发 |
| 线上监控 | 维护三层监控指标看板、分级告警 | 研发、业务 |
| 数据飞轮 | 主导 badcase 归因、标注流程治理 | 标注团队、算法 |

## 九、给 PM 的实操建议

1. **建立"评测集"作为公共语言**：所有讨论以评测集上的指标变化为依据，避免主观争论；
2. **把"质量验收"写进 PRD**：从第一天就明确质量门槛，避免"上线后再说"；
3. **每月一次数据飞轮复盘**：把 badcase、标注、迭代动作形成固定机制；
4. **建立 PM 的"指标看板"**：让 PM 拥有自己的实时数据视图，而不只是依赖研发汇报；
5. **坚持"小步快跑"**：灰度发布 + 快速回滚比一次性大版本上线安全得多。

## 十、本节关键素材索引

| 主题 | 素材源 | 关系 |
|---|---|---|
| 需求边界 | L34-需求边界：如何使用 AI 适用性评估表识别高 ROI 场景？ | 6.2 第二节主体 |
| 评测体系 | L36-评测体系：让AI质量可量化 | 6.2 第四节主体 |
| 持续集成 | L37-持续集成：让 AI 应用稳定演进 | 6.2 第五节主体 |
| 线上监控 | L38-线上监控：知道你的 AI 到底跑得怎么样 | 6.2 第六节主体 |
| 数据飞轮 | L39-数据飞轮：用线上数据持续迭代 AI Agent | 6.2 第七节主体 |
| AI 产品 PRD | 重塑产品研发流程：AI产品的PRD怎么写 | 6.2 第三节主体 |

## 十一、小结

PM 持续闭环是 AI 产品的"生命线"。六步闭环（需求边界 → 原型 → 评测 → CI → 监控 → 数据飞轮）形成了一个完整的飞轮——每一步的输出都是下一步的输入，每一步的反馈都在改进上一步。PM 在这个飞轮中的角色不再是"产品经理"，而是"AI 质量培育者"——这是 AI 时代 PM 角色的根本性升级。
