> **来源标注**
> - 素材源：../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L25-团队角色体系：分工设计与行为规范.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L26-任务链与信息传递：数字员工的协作协议.md；../../12-学习笔记/05-极客时间-Claude Code工程化实战/04｜量体裁衣：从 Sub-Agents 到 Multi-Agent 的工程指南.md
> - 整理时间：2026-06-23
> - 整理目的：第五章第 2 节"协作模式"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 5.2 协作模式

## 一、为什么需要给"协作"分类

PM 在听到"多 Agent 协作"时，第一反应往往是"找几个 Agent 串起来跑"。但实际工程中，Agent 之间怎么连、按什么拓扑连、由谁发起、由谁收口，会直接决定系统的稳定性、可调试性、可扩展性。L23 课程里老师用一句话点破：**多 Agent 不是"找几个 Agent 一起干活"，而是"按业务关系选择合适的协作模式"**。本节把企业级最常见的 4 种协作模式拆开讲清：Orchestrator-Worker（主从）、Hierarchical（层级）、Peer（对等）、Debate（辩论），并给出每种模式的适用场景、典型结构、陷阱与改造路径。

## 二、Orchestrator-Worker 通用模式

### 2.1 结构

这是 L23 主推的范式，也是 70% 企业级多 Agent 系统的默认选择。结构是：

```
[Human / 上游任务]
       ↓
[Orchestrator 主 Agent] ← 全局上下文、任务拆分、汇总
       ↓ ↓ ↓
[Worker 子 Agent 1] [Worker 子 Agent 2] [Worker 子 Agent 3]
       ↓
[执行结果 → 回流主 Agent]
       ↓
[最终输出给 Human]
```

主 Agent 负责三件事：**拆任务、派任务、收结果**。子 Agent 各自只关心自己被分配的那一坨，不感知其他子 Agent 的存在。

### 2.2 适用场景

- 任务可以被**清晰拆分成多个无强依赖的子任务**（如：调研 + 设计 + 实现 + 测试可并行）
- 主 Agent 需要**全局视角**做拆解和汇总（不能每个子 Agent 各自拍板）
- 业务对**可调试性**要求高（主链明确，失败定位快）

### 2.3 工程要点

- 主 Agent 的 system prompt 必须包含**任务拆解规则**、**子 Agent 选择规则**、**异常处理规则**三块
- 子 Agent 的输入必须**结构化**（JSON Schema + 必填字段校验），不能是自由文本
- 主 Agent 要维护**任务状态表**（哪些子任务完成、哪些失败、是否需要重试）
- 消息传递要带**追踪 ID**（trace_id），否则出问题回溯不动

### 2.4 陷阱

- 主 Agent 变成"超级 Agent"：把所有信息都装进自己的上下文，反而比单 Agent 还糟
- 子 Agent 之间的**强依赖**被忽略：明明 B 必须等 A 的结果，主 Agent 却同时派发
- 主 Agent 反复重试：子 Agent 失败后主 Agent 不知道何时放弃，导致无限循环

## 三、Hierarchical 层级模式

### 3.1 结构

```
        [总控 Agent]
       /      |      \
[中层 Agent A] [中层 B] [中层 C]
   /  \         |       /  \
[子A1][子A2]  [子B1]  [子C1][子C2]
```

总控 Agent 管中层，中层管执行层，每层只对上一层负责。L25 课程中老师把这种结构类比为"公司组织架构图"——CEO 不会直接和一线员工对话，中间必须有部门经理。

### 3.2 适用场景

- 业务本身就有**层级关系**（如：总部 → 区域 → 门店、产品线 → 子产品 → 功能）
- 单层 Orchestrator-Worker 装不下——子 Agent 数量超过 15–20 个，单层调度崩溃
- 需要**权限分级**（不同层级的 Agent 看到的工具、数据、决策范围不同）

### 3.3 工程要点

- 每一层都必须有**明确的边界**：知道什么该自己处理、什么该下放
- 跨层通信要走**正式通道**（如状态总线、事件队列），不能父子 Agent 直接传消息
- 每一层都要有**兜底机制**：中层 Agent 挂了，总控 Agent 能感知并接管

### 3.4 陷阱

- **层级过深**：超过 3 层后，调试和延迟都会爆炸
- **跨层旁路**：中层 Agent 偷偷绕过总控 Agent 直接找执行层，破层级后无法维护
- **决策权混乱**：总控 Agent 和中层 Agent 都能做同一类决策，导致执行层收到冲突指令

## 四、Peer 对等模式

### 4.1 结构

```
[Agent A] ←→ [Agent B] ←→ [Agent C]
     ↑_________↓_________↓
     [共享黑板 / 共享消息总线]
```

所有 Agent 地位平等，没有 Orchestrator。Agent 之间通过**共享黑板**（blackboard）或**消息总线**通信，谁都可以发起协作、谁都可以响应。

L26 课程里描述的"任务链与信息传递"协议，本质上就是 Peer 模式的一种工程化实现：每个 Agent 把自己的输出写到共享黑板，下一个 Agent 从黑板读自己关心的字段。

### 4.2 适用场景

- 任务**没有明确的主线**（如头脑风暴、多角度分析、探索性研究）
- 多个 Agent 需要**互相补充信息**（A 做完的事 B 需要参考，B 做完的事 A 也需要）
- 业务需要**去中心化**（不希望单点故障）

### 4.3 工程要点

- 必须有**共享黑板**（可用 KV 存储、文件、消息队列实现）
- 每个 Agent 必须有**读黑板的子集**（不是读全部，否则又退化成单 Agent 上下文）
- 必须有**协调 Agent**（哪怕是 peer 模式，也需要一个"交通警察"防止死锁）

### 4.4 陷阱

- **黑板爆炸**：每个 Agent 都往黑板写，最后黑板里堆了一堆没人用的数据
- **死锁循环**：A 等 B 的输出、B 等 C 的输出、C 等 A 的输出
- **调试噩梦**：没有主链，出问题后不知道是哪一步出错

## 五、Debate 辩论模式

### 5.1 结构

```
         [议题]
        /   |   \
[正方 Agent] [反方 Agent] [裁判 Agent]
        \   |   /
         [结论 / 共识]
```

多个 Agent 扮演**对立的角色**（正方/反方/中立），针对同一议题互相辩论，最后由裁判 Agent 汇总或投票得出结论。Claude Code 04 课里提到的"群策群力"模式，本质就是 Debate 的一种工程化落地。

### 5.2 适用场景

- 决策类任务（如"这个方案要不要上线"、"这个投资标的可不可以"）
- 需要**对抗性思维**避免盲点（单一 Agent 容易陷入确认偏误）
- 业务需要**可解释的结论**（辩论过程本身就是审计依据）

### 5.3 工程要点

- 辩论必须有**结构化议程**（开题 → 陈词 → 质询 → 总结 → 投票）
- 裁判 Agent 必须有**明确判定标准**（不能拍脑袋）
- 辩论轮数要**设上限**（通常 3–5 轮），否则会陷入无穷辩论

### 5.4 陷阱

- **形式主义辩论**：Agent 互相"表演"反对，但不真的提供新论据
- **裁判 Agent 偏袒**：裁判 Agent 倾向于支持某一方，导致辩论流于形式
- **成本爆炸**：N 个 Agent 跑 M 轮辩论，成本是单 Agent 的 N×M 倍

## 六、四种模式的选型对照

| 维度 | Orchestrator-Worker | Hierarchical | Peer | Debate |
|------|--------------------|--------------|------|--------|
| 主线清晰度 | 高 | 中 | 低 | 中 |
| 子 Agent 数量 | 3–10 | 10–50+ | 3–10 | 2–5 |
| 调试难度 | 低 | 中 | 高 | 中 |
| 延迟 | 中 | 高 | 中 | 高 |
| 成本 | 中 | 高 | 中 | 高 |
| 适用任务 | 端到端业务流程 | 大型组织业务 | 探索性任务 | 决策类任务 |
| 推荐起点 | 70% 场景的默认 | 单层撑不住时升级 | 头脑风暴 | 关键决策点 |

## 七、模式组合的常见套路

实际工程中**很少只用一种模式**。常见的组合有：

- **Orchestrator + Hierarchical**：主 Agent 管中层，中层管执行层，适合大型企业系统
- **Orchestrator + Debate**：主 Agent 拆完任务后，对关键决策点启用 Debate 模式
- **Peer + 协调 Agent**：Peer 之间互相通信，由一个轻量级协调 Agent 防死锁
- **Hierarchical + Debate**：高层级用 Debate 做战略决策，低层级用 Orchestrator 执行

## 八、本节小结

- 4 种模式：Orchestrator-Worker（默认）/ Hierarchical（大型）/ Peer（探索）/ Debate（决策）
- 选型依据：主线清晰度、子 Agent 数量、调试难度、延迟、成本
- 实际工程常组合使用，不是非此即彼
- 每种模式都有特定的陷阱，PM 必须在设计阶段就考虑兜底机制

## 九、本地索引

- 上游方法论：[00-方法论总纲/PM设计Agent全流程方法论总纲.md](../../00-方法论总纲/PM设计Agent全流程方法论总纲.md) 第 1 部分第六节"多智能体企业落地"
- 上一节：[5.1 何时用多 Agent](../5.1-何时用多Agent/5.1-何时用多Agent.md)
- 下一节：[5.3 数字员工团队](../5.3-数字员工团队/5.3-数字员工团队.md)
- 同章其他：[5.4 单 vs 多决策](../5.4-单vs多决策/5.4-单vs多决策.md)
- 素材源 1：[L23-Orchestrator 范式](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md)
- 素材源 2：[L25-团队角色体系](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L25-团队角色体系：分工设计与行为规范.md)
- 素材源 3：[L26-任务链与信息传递](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L26-任务链与信息传递：数字员工的协作协议.md)
- 素材源 4：[04｜量体裁衣：从 Sub-Agents 到 Multi-Agent 的工程指南](../../12-学习笔记/05-极客时间-Claude Code工程化实战/04｜量体裁衣：从 Sub-Agents 到 Multi-Agent 的工程指南.md)
