> **来源标注**
> - 素材源：../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L24-认知升级：从任务列表到数字团队.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L25-团队角色体系：分工设计与行为规范.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L26-任务链与信息传递：数字员工的协作协议.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L27-Human as 甲方：如何设计 Agent 团队的人类介入点.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L28-数字员工的自我进化：让 Agent 团队越用越好.md
> - 整理时间：2026-06-23
> - 整理目的：第五章第 3 节"数字员工团队"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 5.3 数字员工团队

## 一、从"任务列表"到"数字团队"的认知跃迁

L24 课程给出了一个非常关键的认知转换：**不要把多 Agent 系统设计成"任务列表的并行执行器"，而要设计成"一个有角色、有分工、有协作协议、有成长机制的虚拟团队"**。这两者的差别，就像"找 5 个临时工搬砖"和"组一支 5 人小队做项目"的差别。

任务列表视角：每个 Agent 是一个函数，接收输入、返回输出，Agent 之间没有连续性、没有"职业素养"、没有"团队文化"。

数字团队视角：每个 Agent 是一个"数字员工"，有明确的岗位（角色）、有岗位对应的工具集和工作规范、和其他员工有协作协议、会随着项目积累经验（自我进化）。

L25-L28 四节课就是围绕"如何把多 Agent 系统设计成一支数字员工团队"展开的——角色分工（L25）、协作协议（L26）、甲方介入（L27）、自我进化（L28）。

## 二、按职能设计 Agent 编队

### 2.1 标准五段式编队

对应企业项目"需求 → 设计 → 编码 → 测试 → 审查"五段式，标准的数字员工编队是：

| 岗位 | 角色名 | 核心职责 | 典型工具集 | 输入契约 | 输出契约 |
|------|--------|----------|------------|----------|----------|
| 1 | 需求分析师 | 把业务诉求拆成可执行的需求项 | 文档读取、需求模板、用户访谈记录 | 业务方原始诉求 | 结构化需求文档 + 验收标准 |
| 2 | 架构设计师 | 把需求转为技术方案与接口设计 | 架构图工具、API 规范库、依赖分析 | 结构化需求文档 | 架构图 + 接口定义 + 数据模型 |
| 3 | 编码工程师 | 按接口定义实现代码 | 代码读写、测试运行、依赖管理 | 接口定义 + 数据模型 | 可运行的代码 + 单元测试 |
| 4 | 测试工程师 | 设计并执行测试用例 | 测试框架、覆盖率工具、Bug 跟踪 | 可运行代码 | 测试报告 + Bug 清单 |
| 5 | 审查工程师 | 审查代码质量、安全、合规 | 静态分析、安全扫描、规范检查 | 代码 + 测试报告 | 审查意见 + 通过/驳回决策 |

### 2.2 编队设计的三个原则

1. **岗位不重叠**：每个岗位的职责边界必须明确，不能"既能做 A 又能做 B"，否则出现责任真空
2. **工具不越权**：每个岗位只能调用自己岗位的工具集，测试工程师不能直接改代码、编码工程师不能直接合主干
3. **交接有标准**：岗位之间的输入/输出必须有**结构化契约**（JSON Schema），不接受自由文本交接

## 三、角色分工与行为规范

L25 课程把"行为规范"分成三层：**职业素养**（所有数字员工共有）、**岗位规范**（按角色定制）、**项目规范**（按具体项目临时添加）。

### 3.1 职业素养（所有数字员工共有）

- **不越权**：不调用自己岗位工具集之外的工具
- **可追溯**：所有操作必须带 trace_id，所有输出必须可复现
- **失败不静默**：遇到异常必须上报，不悄悄吞错
- **遵守成本围栏**：单次任务成本超过阈值必须停下来

### 3.2 岗位规范（按角色定制）

以"编码工程师"为例：

- 代码必须通过 lint 才能提交
- 函数必须写 docstring
- 不允许在代码里写 TODO 凑数
- 公共组件必须先写测试再写实现
- 提交前必须跑过至少一次测试

以"测试工程师"为例：

- 必须覆盖所有正常路径 + 至少 3 条异常路径
- Bug 报告必须含复现步骤、预期结果、实际结果
- 不允许"测一遍就说通过"，必须有覆盖率数据
- 安全相关测试必须有专门标记

### 3.3 项目规范（按项目临时添加）

- 本次项目的代码风格（命名、缩进、注释密度）
- 本次项目必须遵守的合规要求（如 GDPR、等保）
- 本次项目的交付时间窗口
- 本次项目的关键 stakeholder

行为规范是数字员工"职业化"的关键。没有规范，数字员工就是临时工；有了规范，数字员工才是"可复用资产"。

## 四、任务链与信息传递协议

L26 课程的核心是把"任务链"和"信息传递"作为**一等公民**来设计。常见的三种协议：

### 4.1 同步链式

```
需求分析 → 设计 → 编码 → 测试 → 审查
```

每一步必须等上一步完成才能开始。优点是责任链清晰、调试简单；缺点是慢、无法并行。适用于**强依赖**任务（编码必须等设计完成）。

### 4.2 异步并行

```
        ┌→ 调研 A
需求 ───┼→ 调研 B
        └→ 调研 C
        ↓
        汇总
```

多个无依赖的子任务并行派发，最后汇总。优点是快、吞吐高；缺点是需要解决状态同步、消息乱序、部分失败。适用于**无依赖**任务（多竞品调研、多版本 UI 草图）。

### 4.3 事件驱动

```
需求 → [事件总线] → 多个监听者各自响应
```

任务进展以事件形式广播，感兴趣的 Agent 自行订阅。优点是解耦、可扩展；缺点是调试难、行为不可预测。适用于**需要多方协作的复杂场景**。

信息传递的关键约束（无论哪种协议都必须遵守）：

- 每条消息必须有**结构化 Schema**（不接受自由文本作为主消息）
- 每条消息必须有**trace_id + step_id**（出错时能定位到具体步骤）
- 消息大小必须有**上限**（如 32KB），超过则改为引用文件路径
- 消息必须有**版本号**（schema_version），避免协议升级后不兼容

## 五、Human as 甲方——人工介入点设计

L27 课程提出一个非常重要的视角：**Human 在多 Agent 系统中不是"操作员"，而是"甲方"**。数字员工是乙方，乙方干活但甲方有最终否决权。

### 5.1 哪些节点必须人工介入

| 介入点 | 介入内容 | 缺位代价 |
|--------|----------|----------|
| 需求评审 | 业务方确认需求理解是否准确 | 跑偏后所有下游工作白做 |
| 关键决策 | 涉及资金、安全、合规的判断 | 模型拍板一旦失误损失巨大 |
| 异常处理 | 子 Agent 失败且自动重试超过阈值 | 死循环或静默失败 |
| 验收交付 | 最终成果是否满足业务诉求 | 上线后才发现不满足 |
| 重大变更 | 项目方向调整、需求变更 | 全链路返工 |

### 5.2 介入点设计的反模式

- **"全自动"幻觉**：以为数字员工能自己搞定一切，不设计介入点，结果出大问题
- **"无意义介入"**：在每个小步骤都让用户点"确认"，用户立刻关闭系统
- **"介入太晚"**：等到上线后才让用户验收，返工成本爆炸
- **"介入太早"**：用户每秒钟都要看，注意力被消耗殆尽

正确的介入点设计是**关键节点高密度、非关键节点低密度**——平时数字员工自己跑，关键决策时弹出来让用户拍板。

## 六、自我进化机制

L28 课程是整个数字员工体系的"长期价值"所在：**让数字员工越用越好，而不是每次都从零开始**。三种进化机制：

### 6.1 短期记忆 → 长期记忆

每次任务结束后，把过程中的**关键经验**沉淀到长期记忆库（不沉淀所有细节，只沉淀可复用的部分）。例如：

- "本项目里调用 X API 时必须先做 Y 校验"
- "本项目里 Z 工具的返回格式是 W，不是文档里写的 V"
- "本项目里用户偏好 A 风格而不是 B 风格"

### 6.2 失败案例 → 教训库

把每次失败的 case（不管是任务失败还是质量不达标）记录到教训库，下次遇到类似场景时**主动避坑**。例如：

- "上次用 N 模型跑这类任务，准确率只有 60%，建议改用 M 模型"
- "上次设计的 prompt 在第二轮会触发幻觉，建议加 X 约束"

### 6.3 行为数据 → 模型微调

当沉淀的数据量足够大时（通常 1000+ 高质量案例），可以考虑**用这些数据微调基础模型**，让数字员工在底层能力上提升。这是最重的方式，但也是收益最大的方式。

### 6.4 自我进化的边界

- **不能自动改自己的 system prompt**：必须人工审核后才生效
- **不能自动调高自己的权限**：必须走权限变更流程
- **不能自动换底层模型**：必须经过 A/B 测试验证效果
- **必须保留完整 audit log**：所有自我进化决策可追溯

## 七、本节小结

- 数字员工团队 = 角色分工 + 行为规范 + 协作协议 + 介入点 + 自我进化
- 标准五段式编队：需求分析 / 架构设计 / 编码 / 测试 / 审查
- 行为规范分三层：职业素养 / 岗位规范 / 项目规范
- 信息传递必须有结构化 Schema + trace_id + 版本号
- Human 是甲方，介入点设计是"关键节点高密度、非关键节点低密度"
- 自我进化机制：短期 → 长期记忆 / 失败 → 教训库 / 数据 → 模型微调

## 八、本地索引

- 上游方法论：[00-方法论总纲/PM设计Agent全流程方法论总纲.md](../../00-方法论总纲/PM设计Agent全流程方法论总纲.md) 第 1 部分第六节"多智能体企业落地"
- 上一节：[5.2 协作模式](../5.2-协作模式/5.2-协作模式.md)
- 下一节：[5.4 单 vs 多决策](../5.4-单vs多决策/5.4-单vs多决策.md)
- 同章其他：[5.1 何时用多 Agent](../5.1-何时用多Agent/5.1-何时用多Agent.md)
- 素材源 1：[L24-认知升级：从任务列表到数字团队](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L24-认知升级：从任务列表到数字团队.md)
- 素材源 2：[L25-团队角色体系](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L25-团队角色体系：分工设计与行为规范.md)
- 素材源 3：[L26-任务链与信息传递](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L26-任务链与信息传递：数字员工的协作协议.md)
- 素材源 4：[L27-Human as 甲方](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L27-Human as 甲方：如何设计 Agent 团队的人类介入点.md)
- 素材源 5：[L28-数字员工的自我进化](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L28-数字员工的自我进化：让 Agent 团队越用越好.md)
