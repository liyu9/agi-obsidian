> **来源标注**
> - 素材源：../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L03-Multi-Agent系统：Agent、Task、Process的协作美学.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md
> - 整理时间：2026-06-23
> - 整理目的：第五章第 4 节"单 vs 多决策"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 5.4 单 vs 多决策

## 一、为什么需要一棵决策树

PM 在面对一个新需求时，最常问的问题不是"用什么模型"，而是"用单 Agent 还是多 Agent"。这个决策一旦做错，要么是**用大炮打蚊子**（明明单 Agent 够用却硬上多 Agent，白白增加维护成本和延迟），要么是**用筷子吃牛排**（明明单 Agent 已经撑不住却硬撑，结果上下文污染、角色混乱、工具爆炸，最后崩盘）。本节给出一棵可复用的决策树，以及单/多 Agent 在 ROI 上的对比框架，让 PM 在 5 分钟内做出经得起复盘的选择。

L03 课程在介绍多 Agent 时反复强调"**从单 Agent 起步，根据症状升级**"；L23 课程则在工程层补充了"**主 Agent + 子 Agent 是升级的默认路径**"。这两节课合起来，刚好构成"何时单 / 何时多 / 怎么升级"三段式判断。

## 二、决策树（一棵图讲清）

```
                需求进来
                  │
        ┌─────────┴─────────┐
        │                   │
   任务能用一条          任务需要分支/
   主线串到底？          多角色/并行/隔离？
        │                   │
       单 Agent          是否能拆分为
        │                3-7 个子任务？
        ↓                   │
   完成                   ┌──┴──┐
                          │     │
                         能    不能
                          │     │
                          ↓     ↓
                       多 Agent 退回单 Agent
                                 或拆 Workflow
```

### 2.1 第一问：任务能用一条主线串到底？

判断方法：把任务的所有输入、所有可能的输出、所有步骤画成流程图。如果**只有一条主路径**，没有真正的"条件分支或多角色切换"，那就是单 Agent 的甜区。

举例：
- "根据用户描述生成营销文案" → 单主线 → 单 Agent
- "对一段代码做单文件的重构" → 单主线 → 单 Agent
- "读 PDF 回答用户问题" → 单主线 → 单 Agent
- "从 5 个竞品网站调研并生成对比报告" → 多主线 → 多 Agent
- "需求分析 + 设计 + 编码 + 测试 + 审查" → 多角色 → 多 Agent

### 2.2 第二问：任务需要分支/多角色/并行/隔离？

四个信号任一出现，强烈倾向多 Agent（详见 5.1 节）：
- 任务复杂度超单 Agent 边界
- 需要多角色/多技能
- 需要并行提效
- 需要信息隔离

### 2.3 第三问：是否能拆分为 3–7 个子任务？

这是一个**工程现实约束**。如果任务必须拆成 15+ 个子 Agent 才能覆盖，强行上多 Agent 会导致调度复杂度爆炸、维护成本爆炸。这种情况应该：
- **退回单 Agent + Workflow**（用确定性流程拆步骤）
- **或者退回到第二问重新设计**（是不是拆得太细了？）

经验值：3–7 个子 Agent 是工程甜蜜区，少于 3 个没必要多 Agent，多于 10 个应考虑 Hierarchical 层级模式。

## 三、ROI 对比框架

光靠决策树还不够，PM 还需要 ROI 数据支撑选择。下面给出单/多 Agent 在六个维度的对比：

| 维度 | 单 Agent | 多 Agent | 差距 |
|------|----------|----------|------|
| 开发成本 | 低（一个 prompt） | 中-高（多个 prompt + 调度） | 多 Agent 高 2-5 倍 |
| 调试难度 | 低（一条主链） | 中-高（多链 + 消息追踪） | 多 Agent 高 3-10 倍 |
| 端到端延迟 | 低 | 中（子 Agent 串行）或低（并行） | 视任务结构 |
| 吞吐量 | 低（串行） | 高（可并行） | 多 Agent 高 2-10 倍 |
| 任务复杂度上限 | 中 | 高 | 多 Agent 高 5-20 倍 |
| 长期维护成本 | 低 | 中（子 Agent 增删需要回归） | 多 Agent 高 2-3 倍 |

### 3.1 单 Agent 划算的场景

- 任务能用一条主线串到底（参考 2.1）
- 任务量小、调用频率低（< 100 次/天）
- 业务对延迟极敏感（< 1 秒响应）
- 团队没有多 Agent 工程经验
- MVP 阶段、需要快速验证

### 3.2 多 Agent 划算的场景

- 任务包含明显的多角色 / 多技能 / 并行 / 隔离需求
- 任务量中等以上、需要长期复用
- 业务对单点失败零容忍（多 Agent 可降级到部分能力）
- 业务对吞吐有要求（> 1000 次/天）
- 团队具备多 Agent 工程能力（Orchestrator / 协议 / 可观测）

## 四、升级路径：从单到多的 4 步法

PM 选定"先单 Agent 跑通"后，下一步就是**定义好升级触发条件**，避免"做到一半才发现该升级"的窘境。建议按 4 步走：

### 4.1 第 1 步：单 Agent + 工具跑通端到端

这是默认起点。不要一上来就拆 5 个 Agent。先用一个 Agent + 全部工具跑通"从用户输入到业务输出"的完整链路。这一步的目标是验证**业务可行性**，不是工程优雅度。

### 4.2 第 2 步：埋点观测三类症状

跑通后立刻埋三类症状的监控（参考 5.1 节的诊断清单）：
- 上下文污染监控（上下文窗口占用、模型"我忘了"类回复频率）
- 角色混乱监控（system prompt 角色数量、立场漂移检测）
- 工具过多监控（工具数量、工具调用犹豫率、工具描述重叠度）

### 4.3 第 3 步：定义升级阈值

基于第 2 步的数据，定义"什么数值算异常"。例如：
- 上下文窗口占用持续 > 60% → 触发升级
- system prompt 同时定义 > 3 个角色 → 触发升级
- 工具数量 > 15 个 且 犹豫率 > 10% → 触发升级
- 任一症状连续 3 天异常 → 启动升级流程

### 4.4 第 4 步：渐进式升级到多 Agent

升级不是"一晚上把单 Agent 拆成多 Agent"，而是渐进式：
- **第一周**：只把"上下文污染"对应的部分拆出去做子 Agent
- **第二周**：再拆"角色混乱"对应的部分
- **第三周**：最后拆"工具过多"对应的部分
- **每一步都保留回滚路径**

这种"小步快跑"的升级方式，避免了一次性拆完导致所有事情同时崩盘。

## 五、典型反模式

### 5.1 "上来就拆 5 个 Agent"

理由："业务比较复杂，应该用多 Agent"。结果：调度成本 > 业务价值，上线后根本用不起来。

正解：先用单 Agent + 工具跑通，验证业务可行性，再考虑升级。

### 5.2 "单 Agent 撑到死都不升级"

理由："已经写好的 prompt，不想动"。结果：上下文爆炸、模型幻觉、上线后事故频发。

正解：定义升级阈值，到点就升级。**承认单 Agent 不够用**不是失败，是工程理性。

### 5.3 "拆得越细越好"

理由："每个职责都该有专人负责"。结果：10+ 个子 Agent，调度成本爆炸、消息传递混乱、整体延迟比单 Agent 还高。

正解：3–7 个子 Agent 是甜蜜区，多了考虑 Hierarchical 而不是继续拆平铺。

### 5.4 "忽略主 Agent 设计"

理由："让子 Agent 自己协商就行"。结果：调度变成菜市场，谁都拍不了板。

正解：主 Agent 是"项目经理"，它的拆分策略、汇总逻辑、异常处理决定 80% 效果。

## 六、本节小结

- 决策树三问：一条主线串到底吗？需要分支/角色/并行/隔离吗？能拆成 3-7 个子任务吗？
- ROI 对比：单 Agent 开发维护便宜、延迟低；多 Agent 吞吐高、复杂度上限高
- 升级路径：单 Agent 跑通 → 埋点监控 → 定义阈值 → 渐进式升级
- 三大反模式：上来就拆 / 撑到死不拆 / 拆得过细

## 七、本地索引

- 上游方法论：[00-方法论总纲/PM设计Agent全流程方法论总纲.md](../../00-方法论总纲/PM设计Agent全流程方法论总纲.md) 第 1 部分第六节"多智能体企业落地"
- 上一节：[5.3 数字员工团队](../5.3-数字员工团队/5.3-数字员工团队.md)
- 下一节：[5.5 总结（待续）](../) 
- 同章其他：[5.1 何时用多 Agent](../5.1-何时用多Agent/5.1-何时用多Agent.md) · [5.2 协作模式](../5.2-协作模式/5.2-协作模式.md)
- 素材源 1：[L03-Multi-Agent 系统](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L03-Multi-Agent系统：Agent、Task、Process的协作美学.md)
- 素材源 2：[L23-Orchestrator 范式](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md)
