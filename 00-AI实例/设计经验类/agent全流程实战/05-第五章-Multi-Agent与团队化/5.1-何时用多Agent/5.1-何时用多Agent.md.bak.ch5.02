> **来源标注**
> - 素材源：../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L03-Multi-Agent系统：Agent、Task、Process的协作美学.md；../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md
> - 整理时间：2026-06-23
> - 整理目的：第五章第 1 节"何时用多 Agent"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 5.1 何时用多 Agent

## 一、这一节解决什么问题

PM 在拿到一个企业级需求时，第一个判断往往不是"用什么模型"，而是"用单 Agent 还是多 Agent"。绝大多数新人会下意识地把所有任务塞进一个超级 Agent，结果是上下文越来越长、工具越来越多、角色越来越糊，最后跑出一份勉强能交差的 demo，但只要换一次需求就全面崩盘。本节要回答的是：**什么时候单 Agent 已经撑不住，必须升级到多 Agent**。判断标准不靠直觉，而是靠"任务复杂度 × 角色数量 × 工具数量 × 信息隔离"四个维度的客观信号。

在 L03《Multi-Agent 系统：Agent、Task、Process 的协作美学》中，老师把单 Agent 推到极限后会出现的典型症状列成三类——上下文污染、角色混乱、工具过多，并明确指出这三类症状是"必须升级多 Agent"的临界点。L23《Orchestrator 范式：任务层的主 Agent + 子 Agent》则从工程实现的角度补充：当任务层无法用一条主线串起时，就该用"主 Agent 拆任务 + 子 Agent 承接任务"的分层结构。

## 二、单 Agent 适合的边界

在讨论"何时升级"之前，先明确"何时不用升级"。单 Agent + 工具调用依然是 70% 业务场景的最优解，理由有三：

1. **延迟最低**：省掉 Agent 之间传递消息、协商协议、状态同步的开销，端到端延迟通常比多 Agent 低 30%–60%。
2. **调试最简单**：所有推理轨迹、工具调用、上下文变化都集中在一处，问题定位只需要看一条主链。
3. **成本最可控**：不会出现子 Agent 重复读上下文、重复调用工具的浪费。

判断单 Agent 是否够用的最快方法：把任务描述、可用工具、预期输出画成一张图，如果**一条线能从输入走到输出，中间不出现分叉和角色切换**，那这个任务就是单 Agent 的甜区。比如"根据用户输入生成营销文案"、"对一段代码做单文件重构"、"读取一份 PDF 并回答问题"——这些都是典型的单 Agent 场景。

## 三、四个升级触发信号

### 3.1 任务复杂度超单 Agent 边界

当一个任务需要"先做 A、再做 B、然后根据 B 的结果做 C 或 D"这种**条件分支**时，单 Agent 的 ReAct 循环就会变得脆弱——它要么陷入"反复思考要不要分支"的死循环，要么在分支处丢失前序上下文。多 Agent 的好处是：把每条分支交给一个专精的子 Agent，主 Agent 只负责路由和汇总，分支内部的复杂度由子 Agent 自行消化。

### 3.2 需要多角色/多技能

业务里常见的"需求分析 + 设计 + 编码 + 测试 + 审查"五段式，就是典型的多角色任务。如果硬塞给一个 Agent，它必须在同一个 system prompt 里扮演五种角色，结果是**每种角色都被削弱**——既要懂产品又要懂代码、既要会写用例又要会挑刺，最后产出的就是"四不像"。多 Agent 的解法是给每个角色单独的 system prompt、单独的工具集、单独的输出契约，让每个子 Agent 在自己的专业范围内做到极致。

### 3.3 需要并行提效

有些任务的子步骤之间是**无依赖的**（例如同时跑 5 个竞品调研、同时生成 3 个版本的 UI 草图），单 Agent 只能串行做，总耗时 = N × 单步耗时。多 Agent 可以把这些无依赖子任务并行派发，总耗时 ≈ 单步耗时 + 调度开销，并行收益随子任务数量线性增长。L23 课程中演示的"主 Agent 同时派 3 个调研子 Agent，最后汇总报告"的案例，并行度比串行提升了 2.7 倍。

### 3.4 需要信息隔离

某些信息**只应该被特定角色看到**——比如测试 Agent 不应该知道"老板想放水"的备注、代码审查 Agent 不应该看到没脱敏的用户隐私数据。多 Agent 架构天然支持这种隔离：每个子 Agent 有自己的上下文窗口，主 Agent 在派发任务时只传递"它该知道的部分"。这是企业级场景（金融、医疗、政企）对多 Agent 架构的硬性要求。

## 四、三类临界症状的诊断清单

为了把"升级判断"从经验变成可复用的检查项，下面给出三类症状的**自检问题**。任何一类出现 2 个以上"是"，就应该考虑升级多 Agent。

### 4.1 上下文污染

- 同一个对话里是否同时出现了"产品文档 + 代码片段 + 测试报告 + 评审意见"四类内容？
- 模型是否开始把 A 任务的工具调用结果误用到 B 任务上？
- 是否出现"我忘了之前说过什么"、"我混淆了刚才的指令"这类回复？
- 上下文窗口是否长期占用超过 60%？

### 4.2 角色混乱

- system prompt 里是否同时定义了"你是产品经理 / 你是工程师 / 你是测试"等多个角色？
- 模型在不同轮次是否表现出"立场漂移"（前一轮说"用户体验优先"，后一轮说"性能优先"）？
- 是否需要用"现在请切换到 X 角色"这种提示来手动切换身份？
- 输出的风格在同一个任务里出现明显跳跃？

### 4.3 工具过多

- 工具数量是否超过 15–20 个？模型在选工具时是否开始犹豫、选错、漏选？
- 是否出现"工具描述互相重叠"——比如同时有 `search_docs` 和 `search_knowledge_base` 干几乎一样的事？
- 工具的参数表是否变得复杂到模型需要"先思考再填参"？
- 是否有工具被调用的频率低于 5% 但仍占据工具位？

## 五、从单 Agent 升级到多 Agent 的常见误区

- **"多 Agent 一定更好"**：错。多 Agent 带来调度开销、消息丢失风险、状态同步成本，只有触发上述信号时才值得升级。
- **"上来就拆 5 个 Agent"**：错。应该先**用单 Agent + 工具跑通端到端**，再用 5.4 的决策树判断是否升级。
- **"拆得越细越好"**：错。每个子 Agent 都要有自己的 system prompt、工具集、上下文窗口，拆得太细会导致总成本爆炸、维护困难。经验值是 3–7 个子 Agent 是甜蜜区。
- **"忽略主 Agent 的设计"**：错。主 Agent（Orchestrator）是整个系统的"项目经理"，它的拆分策略、汇总逻辑、异常处理决定了 80% 的整体效果。

## 六、本节小结

- 单 Agent + 工具是默认起点，只在出现三类症状时升级多 Agent
- 升级触发信号：任务复杂度超边界 / 需要多角色 / 需要并行 / 需要信息隔离
- 自检清单：上下文污染 4 问、角色混乱 4 问、工具过多 4 问
- 多 Agent 不是越多越好，3–7 个子 Agent 是工程甜蜜区
- 主 Agent 的拆分策略比子 Agent 数量更重要

## 七、本地索引

- 上游方法论：[00-方法论总纲/PM设计Agent全流程方法论总纲.md](../../00-方法论总纲/PM设计Agent全流程方法论总纲.md) 第 1 部分第六节"多智能体企业落地"
- 上一节：[第四章 4.5 工具与能力](../04-第四章-通用设计全流程/4.5-工具与能力/4.5-工具与能力.md)
- 下一节：[5.2 协作模式](../5.2-协作模式/5.2-协作模式.md)
- 同章其他：[5.3 数字员工团队](../5.3-数字员工团队/5.3-数字员工团队.md) · [5.4 单 vs 多决策](../5.4-单vs多决策/5.4-单vs多决策.md)
- 素材源 1：[L03-Multi-Agent 系统：Agent、Task、Process 的协作美学](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L03-Multi-Agent系统：Agent、Task、Process的协作美学.md)
- 素材源 2：[L23-Orchestrator 范式：任务层的主 Agent + 子 Agent](../../12-学习笔记/13-极客时间-企业级多智能体设计实战/03-原文+图片/L23-Orchestrator范式：任务层的主 Agent + 子 Agent.md)
