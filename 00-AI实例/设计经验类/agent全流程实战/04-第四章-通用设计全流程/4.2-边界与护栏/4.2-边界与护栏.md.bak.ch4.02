> **来源标注**
> - 素材源：13-L32 企业安全、OpenAI 护栏实践
> - 整理时间：2026-06-23
> - 整理目的：第四章第 2 节"边界与护栏"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.2 边界与护栏：让 Agent 知道"什么不能做"

## 一、为什么边界比能力更重要

PM 在设计 Agent 时，往往把大量精力花在"让它能做更多事"上。但真正决定 Agent 能不能上线的，不是能力多强，而是**边界是否清晰、护栏是否够硬**。

13 课程 L32 反复强调一个工程现实：**任何生产环境的 Agent 失败，80% 都源于边界没划清楚**。模型本身的能力差异在真实业务里是次要的，护栏才是决定 SLA 的关键。

边界与护栏要回答的核心问题是：

- **能做什么？**（能力边界）
- **不能做什么？**（行为边界）
- **出错时由谁兜底？**（兜底边界）

## 二、边界三问

### 1. 能力边界（Capability Boundary）

能力边界回答"Agent 在哪些范围内可以自主决策"。判定方法：

- **可逆性原则**：动作可逆 → 可自主；不可逆 → 必须人工
- **影响半径原则**：影响 1 人 → 可自主；影响 100 人 → 必须审批
- **置信度阈值原则**：模型置信度 ≥ X 才自主，否则转人工

### 2. 行为边界（Behavior Boundary）

行为边界回答"Agent 在执行过程中不能做什么"。典型清单：

- 不得删除/修改生产数据
- 不得对外发送未经审核的内容
- 不得访问与任务无关的系统
- 不得在没有用户确认下提交订单/转账

### 3. 兜底边界（Fallback Boundary）

兜底边界回答"Agent 失败后世界应当处于什么状态"。这是企业级 Agent 设计的核心：

- **降级路径**：模型挂了 → 切到规则引擎 → 再切到人工
- **状态保存**：失败时不能让事务处于半完成态
- **回滚机制**：可执行的副作用必须可回滚

## 三、护栏三类

OpenAI 与业界主流的护栏实践把"护栏"分成三类，分别布在 Agent 生命周期的不同位置。

### 1. 输入护栏（Input Guardrails）

在 prompt 进入模型前就过滤掉危险/无意义输入：

- **Prompt 注入检测**：识别"忽略之前的指令"类攻击
- **PII 脱敏**：手机号、身份证号、地址先脱敏再进模型
- **越权话题拦截**：与 Agent 角色无关的话题直接拒答
- **敏感词过滤**：行业特定的合规词

实现位置：通常在 LLM 调用前的中间件层。

### 2. 输出护栏（Output Guardrails）

在模型生成结果后、返回用户前进行检查：

- **结构校验**：必须是合法 JSON / 必须包含必填字段
- **事实校验**：关键数字、日期、人名必须对得上知识库
- **合规校验**：不得包含违规词、不得对外承诺
- **置信度校验**：模型自己的 confidence 太低时拒答

实现位置：通常在 LLM 调用后、Tool 调用前的中间件层。

### 3. 行为护栏（Behavior Guardrails）

这是企业级 Agent 最关键的一层，约束的是 Agent 在**多步执行过程中**的行为：

- **步骤白名单**：每一步只能调允许的工具
- **额度限制**：单次会话最多调 N 次工具、累计金额不超过 X
- **顺序约束**：先 A 后 B，不可乱序
- **副作用分级**：写操作必须显式标注为"危险动作"并要求二次确认

实现位置：通常在 Agent 循环的 controller 层。

## 四、HITL 人工介入设计

边界与护栏最终要落到一个具体机制上——**HITL（Human-in-the-Loop）**。HITL 不是"出错了再叫人"，而是**预先设计好介入点**。

### 1. 三种 HITL 介入点

| 介入点 | 触发条件 | 典型场景 |
|--------|----------|----------|
| 决策前 | 不可逆/高风险动作 | 转账、删除、发邮件 |
| 决策中 | 置信度低 / 多选项 | 客服投诉处理 |
| 决策后 | 大批量 / 长期影响 | 内容审核、数据导出 |

### 2. HITL 设计的 4 个关键细节

- **可视化**：必须把 Agent 当前意图、上下文、可选方案展示给人类
- **可改写**：人类可以编辑 Agent 草拟的结果，而不是只能 Approve/Reject
- **可教学**：人类修改后应当被记录为偏好，反哺模型
- **可降级**：人在 N 秒内没响应时，系统必须有降级策略

### 3. HITL 不是越多越好

HITL 越多越好是常见误区。要避免：

- 所有动作都要审批 → 失去 Agent 价值
- 没有审批 → 上线即翻车

正确做法是**按风险分级审批**：
- 低风险：自动通过
- 中风险：异步通知 + 抽样抽检
- 高风险：同步阻塞审批

## 五、企业级安全实践（13-L32 摘要）

13 课程 L32 总结的企业级安全清单：

1. **认证授权**：每次 Agent 调用都要校验调用方身份
2. **审计日志**：所有副作用动作必须落审计日志，保留 ≥ 180 天
3. **密钥隔离**：模型、工具、外部系统的密钥分桶管理
4. **限流熔断**：单租户单分钟的 QPS 上限、超限自动熔断
5. **数据脱敏**：训练数据、日志、上下文三层都要脱敏
6. **红队测试**：上线前必须做对抗样本测试

## 六、与其他章节的衔接

- 边界三问直接消费 **4.1 需求建模**中定义的能力与角色
- 护栏三类是 **4.3 上下文与提示词**的硬约束
- 行为护栏与 **4.5 工具与能力**强耦合，决定哪些工具可被 Agent 自主调用
- HITL 介入点是 **4.7 评估体系**的关键标注维度

## 七、本节检查表

- [ ] 是否回答了"能力/行为/兜底"三个边界问题
- [ ] 是否同时部署了输入、输出、行为三类护栏
- [ ] HITL 介入点是否按风险分级设计
- [ ] 是否考虑了人在 N 秒内不响应时的降级策略
- [ ] 审计日志、限流熔断、数据脱敏是否到位

## 八、本地索引

- 13 课程 L32《企业安全》
- OpenAI 护栏实践（外部参考）
- 上游：`4.1-需求建模\4.1-需求建模.md`
- 下游：`4.3-上下文与提示词\4.3-上下文与提示词.md`
