> **来源标注**
> - 素材源：13-L07 定义 Agent（人设工程）、13-L08 定义 Task（契约驱动）
> - 素材源：`12-学习笔记\01-极客时间-成为AGI产品经理\02-第二章-AI实战\04-十倍速提升-从发现真需求到MVP.md`
> - 整理时间：2026-06-23
> - 整理目的：第四章第 1 节"需求建模"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.1 需求建模：把模糊的业务诉求变成可被 Agent 理解的契约

## 一、为什么"需求建模"是全流程的第一站

PM 拿到一个需求时，最常犯的错误是**直接开始写 prompt**。结果就是 prompt 越改越长、行为越来越飘，模型既不懂它要服务谁，也不懂什么叫"做完了"。本质原因是缺少一个**可被验证的问题模型**。

需求建模要回答三个根本问题：

1. **谁**会调用 Agent？（角色）
2. **做什么**事才算完成？（任务）
3. **怎样算做好了**？（成功标准）

这三个问题不回答清楚，后面的边界、上下文、规划、工具、记忆、评估、部署全都建立在沙地上。13 课程 L07"定义 Agent"讲的就是"人设工程"，L08"定义 Task"讲的就是"契约驱动"，两者合一构成需求建模的核心。

## 二、问题建模五要素

把上面三个问题进一步拆开，得到 5 个不可省略的建模要素。这套五要素既适用于单步任务，也适用于多步 Agent 流程。

### 1. 角色（Persona / Agent Profile）

角色定义的是"Agent 是谁"，包含：

- **身份**：是客服、医生助手、还是研发工程师？
- **能力边界**：能做什么、不能做什么
- **语气与口吻**：正式、亲和、技术化
- **价值观与原则**：遇到模糊指令时如何取舍

素材源 13-L07 强调：好的 Agent 角色定义等价于"人设工程"，要把人设写得像一份真实的岗位说明书，而不是一个空泛的"你是一个 helpful assistant"。

### 2. 任务（Task）

任务定义的是"要做什么事"，必须满足：

- **动词开头**：分析、生成、改写、检索、调用
- **对象明确**：对什么数据/对象执行
- **目标清晰**：做完之后世界会发生什么变化

素材源 13-L08 强调任务应当是"契约驱动"——把它当成一份给开发者的 ticket，而不是一句自然语言。

### 3. 输入（Input）

输入清单要枚举清楚：

- **结构化字段**：订单 ID、用户 ID、表单值
- **非结构化内容**：用户原始问题、附件
- **隐式输入**：当前时间、用户所在渠道、历史行为
- **可选 vs 必选**：哪些可以缺、缺了怎么降级

### 4. 输出（Output）

输出是"契约"最刚性的一环。必须明确：

- **结构化 schema**：JSON schema / TS interface
- **必填字段与可选字段**
- **枚举值与取值范围**
- **失败时的兜底形态**

把输出结构化是后续"评估体系"和"工具调用"能跑起来的前提。

### 5. 成功标准（Success Criteria）

成功标准是 5 要素中**最容易被跳过**的一项。没有它，模型和 PM 都没有"对齐终点"的依据。建议从三个维度共同定义：

- **准确率维度**：答案正确率 ≥ X%
- **过程维度**：步骤数 ≤ N、关键节点必须命中
- **体验维度**：响应时延 ≤ T、用户改写率 ≤ R

## 三、从需求到 MVP 的 4 步路径

素材源（极客时间《成为 AGI 产品经理》04 讲）给出了一条"十倍速"的实操路径，可以直接套用到 Agent 项目中。

### 第 1 步：找场景

不要一开始就"全场景覆盖"。正确的做法是：

- 列出现有业务流程中的**高频、低门槛、高价值**子场景
- 优先选"如果做砸了不会出人命"的场景作为 MVP
- 把场景写成一句话：「让 [角色] 在 [情境] 下，可以 [动作]，结果是 [价值]」

### 第 2 步：画流程

把场景拆成 3-7 步的线性流程，标出：

- 哪些步骤是 Agent 做的
- 哪些步骤是人在做的（人机协同点）
- 哪些步骤需要外部工具

画流程的目的不是写技术方案，而是**先在纸面上看到哪些环节会被 Agent 颠覆**。

### 第 3 步：做 Demo

Demo 的最低标准：

- 能跑通端到端
- 用真实数据（脱敏后）
- 包含 1-2 个典型失败 case
- 给真实用户用一次，观察反应

### 第 4 步：收敛到 MVP

Demo 跑通后，倒推 MVP 范围：

- 砍掉所有"锦上添花"的能力
- 只保留**最核心的成功标准**
- 明确非目标（Non-goals）
- 给出上线时点和验收口径

## 四、需求建模的常见反模式

| 反模式 | 症状 | 修正 |
|--------|------|------|
| 角色虚空 | prompt 里只写"helpful assistant" | 写岗位说明书级别的角色卡 |
| 任务模糊 | "让 Agent 帮我处理售后" | 拆成"识别退货原因→查询订单→生成回复" |
| 缺成功标准 | "效果还行" | 给出可量化的指标阈值 |
| 输出非结构化 | 自由文本回答 | 强制 JSON schema + 字段约束 |
| 把 demo 当 MVP | 上线即翻车 | 倒推最小可用范围 |

## 五、与其他章节的衔接

- 角色与任务定义直接喂给 **4.2 边界与护栏**（决定 Agent 能做什么、不能做什么）
- 输入/输出契约直接决定 **4.3 上下文与提示词**的 schema
- 任务粒度决定 **4.4 规划与执行**的拆解深度
- 成功标准是 **4.7 评估体系**的种子

## 六、本节检查表

- [ ] 是否为 Agent 写了完整的"岗位说明书"级角色卡
- [ ] 任务是否可以一句话动词开头描述
- [ ] 输入清单是否枚举了所有字段（含可选）
- [ ] 输出是否有 JSON schema 与失败兜底
- [ ] 成功标准是否同时覆盖准确率、过程、体验三维
- [ ] 是否画出从场景 → 流程 → Demo → MVP 的 4 步路径

## 七、本地索引

- 13 课程 L07《定义 Agent（人设工程）》
- 13 课程 L08《定义 Task（契约驱动）》
- 极客时间《成为 AGI 产品经理》04 讲《十倍速提升：从发现真需求到 MVP》
- 相关：`00-方法论总纲\PM设计Agent全流程方法论总纲.md` 第 1 部分第五节"需求建模"
- 下游：`4.2-边界与护栏\4.2-边界与护栏.md`
