> **来源标注**
> - 素材源：13-L02（已复制到 `01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）\1.1-四种基础形态\08-L02-解构智能体-Agent解剖学与ReAct范式.md`）ReAct 范式
> - 素材源：13-L18 Prompt 到 Harness
> - 素材源：Andrew Ng 四大模式（Reflection / Tool Use / Planning / Multi-agent）
> - 整理时间：2026-06-23
> - 整理目的：第四章第 4 节"规划与执行"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.4 规划与执行：把任务拆成可控的步骤

## 一、规划能力是 Agent 与 LLM 的分水岭

普通 LLM 是一次性的"问 → 答"。Agent 与之最大的区别是**它能把一个模糊的目标拆成若干可控的步骤并执行**。这一步叫"规划"（Planning），是 Agent 区别于"套壳 ChatGPT"的核心。

但规划并不是越复杂越好。**规划越复杂，状态空间越大，可控性越差**。本章要解决的就是这条曲线上"在哪一段用什么方法"的问题。

## 二、四大基础规划模式

### 1. CoT（Chain of Thought，思维链）

- **做法**：让模型"一步一步想"
- **优点**：单次推理质量高，零额外工程
- **缺点**：仍然是一次性推理，没有外部反馈
- **适用**：单步推理、数学题、简单分类

### 2. ToT（Tree of Thought，思维树）

- **做法**：在关键分叉点展开多个分支，评估后再选最优
- **优点**：能处理"需要探索多条路径"的问题（如策略游戏）
- **缺点**：token 消耗指数级上升
- **适用**：组合优化、策略选择、复杂推理

### 3. ReAct（Reasoning + Acting）

- **做法**：在每一步中"思考 → 行动 → 观察"循环
- **结构**：Thought → Action → Observation → Thought → ...
- **优点**：能接入工具、能根据反馈调整
- **缺点**：循环不可控时容易"卡住"
- **适用**：需要工具调用、需要外部信息、需多步执行的 Agent

> 素材源 13-L02 详细解构了 ReAct 范式，参考：`01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）\1.1-四种基础形态\08-L02-解构智能体-Agent解剖学与ReAct范式.md`

### 4. Multi-agent（多代理协作）

- **做法**：把任务分给多个 Agent，每个 Agent 负责一部分
- **优点**：上下文隔离、并行执行、专精化
- **缺点**：协调成本高、通信协议复杂
- **适用**：研发流水线、内容生产线、多视角分析

## 三、Andrew Ng 四大设计模式

Andrew Ng 在多篇演讲中反复强调的 Agent 四大设计模式，可以作为"规划能力选型"的口袋书：

| 模式 | 核心问题 | 工程实现 |
|------|----------|----------|
| **Reflection（反思）** | 怎么让模型检查自己的输出？ | Critic Agent / Self-Critique / Reviewer Loop |
| **Tool Use（工具使用）** | 怎么让模型与外部世界交互？ | Function Calling / MCP / Skills |
| **Planning（规划）** | 怎么把目标拆成步骤？ | ReAct / Plan-Execute / Tree of Thought |
| **Multi-agent（多代理）** | 怎么分工协作？ | Sub-agents / Agent Teams / 流水线 |

四大模式是**正交的**，可以叠加。例如："多代理 + ReAct + 工具使用 + 反思"就是一个典型的企业级 Agent 架构。

## 四、从 Prompt 到 Harness：13-L18 的范式升级

素材源 13-L18 提出了一个重要观察：**当 Agent 能力越来越强，反而越要重视 Harness（驾驭工程）**。

这条路径上的演化：

| 阶段 | 关注点 | PM 的工作 |
|------|--------|-----------|
| 纯 Prompt | 模型能听懂的指令 | 写 prompt 模板 |
| ReAct | 模型会"思考+行动"循环 | 设计 Thought/Action schema |
| Harness | 模型在受控环境里跑 | 设计 AGENTS.md、Hooks、Sub-agents |
| 编排 | 多 Agent 协同 | 设计团队、流水线、评审 |

PM 的角色从"写 prompt 的人"升级为"设计 Agent 运行环境的人"。

## 五、规划复杂度与可控性的权衡

规划不是越复杂越好，需要在三个维度之间权衡：

```
规划复杂度
    ▲
    │         ToT
    │        ╱  ╲
    │       ╱    ╲  Multi-agent
    │      ╱      ╲
    │     ╱        ╲
    │    ╱  ReAct   ╲
    │   ╱            ╲
    │  ╱     CoT      ╲
    │ ╱                ╲
    └──────────────────────► 可控性
```

经验法则：

- **能用 CoT 解决就别上 ReAct**
- **能用单 Agent 解决就别上 Multi-agent**
- **每次升级规划复杂度前，先确认"流程图"和"评测指标"两件事都齐了**

## 六、规划失败时的回退机制

规划一定会失败，关键是失败后怎么办。常见回退机制：

1. **重试**：同一规划重试 N 次（注意：需要差异化 prompt 避免死循环）
2. **降级**：从 ReAct 降级到 CoT，或从 Multi-agent 降级到单 Agent
3. **兜底**：当所有重试都失败时，转人工（HITL 介入）
4. **学习**：把失败案例写进评估集（接 4.7）

## 七、与其他章节的衔接

- 规划与执行消费 **4.3 上下文与提示词**的 Harness 架构
- 调用的工具集合来自 **4.5 工具与能力**
- 失败回退机制是 **4.2 边界与护栏**的兜底边界
- 反思与评审是 **4.7 评估体系**的雏形

## 八、本节检查表

- [ ] 是否清楚当前任务属于 CoT / ReAct / ToT / Multi-agent 哪一类
- [ ] 是否设计了 Thought / Action / Observation 的 schema
- [ ] 是否避免了不必要的规划复杂度升级
- [ ] 是否设置了规划失败时的回退路径
- [ ] 是否引入 Reflection 模式做自我修正
- [ ] Andrew Ng 四大模式中至少应用了哪几个

## 九、本地索引

- 13 课程 L02《解构智能体：Agent 解剖学与 ReAct 范式》（已复制到第一章 1.1 节）
- 13 课程 L18《从 Prompt 到 Harness》
- Andrew Ng 四大设计模式：Reflection / Tool Use / Planning / Multi-agent
- 上游：`4.3-上下文与提示词\4.3-上下文与提示词.md`
- 下游：`4.5-工具与能力\4.5-工具与能力.md`
