> **来源标注**
> - 素材源：13-L18-L22 记忆系列五讲（13-L18 从 Prompt 到 Harness、13-L19 上下文生命周期、13-L20 文件系统记忆、13-L21 搜索驱动记忆；L22 暂列）
> - 整理时间：2026-06-23
> - 整理目的：第四章第 6 节"记忆机制"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.6 记忆机制：让 Agent 跨对话、跨任务"记住"

## 一、为什么 Agent 必须有记忆

没有记忆的 Agent 每次都"失忆"——同一个用户问两次"我上周的订单到哪了"，Agent 不会记得上周聊过什么。这是企业级 Agent 与"一次性 demo"最大的差距。

但记忆不是"把对话全存下来那么简单"。**存什么、何时存、怎么读、怎么忘**，是 4 个独立的工程问题。

## 二、短期记忆 vs 长期记忆

记忆必须分两层，因为它们的目标、成本、生命周期完全不同。

| 维度 | 短期记忆 | 长期记忆 |
|------|----------|----------|
| 存储位置 | 上下文窗口 | 外部存储（数据库 / 文件 / 向量库） |
| 生命周期 | 单次会话 | 跨会话、跨用户 |
| 内容 | 当前对话、变量、状态 | 事实、偏好、历史、领域知识 |
| 容量 | 有限（窗口大小） | 几乎无限 |
| 检索方式 | 顺序扫描 | 关键词 / 向量 / 结构化查询 |
| 风险 | 过长会污染上下文 | 过期信息会误导模型 |

## 三、四步模式：加载 / 使用 / 更新 / 处理

记忆的工程化落地可以归纳为**四步模式**：

### 1. 加载（Load）

在 Agent 启动或新会话开始时，把相关的长期记忆**主动加载**到上下文中。

- 加载策略：全量 / 按用户 / 按任务 / 按相似度
- 加载时机：会话开始时、按需触发的"召回"
- 加载限额：避免把上下文撑爆

### 2. 使用（Use）

把记忆作为上下文的一部分，喂给模型推理。

- 提示词中显式标注"以下是你需要记住的事实"
- 区分"用户偏好"和"通用知识"
- 引用时尽量带上时间戳，让模型能判断新鲜度

### 3. 更新（Update）

Agent 在执行过程中，**主动写**新的记忆。

- 写时机：用户表达偏好、Agent 完成重要决策、用户纠正
- 写格式：结构化（用户 ID + 键 + 值 + 时间）
- 写权限：哪些记忆可以由 Agent 自动写、哪些必须经用户确认

### 4. 处理（Handle）

记忆需要"治理"：

- 过期清理
- 冲突解决（同主题不同结论）
- 容量控制（总量上限 / 摘要压缩）
- 隐私脱敏

## 四、记忆的实现形态

素材源 13-L18-L22 给出五种主流的记忆实现：

### 1. 上下文窗口内的短期状态

- **位置**：直接放在 system / user message
- **适用**：当前会话的临时变量
- **风险**：超过窗口就丢

### 2. Session Memory（会话级）

- **位置**：内存或 Redis
- **适用**：单次会话内的多轮对话
- **典型实现**：按 sessionId 存键值对

### 3. File System Memory（13-L20）

- **位置**：本地或云端文件系统
- **适用**：笔记、过程产物、跨会话事实
- **优点**：可读、可调试、容量大
- **典型实现**：用 Markdown / JSON 文件作为"长期记忆的笔记本"

### 4. 搜索驱动记忆（13-L21）

- **位置**：向量库 + 全文索引
- **适用**：海量历史对话、领域知识库
- **实现**：把记忆嵌入为向量，召回时按相似度检索
- **风险**：召回质量受 embedding 模型影响

### 5. 结构化数据库记忆

- **位置**：关系型 / KV 数据库
- **适用**：用户偏好、订单历史、可查询的事实
- **优点**：可精确查询、可关联分析
- **风险**：写入逻辑需要工程实现

## 五、记忆的写入策略

| 策略 | 描述 | 适用 |
|------|------|------|
| 总是写 | Agent 每次都把信息写库 | 重要事实、用户偏好 |
| 显式触发 | 用户说"请记住"时才写 | 隐私敏感 |
| 异步摘要 | 会话结束后异步总结关键点 | 长对话压缩 |
| 抽样写 | 抽 N% 会话入库 | 探索阶段 |

## 六、记忆的检索策略

| 策略 | 描述 | 适用 |
|------|------|------|
| 全量加载 | 把所有记忆都喂给模型 | 记忆量小 |
| 关键词检索 | 按关键词匹配 | 事实明确 |
| 向量召回 | 语义相似度 | 模糊回忆 |
| 结构化查询 | SQL / KV 查询 | 需要精确字段 |
| 混合检索 | 多种策略融合 | 企业级推荐做法 |

## 七、记忆的典型反模式

| 反模式 | 后果 | 修正 |
|--------|------|------|
| 把所有对话都存 | 召回质量差 | 写入前做摘要 |
| 不区分用户 | 隐私泄露 + 错乱 | 用户 ID 隔离 |
| 永不过期 | 旧信息误导 | 设置 TTL |
| 只用向量召回 | 精确查询失败 | 混合检索 |
| 写权限过宽 | Agent 写错污染库 | 写前 HITL |

## 八、与其他章节的衔接

- 短期记忆是 **4.3 上下文与提示词**中的 Compaction 对象
- 长期记忆是 **4.5 工具与能力**的特殊工具
- 记忆写入策略与 **4.2 边界与护栏**的护栏强耦合
- 记忆召回质量直接决定 **4.7 评估体系**的过程指标

## 九、本节检查表

- [ ] 是否区分了短期与长期两层记忆
- [ ] 是否设计了"加载 / 使用 / 更新 / 处理"四步流程
- [ ] 写入策略是否考虑了用户隐私和 HITL
- [ ] 检索是否采用混合策略
- [ ] 是否设置了过期与容量控制
- [ ] 记忆库是否按用户 ID 隔离

## 十、本地索引

- 13 课程 L18《从 Prompt 到 Harness》
- 13 课程 L19《上下文生命周期》
- 13 课程 L20《文件系统记忆》
- 13 课程 L21《搜索驱动记忆》
- 13 课程 L22（暂列）
- 上游：`4.5-工具与能力\4.5-工具与能力.md`
- 下游：`4.7-评估体系\4.7-评估体系.md`
