> **来源标注**
> - 素材源：`12-学习笔记\06-极客时间-AI大模型项目落地实战\05｜提示词：为什么说提示词工程是一切应用的基础？.md`
> - 素材源：`12-学习笔记\06-极客时间-AI大模型项目落地实战\06｜核心：如何打造C语言自动判题系统里的核心提示词？.md`
> - 素材源：`12-学习笔记\04-极客时间-AI原生开发工作流实战\06｜上下文的艺术（上）：详解CLAUDE.md与AGENTS.md.md`
> - 素材源：Anthropic 上下文三支柱（Compaction/Note-taking/Sub-agents）
> - 整理时间：2026-06-23
> - 整理目的：第四章第 3 节"上下文与提示词"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.3 上下文与提示词：从 Prompt Engineering 到 Harness Engineering

## 一、范式跃迁：三次升维

很多人以为"提示词工程"就是写 prompt 模板。但在 Agent 时代，**写 prompt 本身正在被重新定义**。这条演化路径经历了三次跃迁：

```
Prompt Engineering（提示词工程）
   ↓
Context Engineering（上下文工程）
   ↓
Harness Engineering（驾驭工程）
```

每一次跃迁，工程师的核心关注点都在上移。

### 1. Prompt Engineering（2023 前后）

- 关注：怎么把一句指令写得更清楚
- 工具：few-shot、CoT、角色设定
- 局限：模型能力一旦升级，原 prompt 全部失效

### 2. Context Engineering（2024-2025）

- 关注：怎么把对的资料在对的时机递给模型
- 工具：RAG、Memory、Tools、Sub-agents
- 局限：上下文堆得越多越好？实际上信息会互相稀释

### 3. Harness Engineering（2025+）

- 关注：怎么把"Agent 与环境的整套契约"工程化
- 工具：CLAUDE.md / AGENTS.md / constitution.md、Hooks、Skills、MCP
- 核心思想：Agent 不再是"一个 prompt"，而是"一套可被工程化管理的运行环境"

Anthropic 的上下文三支柱（Compaction / Note-taking / Sub-agents）就是 Harness Engineering 的代表性实践。

## 二、Context Engineering：上下文三件套

进入 Context Engineering 阶段后，PM/工程师的工作重心从"写好一句话"变成"组织好上下文"。一套完整的上下文由三部分构成。

### 1. 系统层（System Layer）

- **角色卡**：来自 4.1 需求建模的输出
- **行为准则**：能做、不能做、遇到模糊怎么办
- **输出 schema**：JSON 契约

### 2. 任务层（Task Layer）

- **当前目标**：用户原始诉求
- **任务状态**：已完成步骤 / 剩余步骤
- **中间结果**：每一步的产出

### 3. 知识层（Knowledge Layer）

- **长期记忆**：跨会话保留的事实
- **短期记忆**：当前会话的关键变量
- **外部知识**：RAG 检索结果、工具返回值

三层上下文的拼接顺序很关键：**先系统、再任务、最后知识**。倒过来会让模型被"知识噪声"带偏。

## 三、Harness Engineering 的三大支柱（Anthropic）

Anthropic 在 Claude Code 实践中总结出驾驭 Agent 的三大支柱，也是 Context Engineering 走向成熟的标志。

### 1. Compaction（压缩）

**问题**：长对话会让上下文窗口爆炸，模型开始"失忆"。

**做法**：当上下文达到阈值时，触发"压缩"——把历史对话总结为更短的笔记，但保留关键事实、决策、状态。Compaction 是**显式触发的、可控的、可恢复的**，不能简单"截断"。

### 2. Note-taking（笔记）

**问题**：模型没有"工作记忆"，跨任务的关键信息容易丢。

**做法**：Agent 在执行过程中主动写笔记到外部文件（文件系统 / 数据库），关键决策、用户偏好、待办事项都可以记下来。下一次需要时再读回。

### 3. Sub-agents（子代理）

**问题**：上下文一旦被无关任务污染，模型会"分心"。

**做法**：把大任务拆给多个子代理，每个子代理有**独立的、干净的上下文窗口**。它们之间通过结构化消息通信，而不是共享上下文。这是从工程上解决"上下文污染"的核心思路。

## 四、CLAUDE.md / AGENTS.md：Harness 的入口

参考素材源（极客时间《AI 原生开发工作流实战》06 讲），Claude Code 的 **CLAUDE.md** 与社区标准的 **AGENTS.md** 是 Harness 的入口文件，约定 Agent 在该项目中要遵守的所有"宪法级"规范。

一份合格的 AGENTS.md 通常包含：

- **项目简介**：这个项目是做什么的
- **目录结构**：哪些目录是 Agent 应该关注的
- **开发规范**：命名、测试、提交信息
- **禁区**：哪些目录、哪些命令、哪些文件 Agent 不能碰
- **触发器**：什么时候需要 Hooks 介入
- **可调用的 Skills/MCP**：Agent 拥有哪些能力

PM 在做 Agent 设计时，应当把"AGENTS.md 该写什么"当成 PRD 的一部分。

## 五、提示词工程的基础原则（极客时间素材源回顾）

回到基础：好的 prompt 仍然是一切的起点。素材源（极客时间《AI 大模型项目落地实战》05/06 讲）总结了几条经过实战检验的原则：

1. **任务明确**：把"做什么"写在 prompt 最前面
2. **示例驱动**：复杂任务必须给 1-3 个示例
3. **结构清晰**：用 Markdown 标题、列表区隔不同段落
4. **约束在前、激励在后**：先写不能做什么，再写希望它做什么
5. **可验证**：prompt 里要包含"自我校验"的指令

## 六、Context vs Prompt：什么时候该用哪个

| 场景 | 推荐做法 |
|------|----------|
| 单次问答、简单改写 | 提示词工程 |
| 跨会话有记忆需求 | 上下文工程（短期 + 长期） |
| 多工具、多步骤、长流程 | Harness Engineering（Sub-agents + Compaction + Note） |
| 团队协作、项目级 | AGENTS.md + Hooks + Skills 组合 |

## 七、与其他章节的衔接

- 上下文工程消费 **4.1 需求建模**的角色与任务
- 上下文工程受 **4.2 边界与护栏**的护栏三类约束
- Compaction 与 Note-taking 是 **4.6 记忆机制**的工程化落地
- Sub-agents 是 **4.4 规划与执行**中"任务分解"的具体实现

## 八、本节检查表

- [ ] 是否区分了 Prompt / Context / Harness 三层工程
- [ ] 系统层、任务层、知识层的拼接顺序是否正确
- [ ] 是否设计了 Compaction 触发点与策略
- [ ] Agent 是否有写笔记到外部文件的能力
- [ ] 是否使用 Sub-agents 隔离不同子任务上下文
- [ ] AGENTS.md / CLAUDE.md 是否覆盖项目宪法级规范

## 九、本地索引

- 极客时间《AI 大模型项目落地实战》05 讲《提示词：为什么说提示词工程是一切应用的基础？》
- 极客时间《AI 大模型项目落地实战》06 讲《核心：如何打造 C 语言自动判题系统里的核心提示词？》
- 极客时间《AI 原生开发工作流实战》06 讲《上下文的艺术（上）：详解 CLAUDE.md 与 AGENTS.md》
- Anthropic 上下文三支柱：Compaction / Note-taking / Sub-agents
- 上游：`4.2-边界与护栏\4.2-边界与护栏.md`
- 下游：`4.4-规划与执行\4.4-规划与执行.md`
