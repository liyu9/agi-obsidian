> **来源标注**
> - 素材源：13-L12 工具设计哲学、13-L13 Tools 封装五步法、13-L14 MCP 协议、13-L15 代码解释器、13-L16 Skills 生态
> - 素材源：`12-学习笔记\04-极客时间-AI原生开发工作流实战\12｜终极扩展：深入MCP服务器，将AI连接到任何内外部系统.md`
> - 整理时间：2026-06-23
> - 整理目的：第四章第 5 节"工具与能力"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.5 工具与能力：让 Agent 真正"做事"

## 一、工具是 Agent 的手和脚

LLM 本身只能"说话"，不能"做事"。Agent 之所以能落地，关键在于它能**调用工具**——查数据库、发请求、读写文件、执行代码。工具是 Agent 真正接触业务系统的接口。

但工具的封装质量直接决定 Agent 的成功率。**描述不清的工具，模型调不对；粒度太粗的工具，模型不会用**。

## 二、工具三要素

每个工具都应当明确三个要素：

### 1. 名称（Name）

- **唯一**：在同一 Agent 内不能重名
- **可读**：一看就知道做什么（`search_orders` 比 `s1` 好）
- **动词性**：用动词或动宾结构（`create_user`、`get_weather`）

### 2. 描述（Description）

这是模型判断"该不该调这个工具"的唯一依据，必须包含：

- **做什么**：一句话说清能力
- **什么时候用**：典型使用场景
- **什么时候不用**：边界与替代方案
- **使用后果**：是否会写数据、是否会扣费

### 3. 参数 Schema（Parameters）

用 JSON Schema 严格定义：

- **必填字段** vs **可选字段**
- **类型与取值范围**
- **嵌套结构**：对象、数组的层级
- **示例值**：模型最容易理解的是给一个 example

## 三、Tools 封装五步法（13-L13）

13 课程 L13 给出了一套工具封装的实操流程：

1. **识别能力**：从需求出发，列出 Agent 必须做的事
2. **拆分粒度**：把"大事"拆成"小事"，每个工具只做一件事
3. **设计 Schema**：用 JSON Schema 严格定义输入输出
4. **实现与测试**：单测 + 集成测试 + 对抗性测试（看模型是否调对）
5. **接 Agent 框架**：注册到 Agent 的工具表，配置调用日志

## 四、三大工具协议演进

工具调用协议经历了三代演进。

### 1. Function Calling（原生函数调用）

- **原理**：模型直接输出结构化的"调哪个函数 + 参数"
- **代表**：OpenAI Function Calling、Anthropic Tool Use
- **优点**：零额外组件，原生支持
- **缺点**：每个 Agent 框架有自己的 schema 协议，跨平台难

### 2. MCP（Model Context Protocol）

- **原理**：模型与工具之间通过统一的 MCP 协议通信，工具以"Server"形式独立部署
- **代表**：Anthropic MCP 协议
- **优点**：标准化、可复用、生态化
- **缺点**：需要额外部署 MCP Server

> 素材源（极客时间《AI 原生开发工作流实战》12 讲）深入讲解了 MCP 的架构与部署：
> `12-学习笔记\04-极客时间-AI原生开发工作流实战\12｜终极扩展：深入MCP服务器.md`

### 3. Skills（技能封装）

- **原理**：把"多个工具 + 提示词 + 流程"打包成一个 Skill，Agent 拥有 Skill 后等于"掌握了一项能力"
- **代表**：Claude Code Skills 体系
- **优点**：高内聚、可发现、可教学
- **缺点**：与具体 Agent 框架耦合

> 三大协议并不互斥。一个成熟的 Agent 运行时往往是三者并存：
> - 原子操作用 Function Calling
> - 跨系统集成用 MCP
> - 复合能力用 Skills

## 五、工具原子化原则

工具设计的最大反模式是"万能工具"——一个接口承担 10 个职责。正确做法是**原子化**：

- **单一职责**：一个工具只做一件事
- **可组合**：原子工具通过编排组合成复杂流程
- **可测试**：每个原子工具都要有独立的测试
- **可观测**：调用日志、参数、返回值、耗时都要落库

举例：不要做 `process_order(order)`，而要拆成 `validate_order`、`charge_payment`、`update_inventory`、`send_notification`。

## 六、代码解释器：把"会写代码"变成工具（13-L15）

13 课程 L15 专门讲了"代码解释器"作为工具的特殊价值：

- **能力**：让 Agent 拥有"读代码—写代码—执行代码"的完整闭环
- **场景**：数据清洗、报表生成、文件转换、自动化脚本
- **风险**：代码执行是**高风险操作**，必须放在沙箱里
- **护栏**：禁止访问内网、禁止读敏感文件、限制执行时长

## 七、Skills 生态：把"团队经验"沉淀为可复用资产（13-L16）

Skills 是 Claude Code 体系中最高阶的工具抽象，本质是把"团队 know-how"封装成可被 Agent 调用的能力包。

一份合格的 Skill 包含：

- **SKILL.md**：使用说明、触发条件、输入输出
- **scripts/**：可执行脚本
- **examples/**：典型用法
- **tests/**：回归测试

PM 的视角：Skills 库是 Agent 团队的"能力货架"，随着项目演进持续积累。

## 八、工具的设计反模式

| 反模式 | 后果 | 修正 |
|--------|------|------|
| 万能工具 | 模型不会用 | 拆成原子工具 |
| 描述模糊 | 模型调错工具 | 写明"什么时候用 / 不用" |
| 无 schema | 模型乱填参数 | 严格 JSON Schema |
| 无幂等性 | 重试导致重复副作用 | 设计幂等键 |
| 无降级 | 工具挂了 Agent 整个挂 | 设计降级路径 |

## 九、与其他章节的衔接

- 工具调用受 **4.2 边界与护栏**的行为护栏约束
- 工具是 **4.4 规划与执行**中 Action 的具体实现
- 代码解释器是 **4.8 部署可观测与商业化**中算力成本的重要来源

## 十、本节检查表

- [ ] 每个工具是否同时具备名称、描述、Schema 三要素
- [ ] 是否避免了"万能工具"，保持原子化
- [ ] 工具是否做了幂等设计
- [ ] 是否规划了 Function Calling / MCP / Skills 三类协议的使用场景
- [ ] 代码解释器等高风险工具是否放在沙箱中
- [ ] Skills 库是否开始积累团队 know-how

## 十一、本地索引

- 13 课程 L12《工具设计哲学》
- 13 课程 L13《Tools 封装五步法》
- 13 课程 L14《MCP 协议》
- 13 课程 L15《代码解释器》
- 13 课程 L16《Skills 生态》
- 极客时间《AI 原生开发工作流实战》12 讲《终极扩展：深入 MCP 服务器》
- 上游：`4.4-规划与执行\4.4-规划与执行.md`
- 下游：`4.6-记忆机制\4.6-记忆机制.md`
