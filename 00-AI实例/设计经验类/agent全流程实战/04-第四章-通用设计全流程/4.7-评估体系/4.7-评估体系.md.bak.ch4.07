> **来源标注**
> - 素材源：`12-学习笔记\06-极客时间-AI大模型项目落地实战\17｜评估：如何对电商客服模型专有训练进行能力测评？.md`
> - 素材源：`12-学习笔记\06-极客时间-AI大模型项目落地实战\19｜奖励模型：为什么要用评估专家及其设计方案.md`
> - 素材源：13-L36 评测体系、13-L38 线上监控
> - 整理时间：2026-06-23
> - 整理目的：第四章第 7 节"评估体系"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 4.7 评估体系：怎么知道 Agent 做得好不好

## 一、为什么必须有评估体系

很多团队 Agent 做了一版就跑上线了，靠用户反馈迭代。这是把"用户当评测集"，代价是品牌信任。**没有评估体系，Agent 就是黑盒跑黑盒**。

评估体系要回答三个问题：

1. **离线**怎么测？（Golden Dataset + LLM-as-Judge）
2. **在线**怎么监控？（实时指标 + 异常告警）
3. **迭代**怎么知道有没有进步？（指标前后对比）

## 二、指标三级：任务 / 步骤 / 过程

素材源 13-L36 把 Agent 的评估指标分成三级，PM 应当**三级都设**，不能只看任务级。

### 1. 任务级（Task-level）

- **定义**：最终结果是否正确
- **典型指标**：答案准确率、任务完成率、用户满意度
- **优点**：直观、容易对齐业务
- **缺点**：无法定位"哪一步错了"

### 2. 步骤级（Step-level）

- **定义**：多步任务中每一步是否正确
- **典型指标**：工具调用准确率、参数正确率、状态机命中
- **优点**：能定位失败环节
- **缺点**：需要把 Agent 内部 trace 完整落库

### 3. 过程级（Process-level）

- **定义**：执行过程是否合理
- **典型指标**：步骤数、Token 消耗、循环次数、重试率
- **优点**：反映效率与稳定性
- **缺点**：与最终结果不完全相关

> 经验法则：任务级决定"要不要上"，步骤级决定"哪里要改"，过程级决定"要不要优化"。

## 三、离线评估：Golden Dataset

### 1. 什么是 Golden Dataset

Golden Dataset 是一组**带标准答案**的测试集，包含：

- 输入（用户问题 / 任务描述）
- 期望输出（正确答案 / 期望行为）
- 期望路径（可选：期望经过哪些步骤、调用哪些工具）

### 2. 构造方法

- **真实案例**：从历史对话中脱敏采样
- **人工编写**：专家按边界场景设计
- **对抗生成**：用 LLM 生成对抗样本，再人工校对
- **用户反馈**：把"用户改写后满意"的对话入库

### 3. 黄金集的维护

- **覆盖率**：覆盖主要场景 + 边界场景 + 失败模式
- **新鲜度**：定期更新，避免被模型"背答案"
- **规模**：起步 200-500 条，迭代到 2000+ 条

## 四、在线评估：LLM-as-Judge

离线 Golden 集合规模有限，不能覆盖所有线上情况。**LLM-as-Judge** 用一个强模型当"裁判"，对线上结果打分。

### 1. 评分维度

- **事实正确性**：答案是否包含事实错误
- **指令遵循度**：是否完成了用户要求
- **风格契合度**：语气、用词是否符合人设
- **安全性**：是否越权、是否泄露

### 2. 评分 prompt 设计要点

- **给量表**：1-5 分或 1-10 分
- **给维度**：分维度独立打分
- **给示例**：附正例反例
- **强制结构化**：JSON 输出便于聚合

### 3. 评估专家（奖励模型）

素材源（极客时间 19 讲）介绍了"评估专家"的设计：

- 用专门训练的 Reward Model 替代通用 LLM 当 Judge
- 优点：评分稳定、一致性高、可解释
- 缺点：需要标注数据训练

### 4. LLM-as-Judge 的局限

- **位置偏差**：模型偏好放在前面的答案
- **长度偏差**：模型偏好更长的回答
- **自我偏好**：模型偏好自己生成的答案

修正方法：随机化位置、长度归一化、用多个 Judge 取平均。

## 五、线上监控（13-L38）

线上监控关注**实时性**与**可发现性**。

### 1. 关键指标

- **可用性**：P99 延迟、错误率
- **质量**：在线 Judge 平均分、用户改写率
- **成本**：Token 单价 × 调用次数 = 每日成本
- **安全**：越权调用次数、敏感词命中数

### 2. 异常告警

- 指标突变（与昨日同时段对比 ±X%）
- 错误率超过阈值
- 关键链路环节连续失败
- 用户负面反馈激增

### 3. 反馈闭环

- 用户 thumbs up/down
- 抽样人工 review
- 评估集自动扩充
- 模型 / Prompt 迭代

## 六、A/B 测试与回滚

评估体系最终要支持**对照实验**：

- **A/B 测试**：新旧版本同时跑，按指标分流量
- **灰度发布**：1% → 10% → 50% → 100%
- **快速回滚**：指标劣化时一键回到上一版本
- **实验记录**：每次实验的目标 / 假设 / 结果都要落档

## 七、与其他章节的衔接

- 评估指标直接消费 **4.1 需求建模**的成功标准
- LLM-as-Judge 需要 **4.3 上下文与提示词**提供的 Judge Prompt
- 步骤级评估依赖 **4.4 规划与执行**的 Trace 数据
- 线上监控是 **4.8 部署可观测与商业化**的子模块

## 八、本节检查表

- [ ] 是否同时设计任务 / 步骤 / 过程三级指标
- [ ] 是否建立了 ≥ 200 条的 Golden Dataset
- [ ] LLM-as-Judge 的 prompt 是否分维度 + 量表
- [ ] 是否设置了线上告警与回滚机制
- [ ] 用户反馈是否进入评估闭环
- [ ] 是否做过至少一次 A/B 实验

## 九、本地索引

- 极客时间《AI 大模型项目落地实战》17 讲《评估：如何对电商客服模型专有训练进行能力测评？》
- 极客时间《AI 大模型项目落地实战》19 讲《奖励模型：为什么要用评估专家及其设计方案》
- 13 课程 L36《评测体系》
- 13 课程 L38《线上监控》
- 上游：`4.6-记忆机制\4.6-记忆机制.md`
- 下游：`4.8-部署可观测与商业化\4.8-部署可观测与商业化.md`
