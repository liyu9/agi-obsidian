> **来源标注**
> - 素材源：12-学习笔记\07-极客时间-跟月影学前端智能体开发\01｜如何在前端调用文本大模型API.md
> - 素材源：12-学习笔记\07-极客时间-跟月影学前端智能体开发\02｜如何使用流式传输减少等待时间.md
> - 素材源：12-学习笔记\07-极客时间-跟月影学前端智能体开发\03｜如何在前端调用图像大模型API.md
> - 素材源：12-学习笔记\07-极客时间-跟月影学前端智能体开发\04｜如何在前端调用语音合成和视觉模型.md
> - 素材源：12-学习笔记\07-极客时间-跟月影学前端智能体开发\09｜用户体验：如何解决流式传输与JSON结构化的矛盾.md
> - 整理时间：2026-06-23
> - 整理目的：第八章第 8.1 节"前端智能体架构"
> - 说明：本节为"叙述+本地索引"形态，整合本地课程要点，不复制原文。

---

# 8.1 前端智能体架构

## 一、本节定位

本节聚焦**前端工程师视角下的智能体架构**。如果说第二~六章的 Agent 方法论是"语言无关、平台无关"的通用框架，那么本章 8.1 的核心问题是：**当一个智能体必须运行在浏览器里、必须忍受 HTTP / JSON / 浏览器沙箱这些约束时，整体架构长什么样？**

前端 Agent 的特殊性来自四个不可回避的现实：

1. **网络边界**：浏览器只能主动发请求，所有"大模型推理"必须通过 HTTP API 或 WebSocket 完成，前端不是推理的宿主。
2. **CORS 与 Key 暴露**：API Key 一旦放在前端等于公开，需要在 Server 端做一层 BFF（Backend For Frontend）代理。
3. **流式优先**：大模型推理时间以秒计，前端不能"等齐再渲染"，必须 SSE / Fetch Stream。
4. **JSON 封闭性**：模型输出的 JSON 是封闭结构，前端解析必须"等齐"，这与流式渲染天然矛盾。

这四个特殊性共同决定了前端 Agent 的整体架构：**前端薄壳 + BFF 中转 + LLM API 集群**。

## 二、整体架构：三层视图

把 01-04 讲中实际跑过的"调 Deepseek、调 Moonshot、调 FLUX、调火山 TTS"四类调用串起来，可以得到一个稳定的前端 Agent 三层架构：

```
┌──────────────────────────────────────────────────────────┐
│  第一层：浏览器（View 层）                                  │
│  - UI 渲染、用户输入、富文本展示                              │
│  - 维护 session / memory / timeline 等前端状态             │
│  - 处理 SSE / Fetch ReadableStream 流                     │
│  - 使用 jsonuri + JSONParser 动态还原 JSON                │
└──────────────────────────────────────────────────────────┘
                          ▲          ▲
                          │SSE/POST  │SSE/POST
                          ▼          ▼
┌──────────────────────────────────────────────────────────┐
│  第二层：BFF / Edge 函数（Server 层）                       │
│  - 代理大模型 API，隐藏 API Key                            │
│  - 合并多模型调用（文本 + 图像 + 语音）                      │
│  - 实现读写分离、EventChannel 异步通知                      │
│  - 实现 Ling 工作流节点的"汇聚与再分发"                     │
└──────────────────────────────────────────────────────────┘
                          ▲          ▲
                          │HTTPS     │HTTPS
                          ▼          ▼
┌──────────────────────────────────────────────────────────┐
│  第三层：模型 API 集群（Model 层）                          │
│  - 文本大模型：Deepseek / Moonshot / 通义千问 / GPT 系列   │
│  - 图像大模型：FLUX / Stable Diffusion / 火山视觉          │
│  - 语音模型：火山 TTS / 阿里 CosyVoice                    │
│  - 工具/嵌入模型：bge-m3、text2vec 等                     │
└──────────────────────────────────────────────────────────┘
```

这张图对应**前端特殊性**下的"端到端"视图：

- **View 层**只关心两件事：拿数据、画界面；**绝不直接放 API Key**。
- **BFF 层**是前端的"智能体运行时"，在这里把多模型输出按 JSON 协议汇聚成单一 SSE 流推回浏览器。
- **Model 层**完全是黑盒，前端只关心协议（OpenAI 兼容、Coze、火山私有协议）。

> **与第二~六章的对应**：本节不重复"Agent = LLM + Planning + Memory + Tool Use"那一套通用公式，而是把这套公式**落到 HTTP 请求的物理形态**上。

## 三、文本大模型的浏览器调用范式

### 3.1 最小可运行闭环（Deepseek 为例）

参考 01 讲，浏览器端调文本大模型的"Hello World"路径如下：

1. 注册 [Deepseek Platform](https://platform.deepseek.com/)，拿到 API Key（注意 Key 在浏览器侧**仅用于开发演示**，生产必须迁到 BFF）。
2. 创建 Vue+Vite+TypeScript 项目（Trae Builder 一键生成项目骨架）。
3. 配置 `.env.local`，把 Key 放在 `VITE_DEEPSEEK_API_KEY`。
4. `fetch('https://api.deepseek.com/chat/completions', { ... })`，body 严格遵循 OpenAI 兼容协议：
   - `model`：模型名（如 `deepseek-chat`）
   - `messages`：system + user 的消息数组
   - `stream`：是否流式
5. 拿到 `data.choices[0].message.content` 渲染即可。

> **关键认知**：Deepseek / Moonshot / 通义千问 / GPT-4 / 智谱 GLM / Ollama（本地）几乎都遵循 **OpenAI Chat Completion 协议**。前端工程师只要**学会一套**，就能切换到任意一家。这是前端 Agent 在协议层的最大红利。

### 3.2 为什么必须流式

参考 02 讲。大模型的标准模式是"提交数据 → 服务端推理 → JSON 整体返回"。问题在于**推理耗时和 token 数线性相关**：写一篇千字作文可能要 30 秒以上，如果等齐再返回，前端体验不可用。

主流大模型平台默认采用 SSE（Server-Sent Events）流式响应，**逐 token 实时返回**。前端处理路径：

1. `fetch` 的 `response.body` 是一个 `ReadableStream<Uint8Array>`。
2. 用 `TextDecoder` 解码成字符串。
3. 按 SSE 协议按行解析（每行以 `data: ` 开头，`data: [DONE]` 结束）。
4. 每解析出一段 `delta.content` 就触发一次 UI 更新。

这套范式在 02 讲里给出过完整代码，**本质上是把"HTTP 响应"当成"事件流"来消费**——这是后续 Ling 框架、MCP 工具调用、Agent 多步推理统一的传输底座。

## 四、多模态在前端的实现

### 4.1 图像生成：FLUX + 轮询

参考 03 讲。图像生成和文本生成的关键差异是：

| 维度 | 文本 | 图像 |
|------|------|------|
| 输出格式 | 增量 token | 整张图 URL |
| 耗时 | 1-30 秒 | 5-60 秒 |
| 协议 | SSE 流式 | 提交任务 → 轮询状态 |
| 失败重试 | 增量无意义 | 必须可重试 |

FLUX 的典型调用模式是：

1. POST 提交 prompt → 拿到 `task_id`。
2. 轮询 GET 任务状态，状态包括 `Pending` / `Ready` / `Failed`。
3. Ready 时从响应中取 `sample`（图片 URL）。
4. 前端 `<img src="...">` 渲染，并展示进度条（`progress` 字段）。

> **前端特殊性**：图像生成在浏览器侧的"等待感"比文本更强，必须设计**显式的进度反馈**（如进度条、占位骨架屏、已耗时提示），否则用户会误以为页面卡死。

### 4.2 语音合成：火山 TTS 私有协议

参考 04 讲。语音合成的特殊性在于**音频流是二进制**。火山引擎 TTS 的典型做法是：

1. 前端发起 POST，body 含文本 + 音色 ID（`VITE_CLUSTER_ID`）+ APP ID + AccessToken。
2. 服务端返回二进制音频流（PCM/MP3）。
3. 前端用 `AudioContext.decodeAudioData` 解码 → `AudioBufferSourceNode` 播放。

> **前端特殊性**：浏览器对音频自动播放有严格限制，必须在**用户首次交互后**才能播放音频；移动端还要处理 iOS Safari 的 AudioContext 唤醒问题。

### 4.3 视觉理解（图片理解）

参考 04 讲。视觉理解模型（多模态大模型）通常接收 **base64 图片 + 文本 prompt**，返回文本结果。技术上仍是 OpenAI 兼容的 chat 接口，区别只是 `messages` 里的 `content` 数组允许混合 `type: "image_url"` 和 `type: "text"`。

```
messages: [
  {
    role: "user",
    content: [
      { type: "text", text: "这张图片里有什么？" },
      { type: "image_url", image_url: { url: "data:image/png;base64,xxx" } }
    ]
  }
]
```

这是前端 Agent 做"拍照记单词"、"AI 面试官"等场景的**基础协议**。

## 五、流式与 JSON 的结构性矛盾

### 5.1 矛盾的来源

参考 09 讲。这是前端 Agent **最经典**的工程难题：

- **流式**：用户体验要求"逐 token 渲染"。
- **JSON**：前端数据结构要求"封闭、完整、可解析"。
- **矛盾**：JSON 在 `}` 到达前是非法数据，传统 `JSON.parse` 必失败。

### 5.2 解决思路：动态 JSON 解析

业内三派做法：

| 方案 | 优点 | 缺点 |
|------|------|------|
| NDJSON（每行一个 JSON） | 简单 | 限制模型输出，影响推理 |
| JSONStream 库 | 通用 | 仅 Server 端可用 |
| **动态 JSON Parser**（Ling 方案） | 真正流式、前端可用 | 需要自研或依赖 Ling |

Ling 框架中的 `JSONParser` 思路：

1. 把 JSON 当成**树形结构**，每个节点用一个 URI（如 `/outline/0/title`）定位。
2. 流式到达的每段 token，按"当前是否在某个 key / value 内"更新对应 URI 的状态。
3. 通过 `data` 事件 emit `{ uri, delta }`，前端订阅即可。
4. 前端用 `jsonuri` 库把 URI 解析回 JS 对象，**不需要等齐**。

### 5.3 落到代码：移除 Node 依赖

Ling 的 parser 原生依赖 `node:events`，要在浏览器使用需要替换为自定义 `EventEmitter`：

```
class EventEmitter {
  private listeners: { [key: string]: ((...args: any[]) => void)[] } = {};
  on(event: string, listener: (...args: any[]) => void) { ... }
  emit(event: string, ...args: any[]) { ... }
}
```

替换后，**整套 JSON 流式解析逻辑可以在浏览器侧运行**——这正是 Ling 后续能直接在 Vue 项目里"边推边渲"的前提。

## 六、9 讲小结：流式 JSON 的设计模式

09 讲把上面这套范式总结成一个可复用的设计模式：

1. **服务端**：模型输出流式 JSON → 用 JSONParser 切成 `{uri, delta}` 事件 → 用 SSE 推到浏览器。
2. **浏览器**：订阅 SSE → 用 jsonuri 把增量应用到本地对象 → Vue 响应式触发 UI 更新。

> 这个模式在 11 讲 Ling 框架、23 讲多轮对话、37-38 讲 RAG 中**反复出现**，是前端 Agent 工程化的"母模式"。

## 七、与方法论总纲的对应

| 总纲方法论 | 本节前端特殊化 |
|------------|---------------|
| Agent = LLM + Planning + Memory + Tool | View（前端壳） + BFF（智能体运行时） + Model（LLM API） |
| Tool Use | Tool 全部走 BFF 代理，浏览器只发业务请求 |
| Memory | 前端维护 session / timeline / localStorage，BFF 维护 Redis |
| 流式输出 | SSE / Fetch ReadableStream，前端用 jsonuri 还原 JSON |
| 多模态 | 文本/图像/语音各自一套协议，但都过 BFF 统一出口 |

## 八、本地索引

- [01｜如何在前端调用文本大模型API](../../../../12-学习笔记/07-极客时间-跟月影学前端智能体开发/01｜如何在前端调用文本大模型API.md)
- [02｜如何使用流式传输减少等待时间](../../../../12-学习笔记/07-极客时间-跟月影学前端智能体开发/02｜如何使用流式传输减少等待时间.md)
- [03｜如何在前端调用图像大模型API](../../../../12-学习笔记/07-极客时间-跟月影学前端智能体开发/03｜如何在前端调用图像大模型API.md)
- [04｜如何在前端调用语音合成和视觉模型](../../../../12-学习笔记/07-极客时间-跟月影学前端智能体开发/04｜如何在前端调用语音合成和视觉模型.md)
- [09｜用户体验：如何解决流式传输与JSON结构化的矛盾](../../../../12-学习笔记/07-极客时间-跟月影学前端智能体开发/09｜用户体验：如何解决流式传输与JSON结构化的矛盾.md)
