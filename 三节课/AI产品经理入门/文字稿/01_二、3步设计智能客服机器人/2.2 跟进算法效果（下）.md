其实AI产品经理跟进算法需求的步骤 会根据不一样的业务场景 和公司的组织架构有一定的差异。比如说如果你们公司有自己的算法中台 算法中台上已经有很多算法的原子能力。那么你新提的算法需求 首先要看一下算法中台现有的能力能否满足。

如果满足不了再来做新的需求。如果说你当前正要做的这个算法的场景是很复杂的。那么可能在需求评估阶段已经需要算法工程师来介入。一起来评估这个需求的复杂度。进行拆分以后有一些子的算法需求出来。来明确子算法需求的输入输出是什么。

所以这边会给到三个通用型的步骤。如果你这边要做一个新的算法需求 可以匹配一下通用型的步骤 根据不一样的情况进行灵活的调整。第一步来提出算法需求。作为A产品经理。如果你已经很明确这个算法需求是要做什么了。

那么你可以明确一下这个需求它的一个输入输出是什么。给到算法工程师。并且明确一下当前要做这个算法需求应该实现的业务指标是什么 这个指标达到什么样的一个值 才能满足上限的一个要求。第二点 持续的进行效果的一个跟踪。

你需要持续的跟算法工程师进行沟通 来了解他的实现方式是什么。并且给他提供一些训练算法模型需要的标注数据。持续的在跟进他的一个实验的结果。最后进行一个多数据源的验证。验证ok以后再进行上线。第三步 持续进行效果优化。在算法需求上线以后。

产品经理这边需要持续的去收集一些bad。case。来配合算法工程师来训练他的模型。提升我们的业务指标。这边举一个例子。某企业在查看用户对话记录的时候。发现经常用户有负面情绪的时候。机器人回复答案依旧是就事论事的回复。

这样整体感觉对话就会比较生硬。比如说下面这个场景 用户说要命了 我的衣服已经寄回去七天了 你还不给我退钱 我要投诉你了。那机器人还是回答你的订单 我们客服已经收到货了 正在处理中。其实我们想一下这个场景。如果你是在京东或者在淘宝上。

你去找一个店家的人工客服来投诉。说你媳服已经寄回去七天了。怎么还不退钱。那个客服他大概率会先跟你说一下。亲 您不要生气 然后小二马上帮您查查看 再给您说一下。当前这个订单我们已经收到货了 正在处理中 马上就会给您退钱的 请您放心。

这样的话用户的体感就会好很多。所以期望在机器人应答的流程中 也能够添加一些情感识别工具。当用户提的这个问题明显带有情绪的时候 机器人首先予以安抚 再给出答案。针对情感识别这个算法需求 我们可以明确这个需求的输入就是用户的问题。

它的输出就是用户的一个情绪值是中性的 正面的还是负面的。当然现在市面上有一些智能客服机器人 在负面情绪里面还会细分一下他是焦虑还是生气。但是本次举例的时候 我们会把所有的负面情绪都归到负面这一类里面。

情绪识别这个算法模型上线的时候 我们期望总的一个模型的情绪识别准确率能够达到90%以上。经过技术调研我们发现业内来实现情感识别主要是使用分类算法。也就是将用户的每一个问题都定位到一个情绪的分类中。下面我们简单介绍一下什么是分类算法。

分类算法分为二分类和多分类。二分类会比较简单。这个模型前期比如说他学习了大量的黑羊和白羊的照片 训练了一个算法模型。这个模型它自己的一个作用就是你给我一次羊 我告诉你这个羊是黑羊还是白羊。

接下来来了一只羊是个黑羊 然后这个模型也判断它是一个黑羊 那么这个模型的判断就是正确的这是二分类的场景。我们要实现的情感识别 需要将用户的问题归类到正面 负面和中性里面去。就是一个三分类的问题 或者说叫一个多分类的问题。

在多分类的这个问题场景下。模型需要去学习的一些标注数据时。当你给我了2万个问句。这些问句你人为标注分别都是正面。负面还是中性的那算法去学习你这个标注的数据。学习完成以后。我训练了一个算法模型。这个模型的作用就是你接下来给我一个句子。

我模型就会告诉你这个句子经过我的判断。它是正面的还是负面的。还是中性的一个情感。这里稍微提一句 作为A产品经理。你为什么应该要了解一下技术是用的什么样的算法。或者是说用一个什么样技术来实现的。

还是以情绪识别为例 目前我们是用的分类模型对用户的问句进行一个归类。但是有这样一个场景。两个句子 A句子我抓狂了 我要去315投诉。B句子到底行不行。我很着急。其实A句子比B句子从我人为的感知上来讲 它要负面的多。

因为用户马上就要去投诉了 他已经很生气了。但是算法模型给出的结果 A句子和B句子他的打分其实是相差不多的 甚至于有可能都是一样的。那这是为什么。

因为我们用的是分类模型 A句子和B句子我们都把它归到负面这一类 而且我给他的知信度都相对比较高的。所以说你不能因为A句子负面的情绪值更高 B句子负面的情绪值相对低一点。你就觉得算法模型应该给A的分更高 B的分更低。

作为一个A产品经理 在这方面对这个技术是要有一定理解 并且能够给到一些可解释性的。那么针对情绪识别的这个需求 我们要提供标注数据。作为产品经理你应该需要理解 标注数据的准确率就是算法模型能够实现效果的一个上限值。比如说你标注了2万条问句。

这2万条问句里面有95%是标注正确的。还有5%是不正确。或者是说模棱两可的。那么模型它最多能够达到的就是一个95% 当然可能还达不到 也就是它的上限值也就到了95%。因为它就学习的是你人工给我的一个标注的一个结论。

所以我们在做数据标注的时候。通常是比较建议同一批语料至少会有两个人来进行标注。最后我们取一个交集 当两个人不一致的部分 我们再进行一个核对跟校验 这就是提供标注数据的一个重要性。

作为AI产品经理 在跟进算法实验结果的过程中 首先你需要掌握常见的衡量算法效果的指标有哪些。还是以刚才情绪识别这个需求为例 我们提供了2万的标注数据给到算法工程师这边。我们是会建议其中80%的数据用于训练集 20%的数据用于验证集。

什么意思呢。比如说2万条数据里面应该有16000条数据用于算法去训练一个分类模型。还有4000条数据用于对这个模型的效果进行一个测试。此处我们引入一个混淆矩阵的一个概念 根据算法模型来预测正向情绪的一个结果。

得到这样四个基础的指标 把它放到混淆矩阵的这个表格里面。比如说在4000条测试集里面。真实值是正向情绪的句子模型也认为它是正向的数量。我们把它记为tp放在表格的左上角。

那4000条测试集里面真实应该是正向情绪模型认为它是负向或者是中性的数量。我们把它记为F把它放在这个表格的左下角。在4000条测试集里面。它的真实值应该是负向或者是中性。但是模型认为它是正向情绪的这种句子的数量。我们把它记为F。

P放在表格的右上角。那4000条测试集里面真实值应该是负项或者是中性的句子。模型认为是负项或者是中性的句子的数量。我们把它记为tn放在表格的右下角。将这四个指标一起呈现在表格中。就得到了这样一个混淆矩阵。

那通过这个混淆矩阵其实你也能看得出来 左上角的tp和右下角的tn就是模型预测正确的数量。下面我们看一下正向 负向 中性三种分类模型计算的一个结果 混淆矩阵是怎么样的。首先模型预测它为正向并且真实也是正向的句子是有1435句。

那模型预测它是负向并且它真实也是负向的句子有5643句。模型预测它是中性真实确实也是中性的句子有3247条。那么我们就可以计算出来 实际上这个模型总的预测的一个准确率应该在98%以上。这个效果还是比较喜人的。

但是具体在每一个业务场景里面会有一定的差异。首先我们回溯一下为什么要做这个需求。我们是期望用户在表达了一些负向或者是说正向情绪的时候。我们先给到用户一个情绪的响应。再回答当前用户的一个问题。

比如说当前用户他本身就是有负向情绪的 但是我把它识别成了一个正向情绪的。我们从表格里看出来应该是有十个。那这种句子用户本来已经有负向情绪 还识别成正向情绪。比如说我还给了他一个很正向的响应 那这种场景其实就会很难受。

即便是用户有负向情绪 我把它识别成了中性 那大不了就是没有给用户一个响应 没有给用户一个安抚 直接回复了答案。但是如果用户已经有负向情绪 你把它识别成正向 还给了他一个积极热烈的响应 那这种肯定是不能接受的。

我们再来看 如果说当前用户是有正向情绪 但是模型把它预测成了负向情绪 给了他一句安抚。比如说用户已经表达了感谢 或者是表达了一些比较开心的一些状态的情绪的时候 但是模型把它判断了一是个负向情绪。

还给了他一句安抚 亲 您不要着急 我们马上帮您看一下。这个在业务场景里面就会感觉违和感很重。所以这个混淆矩阵呈现出来以后。具体参考哪一个指标。这个是根据业务场景会有一些差异的。还是拿刚刚有说到的这个场景为例。

我们如果仅仅是想用户有负向情绪的时候进行安抚 那这个结果没问题。但如果你想添加上有正向情绪的时候进行鼓励。负向情绪的时候进行安抚。那就要考虑一下你当前这个业务场景能不能接受正向的时候预测成负向。负向的时候预测成正向。

有这种偏差了 得到了正向 负向。中性它们分别的一个混淆矩阵以后。我们需要引入三个算法指标。首先是准确率 算法认为对的样本里面有多少确实是对的 它所占的比率是多少。还是以正向情绪为例。算法认为对的样本一共有1435加30就是1465。

确实是对的 一共有1435。所以针对正向情绪算法的一个准确率就是1435除以1465。那么召回率是本来是对的样本里面算法找出来了多少是对的所占的比率。在正向情绪的这个混淆矩阵里面。我们可以看出来。本来是对的样本一共有1435加16。

一共是1451。算法找出来多少是对的 1435。所以针对正向情绪的召回率应该是1435除以1451。通过对准确率跟召回率的介绍。其实你已经看出来了。我们期望算法给我的结果是准确率越高越好。召回率也越高越好。

所以就引入了一个F1值来平衡准确率。F1值的计算公式 它的分子就等于2乘以准确率乘以召回率 它的分母就是准确率加上召回率。用于平衡准确率跟召回率 我期望他们都越来越好。针对正向负向中性准确率 召回率F1值的一个计算结果就显示在屏幕上。

当我们了解了算法的一个实际的计算效果 接下来在上线之前还要进行一个多数据源的验证。也就是说基于当前你自己的业务场景 你需要预留不同场景下的一些测试语料来输入模型进行验证。看一下不同场景下测试语料的测试结果 是不是它的值都能够符合预期。

还是以情绪识别这个需求为例 我们训练一个多分类的情绪识别的模型。接下来我们会从售前的问题里面抽一部分问题。或者是说从售后的问题里面抽一部分用户的问题。都灌到这个模型里面。让模型给我跑一个结果。我自己再进行一个验证。

就是人工再去看一下模型跑的一个结果是不是对的。比如说我从售前里面抽了100个问题。从售后里面抽了100个问题。最后标注了以后会发现模型给我的准确率基本上也都能达到98%以上。那这个就ok如果说售前跟售后。或者是说不同的业务场景。

或者是说不同渠道的用户。他不一样的一些问法。不一样的一些表达方式。情感识别模型给到我的结果并不能符合我当前的一个预期。那么就需要给情感识别模型再灌一些语料 让他重新训练。

或者是说针对不同的业务场景 或者是不同的行业来训练不一样的情感识别模型 就不是一个通用的模型来包打天下了。作为AI产品经理 你的算法功能上线以后 你需要持续的去关注效果的优化和迭代。优化迭代主要集中在以下两个方面。

第一个方面收集线上的bad case 还是以刚刚那个情绪识别这个需求为例。你持续需要去关注一下线上有哪些用户的问句 明明是负向的被识别成正向了 或者明明没有情绪被识别成有情绪了等等。

把这些问句收集回来进行标注 然后提供给算法工程师这边 让他更新一下自己的模型 并且测试上线。第二个方面。因为我们的业务是在持续的变化的。所以隔一段时间你最好能够收集一些线上的更新的一些用户的问句语料进行标注。

提供给算法工程师这边去更新它的模型。本章首先我们介绍了作为AI产品经理应该了解算法技术到什么程度。接着介绍了智能客服机器人的算法应用有哪些。并且着重介绍了一下跟进算法效果的流程。当然这个流程步骤会根据不一样的组织架构和业务场景会有所调整。

这边建议的三个步骤。第一个提出算法需求。你要保证你的需求颗粒度是足够细的。并且能够明确算法模型的输入和输出分别是什么。这个算法需求要满足的业务指标是什么。你的业务指标预期是多少。

第二步进行算法需求的一个效果跟踪 你需要了解算法工程师的实现方式是什么 并且提供标注数据给到他。在跟进实验结果的阶段。我们介绍了混淆矩阵准确率。召回率和F1值这几个常见的来衡量算法效果的指标值。最后进行一个多数据源的验证。

第三步 上线以后持续的进行效果优化 收集真实的一些用户的数据语料。根据业务场景的调整变化 及时来优化迭代你自己的一个模型。