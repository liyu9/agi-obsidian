好，接下来我们来讲模型能力选型和api调用的实战指南。那上节课我们学到的A卷的框架呢，主要关注的是如何组织和执行任务。A卷呢作为一个智能体，它是能够接收输入执行任务，并且根据上下文去进行反馈的。如果你现在要做一个AI产品呢，要靠谁来执行呢。

哪个模型来提供能力，它在哪里。它是怎么听懂你的指令，怎么样去响应用户的请求的呢。模型选型其实就是选择给这个agent它提供一个智能的核心工具A模型。这些模型它其实就是agent执行任务的大佬。

那这节课我们就来讲一下你要接通哪个大佬，怎么样把它更高效、更安全的、更聪明的用起来。这一步它不是一个简单的选哪一个模型就好，而是说你要根据具体的产品需求。用户的使用场景、技术架构、成本限制来选择最适合的A模型。

那我们简化理解，就是A件的框架呢它是负责将任务串联起来，确保任务有条不紊的去执行。那模型能力选型则是根据A件的大脑去使用哪个A模型来理解和处理任务。

如果你的A件的框架它是需要文本理解和生成文本的功能，那么你选择的模型很可能就是T或者是B R T。如果任务是图像识别，那么你选择的模型很可能就是或者是那第一部分呢。模型选型它不是一个技术问题，它是一个产品策略。为什么现在就要选模型。

首先因为这个决定了你的产品能不能够最终上线。第二个，不同的模型对应的能力响应费用和安全用户体验，它的影响是完全不一样的。它不是说等技术选好了再来的问题，它是需要产品经理一开始就要去拍板的一个决策。

那么在决策过程中间，你需要面对的几个维度，首先是智力强度，然后是语言适配。然后是响应速度，成本控制和介入便利。首先智力强度来说的话，gb 4它依然是王者。Cloud它是表现很稳定的，这个它的成本是更加友好的。

语言适配ger它对中文是比较友好的。Cloud它风格会比较温和，响应速度GPT的响应速度会稍微慢一点。那deep速度响应会快一点。成本控制的话，open a计费当然是有点贵的。Cloud它是会有限流，jamany它是有一个免费度的接入便利。

上面呢launching它的配置适配就比较多，它是能够直接调用的deep开放的ap本地部署适配transform接入都很方便。这个我们平时要对大概的这个模型都有所了解。第二部分是怎么接通的api调用的产品人视角。那api是什么呢。

其实这个如果做过产品经理的人就不用说了，大家都知道。如果是你现在还没有做过api这个东西，那可以简单的打个比方。比如你写一段prote模型会接到请求，他会返回一个内容给到你。

那这个时候呢有可能会遇到网络不稳定，有可能对接的人太多，有可能你说的话他听不懂。所以这个时候的话，你就必须要搞清楚模型是怎么听懂，你说话，又是怎么把话给你说回来的。那常见的这个调用方式就可以有api调试，有项目集成，流式输出和数据管理。

你了解一下就可以了。第三部分就是理想情况下，但是很多时候其实是非理想场景的。我们要能够做长期稳定运行的产品呢，就需要为这个模型设计一套防抖动、防熔断、防失控的策略。

常见的非理想场景的模拟就包括有延迟、调用失败、成本爆炸，还有一个国内合规问题。这个都是几个异常情况，也是产品经理需要关注的点。那第四部分呢就是我们怎么样去评估api调用的成本和效益。

那我们的调用控制点，成本控制的几个要素分别是说调用的次数是乘以单价的。比如说我们的open a它是按照token去计费的那每1000个token他会收你多少钱。看似不是很多，但是如果用户刷一整天的聊天记录，这个成本就会很可怕了。

就是请求响应时间。如果我们做的是对话的或者是搜索推进功能的，它对响应时间的要求就非常高。所以我们需要在交互设计上面也要考虑怎么样去做缓冲，这就是一个吞吐量限制。比如有些api每秒钟只允许你发几十次请求，那用户一多就爆炸。

还有一些在测试阶段是ok的，但是这是上线他可能会崩。那另外还有一个地域部署问题，就是你在中国调用美国的api的时候。可能会出现有点卡的现象，那部署地选错了，体验会拉垮。还有一个就是跟大家说一下成本优化的一个小技巧。

我们在调用的过程中间，可以用哪些小策略把我们的成本优化下来呢。比如说你可以批量的去请求，你不要一条一条的去请求，就合并起来一起发。能够便宜不少。第二个呢就是缓存机制。

你缓存就是常见的这种热门问题，常见的提示语的话，你就没必要每次都重新算一遍。你缓存之后直接返回就可以了。再有一个就是调用监控，那每天调用的次数超限的次数，异常的响应到底是什么样子的。

再有一个就是分级的调用策略，用户他的级别不同，你给他用不同的模型。你你比如说普通的用户会调g p 4，然后vip用户调gp so这个时候用户的分层。所以这个成本就能够节省下来不少。

第五部分就是怎么样去跟app I进行一个集成api调用的基础的流程。其实我觉得大家应该是都做过产品经理的，都应该知道的，不需要说太详细吧。你首先查阅一下api说明书嘛，了解了一些请求参数之后，你调试一下去测试一下。

看模型有没有听懂你的说话，他会给到你一个返馈结果嘛。他回信了你，你还得把回信变成一万里的文字、图像、情绪条、标签这些可以交互的内容嘛。那集成A模型的4种常见方式。首先就是直接调用api这个是最直观的，写个请求就能够跑的。

另外一个就是封装接口，把调用包组成一个组件，统一的去管理参数和返回值。这个比较适合于中大型的项目。那lan集成呢就是适合多步任务，上下文记忆和链式调用的。另外一个就是流式输出，流式输出就是能够让用户像直播一样看到AI在一步一步的输出。

这个比较适合于对话机器人实时的生成和半模型的补偿方案。那不是所有的项目都需要用到lan也, 不是所有的调用都需要自己写代码。你只需要知道这个模型在哪儿，你怎么样去使用它就可以了。到这节课的话，你已经学会了选模型、算成本、接api了。

Ok那我们今天就讲到这里。