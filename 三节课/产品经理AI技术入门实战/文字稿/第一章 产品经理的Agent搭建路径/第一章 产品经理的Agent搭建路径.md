好，接下来我们开始第一讲，我们以一个案例开始。如果你的公司现在已经有了很多的数据或者是实库，老板跟你说我想做个agent。你作为这个产品经理接到这个活的时候，你的搭建路径应该是怎么样的。我们先把这个项目随便起个名字叫绿豆。

我们先说答案，你可以把这个思考路径理解成为这样一段旅程。第一步，现在我手里有数据，数据是什么形式的呢。就是有文本、有图像、有语音。那第二步接着就是我要让它变成A能读懂的形式。三步我把它送入大模型进行理解或者生成。

第四步，我也可能通过prote去控制它，或者通过rag让它更准确。第五步，我通过api调用服务去进行推理。第六步我组织一套逻辑，让AI一步步的去完成任务，形成一个ag件的绿豆。这个逻辑1234五六步看起来是不是非常的清晰。

可能有人会说我不太懂rag是什么，那agent的又是什么的。我是听说过的，其实词好，ok整个文章结束之后，我会给大家发一个常见的术语表。但是呢现在你不太懂也没关系。

我们回到刚才的流程，我们来还原一下这张图的理解结构，首先还是跟大家总括一下我们整体的流程逻辑。他是说A的技术是通过输入数据的类型，然后调整转换方式进行模型处理去输出应用。你可以把它理解成为一个A产品从数据到功能上线的一个完整的路径链条。

它会根据处理的数据类型分出三大分支。我们所有人都要有一个意识，在AI的世界里边，一切一切的开端都是从你手里有什么数据开始的。你要做的第一步就是给你的数据分门别类。因为A它不是全能的，它擅长的能力跟数据类型是紧济相关的。

我们通常把数据类型分为三类，大家应该都知道，比如说文本类的、图像类的和语音类的。文本类就是我们常见的，比如文档、聊天记录，用户输入图像类的那就更简单。照片视频识别场景。语音类的就是比如通话录音语、命令音频的内容。

简单的说你有什么样的数据就要用什么样的A能力去处理，就像你不会让摄影师写文案一样的。当然也不会让文案配音。那所以AI它也是一样的。它的文本类的，我们刚才说的三类数据大家还记得吗。它的文本类的数据我们是用nlp也就叫自然语言处理去处理它的。

我们的图像是用C V就是计算机视觉去处理它的，我们的语音是通过asr和tts去处理它的。这一步叫判断入口。你搞清楚了数据的类型，你才知道下一步怎么样去处理，用什么模型。选什么工具。二层就是每一类数据要先进行一个AI的可理解转换。

你比如说我们的A 2P就是语言类的，可能我们是用embedding去处理的。我们的声音类的就是A R和T去处理的。那么什么是embedding。Embedding它其实就是把文字转成向量，这个其实大部分产品经理目前都应该知道这个概念了。

就是AI它是没办法去理解我们人类的这种所谓的自然语言的，它只能够去理解数字。那embedding它的作用就是把人话变成机器人弄的数学形式。那什么是向量呢。向量它是一个多维的数字数组，代表的是一段话或者一个词的语义坐标。

举个例子，今天很开心和我心情很好，经过embedding之后会变成两个在语义空间里很近的向量。那asr和t又是什么。它其实是一个语音识别和语音合成的名词。就asr它是把语音转成文字，t它是把文字转回语音。

这个里边的话在客服和外呼是用的比较多的，这个概念到是大家知道一下就行了。到这一步了，其实还是A在做预处理产品实现阶段的第一步数据准备好。接下来我们进入第三层，第三层才进入生成式AI，也就是我们所谓的AI I G C加上大模型的阶段。

那上面的数据包括文本、图像、语音，它在经过处理转换之后，可以进入生成式AI阶段。也就是说A C它是上面数据可以走的一条能力路径。那源模型大家应该都知道了吧，这个就不用说了，就GPTdc这些都是很典型的大语言模型。

然后我们可以把大圆模型理解成为A C的一部分，也可以说A C是以A为核心的生成能力体现。那promote它和微调它是什么关系呢。Promote它是你用自然语言去告诉大模型该怎么去回答这个属于你的轻调用。

那微调呢它是属于你要用新的数据再去训练模型，它是属于深定制。这层就是我们的rag api调用和推理服务。这里是很容易混淆的部分。我们先来简单的拆析一下rag它是什么东西呢。它是一个搜索加生成结合的A结构，中文常常称呼它为引入检索增强。

就是代表这个模型他是先搜，他搜什么呢。他去查文档，查数据库，然后再去生成回答。它特别适合什么呢。特别适合我们现在常用的企业内部问答知识库机器人合规问答。那app I调用又是什么意思呢。

App I调用就是产品里边他不会自己去训练模型，而是去远程的调用大模型的服务。比如OpenAI的接口，比如deep sick的接口。那调用的过程包括什么呢。包括发送用户的输入，比如说问题、内容，系统把这些内容转成脱Ken模型去生成。

结果输出再传回给产品前端去显示。所以这里的含义就是你调用了ra结构或者是模型服务，这些模型完成了推理。就是上线运行中的判断和生成。那推理服务又是什么意思呢。推理服务就是你在模型被部署之后，你每次请求在线思考一次的过程。其实就是它的推理过程。

这个大家都用deep，用多了其实都知道了。那最后一层是什么呢。最后一层就是AI agent。这AI agent呢它其实就是把大模型赋能到一个能够思考、能够拆解任务。能够自己动手完成操作的智能体。

比如说你让他写一封邮件并发送，就会自动的去写起草信。确认内容，打开邮箱，再点击发送，完成整个任务链，所以它不是一个大模型生成的结果。它是利用大模型加上memory、加上action，加上目标管理去实现的一个AI的执行系统。

也就是说，A键呢它并不是某一步的产物，而是你设计了一个流程。把前面的所有的AI能力组织起来，形成一个能干活的AI角色。好，那现在我们名词刚才给大家过了一遍，流程呢，也跟大家讲了一遍。基本上就懂了。

那你说那我们是不是就可以使出一个AI agent呢。并不是的。但是呢我们可以记住，你以后要设计一个AI产品，尤其是要做一个AI agent的时候。就可以这样来操作。给大家把步骤列了一下，第一步你确定你的输入数据类型，你要处理什么。

你到底是要处理文本，还是处理图像，还是处理语音。产品经理的判断点是是不是需要识别结构，是不是有语义模糊。第二步就是转换数据，这一步就是要让AI听懂你说什么。比如说我们的文本转换呢是通过embedding，语音转换呢。

我们刚才说了是asr图像转换呢，它就是通过图像分类。那产品经理思考的就是我的输入需要提前的去标准化处理吗。我是需要找相似的文档去做语义搜索吗。第三步呢就是用什么能力来处理，这个时候要选择模型的能力。

比如说你需要内容生成，这个时候你就需要大模型对吧。你需要精确回答，这个时候你要检索增强。你如果只是打标签做判断，那普通分类的模型就可以了。你如果需要多模态的理解，这个时候你就需要大模型加上图文输入。

那产品经理的定位就是大模型到底是用还是不用，你要去判断。第二个成本一定要去考虑。第三个能不能够使用已经有的模型A P I，这些都是我们产品经理需要去思考的。第四个是步骤控制输出，用pro模板和微调控制大模型行为的方式。

第一种呢promote设计，它是控制生成结果结构的。第二个是微调，它是适配于垂直行业风格的。第三个是模板封装，它是让用户的交互更安全。那所以我们产品经理写P的时候要干嘛。首先我们要写promo示例，第二个我们要定义输入输出的格式。

第三个我们要做兜底的策略和失败的for方案。第五步我们就要对接服务。首先呢我们通过api对接模型服务，判断这个模型是来自OpenAI还是里云。还是腾讯，还是要用模型。之后我们等待推理拿到结果，再结合你的产品业务逻辑去输出用户的界面。

产品经理在这里的话，我们就需要跟我们的研发去沟通接口的结构。我们要跟我们的算法去确认模型的调用的频率、token数和延迟。再去评估整体功能的用户体验流畅度、步骤量。

如果要做成一个agent，最终你想做的并不是答一句话，而是自动处理流程、多步执行任务、持续记忆和反馈学习。那么你就可以用上面的模块拼出一个agent，你会用到的组件一般的就是大模型promote rag memory加调用的工具接口。

也就是说agent基本上就可以等于大模型加工作链路设计加用户意图管理加反馈机制。Ok, 恭喜你，第一讲我们讲完了，你也学会了agent的搭建思路。是不是很简单呢。今天呢我们先把概念流程结构装进脑袋里边，未来我们会一节一节的去拆解填充内容。