接下来我们进入第二讲prompt eming token他们三个是AI理解人的语言的几个秘密武器。先让我们来看一下产品经理最早能够掌握能够理解的认知prompt。那很多人学AI一上来就说你要知道大模型，你要懂机器学习，你要马上学神经网络。

或者是你要能够理解模型选型。但是呢我们很多小白其实会一脸懵逼，因为最常见的真实问题其实是我写的问题。A为什么会答非所问。我觉得我已经说的很清楚了，为什么他给我的答案我不满意。

那这个背后其实就是你并不是很了解prompt token embedling它的底层运作机制。所以我们要直接的去切入AI是如何去理解你这件事的。而在学习prompt telling token的时候，我们会顺带的介绍文本是怎么变成数字的。

模型是怎么分配阅读配的，多轮交互是怎么通过指令去控制。那让我们在行动中逐渐的去建立结构的认知。学完这一篇呢你将会理解为什么有些能够生成整套方案，而有些只能够胡说八道。什么是embedding。为什么它是AI理解语义的根本。To又是什么。

为什么呢。在你聊同一个窗口聊很久之后，它会告诉你超过字数了。首先我们来看prompt，它不是问题，而是指令说你以为你在问问题。其实你在写任务说明书。那prompt它就是你给A的工作交代。我们来举一个产品人很熟悉的比喻。

比如说你不会直接跟开发说，你来给我做个按钮，你会跟他说给我一个蓝色的圆角。写着提交申请的按钮，放在表单的右下角，用点击后会调用pos的接口。那这是一个比较完整的描述，加目标说明，加格式限制的内容，这才是一个有效的提示词。

所以烂的提示词就是我的agent，你来帮我写个评价呗。那好的提示词是请你用三句话来评价某一个新的产品，分别从比如用户价值、技术可行性或者商业潜力的角度出发。要求每句不超过30个字。

所以你来看一下这个prompt，它不是问问题，它是发指令，是你给AI的角色扮演脚本。所以产品经理它其实是有天然的写提示词的优势的。这个其实我们类比一下就知道了，它是一个活脱脱的很微缩版的P R D。所以提示词它跟写代码没有任何关系。

他是写产品说明书。那我们产品经理写P R D的时候，本来就是需要把模糊变成清楚。把隐性目标变成写性目标，把业务语言翻译成技术理解。然后我们会需要去限定AI的边界，输出的格式和输入的条件。

这个你听下来是不是感觉两边是一模一样的，所以我们同样的可以来穿插比对一下。产品经理他的强项是结构能力，能够把需求拆成步骤。那其实词它就需要我们拆解成为角色加任务加限制。产品经理它是能够写清楚业务逻辑，它的表达能力很强。

其实词它就需要你写清楚上下文和目标输入。产品经理他需要你设定接口返回内容，达到预期的控制。那提示词呢它同样的是需要你去设定AI的输出格式。产品经理它是需要有兜底策略。你在写提示词的时候，同样的你可以加入，当你不确定的时候你怎么样。

这个也是一个兜底策略。同样的产品经理最重要的是需要懂业务的场景和业务的闭环，那就需要精细的去设定用途和边界。这就是我们产品经理对从自身的角度的一个理解。就进入第二部分embedding，你可以把它理解成为是一个AI的语言定位系统。

因为AI它不理解文字，它理解的是数字向量。你说的每一个词AI它并不会看自然语言的字面意思，而是会把它转成一串数字。放到一个巨大的语义空间。你可以把这个空间想象成为一个三维坐标。首先是相似的词会靠的很近。

比如猫猫狗狗、老鼠兔子这些小动物，那毫无相关的词它们会隔得非常远。就比如说芹菜和WiFi基本上没有什么关键关系，他们的距离就远。那他就是通过这样的数字向量来做一个定位，那他到底在流程里边他是怎么来做的呢。

我们来落地一个定位，它在整个大模型的流程里边，它是一个进入模型前的翻译器。它的功能就是把你的文本输入翻译成为AI能懂的语义坐标。那你这么一想，觉不觉得他就是产品经理在做需求梳理的时候，用户的语结构化成为字段呢。

我举个例子，就是我们这里有个真实的小剧场。你比如说你给GPT说帮我写一段面试后的感谢邮件，去表达你的专业和诚意。这句话呢它并不会直接的去传到大模型里边，而是会先经过embedding的处理。

首先是token，它会把整句话拆成token，embedding, 它是会把魅然后转换成为语义向量。比如说感谢它是一个方向的邮件呢，它把它列为一个对象类型。

专业呢它会选择情绪和风格的标签，陈页呢一些情感的调性的点，它会把它转换成为不同位置的语义向量。再把这些向量送到transformer里边去进行一个推理和生成在输出的结果code再变成一句一句的话。它的一个运作流程。

所以你在这里可以看到invding它的作用是什么，它就是在输入前会把文本转换成向量。模型处理中的时候，它是建立语空间的相似性，去建立一些相关的标签特征距离的规则。当你在搜索过程中的时候，它会查找语义最相关的信息，就像在知识库里面查关键词。

那em它还有一些什么具体的应用场景呢。主要的有三种，首先是语义搜索，用户在比如说用户会搜怎么提升团队效率。他实际上他可以搜出一个ok R的管理工具，这个时候靠的是一个语义相似度。

那另外就是rag这个系统中的召回内容，用户提问题到向量，到去知识库找到最相关的角落。然后是多模态的跨类型的匹配，把图像音频也转化成为向量和文本的向量合并。再把它进行一个内容的生成，其实就是这样来的那一句话总结就是。

Embedding它是AI理解语言世界的地图坐标系统，是所有的语义推理和搜索的起点。有了它AI才能够真正的理解你的意思，所以embedding是非常重要的。那第三部分呢就是token。Token呢其实它是一个AI世界的语言碎片加上预算。

如果你是做产品经理的，你对token其实是非常熟的，应该是非常熟的一个东西。但是这个token它跟互联网token是不一样的。常常遇到的token其实分三种，第一种是我们互联网的token。

你比如说我的账号系统或者是api的权限管理里边都会有一些token。比如说健全接口，用户登录，api调用授权，这都是我们互联网常见的token。

还有一个就区块链的token也是出来很久了，那这个里边的token它的含义是一个数字资产或者是凭证。但是我们的AI语言模型里边的token，它其实就代表着最小语言的处理单元。它这个处理单元它是控制模型输入输出的上限和计费单位。

你可以这样去理解，token它是AI读文字的颗粒单位。它既不是一个字，也不是一个词，而是能够被模型分割处理的原片段。你说到这里是不是会觉得有点像我们之前产品经理跟开发提到的那个字符长度。很好去进行一个对比。

我们就从产品的视角去进行一个完整的对比。你比如说可他所属的体系当然是一个大元模型，拆分方式是按照字词音节词块去切的那我们产品说的字符长度呢。它一般是按照字节或者字符数去计算的那英语单词通常会被拆分成为多个token。

但是像字符长度的时候，英语单词它基本上是等于多个字符，每个字母是一个字符。那每一个汉字基本上是会算一个脱Ken，但是每个汉字呢，它是相当于一个字符或者是两个字节。那应用场景的话，我们会控制这个token，它是控制AI看和说的范围。

字符长度呢它是限制表单输入的长度，或者是接口参数的限制。所以真实的差距就是一个英文句子看起来很短，但是有可能是3到5倍的token数。而字符长度看起来很多，但是它不会影响语的理解。

我们这里拆个例子举例，就比如说cap它是一个token，understand它这个时候会切。比如说and它就按照这个音节去拆分2到3个token。在中文的话基本上每个汉字都是一个token。

这拆分规则的话，其实大家也不用太去在意它，就是说了解一下就行了。看到产品功能是什么。Token它是相当于AI的一个阅读配加上一个输出的限制器。它有三大功能角色，首先是一个输入的配计量，然后是模型处理的切分器。再就是一个输出内容的预算器。

Token它其实就是决定了模型一似能够看多长，能够思考多久。能够说多少话，它其实是一个限制器，这个东西就可以了。那在我们前面提到的大模型的五层路径中间，to的位置又在哪呢。

它其实是在输入前和生成后，它是围绕着promote output两侧的一个硬性约束机制。我们等会儿再讲output。要注意的是呢，不管是promote还是output，它都是在消耗token的度。

Token的度呢其实我们先记一下一个公式，就是token的总用量。它是等于1个promote输入token加上输出token加上上下文历史的token。

所以也就是了解了这个token的度，你才能够想明白为什么deep sick是那么冷冰冰的一个设计。GPT t为什么又保留着大量的记忆。同时我们也可以推出来，为什么token对产品经历很关键。因为你作为产品人，你会遇到这些实际的场景。

首先如果你写的promote超长，结果A I输出残缺，是因为token超上限了。他会自动的截断。那另外呢用户上传的知识库太大，系统会读取失败，也是因为token爆掉照会异常。那你如果是上传的知识库太大，系统读取失败，就会导致token爆掉。

召回异常。如果是输出的结果太短，会默认投稿分配不够。那付费模型和免费模型就是因为token它的预算完全不同，直接影响效果。那肯相关的坑有哪些呢。就是我们刚才说的，比如说你的知识库太大，或者是说他的设计太随意。会导致token的冗余。

那你这个时候需要做什么呢。你需要去模板化、结构化promote。如果是你的输出不够完整，它会导致生成token不够。那这个时候你需要增加的是max token的参数。

另外呢这就是成本不明确，它计费是基于token的，这个时候你需要加入token的预算管理逻辑。好，接下来我们来总结一下今天的学的三个概念。Prot它是理解成工作指令，它就是告诉AI你要去做什么。

Em它是一个语义的地图，把你的话定位到模型世界中间。Token就是一个文字碎片加上一个阅读预算限制模型能够看多长。说多少，花多少钱。好，我们的逻辑基本上就是这个样子。

首先是organization zation就是切碎，token ser干的。是打坐标，是inving layer干的，然后是transformer去处理它。

再就用向量用于检索它的逻辑完整的基本上就是可以这么理解，先是把一句完整的话切成AI能处理的小颗粒。接着就把每个token变成一个语义坐标，方便AI理解和存储。

再带着这些向量去分析它的关系，做思考，在的或者agent的场景用embedding向量去查资料。