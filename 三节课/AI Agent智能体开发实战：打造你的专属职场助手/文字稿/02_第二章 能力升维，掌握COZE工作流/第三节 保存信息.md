接下来我们需要完成信息的保存。当使用大模型已经将指定字段的信息提取出来，下一步我们需要添加新增数据。但添加新增数据之前，我们需要先将这个数据库表创建好，所以我们返回到资源列表。

我们创建资源当中的数据库，创建co子数据库，我们参考数据表字典表明是周报Weekly Report数据表描述我们可以暂且省略。单击确认。接下来前四个字段是默认的，我们需要新增字段，第一个字段report date。这段描述是报告时间。

类型我们选择的是str文本，因为考虑到我们不同的客户经理在填写报告时格式不统一。所以我们最好是选择文本型来进行存储。第二个字段，客户经理I D字段描述客户经理I D类型，string文本型继续新增下一个字段。客户经理的姓名。

类型为文本型，继续新增下一个字段部门department字段描述所属部门名称类型string文本型新增下一个字段。New accounts字段描述为新增开户数类型为int整数新增下一个字段A M资产管理规模竞增。类型整数新增下一个字段。

对公存款竞争类型整数新增。下一个字段。零售存款竞增类型为整数，这里的字段我们不必要求它们为必要。因为我们的部分客户经理可能会存在某些指标缺失，那我们需要将这些缺失信息找出来。我们将这个表结构配置好之后呢，我们单击保存。

紧接着我们需要在工作流当中来实现，将刚刚大模型提取的这段信息保存到这个Weekly Report数据库当中。那下面我们可以单击资源库当中Weekly Report这个工作流可以重新打开我们刚刚创建的工作流。

在大模型节点后面我们可以添加新增数据这个节点，这里的节点都是与数据库的操作相关的。可以增加数据更新、数据查询、数据删除、数据缩扣。自定义是指输入指定的搜代码即可实现新增更新查询也可实现删除。如果我们选择的是新增数据，那么它只能插入数据。

我们单击新增数据这个节点，打开这个节点的配置信息。首先我们需要添加数据表，注意仅支持添加一个数据表。这时候我们可以将创建好的Weekly Report这个数据库表添加进来。然后我们要选择并设置字段，我们可以将所有字段添加进来。一共八个字段。

接下来我们依次去引用变量值，report date, 引用前面大模型提取出来的report date。客户经理ID引用客户经理ID客户经理姓名引用对应的姓名，部门引用对应的部门信息。

新增开户数引用对应的新增开户数信息后面的每一个字段依此类推。这样就能够准确的将输出的每一个字段信息保存到对应字段下，这就是为什么我们前面大模型希望他以json格式输出。输出的变量不需要我们去填写，它会有行号和输出的output list。

是一个数组形式。接下来结束节点，结束节点是指这个工作流执行完成之后，我们希望看到的内容。如果说我们希望能够看到保存到数据库当中的记录，那我们可以在结束节点当中引用新增数据。这个节点它的output list。

最后我们优化布局，接下来我们试运行。我们还是使用事例一这个客户经理周报来试运行一下。运行成功，我们来看一下结束节点的输出。第一个output是我们的报告，第二个output是我们大模型提取的每一个字段信息。

第三个output list是我们保存到数据库当中的记录ID。但看不到具体的记录数据记具体的记录数据我们可以回到数据库当中去查看。我们返回到资源库列表，我们单击Weekly Report这个数据库表来看一下测试数据。

发现这就是我们刚刚智能体输出的ID。那么这条ID的记录对应的内容就是从这份报告当中提取的信息。这样便能够实现将非结构化文本的内容以表结构形式存储。但是现在我们仔细核对一下数据会发现最后两项指标的数据，它并没有能够实现一个精准的识别和提取。

出现这种情况呢也是很正常的，毕竟AI大模型它会出现幻觉或者不稳定的情况。会跟这个大模型的聪明程度相关。那假设现在呢我们来换一个大模型，尝试重新试运行一下，我们返回到工作流。选中提取字段信息这个大模型一开始我们是使用的豆包。

我们呢可以去找到deep pstick v三这个大模型，用deep g隔壁三大模型。我们现在呢来重新试运行一下。好，我们来看一下output结果。这时候300万、100万、20万，是不是可以将这几个核心kpi的指标数据能准确提取出来。

那既然大模型可以准确提取出来，数据库当中自然也会存在。我们也可以在这里单击查看数据。好，这时候我们需要看第二条记录。这里注意它新增的记录是排在前面的，所以我们要看一下第一条记录。两条记录进行对比。

我们会发现用deep siv 3模型，它是可以准确提取后两个指标数据。而之前的豆包基础模型呢它是无法提取的。所以大家在制作的过程当中，可以根据当前模型的运行结果来合理的选择。也不代表豆巴大模型就一定会失败。

那截止到目前，我们已经实现了保存信息这个功能。