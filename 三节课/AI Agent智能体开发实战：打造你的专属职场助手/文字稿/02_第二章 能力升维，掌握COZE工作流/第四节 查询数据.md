接下来我们实现查询数据这个功能，在工作流当中已经完成了新增数据。接下来我们插入节点查询数据，我们打开查询数据的配置，首先需要添加数据表。我们将report这个数据表添加进来。接下来设置查询的字段，我们呢可以将这个表当中的所有字段来查询。

其中这里的前四个字段是系统默认生成的，我们可以从报告时间开始依次将其添加进来。报告时间，客户经理ID客户经理姓名、所属部门、新增开户数以及后三个指标。我们将这个表的八个字段全部进行查询。接下来我们可以设置查询条件。

如果说我们不添加任何的条件，那它便会将这个表的所有数据查询出来。我们先不添加条件，这里的排序方式我们也可以先暂时不配置，直接去查看这个表的所有数据输出的变量。我们不需要设置默认就是一个output list，里面包含了我们选择的查询字段。

这时候我们的结束节点可以考虑直接去引用我们查询数据的output list。优化一下布局。接下来我们试运行。这时候它提示我们查询数据的变量值不可为空，条件不可为空。但是注意这时候呢系统提示我们有错误，为什么。

因为查询数据这个节点，它的变量值条件是不能为空的。所以我们现在回到查询数据这个节点，尽管这里的查询条件我们不需要。但是我们依然要选择。那我们应该如何选择呢。这时候可以考虑把我们的记录ID选中，只要保证这个记录ID是大于等于0的。

就是相当于我们要查询所有的记录。所以我们可以这样去设置条件。好，条件设置好之后，我们再来尝试试运行，这时候便没有提示。那我们还是使用事例一这份客户经理周报。运行成功，我们来预览一下结果。第一个output是报告，这个没问题。

第二个output一是提取我们这八个字段的信息，四个指标的数值也都能准确提取出来。最后数据库当中的output list有三条记录，其中这个记录是我们第一次使用德包模型。没有查出最后两个指标。

第二次是采用deep sick模型，四个指标都查出来。第三就是我们刚刚执行的所有的记录也都准确查询出来。由此可见，只要我们每运行一次，同样的信息会提取出来保存到数据库当中。这样呢就会导致我们现在数据库当中有三条重复的记录。

现在我们回到数据库来查看一下他的数据，当前有三条记录全都是张三这个人的。在这里呢可以点击删除，但是这也是在运行之后手动删除，这是不合理的。使用场景不准确。那准确的做法应该是在工作流当中，当我们每次需要上传新的报告。

它应该先要将当前的数据库清空，清空之后呢，再重新将上传的报告内容存储到数据库表当中。然后基于当前新的数据进行查询。所以在这里我们需要添加一个删除数据节点。那么删除数据节点我们放在哪里比较合适呢。

从当前这个工作流当中来看，我们肯定要放在新增数据之前。如果放在新增数据后面，那就是相当于我新的信息增加进来，然后呢立刻就将它删除。删除之后我们查询的结果一定是为空的。所以只要放在新增数据这个节点之前，任意一个位置都可以。

那不妨我们就放在开始节点的后面。当我们想要调用这个工作流，第一步就先将当前数据表里的记录进行清除。然后呢重新去阅读新的报告，提取新的报告文本，来新增新的报告记录。然后对于我们新的报告记录进行查询输出。

我们在开始节点后面新增删除数据，我们打开删除数据它的配置界面。首先我们要添加数据表，将Weekly Report添加进来。删除的条件我们可以选择I D ID只要是大于等于0的，我们需要全部删除。

然后输出的是output list和ronow，输出的变量我们不用去修改。它是默认的。我们来优化一下这个布局，再来重新试运行。这时候我们来看一下output list只剩一条记录。

那现在我们回到资源列表，打开数据库表来看一下测试数据，发现只有我们刚刚最新提取的这一条记录。而之前的历史记录都已经被清空。截至目前，我们的工作流已经能够实现查询数据这个功能。