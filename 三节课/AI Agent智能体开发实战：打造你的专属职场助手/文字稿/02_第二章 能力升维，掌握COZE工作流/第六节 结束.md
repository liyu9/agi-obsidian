当前我们来回归一下我们这个工作流它的一个完整流程。首先开始的时候，我们可以上传一份报告，它会先将当前表当中的所有记录删除。紧接着通过插件读取word报告，再用大模型对读取到的文本报告内容提取我们指定的字段信息。

将提取出来的字段信息保存到数据库当中，再根据该数据库当中的表来进行查询数据。基于查询的数据结果，使用大模型撰写一份分析报告。在使用创建文档这个插件，将文本型的报告内容以p df文件的形式生成。最后结束节点输出我们pdf文档的下载链接。

那假设我们现在有多位客户经理的周报，需要去进行一个批量上传。来进行一个宏观视角的整体分析，我们该如何做呢。首先当我们上传5份word文档之后，我们每一份word文档都需要来进行读取、提取、保存。然后查询。

那也不能对单份报告的数据进行查询，应该是将5份报告的数据全都上传保存到数据库当中之后。进行一个统一查询。所以在查询之前，我们需要保证5份报告的记录都保存到数据库当中。那么就需要将这5份报告同时进行读取、提取和新增。

所以这三个节点我们需要进行一个批量处理。那如何去批量处理呢。我们可以添加批处理节点。这个P处理节点呢它是由P处理和P处理体共同构成的这两个是默认为连接。

我们可以将需要实现批处理功能的节点拖拽进来，比如read的节点、提取字段信息节点、新增数据节点拖进来之后呢。连接线便消失了，我们可以手动进行一个连接。首先位的节点需要去和P处理连接，然后read的节点与大模型节点连接。

大模型节点与新增数据节点连接，新增数据节点呢在与我们的P处理体连接。这三个节点的顺序我们依然不变。那传入到read节点的变量，这个时候注意它不能是来自开始节点的变量。而应该是来自P处理节点的变量。新增数据的输出也应该传入到批处理节点当中。

那我们首先来调整一下开始节点，在开始节点这里我们要上传多份文件。所以它的变量类型必须是数组，我们选择array数组里面的file文件。选择里面的D O C word类型。

这样呢我们的智能体先进行一个表里面数据的删除动作，紧接着下一步来进行批处理。那么批处理并行运行的数量默认是十，也不建议大家超过10。如果说并行的运行数量太大，那会导致它运行失败，所以最好是小于10或者小于5。P处理的次数上限默认是100。

对于这个P处理，我们的输入变量，变量名默认是input，而它的变量值我们要引用开始节点的input。开始节点它的input的变量传入到P处理，紧接着呢P处理它的变量再传入到P处理体。

所以这时候read节点它输入的变量应该来自于P处理里面的类型为D O C变量。紧接着呢这五份报告批量的读取、提取和保存。最后我们的批处理输出要引用我们批处理体当中新增数据的输出。注意这里面row now有一个陈N因为里面会有多条记录。

批处理完成之后，接下来就可以针对这份包含五条记录的表进行查询。所以我们将批处理节点与查询数据节点进行连接。查询数据节点它是直接使用数据表查询指定字段，分析报告输入的变量是来自于上一个节点。

查询的结果不变，输出的报告呢变量名是output，创建文档引用的变量来自于分析报告也不变。结束我们依然是选择创建文档生成的网址链接，所以后续的几个节点我们是无需修改的。我们优化一下布局，来看一下整体的工作流的改变。

只是将中间读取报告插件，提取字段信息插件。下面我们来下面我们再从整体来看一下，当前的工作流中间的批处理是用到了read节点。提取字段节点以及新增数据节点。这样就能够实现5份报告上传，先清空当前表里的数据，5份报告批量的存到数据库当中。

再针对当前的数据库表进行查询，撰写分析报告。现在我们来试运行一下。我先清空当前上传的历史文件记录，我们可以选择批量上传5份报告。然后试运行。试运行已经成功，我们来看一下相关节点下的运行结果，首先，提取字段信息这个大模型的运行结果。

我们可以看一下输出的内容，这是第一份报告提取的信息，这是第二份报告提取的信息。这是第三份报告提取的信息。其中我们的第三份报告它是有一定的信息缺失的，所以呢他提取出来的数据为零。第四份报告也是存在信息缺失的。

最后第五份报告从这个大模型节点来看，运行结果是没有问题的。那么因此我们的数据库当中该表里面也会有五条记录。那再回到最后，我们来看一下结束节点输出的p df文档下载链接。我们选中这个网址，用浏览器打开下载分析报告。

支行客户经理周报分析报告，多位客户经理成功开设了新账户，其中赵琴新开了六个账户。表现是较为突出的。下面有具体的业绩情况分析，每位客户经理他们实现的A M增长值。其中王五是0，赵情是最为显著。

然后公司的存款增长情况，零售存款增长情况，新账户开户数量情况等等等等。以及遇到的问题及解决方案，还有下周工作建议与展望。这样的一份报告便是从整个支行视讲，对所有客户经理的一个周报情况进行了一个统计分析案例。

银行客户经理周报统计分析的演示示例已经结束，我们总结一下此类案例的应用价值。第一方面可以自动化信息完整性审核，智能体能迅速扫描所有的周报。自动标记信息缺失项，生成信息完整度评估，帮助支行行长快速识别问题周报。

第二方面，高效数据的整合与汇总，自动从多份word文档中提取结构化数据。确保数据提取的准确性和完整性，消除人工错误。第三个方面，支行全局业务视角能提供支行所有客户经理工作情况的整体视图。实现客户经理的横向比较。第四个方面，提升了管理效能。

支行行长从繁琐的报告审核中解放出来，将更多精力投入到战略思考和团队建设。提升整体的管理效率和质量。第五个方面，客户精力的工作质量得到提升，通过及时反馈提高周报质量。引导客户经力，关注关键业务质量，促进工作方法的改进和业绩提升。

本节案例银行客户经理周报统计分析就介绍到这里，感谢大家的收看。