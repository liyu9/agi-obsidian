下面我们继续来完善该工作流的功能，目前已经能够实现读取word报告。接下来我们需要从报告的文本内容当中来提取我们指定的字段信息。文本信息的提取处理。我们需要借助大模型，这时候我们可以在read读取报告这个节点后面添加大模型节点。

我们单击大模型节点，首先我们可以单击重命名该当模型要实现信息的提取。我命名完，我们往下接着看，模型呢就先使用默认的豆包1.5 pro模型暂时无需配置技能大模型输入的变量。我们可以引用前一个节点read读取报告的参数data。

我们需要将报告的文本内容传入到当前大模型节点，让大模型对这个文本报告内容进行处理。到底要做哪些处理呢，我们需要通过系统提示词来给大模型配置角色、技能和限制。如果我们自己不会写系统提示词，我们可以先用自然语言描述一下。

希望智能体能完成什么工作。然后我们用自动优化。比如你是文本信息提取专家，能够从用户输入的文本内容中提取以下指定字段信息。注意我们这里呢让它以json格式输出，其实呢就是以列表的形式输出。保证他们一个字段一个字段都能够准确完整清晰的列出。

然后我们将数据字典当中的字段名字段描述放进来，数据类型可以添加。也可以不用添加。我们将这段描述放在自动优化当中，我们单击发送。这时候呢AI便生成了角色技能和限制，我们做一下浏览角色。

你是一个专业的文本信息提取专家，擅长从用户输入的文本内容中精准提取关键信息。并以json格式输出。技能一，信息提取提取以下指定字段，紧接着呢将提取到的信息以json格式输出。示例如下，用大括号来表示数组，也就是列表有字段名和字段的描述。

没有问题之后呢，我们可以单击替换，这样呢系统提示词我们就配置好。紧接着我们可以在用户提示字这里引用输入参数当中的变量来作为大模型的用户输入。我们只需要按照格式输入一个英文输入法下的大括号，便可以去选择我们前面指定的输入变量input。

注意，只有这里输入当中有变量，才能在用户提示词这里引用，否则是无法引用的。那么这个大模型就会将input参数里面的内容作为输入传入，那大模型便会从中进行以下字段信息的提取。并且以json格式输出。那么在输出这里我们的格式就选择json。

那么输出的变量名我们就用默认的output变量类型。大家可以选择object对象，然后我们可以新增子项，我们可以让它每一个字段名依次输入。首先报告时间。再来添加子项客户经理I D再来添加子项客户经理姓名，再来添加子项所属部门名称。

继续添加此项新增开户数。继续添加子项。资产管理规模竞增，继续新增子项，对公存款竞增，再添加最后一个子项。零售存款竞增。然后我们调整一下输出的每一个变量名它的类型。前面四个字段都是文本型，后面四个字段都是int整数数值型。

这里的变量名可以自己填写，也可以参考AI大模型生成的变量名。做好之后，我们的大模型就配置好了。紧接着呢我们来调整一下结束节点，我们把大模型的输出内容传入到结束节点当中。也可以既看读取的报告内容，也看我们大模型提取的信息。

我们只需要添加输出变量即可，我们可以去引用大模型提取字段信息。这时候大家观察会发现，我们可以是单独去选择输出这个变量下的每一个子变量项等。当然也可以直接选择output这整个点亮。接下来我们优化布局，可以尝试试运行。

刚刚上传的示例周报依然存在，我们可以单击试运行。运行已经成功。我们来看一下结束节点。首先第一个output输出的是报告内容，第二个output输出的是我们要提取的字段信息。

我们会发现它是能够实现精准提取的那截止到这儿，我们已经完成了读取报告和提取信息。