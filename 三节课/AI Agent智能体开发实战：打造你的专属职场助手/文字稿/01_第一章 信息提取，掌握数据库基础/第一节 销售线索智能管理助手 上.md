大家好，欢迎收看课程智能体开发实战，打造你的专属职场助手。本节课程信息提取掌握数据库基础。本节会涉及以下知识点，案例销售线索智能管理助手，通过自然语言快速记录信息数据库的基础概念。

数据库的语言分类，表格结构数据的特征以及表结构数据的特征概述。我们先来看案例销售线索智能管理助手，此类案例可以解决以下的需求痛点。一、当销售人员需要手动将客户信息填入多个表单字段，耗时且繁琐。导致信息收集效率低下。

2、重要的细节容易在记录过程当中遗漏或者错误输入，导致信息丢失与不准确。3、不同销售人员的记录格式和内容不一致，难以形成统一标准，导致数据标准化困难。4、销售流程碎片化，比如信息记录与cm系统录入分离，导致流程断电。

5、外勤时难以完整记录客户信息，需要回到办公室二次整理，导致移动办公受限。接下来查看一下案例背景。某软件公司开发了一款名为智慧逍客的销售线索管理系统。系统最新的功能是通过自然语言处理技术，让销售人员能够直接通过语音或文字记录客户信息。

而不必手动填写多个表单、字段。销售人员可以在客户拜访后、通话后或者展会结束后，以自然语言方式快速记录线索信息。智能体负责从这些非结构化部门当中提取关键信息，并将其存储到标准化的数据库中。便于后续的跟进和分析。

接下来我们进入到案例的演示示例，我们进入到智能体工作空间。在这里我们首先选择资源库，我们需要创建一个数据库资源，用来存储我们的销售线索。我们单击创建扣子数据库，我们需要先给数据表命名，这个表的名称是必填项且无法修改。

我们可以参考一下提供的数据表字典表明sell source。我们存储以下几个字段。数据表描述呢是可以不填的，它为非必填项。我们单击确认，紧接着会进入到新建数据表的界面，默认单用户模式。

我们来看一下，这里有存储字段的名称，注意存储字段的名称就是指我们表的每一个列的列名。且必须为英文描述。是可以对这个字段的补充信息，然后选择数据类型，选择是否必要。如果是必要字段，那必须要提供对应的信息，否则无法保存该行数据。

如果是非必要字段是可以缺失该字段信息的那一行数据仍可以被保存在这个表当中。点击删除可以删除字段。那么注意前面的四个字段是智能体数据表默认必须要存在的字段，我们是无法删除的。它们分别是数据的唯一标识，数据产生或使用的渠道，用户唯一标识。

这是由系统生成的，以及数据插入的时间。我们只需要单击新增便可以去添加自定义的字段。这里呢提示我们字段名的格式只能包含小写字母或者数字，或者下划线必须要以英文字母开头。然后有参考的格式abc或者abc 123，或者abc下划线123。

那么在这里我们将表格字典当中的字段名依次添加进去。首先第一个字段，company name, 公司名称。字段描述，我们用中文来做一个描述。数据类型为文本型，选择string好，添加第二个字段，contract name。

描述是联系人姓名，类型是文本型，继续添加第三个字段，字段名position字段描述是联系人职位。类型是文本型，继续新增。第四个字段，字段名是email，字段描述为电子邮箱，类型是string文本型。

继续新增下一个字段，字段名是凤，字段描述是电话号码。类型是文本性，继续新增下一个字段，字段名是wchat，这段描述是微信号。类型是str文本型，继续添加下一个字段product interest。字段描述是感兴趣的产品或者服务类型。

文本型，继续新增下一个字段budget预算。预算是指的金。所以类型我们要选择整数型，继续新增下一个字段。Decision timeline决策时间线，这里类型我们选择文本性。继续新增下一个字段。

字段名是priority，描述为优先级，类型为string。文本性继续新增下一个字段，next follow up描述是下次跟进时间。类型最好也选择文本型。因为我们是用自然语言来描述，有可能会说下周一，而不会说具体的某一年某月某日。

所以最好选择文本型，继续新增下一个字段，source线索来源。类型为str文本型，最后一个字段note字段描述为备注类型是tri文本性。截止到这里，我们所有的字段名都已经新增好，包含字段描述以及数据类型。

我们不需要给某一个字段指定是否必要，因为我们跟进的不同销售线索情况是不一样的。有可能他有预算，有可能下一条线索它没有预算，所以我们不选择必要。当我们把表的这个字段名称建好之后，我们单击保存。

保存好之后，我们可以去查看当前的表结构信息，单击测试数据可以去看当前的。行，现在呢里面是没有记录的。好，我们返回。此时在我们的资源库当中便存在于一个名为cell source的数据库资源。

那现在我们回到项目开发列表，这时候我们可以单击创建智能体。我们可以选择AI创建，先用自然语言描述一下你希望创建一个什么样的智能体。现在我们回到项目开发列表，我们可以单击创建智能体，选择AI创建。

我们先用自然语言描述你希望创建一个什么样的智能体。比如当用户上传文本内容后，能提取以下关键字段的信息，保存到指定的数据库表中。我们将我们需要的字段进行一个展示。随后我们单击。哎生成。智能体生成好之后呢，我们单击确认，进入到智能体的编排界面。

我们先浏览一下左侧的人设与回复逻辑的系统提示词。你是一个文本信息提取与存储助手，在用户上传文本内容后，能够精准的从文本当中提取公司名称、联系人姓名等等等等我们所提及的字段信息。并将其保存到指定的数据库表中。

那么既能第一个技能提取关键字段信息，我们检查一下是否将我们所提及的字段都有写出来。技能二能保存信息到指定的数据库表。那在这里呢我们可以将数据库表做一个添加。接下来我们进入到编排界面，插件我们暂时是不需要的，我们可以回到下方的记忆找到数据库。

数据库是指以表格结构来组织数据，它也支持我们定义表结构，并将用户的数据存储在表中。那我们刚刚呢已经定义好表结构了，我们只需要添加就可以。我们找到我们刚刚的source这样的一个数据库，我们单击添加添加该数据。表现在呢数据库已经添加成功。

那接下来我们在左侧的人设与回复逻辑提示词当中，可以呢引用一下该数据库的表。比如在技能二这里获取提到的关键字段信息后，将其整理成符合指定数据库表结构的格式。把整理好的信息保存到指定的数据库表中。我们可以使用英文输入法下的大括号。

好，那当我们输入大括号，大家会发现暂无已配置的技能。由此可见在系统提示当中，我们能引用的只能是技能下的插件、工作流和触发器。当然了，我们前面的案例当中也会发现是可以引用知识库的，但是唯独数据库在这里是无法使用。

那既然无法使用，我们可以不必再次去使用大括号来引用强调。那不妨我们来尝试测试一下它的功能。比如在这里我们准备了一些自然语言输入的素材，我来选中第一条记录。今天下午两点在国贸大厦遇到了来自里巴巴的李明，他是他们采购部门的经理。

他对我们的企业软件解决方案很感兴趣，特别是库存管理模块。他提到他们计划在接下来我们可以尝试进行对话，来对该智能体的功能做一个预览和调试。我们准备了一些自然语言输入的素材。

比如说我们来看第一条素材，他是这样描述的，今天下午两点在国贸大厦遇到了来自里巴巴的李明。他是他们采购部门的经理，他对我们的企业软件解决方案很感兴趣。特别是库存管理模块。他提到他们计划在三个月内更新系统，预算大约为100万元。

我们交换了名片，他的电话是邮箱室。那接下来我们将这个自然语言复制输入到对话框当中，单击发送。我们来看一下智能体能否从当前的文本当中提取关键字段信息，并将其保存到sales source这个表当中。现在呢智能体运行完毕了也提示已经保存成功。

那么我们如何去查看呢。我们首先呢可以单击这个运行完毕按钮，会发现它已经调用了数据库。好，我们再来看一下下方，这是复制复制AI生成的答案。这个是让它重新生成，这个是删除。这里呢有一个调试按钮，这个调试按钮我们也可以在右上方单击查看。

我们打开调试详情能看到调用数，它调用了大模型，调用了哪些工具。再往下有节点，开始的时间、结束的时间，有没有运行成功等等这些基础信息。再往下是能够看到我们输入的文本内容以及输出的文本内容，但是依然无法看到表当中是否存在该条记录。

那这个时候呢我们可以点击这里的sell source，点击之后呢便能够打开我们的sell source编辑界面。我们刚刚第一步是添加的表结构，这时候我们可以查看测试数据，便发现里面存在了一条记录。前面的四个字段信息是系统默认生成的。

那么从company name公司名称开始，便是智能体从我们输入的自然源当中提取的信息。因为没有提及微信号，所以微信号这个字段下是为空的。100万。由于呢预算这个字段类型是整数，因此它把这个100万换算成了单位为元进行存储。

那么说明此时的这个智能体的功能是没问题的那我们返回。刷新一下我当前的工作空间。我们重新进入到刚刚的智能体。当我们输入一条自然语言文本，它是可以保存成功。那假设我现在批量的输入。五段自然语言信息它能否批量的保存成功呢。我们来进行一把测试。

这时候智能可回复，我们已成功从第一段文本中提取到这些字段。接下来继续提取后续文本，然后已成功从第二道文本当中提取到这些字段。由此可见它是可以批量保存的。保存成功，智能梯已经运行完毕。我们也可以回到项目开发这个列表，我们选择资源库。

然后我们选择所有类型数据库，扣子数据库，这样可以起到一个资源类型的筛选。那现在只剩我们的数据库资源，我们可以准确找到sales source这个数据库。我们可以看一下测试数据，那么目前一共有五条记录。

这五条记录就是通过从我们刚刚输入的文本信息当中进行的一个提取和保存。这就是我们的销售线索管理智能体。