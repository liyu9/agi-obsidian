让我们顺利完成销售线索信息的存储。接下来我们是否可以让智能体来查询数据。比如我们需要在已有的人设回复逻辑当中继续添加第三个技能，我如果我们不会去写技能信息。我们可以单击自动优化提示词。比如我们可以选择异能三查询数据。

用户输入查询需求后，从数据库中查询相关信息并输出。紧接着呢我们来单击发送。这时候呢它便会将整个人设与回复逻辑重新生成，在之前的记础之上添加了一个技能3。前面是进行提取信息，保存信息，技能三便是查询信息，我们可以进行替换。

那如果说我是想针对当前的提示词内容一部分进行修改，我们可以选中这部分文本再来单击自动优化。那么这时候它只是针对当前这一句提示词来进行编写或者优化。当我们添加了技能三之后，下面我们来尝试查询，比如，请查询字节跳动的销售线索信息。这时候呢。

这时候他便调用数据库，能够从数据库当中查询到公司名称为字节跳动的记录。并将其输出。这样的话我们的智能体既能实现提取信息、保存信息，也能实现查询信息。那接下来我们来看一下它的运行过程，我们可以单击调试。

在调试当中，我们输入的文本是请查询字节跳动的销售线索信息，输出的便是公司名称为字节跳动的记录。那么这个结果是如何查询到的呢。它是调用数据库，那么在数据库当中要想查询信息，它是必须要通过数据库语言。

在这里请大家先看一下数据库语言，用select进行查询。Select星星号代表的是这个字段的所有from代表的是来源。

From后面紧跟的是表明cell source，所以这一部分的内容是指从cell source这个表当中去查询它的所有字段。那查询条件是什么呢。条件用will来表示，条件是公司名称，这个字段下的值要等于字节跳动。

因为字节跳动是文本，那么文本要用单引号引用，这是一个完整的数据库查询语句。这个大家可以做一下基本的了解。通过这样的一个查询语句在数据库当中执行，我们便能够得到这样的一个查询结果。这就是我们完成的销售线索智能管理助手案例。

销售线索智能管理助手的演示示例已经结束，我们来总结一下此类案例的应用场景。一外部销售拜访场景，销售代表在客户拜访后可以直接口述或发送文字记录访谈内容。智能体自动提取关键信息并存入系统，无需手动填表。

2、电话的销售与跟进场景，销售人员在通话结束后能及时记录谈话要点。智能体分析对话内容并更新客户档案，保持信息的时效性。3、展会与行业活动场景，在人员密集的展会或者活动中，能快速记录大量潜在客户信息。智能体自动整理并分类，便于后续有序跟进。

4、销售团队协作场景，不同的销售人员使用统一的信息记录方式。智能体确保所有数据按相同标准存储，提升了团队的协作效率。5、远程工作场景。在远程办公环境下，销售人员可以通过多种设备和渠道输入销售线索。智能体保证信息的同步和整合。

6、销售的培训与入职场景，新入职的销售人员能更快速的适应工作流程。无需记忆复杂的数据录入规则，降低入职门槛。此类案例的应用价值主要体现在以下几个方面，一、可以节约时间成本。销售人员每天可以节省大量的数据录入时间，转化为更多的客户接触机会。

2、能优化人力成本，减少对专职数据录入人员的需求，降低人力成本。3、销售人员更关注于客户互动，而非数据录入，大大提升了客户的体验。4、形成标准化的客户信息库，沉淀组织销售知识。本节销售线索智能管理助手的案例就介绍到这里，感谢大家的收看。