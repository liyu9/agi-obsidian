接下来我们要调整一下其他相关节点的变量。首先我们从开始依次来检查，开始节点上传excel文件，输入文本类型的分析需求没有问题。紧接着用该插件来解析表的字段结构信息也没有问题，它直接引用开始节点输入的excel文件。

分析需求大模型我们刚刚才配置好也没有问题，只不过要注意它输出的变量是数组。那接下来循环体，我们要将每一个分析需求传入到循环体当中，所以循环它的设置类型。我们要选择使用数组循环，它也可以指定循环次数。

但是呢我们输入的分析需求它不会限制指定的次数。有可能这一次输入的是一个需求，也有可能下次输入的是两个需求。所以我们就让它按照使用数组循环，那么自然是要去引诱循环的数组变量。这个变量从哪里来。

从我们识别分析需求这个大模型当中来，我们选择该变量，这个时候变量名我就先命名为question。输出变量我们暂且先不设置。当我们每一个分析需求传入到循环体当中，首先要通过该大模型根据传入的分级需求撰写索扣代码。

所以表明字段名是不变的，从这个插件结果当中去引诱。但是呢分析需求我们必须要引诱循环当中的分析需求。注意一下循环这里可引用的参数有两个，一个是索引，也就是序号。另外一个才是真正的文本型的内容，所以我们要选择文本类型的变量。

紧接着系统提示词我们不用更改，保证它能够输出纯净的缩扣代码。或者还可以再加一个注意事项，因为我们使用的原文件字段名是中文。有的时候呢大模型它会偏偏的将这个中文翻译成英文，导致这个代码当中出现的是英文字段名。

那么在执行的时候就会出现错误提示，它无法找到对应的英文字段名。好输出的呢是我们的文本型的代码，也就是搜狗代码。紧接着在excel的。紧接着在excel工具上来执行搜狗代码，这里excel文件引用开始节点没问题。

so代码呢执行的是前面这个大模型，输出的代码也没有问题，紧接着这个大模型用来生成绘制图表的参数。它引用的是分析结果，是前面这个节点生成的变量参数也没有问题。然后绘制图表，撰写分析报告，注意撰写分析报告，这里面我们也用到了分析需求。

所以呢同样我们得改成从循环题当中传入的分析需求。紧接着我们再来选择循环这个节点，那么它输出的变量自然是我们撰写分析报告指的大模型它输出的分析报告。注意这里的分析报告不止一个，因为我们会循环执行每一个分析需求。

它会输出每一个分析需求对应的分析报告。所以呢循环体它输出的变量类型也变成了数组类型。紧接着我们要将分析报告转成文档，那这时候输入的markdown格式的报告内容。我们需要去引用循环的输出。

最后呢结束节点我们可以只保留让它输出我们create document这个节点它的data也就是我们的报告。那现在我们重新优化一下布局，从整体视角来检查一下整体的工作流程。在我们检查完每个节点它的变量选择之后呢，现在我们试运行一下。

我们还是同样的云文件，同样的分析需求。此时运行完毕，并且提示运行都是成功的那现在呢我们先来看一下报告的结果。我们打开浏览器粘贴链接下载报告。在这份报告当中，首先针对第一个每个市场级别的销量分析报告有对应的背景分析目的以及分析结果。

我们再往下看一下图表内容，这是每个市场级别的销量，再继续往下是我们每个市场分布的销售合计。有表格数据。紧接着呢我们来看一下图表，它也是用的粗桩柱形图展示每个市场分布的销售合计。以及有对应的结论和建议。

那么通过这样的一个方式，就能够避免它将不同的分析需求用同一个图表展示。又或者能够避免无法识别分析需求，导致有部分分析需求的分析结果无法实现。现在我们来仔细看一下工作流当中的每个节点的执行结果。首先开始上传的excel文件会解析成。

接下来我们来仔细看一下每一个节点的运行结果。首先上传的excel文件会以链接的形式传入，然后呢有我们输入的分析需求。这个插件节点没有问题，它拭解析表明字段名等结构信息的重点看一下识别分析需求。它会以数组形式输出每一个分析需求。

紧接着我们来看一下循环。其中输入是由两个分析需求依次输入。那接下来我们来看一下循环体，我们有一二，那么第一次传入的分级需求是需求一。那根据我们输入的表明字段名和分级需求一，它可以撰写一个数据库代码。

紧接着呢会执行分析需求一的分析代码，得到我们的分析结果。紧接着再根据分析需求一得到的分析结果生成option参数，再来去执行它的option参数。得到我们需求一的图表。紧接着呢就能够得到我们需求一的分析报告。

那么同理当需求一执行完毕，它会传入需求2。那首先会根据需求二生成搜口代码，再来去执行需求二代码的结果。再基于结果生成option参数，生成对应的图表，生成对应的分析报告。

当循环完毕，我们的循环体就便会输出两部分报告内容，只不过呢它会将它们拼接在一起。紧接着会将我们整个循环体生成的报告创建成p df文档，然后通过结束节点输出我们的文档。这就是我们一个完整的带循环体工作流的智能体，既能实现单分级需求。

也能实现多分级需求。但是呢我们借助这个智能体，也目前仅能完成单表的分析。工作流做好之后，我们单击发布。发布成功之后，我们回到项目开发列表，我们单击创建智能体。在这里我们也不需要用AI创建，因为我们是让他通过按照工作流的指定流程来完成工作。

所以无需复杂的人设与回复逻辑。我们呢给智能体命名。单击确认进入到智能体的编排界面。我们将刚刚的数据分析工作流添加进来，然后在人设与回复逻辑当中说明。当用户上传excel文件以及输入分析需求后。调用工作流调用哪个工作流呢。

我们引用一下调用工作流生成分析报告。那现在我们来使用这个智能体进行一个预览与调试。我们上传原文件，假设我先输入一个分析需求，分析每个省份的销量合计。智能题已经运行完毕，我们可以点击链接下载数据分析报告。我们查看一下数据分析报告的内容。

此份数据分析报告就是围绕省份的销量进行分析，有关键发现结论。只不过与之前不同的是，关键发现它没有用表格展示。我们来看一下图表，它是用了每个它是用了柱形图来展示我们每个省份的销量合计。然后呢，它是使用明细数据在下方进行了一个展示。

那接下来我们删除对话记录，我们尝试呢输入两个分析需求。我们同样上传一份文件，我们输入分析每个。市的分布的销量合计，再分析每个产品类型的销量合计。此时智能体已运行完毕，我们来点击链接下载分析报告。

首先是围绕市场销16首先是围绕不同市场的销量分析情况展开分析。我们有分析结果，有图表展示，他使用的图表是粗装出形图，展示每个市场分布的销量合计。紧接着是我们围绕不同产品类型的销量合计进行分析。

得到咖啡是2044，茶1873，浓缩咖啡2294，花茶是2060。我们等一会儿在数据透视表当中来做一个核验，同样它是使用了粗状柱形图来展示数据。那么这样的不同分级需求的报告也是能够准确输出的，而且互相不会有影响。

那下面呢我来打开表格数据，我们插入一个数据透视表，检查一下每个产品类型的销量。合计查1873没问题，花茶2060没问题，咖啡2044也是没问题的。浓缩咖啡2294也没有问题，所以分析结果是完全准确的。

假设我们想要查看这个工作流每一个节点的运行结果，如何做呢。我们打开调试，在调试详情当中我们能看到调用数，在调用数当中我们找到调用的工作流。我们可以单击查看工作流详情。这里就会进入到工作流的创建界面。

然后呢，它保留了我们刚刚每一个节点的运行结果。这时候大家就可以去展开运行结果，查看它每一步的运行情况。做好之后大家可以自行选择是否要进行发布。那这就是我们本节课程给大家介绍的数据分析。那么这就是我们本节给大家介绍的数据可视化分析智能助手。

数据可视化分析助手案例的演示示例已经结束。我们来总结一下该类案例的应用场景，它适用于所有数据分析相关的场景。比如企业的业务数据分析、销售数据分析、客户行为分析、市场趋势洞察、财务报表分析等等。

此类案例呢可以大大的提高决策效率，比如能快速从数据中提取关键见解。缩短决策周期，减少人为偏见，提供基于数据的客观分析。还可以降低运营成本，比如减少对专业数据分析人员的依赖，自动化重复性分析工作。释放人力资源。

还可以提升工作效率，比如将数小时的分析工作缩短至几分钟，能自动生成专业报告。避免繁琐的手动整理。最后能实现标准化，可以确保分析方法的一致性和可重复性，建立组织内统一的数据分析标准。本节案例数据可视化分析助手就介绍到这里，感谢大家的收看。