那接下来我们要尝试让智能体去执行搜购代码。也就是说执行完搜购代码，我们是能够得到这段代码的查询结果，是能够将每个市场级别的销量合计值的结果输出。那如何在该excel当中去运行代码呢。我们同样需要借助外部工具。

我们选择插件依然是搜索circle on这个插件。我们使用on excel这个插件可以实现在excel上面运行circle代码。我们来添加给这个节点配置信息输入的变量。

首先变量名count是指的返回条数，其实呢这个返回条数我们可以尽量的写大一些。紧接着文件链接，我们需要把开始上传的这个excel文件作为它的一个变量。紧接着呢是搜狐代码，搜狐代码是我们上一个节点大模型撰写的搜狐代码。

重点是这两个变量，它需要在指定的excel文件上来执行指定的代码。那在结束节点这里我们可以去引用circle on excel这个节点的输出变量。也就是data数据结果。优化一下布局，我们现在重新试运行。数据源不变，我的分析需求也不变。

好，这个时候运行结束，我们来看一下它的output，很显然是error。有报错有报错。所以我们要尽量保证运行的代码是纯净的。大家看一下，我们选中这个节点，它输入的三个变量count link其实都没有问题。

但是传入的so代码你会发现包含了很多不相关的文本内容，有这些符号。有斜杠N换行符号，有文本注释内容，但其实代码执行的应该只能是select到分号结束这段信息。所以呢，现在我们需要回到大模型来进行一个调整。

你是一个专业的数据分析师，精通买so语言，擅长使用买so进行数据查询分析这些都没问题。我们重点看技能，当用户提供了数据内容和分析需求，请仔细分析。在根据分析的内容生成买so查询代码。

但是第三点我们要要求他注意，请生成稳境的缩口代码，不需要包含其他不相关的文本内容以及各种符号。所以回复示例我们只需要保留一个纯净的缩代码就可以了。好，这时候输出格式是mark down，我们待会儿再来运行一下。

如果说输出内容当中存在一些mardown这种点号的符号，我们就改成输出文本。好，现在大模型我们调整好之后呢，让它重新试运行一下。好，这时候我们是能够看到分析结果。我们首先来看一下大模型节点下方的运行，输入字段名需求表明输出一段代码。

然后呢在这个节点当中，它是直接将纯代码进行了一个传入。当然这个换行是不影响的。好，紧接着呢输出运行成功，能够看到一级市场3199，二级市场5072。那我们来在excel当中验证一下，我创建一个数据透视表，统计每个市场级别的销量。

合计一级市场3199，二级市场5072，结果是完全准确的，那么截止到这儿。我们就完成了执行缩扣代码这一项工作。要注意的点就是前面让大模型生成so扣代码的时候，一定要强调它只生成纯净的缩扣代码。不要有不相关的文本和各种符号。