大家好，欢迎收看课程数据驱动构建智能可视化助手。本节会涉及以下知识点介绍，案例数据可视化分析，助手表结构数据的分析。数据可视化分析。我们先来看一下此类案例可以解决的需求痛点。一、数据分析门槛比较高，传统数据分析需要专业的技能和编程知识。

普通用户难以上手。2、分析流程繁琐，从数据处理到可视化再到报告撰写，通常需要多个工具和手动操作。3、效率低下，人工分析耗时长，尤其是面对大量或者复杂数据的时候。4、人工分析容易受主观因素的影响，导致结果不一致，也导致分析质量不一致。

5、雇佣专业的数据分析师或者数据分析师团队成本比较高昂。接下来我们进入到该案例的演示示例。现在我们要完成数据可视化分析助手。首先我们要思考清楚，希望这个智能体能完成哪些功能。

我们可能要上传一份excel文件，最终呢要让智能体去分析这份excel文件数据来输出分析报告。那么在这个中间的过程，它需要能熟悉这个表格数据的表结构信息。并且通过写搜代码的形式来完成分析。

因为只有通过撰写搜代码来分析，得到的分析结果才是相对准确的。也许有人会尝试，我直接在某个对话大模型当中上传文件，它也可以分析。但是呢得到的分析结果不一定是非常准确的。所以在这里我们需要写精确的搜代码。

如果要写精确的搜代码，那必须要让它识别表明和字段名，所以解析表结构是非常重要的。紧接着再根据分析结果绘制图表，最后呢再将图表信息和数据分析结果等其他信息整理生成一份分析报告。那下面我们依然需要创建一个工作流，让它按照指定的流程来进行工作。

我们进入到扣子智能体的工作空间，我们选择资源库，我们单击创建资源里面的工作流。首先我们要命名，我们就叫数据分析data a analysis工作流描述。帮用户上传数据文件，调用工作流，完成数据分析。每个工作流一定会有一个开始节点和结束节点。

现在我们先来完成第一件事情，上传excel文件之后，它能够解析表结构信息。首先我们来查看一下准备的数据文件，它非中国市场销售数据。

它包含了订单ID交易时间、市场级别、市场分布、省份、咖啡因类型、产品种类、产品名称、销量、销售、总成本这些字段。那接下来我们希望智能体在我们上传文件之后，它能够解析出表明和字段名。方便后续来撰写数据库代码。

在节点当中没有能够直接识别表结构字段的节点，我们呢需要借助第三方工具。所以我们需要调用插件。在插件当中我们给大家推荐circle on excel这个插件。我们输入关键词搜on便能够搜索到这个插件。

这个插件其中有一个插件的名字叫get excel schea。它是能够获取excel表的表结构信息的，包含sheet名和字段名。我们来添加这个插件，插件添加好之后，我们连接开始节点以及结束节点。我们先选中开始节点来配置一下它输入的变量类型。

我们要上传的是excel文件，所以变量类型选择file文件里面的excel。对于get excel schema这个节点，它输入的变量名默认名是link。其实是指的excel文件的链接。

那么这个excel文件就是来自于拍始节点的input，这个节点它会输出字段名表明以及示例数据。行，最后我们结束节点来引用一下get excel schema这个节点的内容。现在我们来试运行一下上传。Excel文件数据单击示例行。

我们来查看一下输出的变量字段名，它都能够识别出来，并且还提供了示例数据行。这是我们实现的第一个功能，上传excel文件之后，能够解析出该表的结构信息。