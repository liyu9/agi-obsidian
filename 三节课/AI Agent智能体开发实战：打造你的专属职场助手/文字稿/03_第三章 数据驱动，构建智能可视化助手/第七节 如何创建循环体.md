那现在我们来看一下如何创建循环体。首先我们要明确好哪些节点要放在循环体当中执行。我们针对每一个分析需求要写一个对应的分析代码，然后呢进行代码的运行。根据代码得到的分析结果绘制图表，再将该分析需求的结论输出分析报告。

紧接着输入第二个分析需求，依然是从撰写代码开始，执行代码，生成图表。生成对应的分析结论。所以我们可以考虑将撰写搜Q代码开始到生成分析报告为止，这五个节点放在循环体当中。那么这个循环体我们就要依次输入每一个需求。

所以在这个循环体之前，我们要对开始输入的分析需求进行识别划分。那么需求是文本级对文本进行处理，我们需要借助大模型，所以我们可以先考虑添加一个循环体。再考虑添加一个大模型。识别分析需求，我们添加节点找到循环，循环它是由循环和循环体共同构成。

中间有一条连接线，我们呢依次将撰写搜代码节点、excel运行搜代码节点、生成op参数节点、绘制一叉的图表节点。已知以及撰写分析报告节点拖入到我们的循环体当中。我们先在内部连接这五个节点。

然后在循环体当中，第一个节点要与循环体连接，最后一个节点也要与循环体连接。紧接着这个循环，我们要与前后节点进行连接。我们优化一下布局，看一下整体视角，流程是不变。

只是我们将中间的五个节点放在一个循环体当中，那么循环这里我们要输入的是每一个不同的需求。所以呢我们需要在这里添加大模型，至于解析表结构它是不影响的。所以大模型放在它的前面还是放在它的后面没有任何的关系。

那假设我们在这个节点添加大模型，我们给这个大模型命名，它是作为识别分析需求使用的。紧接着呢我们要对这个大模型进行配置。首先它输入的变量要引用我们开始节点输入的需求question变量。

变量名我们就使用默认的input，紧接着我们要通过系统提示词来配置大模型的技能。我们可以先用自然语言描述需求，比如你是一位专业的数据分析师。擅长精准识别用户的分析需求并将其合理划分。

但是呢我们要要求它以数组形式输出每一个分析需求，以便后续的循环体能够轻松识别。我们将这一段自然语言描述的信息粘贴到提示词对话框进行发送。待角色技能限制生成完毕之后呢，我们单击替换，但是我们需要浏览一下角色。

你是一位专业的数据分析师，具备深厚的数据处理和分析能力，擅长精准识别用户的分析需求。并将其合理划分，以数组形式输出每一个需求，方便循环体能够轻松识别角色是没有问题的。

接下来技能识别分局需求，当用户上传数据文件，我们是在这个环节不需要上传数据文件。只是提出数据分析相关需求。所以呢我要做出调整，当用户提出数据分析相关需求时，仔细理解用户意图。精准提炼出具体的分级需求。

将每个分级需求以字符串形式存放在数组中，然后返回包含分析需求的数组。回复示例。需求一，需求2，事例结束没问题。技能二我们是不需要它执行数据分析工作流的，我们可以删除限制。只围绕提出的数据分析需求展开工作。所以呢我们可以将前面删除。

分级需求必须以数组形式输出，没问题，分析结果我们是不需要分析结果的。可以删除。好，这样呢我们能够做到让大模型只需要做一件事情就可以。紧接着我们输入用户提示词，也就是分析需求。

我们引用变量input输出，输出的是数组，所以我们的变量类型要选择而瑞数组。选择a ay数组下的文本变量名，我们就先使用默认的output。那么这时候识别分析需求的大模型我们就配置好了。