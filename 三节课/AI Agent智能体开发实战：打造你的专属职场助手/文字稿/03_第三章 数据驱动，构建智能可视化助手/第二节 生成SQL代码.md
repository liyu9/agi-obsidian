接下来我们要让智能体实现第二个功能，可以基于当前的表明字段名再给出一个分析需求。能够让它撰写搜代码，撰写搜代码这件事情我们可以交给大模型。因为大模型是擅长写文本，写代码，这时候我们在获取表结构节点的后面添加大模型节点。

针对这个大模型，我们首先呢可以纯命名。它是用来撰写搜代码的。模型我们可以暂且先使用豆高模型，如果说它的代码写不出来，可以考虑再换成一些更聪明的模型技能。无需添加。我们给它设置一下输入的变量，我们需要将解析出来的表结构信息输入。

由于我们需要让它根据分析需求，根据表明，根据字段名来撰写搜狗代码。所以这里呢我们最好分开去引用。首先这是表明我们可以重新命名一下变量名。然后再来添加一个变量，我们引诱字段名，变量名我们就使用field name。

那还需要再添加一个变量，是我们用户输入的分析需求。所以这个分析需求我们需要在开始这里上传excel文件之后也要同步输入我们的分析需求。毕亮名，我先用question代替分析需求呢是文本，所以类型是这两个变量。

我们给它勾上必填，我们必须要上传数据文件，必须要写上分析需求。你才好让智能体去完成分析工作，所以这两个变量是缺一不可的。那这时候我们在大模型这里便可以添加输入的变量，引用开始里面的question变量作为我们的分析需求。

那接下来我们需要让大模型根据用户的分析需求，根据表名字段名来撰写查询代码。也就是我们前面介绍的select。那系统提示词如何撰写，我们同样借助AI来自动优化提示词。首先我们先要用自然语言描述一下。

比如，你是一个专业的数据分析师，擅长使用买so语言进行数据查询分析。能够根据给定的数据内容和分析需求，生成精确的买so查询代码来满足数据查询需求。为什么一定要指定买搜口。

虽然说所有的数据库系统都使用的搜语言，但是不同的软件它们在语法上会存在一些略微的差异。所以我们最好指定其中一个软件，我们可以要求使用买so口查询代码。那我们将这段自然语言的描述信息粘贴到提示词对话框当中来发送。

紧接着AI便生成了角色技能以及限制。我们做一下浏览技能是当用户提供内容和分析需求，可以仔细分析生成买搜查询代码。它还要对生成的代码进行必要的注释，解释代码的功能和关键步骤。这个我们是否需要呢。

可能对于初学者不懂数据库代码的人来讲，他可能会愿意去看一些注释。但是如果我们后续要让它去运行代码，那其实最好得生成纯净的代码。不需要那些解释的文本内容。我们先让它生成。

接下来我们给用户提示词表明，引用table name、字段名、build name分析需求question。用户输入这三个提示词，大模型便开始生成买足狗代码，输出的代码它是文本。所以呢我们用mardown或者文本东行，我们用mardown。

好，接下来变量名我们就是默认的output，我们将结束节点引用撰写的缩扣代码。我们来看一下最终生成的缩扣代码是否准确。现在我来进行试运行，我们还是使用相同的练习文件，这时候呢我来给一个分析需求。

比如说我想分析每个市场级别的销量，合计市场级别一共有两个，一级市场和二级市场。现在我们单击试运行。我们来看一下输出的output内容，select查询。Select后面跟的是查询的字段名，要查询市场级别销量合计是要对销量字段求和的。

As是重命名，重命名为总销量。From是从这个表里面查询分组，按照市场级别分组。那其实这一段数据库代码是准确的。但是呢我们的输出的内容当中不仅包含了代码，还包含了一些注释内容。但是呢我们结果当中输出的不仅有代码，还包含了一些注释内容。

那待会儿我们来试一下，让它去运行代码能否成功。如果说这些注释或者解释信息不影响代码的执行，我们可以保留。但建议大家是不保留的，其中这个分号一定要保留。因为在买缩口当中，分号代表命令终止符，说明从select开始到封号结束是一个完整的命令。

那现在我们已经实现了生成搜代码这个功能。