现在我们来进一步完成绘制图表这个功能。现在我们需要根据分析结果来绘制图表。那么绘制图表显而易见，我们需要去调用外部工具。我们单击插入节点选择插件图表，在这里我们推荐选择一char图表。

使用这个each chart生成器名称叫create chart。它的参数就是一个option参数。Each char的图表它是需要根据格式为json的option参数来进行一个图表的绘制。

所以呢我们需要将这个J算格式的option参数作为它的变量输入。那我们的option参数如何来呢。中间我们可以借助大模型，让大模型根据当前的分析结果，将它转化成json格式的option参数。大模型是擅长文本处理的，所以我们需要添加大模型。

那对于这个大模型，我们首先重命名一下，它是用来生成。Option参数的输入的变量内容，我们要选择是这。接下来我们给它选择输入的变量，我们要去引用前一个节点执行得到的分析结果。我们要让大模型根据当前的分析结果转化成杰森格式的option参数。

能够让图表识别出它的轴和值来选择合适的图表，那么这个大模型的系统提示词我们该如何撰写呢。还是借助A自动优化提示词，我们可以先用自然语言描述一下希望这个大模型完成的工作。

比如，你是一个专业的数据分析师，擅长将用户上传的数据转化成json格式的。能用于绘制1 ch图表的option参数。最好。我们要求它以柱形图、丙图、折线图为基础图表进行绘制。因为一些复杂的图表它是绘制不出来的。

我们用基础图表绘制的成功率比较高。将对段描述粘贴到提示词对话框当中，点击发送。这时便生成了角色技能以及限制我们来进行替换，那我们来浏览一下角色技能。

角色是数据分析师，擅长将上传的数据转化成J森格式的，用来绘制1 child图表的option参数。以柱形图、并图、折线图为例进行绘制，这个没有问题。在分析的过程当中，我们不需要让它提供解释和建议，因为我们只需要生成纯净的option参数。

当用户上传数据文件，仔细检查数据内容，然后呢转化成option参数。不需要说明参数的结构和含义。所以呢第三点包括回复示例，我们可以直接删除。技能2，图表绘制讲解我们也不需要，我们只需要他完成一项工作即可。

接下来我们来设置用户提示词，我们只需要将输入的变量input上传就可以。所以在技能当中我们是不需要上传文件的，只需要让它将数据转换成option参数即可。最后呢它会输出J份格式的参数，变量名是output。

那这时候我们单击create chart这个插件节点，它输入的变量option便可以去引用前面的这个大模型的输出变量output。最终呢它会生成一个U 2L地址。我们通过打开网址的形式来查看图表结束节点，我们不光要看分析结果。

我们同时再来看一下图表。好，现在呢我们来试运行同样的数据源文件以及分析需求。即使我们运行失败，那我们来检查一下在绘制图表这个节点运行失败原因是什么呢。生成的option参数，我们先来检查一下。

J层格式的参数是没有问题的，但是呢传入到create chart当中就会导致这些文本注释的内容也传入进来。所以导致它运行错误。因此我们在这个大模型当中也要给它优化一下。注意。请生成纯净的option参数内容，不要有无关的文本信息或者。符号。

或者文本格式符号。在限制这里，我们也是要保证它回答简洁明了，只包含纯净的参数。好，这时候呢我们再来重新试行一下。这时候运行成功。我们来看一下最终输出的结果，分析结果依然是准确的。图片它会生成一个U 21地址，我们用浏览器打开。

它是以编辑图片的形式供我们去下载查看，这就是我们的一叉的图表。它绘制的是柱形图。那么我们绘制图表的功能就已经实现了。