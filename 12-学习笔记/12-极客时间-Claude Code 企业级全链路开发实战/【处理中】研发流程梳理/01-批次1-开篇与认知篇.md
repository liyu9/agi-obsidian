# 批次1：开篇与认知篇

> 本批次来源：《极客时间-Claude Code 企业级全链路开发实战》第01-02讲及开篇词
> 涉及文档：
> - 开篇词｜一个人用 Claude Code 造一个简版 Dify，需要多久？
> - 热点速递｜Claude Code 源码泄漏：热闹看完，我们聊点真问题
> - 01｜你是架构师，Claude Code 是你的工程团队
> - 02｜规范驱动开发（SDD）：让 AI 永远在轨道上

---

## 一、产品边界定义

### 流程环节说明
产品边界定义是整个研发流程的起点。在动手之前，必须明确"做什么、不做什么、做到什么程度"。这是架构师要做的第一件事，决定了项目的范围和约束。

### 课程原文内容

#### 1.1 三问裁剪法

课程中提出的产品边界拆解方法，通过三个问题确定产品范围：

> "这套框架还包含一系列支撑拆解过程的具体方法论：……三问裁剪法帮你拆解产品边界——做什么、不做什么、做到什么程度。"

#### 1.2 产品边界的实际操作

开篇词中记录了实际的操作过程：

> "那一天我几乎没写一行代码。我花了整个晚上做三件事：定义产品边界（一页纸写清楚做什么和不做什么）、设计核心数据模型（五六张表的 ER 图）、写 CLAUDE.md（告诉 Claude Code 这个项目的全部上下文和规范）。"

#### 1.3 课程实例

以Hify项目为例，产品边界定义包含：
- 做一个简版Dify，支持多模型管理、Agent配置、对话交互、MCP工具接入
- 基于Spring Boot + Vue技术栈
- 采用模块化单体架构
- 前后端完整，本地可运行
- 目标用户：团队内部几十人

---

## 二、核心数据模型设计

### 流程环节说明
在明确产品边界后，需要设计支撑产品功能的核心数据模型。这包括确定实体、实体关系、以及关键的业务数据结构。

### 课程原文内容

开篇词中记录的设计工作：

> "我花了整个晚上做三件事……设计核心数据模型（五六张表的 ER 图）"

> "设计核心数据模型。Hify 需要存储模型提供商、Agent、对话历史、MCP 工具配置等核心数据，ER 图是后续开发的蓝图。"

---

## 三、工程规范制定

### 流程环节说明
工程规范是AI辅助开发的基础设施。通过提前制定明确的规范，让AI在定义的轨道上执行，而不是自由发挥。规范贯穿整个项目生命周期，需要持续迭代完善。

### 课程原文内容

#### 3.1 为什么需要工程规范

课程中通过对比展示了规范的重要性：

> "做 Provider 模块时，我在指令里写得很细：实体叫 Provider 不加前缀、返回格式用 Result<T>、错误码按模块分段、不要设计模式。Claude Code 的输出很干净，命名统一、格式规范、结构简洁。"

> "两天后做 Agent 模块，我写指令时没有重复那些规范（觉得它应该"记住"了），只说了"实现 Agent 的 CRUD 接口"。"

> "实体类叫 AgentConfig——加了个 Config 后缀，Provider 模块不加后缀，两个模块风格不一致。返回格式有的走 Result<T>，有的直接返回 ResponseEntity。错误码没有按模块分段，和 Provider 的错误码撞号了。更离谱的是它给 Agent 加了一个 Builder 模式——Provider 模块用的是直接 new，两个模块的创建方式都不一样。"

**教训**：Claude Code 没有长期记忆，每次新对话都是从零开始。

#### 3.2 规范的核心内容

课程中定义的四大规范领域：

**命名规范**：
```
# 命名规范
实体类大驼峰，不加前缀后缀。例如 Provider、Agent、ChatMessage。
字段小驼峰。例如 apiKey、baseUrl、modelName。
接口路径：/api/v1/{资源复数名}。
```

**接口规范**：
```
# 接口规范
所有接口统一返回 Result<T>：{ code, message, data }
列表字段空时返回空数组 []，不返回 null。
分页参数：page（从 1 开始）、pageSize（默认 20）。
```

**错误码规范**：
```
# 错误码
四位数字，按模块分段：
1000-1999 通用 | 2000-2999 Provider | 3000-3999 Agent
4000-4999 Chat | 5000-5999 MCP
```

**设计原则**：
```
# 设计原则
不引入不必要的设计模式，除非明确要求。
不做过度抽象，一层能解决的不要拆成两层。
不引入技术栈以外的依赖，需要时先确认。
```

#### 3.3 规范分层管理

课程中展示的三层规范体系：

> "我把 Hify 的规范分成三层：
> - **全局规范**：写在 CLAUDE.md 里，整个项目通用。命名规则、接口格式、错误码体系、设计原则、行为约束。项目初期定好，后面很少改动。这是地基。
> - **模块规范**：某个模块开发前单独定义。比如 Provider 模块有自己的接口契约，对话模块有自己的数据流定义，工作流模块有自己的节点类型规范。
> - **任务规范**：每次给 Claude Code 下达具体任务时的补充说明。比如"这次不需要分页"、"异常要区分这三种情况"。临时性的，不落成文档。"

---

## 四、人机分工决策

### 流程环节说明
AI辅助开发需要明确人与AI的分工。不是所有事情都交给AI，也不是所有事情都由人来做。需要根据任务的性质和风险程度来决定分工。

### 课程原文内容

#### 4.1 三层分工模型

课程中提出的三层分工框架：

> "我把它分成三层，你可以直接拿来用。
> **第一层，必须你做的**：产品边界、架构决策、技术取舍、跨模块一致性。拿 Hify 来说：做不做 RAG 知识库？用微服务还是模块化单体？线程池参数设多少？这些决策只有你能做。
> **第二层，AI 做你验收的**：业务代码、接口开发、前端页面、测试用例、文档。这是工作量的大头。有一个硬标准：任何一行代码你都要能说清楚它在干什么。说不清楚，就不能用。
> **第三层，AI 全权处理的**：格式化、样板代码、简单重构、启动脚本、Makefile。扫一眼没问题就行。"

#### 4.2 明确的指令示例

好的分工需要清晰的指令：

**第一种方式（模糊指令）**：
> "帮我用 Spring Boot 实现一个模型提供商管理的 CRUD 接口。"

**第二种方式（清晰指令）**：
> "按照 CLAUDE.md 中的接口规范，实现模型提供商的 CRUD 接口，使用 MyBatis-Plus，错误码按规范定义，连通性测试的异常区分网络超时（2001）、认证失败（2002）、模型不存在（2003）三种情况。不需要工厂模式，用最简单的方式实现。"

---

## 五、AI输出验证

### 流程环节说明
AI生成代码后，必须经过验证才能使用。课程提出了"三步检查法"作为验证的标准流程。

### 课程原文内容

#### 5.1 三步检查法

课程中详细描述的验证流程：

> "**第一步，查意图：它做的是不是你让它做的？**
> 你打开代码，发现它确实实现了删除接口和关联检查。但它额外做了两件事：加了一个软删除机制（不是真删，标记 deleted=true），还加了一个操作日志记录。软删除合不合理？也许合理。操作日志要不要？可能要。但问题是你没要求。它自作主张扩大了范围。"

> "**第二步，查质量：风格、规范、一致性。**
> 你看命名，发现检查关联的方法叫 checkProviderUsage，但项目里其他类似方法都叫 validateXxxBeforeDelete。返回格式上，其他接口返回 Result<Void>，它这里直接返回 ResponseEntity。"

> "**第三步，查边界：错误处理、异常、潜在风险。**
> 你发现一个问题：它检查了"有 Agent 在使用则拒绝删除"，但没有处理并发场景——检查通过后、删除前，刚好有新 Agent 绑定了这个提供商怎么办？另外，传入的 ID 不存在时，它抛了 500 而不是 404。"

#### 5.2 验证顺序的重要性

课程中特别强调：

> "顺序很重要：先查意图，再查质量，最后查边界。如果意图就错了，后面两步白查。"

---

## 六、SDD规范驱动开发闭环

### 流程环节说明
规范驱动开发（Specification-Driven Development, SDD）是一套持续运转的闭环方法论，包含"定规范→AI执行→人验证→迭代规范"四个步骤。

### 课程原文内容

#### 6.1 SDD四步闭环

> "SDD 不是写个规范然后照着做，它是一个持续运转的闭环。四步，循环往复。
> **第一步：定规范**。在写任何业务代码之前，先把基础规范定下来。
> **第二步：AI 按规范执行**。下达任务时，明确引用规范。不是"帮我做个 Agent CRUD"，而是"按照 CLAUDE.md 中的规范，实现 Agent 的 CRUD 接口"。
> **第三步：人验证输出**。用三步检查法，检查 Claude Code 的输出是否符合规范。
> **第四步：迭代规范**。你在验证输出时，一定会发现规范没覆盖到的地方。关键是每次发现缺口，都要补上。"

#### 6.2 规范迭代实例

课程中展示了规范的迭代过程：

**第一周发现的缺口**：
> "Claude Code 在处理空列表时返回了 null。前端拿到 null，页面直接白屏——它期望的是一个空数组 []。这说明规范里没定义"空值怎么处理"。补上：
> 列表字段空时返回空数组 []，不返回 null。字符串字段空时返回空字符串 ""，不返回 null。"

**第二周发现的缺口**：
> "做 Agent 模块时，让 Claude Code 改了一个 Provider 的接口签名。前端调 Provider 的代码全部报错。它"觉得"新签名更好，就顺手改了。这说明规范里缺少"改代码时的行为约束"。补上：
> 修改已有代码前，先理解相关模块的设计意图。不要为了实现新功能破坏已有模块的接口契约。"

**第三周发现的缺口**：
> "做对话引擎时，Claude Code 把业务逻辑写在了 Controller 里——事务管理、数据校验、外部调用全塞在一个方法。这说明规范里缺少"代码放哪里"的约束。补上：
> Controller 只做参数校验和调用 Service，不写业务逻辑。跨模块调用走 Service 接口，不直接引用其他模块的 Mapper。"

#### 6.3 规范质量标准

课程中总结了三条规范质量标准：

> "**具体，不模糊**。没用的规范："代码要简洁。" Claude Code 对"简洁"的理解和你不一样。有用的规范："不引入不必要的设计模式（工厂、策略、观察者等），除非明确要求。每个功能用最简单直接的方式实现。"

> "**有优先级，不贪多**。规范不是越多越好。太多了 Claude Code 也处理不过来。我的原则是：AI 反复跑偏的地方写细，不跑偏的地方不写。"

> "**带原因，不只是规则**。对于涉及工程权衡的规范，不能只告诉 Claude Code"不要这样做"，还要告诉它为什么。它需要理解约束的原因，才能在类似场景中举一反三。"

---

## 本批次涉及研发流程汇总

| 流程环节 | 课程来源 | 关键操作 |
|----------|----------|----------|
| 产品边界定义 | 开篇词 | 用三问裁剪法明确"做什么、不做什么、做到什么程度" |
| 核心数据模型设计 | 开篇词 | 设计五六张表的ER图 |
| 工程规范制定 | 02讲SDD | 制定命名规范、接口规范、错误码规范、设计原则 |
| 人机分工决策 | 01讲三层分工 | 区分必须人做、AI做+验收、AI全权处理 |
| AI输出验证 | 01讲三步检查法 | 查意图→查质量→查边界 |
| SDD规范驱动开发 | 02讲四步闭环 | 定规范→AI执行→人验证→迭代规范 |

---

## 下一批次预告

批次2将覆盖：产品定义与架构设计
- 产品定义：动第一行代码之前你该想清楚什么
- 架构设计（上）：应用架构与代码组织
- 架构设计（下）：部署架构、性能预判与数据设计
- CLAUDE.md与工程规范：实战必备的工程规范
