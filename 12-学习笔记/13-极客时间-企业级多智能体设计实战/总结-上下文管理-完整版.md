# 上下文管理完整指南（L18-L22）

> 来源：极客时间《企业级多智能体设计实战》模块三
> 涵盖课程：L18 范式认知 / L19 上下文生命周期 / L20 文件系统记忆 / L21 搜索驱动记忆 / L22 综合实战
> 整合日期：2026-06-10

---

## 目录

- [一、认知框架：记忆的本质](#一认知框架记忆的本质)
- [二、核心问题：要解决什么](#二核心问题要解决什么)
- [三、上下文生命周期：六个干预节点](#三上下文生命周期六个干预节点)
- [四、加法体系：让模型知道该知道的](#四加法体系让模型知道该知道的)
- [五、减法体系：让上下文没有不该有的](#五减法体系让上下文没有不该有的)
- [六、三层记忆架构的实现细节](#六三层记忆架构的实现细节)
- [七、工程落地的关键实现](#七工程落地的关键实现)
- [八、最佳实践（生产级经验）](#八最佳实践生产级经验)
- [九、反模式与避坑指南](#九反模式与避坑指南)
- [十、关键经验总结](#十关键经验总结)

---

## 一、认知框架：记忆的本质

### 1.1 记忆的本质是构建 message list

**大模型本身没有记忆**。每一次 API 调用都是独立的，模型不知道你上一句说了什么。ChatGPT"记住"你之前说的话，是因为应用层把你之前的对话历史悄悄塞进了这次请求的 message list 里。

```python
# 这就是"记忆"的全部真相
messages = [
    {"role": "system",    "content": "你是 XiaoPaw，一个飞书工作助手..."},
    {"role": "system",    "content": "用户偏好：周报格式先汇总再列计划..."},  # ← 这就是"记忆"
    {"role": "user",      "content": "帮我发周报"},
    {"role": "assistant", "content": "好的，我来按你喜欢的格式..."},
]
```

**你往 messages 里塞了什么，模型就"知道"什么；你没塞的，它就不知道。没有魔法，只有信息流设计。**

### 1.2 电脑类比：贯穿整个模块

| 类比 | 对应 | 特性 |
|---|---|---|
| **模型本身** | CPU | 只负责计算，不存储 |
| **上下文窗口** | 内存（RAM） | 容量有限，断电就没了 |
| **外部存储**（文件、数据库）| 硬盘 | 持久化但需要主动读取 |

**记忆系统的核心就是设计一套机制：什么时候从"硬盘"加载到"内存"，什么时候从"内存"写回"硬盘"，"内存"快满了怎么腾空间。**

### 1.3 三代演进

| 时代 | 核心动作 | 角色类比 | 上下文特征 | 课程 |
|---|---|---|---|---|
| **Prompt Engineering** | 人写全部内容 | 骑手拉缰绳手把手控制 | 用户提问就是全部上下文 | — |
| **Context Engineering** | 工程逻辑构建 message list | 教练规划赛道和路线 | 工具返回值、中间步骤进上下文 | L19 |
| **Harness Engineering** | 预加载索引 + 模型按需探索 | 牧场主建围栏、水源、草料站 | 模型自己决定下一步 | L18 引入概念 |

**控制权从"写内容"转向"造环境"。** 即使 Claude Code 这种最先进产品，也依赖 CLAUDE.md 预加载项目结构、rules/ 预加载约束、MEMORY.md 前 200 行自动注入记忆索引——**预加载关键索引 + 模型按需探索的混合模式**，不是完全放手。

### 1.4 三层记忆架构

| 层级 | 课程 | 存储介质 | 职责 | 触发方式 |
|---|---|---|---|---|
| **上下文层** | L19 | ctx.json + raw.jsonl | Bootstrap 预加载、剪枝、压缩、跨 session 恢复 | 启动时 + 每次 LLM 调用前 |
| **文件层** | L20 | soul/user/agent/memory.md | Agent 主动写入偏好、SOP、主题索引 | 对话中触发（memory-save / skill-creator） |
| **搜索层** | L21 | pgvector + BM25 | 海量历史语义检索、混合召回 | 隐式触发（按 description 路由） |

**完整闭环**：Bootstrap 加载文件 → 对话产生记忆 → 写通道持久化 → 下次 Bootstrap 自动生效 → 大量历史走搜索层按需召回。

---

## 二、核心问题：要解决什么

### 2.1 Agent 的四大记忆缺陷（L18 提出）

| 缺陷 | 表现 | 解决课程 |
|---|---|---|
| **跨 session 失忆** | 重启后昨天说的全忘，每次从陌生人开始 | L19 Bootstrap + ctx.json |
| **长对话崩溃** | 聊久了 token 超限报错，工具越强越刺眼 | L19 剪枝 + 压缩 |
| **偏好学习为零** | 说一百遍"别用表格"下次还犯，没有写通道 | L20 memory-save |
| **历史回忆空白** | 问"上次那个分析结论"一问三不知 | L21 pgvector 搜索 |

### 2.2 Context Rot（上下文腐化）的物理根因

这不是经验之谈，而是 Transformer 架构的物理规律：

- **注意力是有限预算**：1000 token 每个分到 1/1000，100000 token 每个只分到 1/100000。上下文越长，每条信息被"认真看"的程度越低
- **40% 拐点**：上下文使用量超过约 40%，模型能力显著下降（不是线性，是明显拐点）。**研究数据显示性能下降 15-47%**
- **U 型注意力分布**：开头和结尾关注最多，中间最易被忽略（Liu et al., TACL 2024）
- **架构层无法规避**：错误信息一旦进入上下文，会持续影响后续输出（Context Poisoning）

**Context Engineering 不只是能力问题，更是经济问题**——Transformer 计算复杂度是 n²，上下文翻倍成本翻四倍。

### 2.3 文件索引的三大崩溃时刻

1. **规模爆炸**：`memory.md` 200 行硬上限撑破
2. **语义模糊查询**："上次那个航班"类查询无精确分类
3. **维护成本爆炸**：写入、去重、时间戳本身消耗注意力预算

**解法只有一个：把记忆从"全量加载"变成"按需检索"。** 这就是搜索驱动的记忆系统要做的事。

### 2.4 记忆腐化的三形态（L20 提出）

| 形态 | 表现 | 危害 |
|---|---|---|
| **过期事实** | 六个月前的项目状态、已被推翻的技术决策与今天的内容并排存放 | Agent 用错误历史做今天的决策 |
| **相互矛盾** | user.md 说偏好简短回复，agent.md 说详细解释每个步骤 | Agent 行为不一致 |
| **死链/野文档** | memory.md 指向已删除文件；workspace/ 里有未被索引的孤立文件 | Bootstrap 静默失败 |

**反直觉结论：过时的事实比缺失的信息危害更大。** 缺失时 Agent 会说不确定；有错误记忆时 Agent 会自信地给出错误答案。

### 2.5 不干预的三种生产级崩溃

1. **Context Overflow（上下文爆炸）**：100 款商品对比，30 轮后 token 超限
2. **Context Rot（上下文腐烂）**：50 个文件后重复提意见、相互矛盾，模型真的变笨
3. **Context Poisoning（上下文污染）**：错误信息被 Agent 当事实持续影响后续输出

---

## 三、上下文生命周期：六个干预节点

**Agent 的上下文是"生长"出来的，不是"写"出来的。** Workflow 里上下文是你直接写的（Step1→Step2→Step3 完全可控），但 Agent 运行在 ReAct 循环里，模型自己决定下一步，工具返回自动塞进 message list。

**上下文生命周期的本质，就是你在 ReAct 循环中能够干预上下文的时机窗口。**

### CrewAI 提供的六个节点

| 节点 | 触发时机 | CrewAI 支持 | 典型用途 |
|---|---|---|---|
| **① Bootstrap** | Agent 启动前 | ✅ backstory 参数 | 注入身份、规则、记忆索引 |
| **② Pre-Model** | 每次 LLM 调用前 | ✅ @before_llm_call Hook | 剪枝、压缩、session 恢复 |
| **③ Post-Model** | 每次 LLM 调用后 | ⚠️ @after_llm_call（有副作用） | 脱敏、格式化（实践中不用） |
| **④ Pre-Tool** | 工具调用前 | ❌ 需手搓 | 权限校验、参数修正 |
| **⑤ Post-Tool** | 工具调用后 | ❌ 需手搓 | 结果清洗、敏感信息过滤 |
| **⑥ AfterTurn** | 整个 turn 结束后 | ❌ 需手搓（在 kickoff 后做） | session 持久化、异步建索引 |

**CrewAI 官方提供 Bootstrap（backstory）和 Pre-Model Hook（@before_llm_call）两把手术刀**，其余节点理解原理即可，有需要时可手搓。

---

## 四、加法体系：让模型知道该知道的

### 4.1 加法的四种主要手段

| 手段 | 机制 | 类比 | 实现位置 |
|---|---|---|---|
| **Bootstrap 预加载** | 启动时固定加载（人设、规则、记忆索引） | 员工手册 | backstory 注入 |
| **工具返回注入** | Agent 调工具后结果自动进上下文 | 员工查完资料多了一条信息 | ReAct 循环自动 |
| **记忆检索注入** | 从持久化存储中检索相关记忆注入 | 翻看工作笔记 | search_memory Skill |
| **Skill 按需加载** | 渐进式披露，模型判断需要时加载完整内容 | 翻操作手册的具体章节 | SkillLoaderTool |

### 4.2 Bootstrap 四件套（缺一不可）

```python
def build_bootstrap_prompt(workspace_dir: Path) -> str:
    """soul + user_profile + agent_rules + memory_index 四件套注入。"""
    parts = []
    for fname, tag in [
        ("soul.md",   "soul"),           # 身份：我是谁
        ("user.md",   "user_profile"),   # 用户画像
        ("agent.md",  "agent_rules"),    # 行为规范（可进化）
    ]:
        path = workspace_dir / fname
        if path.exists():
            parts.append(f"<{tag}>\n{path.read_text(encoding='utf-8').strip()}\n</{tag}>")

    # memory.md 限制 200 行防膨胀：只是导航索引，不是内容仓库
    memory_path = workspace_dir / "memory.md"
    if memory_path.exists():
        lines = memory_path.read_text(encoding="utf-8").splitlines()[:200]
        parts.append(f"<memory_index>\n{chr(10).join(lines)}\n</memory_index>")

    return "\n\n".join(parts)
```

**关键点**：
- **结构化标签分块**：`<soul>` / `<user_profile>` / `<agent_rules>` / `<memory_index>`，模型定位更准确
- **硬上限保护**：每一块都有字数/行数上限，防止膨胀
- **只注入"导航骨架"**：索引 > 全文，文件是目录不是书
- **agent.md 是自我进化日志**：用户说"以后发消息前先确认"，Agent 增量追加到 agent.md，下次 Bootstrap 时自动注入

### 4.3 写通道：memory-save（语义记忆）

**memory 文件结构**：
```
workspace/
  ├── soul.md          # 人设：固定，工程师写，不被 memory-save 修改
  ├── user.md          # 用户偏好：结构化条目，memory-save 的主要写入目标
  ├── agent.md         # 行为规范：Agent 的增量自我进化记录
  ├── memory.md        # 记忆索引：只存指针，≤200 行，Bootstrap 全量加载
  ├── memory_course.md # 主题文件：只在需要时按需加载
  └── ...
```

**memory.md 只存指针不存内容**：
```markdown
# XiaoPaw 记忆索引

## 工作项目
- 极客时间课程开发 → memory_course.md  [updated: 2026-03-10]
- 个人投资跟踪 → memory_invest.md  [updated: 2026-02-20]

## 重要决策
- 架构决策：MCP 优先于自定义 Tool → memory_arch.md  [updated: 2026-01-15]
```

**类比**：目录永远很薄，书可以很厚。Bootstrap 每次全量加载 memory.md，200 行是硬上限。

**memory-save 四步规范**：

| 步骤 | 内容 |
|---|---|
| **① 准入控制** | 四信号：Utility（三个月后还有价值吗）/ Confidence（有直接证据吗）/ Novelty（是否已存在）/ Type（稳定信息 vs 瞬态）|
| **② 分类路由** | target ∈ {soul, user, agent, memory_index, topic} |
| **③ 写入前检查** | memory.md 行数三级阈值（<150 正常 / 150-179 预警 / ≥180 停止） |
| **④ 更新优于追加** | 找到旧记忆用 str_replace 精准更新；完全找不到才追加 |

**两种触发方式**：
- **显式触发**：用户主动说"把刚才聊的记下来"——信号明确，误判率低
- **主动触发**：description 里描述场景——但 Agent 的主动记忆意识没有你期待的那么强，要写得足够具体

**为什么用 Skill 不用 Tool**：
1. **容量限制**：BaseTool description 装不下几百行的规范
2. **安全隔离**：主 Agent 不持有 FileWriterTool，写操作全部通过 Sub-Crew + 沙盒完成，提示可以被忽略，挂载不能

### 4.4 写通道：skill-creator（程序记忆）

**一句话规则**：
- 记**是什么/有什么** → memory-save
- 记**怎么做 + 需要标准化执行** → skill-creator

**为什么 SOP 不能直接写进 memory 文件**：
1. **执行可靠性**：自然语言 SOP 模型每次执行只是参考，会漏步骤、顺序错、跳过 CRITICAL
2. **路由精准性**：SKILL.md 有 frontmatter，SkillLoaderTool 精准路由；memory 文件只能全文读
3. **可迁移性**：Skill 可以分享给团队其他 Agent，memory 只属于这个实例

**七阶流水线（教学场景用简化三步）**：

```
完整版：Capture Intent → Draft → Eval → Review → Iterate → Optimize → Register
简化版：Capture Intent → Draft → Register
```

**SKILL.md 写作规范**：
- **frontmatter**：`name`（{动词}-{名词}，kebab-case）+ `description`（≤1024 字符，"a little bit pushy"）+ `allowed-tools`（最小化原则）
- **正文**：explain the why——每步操作必须说明原因，模型才能在 edge case 举一反三

**注册步骤**：
```
1. 检查 load_skills.yaml 是否同名（避免静默覆盖）
2. 写入 /mnt/skills/{skill-name}/SKILL.md
3. 追加条目到 load_skills.yaml
4. Read-back 验证落盘
5. 返回 JSON
```

---

## 五、减法体系：让上下文没有不该有的

### 5.1 减法的三种主要手段

| 手段 | 机制 | 关键点 |
|---|---|---|
| **压缩/截断** | 旧对话压成摘要或截断超出窗口部分 | **30-45% 主动触发**，不要等 80% |
| **选择性遗忘** | 主动丢弃低价值信息 | 不是所有信息都值得留 |
| **Sub-agent 隔离** | 任务委派给子 Agent 用干净上下文独立工作 | 主 Agent 上下文始终精简 |

### 5.2 剪枝（Pruning）：控制 Tool Result 膨胀

**最大的上下文膨胀体不是对话，是 Tool Result**。一次搜索工具调用可能返回 10,000 tokens 的网页全文。

**剪枝黄金规则**：
- 对话消息尽量不动（有语义连贯性要求）
- Tool Result 可以大刀阔斧（它只是"原材料"，模型用完就不需要了）
- 关键 ID/数字剪枝前先提取出来单独保存

**实现**：
```python
def prune_tool_results(messages, keep_turns=10):
    """超出 keep_turns 的 tool 消息内容替换为 [已剪枝]，保留消息占位。"""
    user_indices = [i for i, m in enumerate(messages) if m.get("role") == "user"]
    if len(user_indices) <= keep_turns:
        return

    cutoff_idx = user_indices[-keep_turns]
    for i in range(cutoff_idx):
        if messages[i].get("role") == "tool":
            messages[i]["content"] = "[已剪枝]"  # 保留结构，清空内容
```

**为什么保留占位不删除**：删除 tool 消息会导致 `tool_call_id` 引用断裂，Qwen/OpenAI 格式校验报错。保留消息结构但清空内容是安全做法。

### 5.3 压缩（Compaction）：把历史变成摘要

**触发阈值**：**30-45% 主动触发**（不是 80%）。Context Rot 表明上下文变长本身就导致性能下降，主动保持干净比被动救火更有效。

**任务类型影响阈值**：
- 问答/助手类（30-50%）：上下文独立性强，可以激进压缩
- 长任务/调试类（70-85%）：状态依赖强，压缩要保守

**Lossless 策略**：压缩必然丢失信息，关键策略是**压缩前先持久化**——把重要信息写到磁盘，磁盘上有完整记录。`append_session_raw()` 必须每次 kickoff 后都执行，raw.jsonl 是 append-only 原始完整历史，ctx.json 是压缩快照。

**压缩实现关键点**：
- **分块摘要**（chunk_by_tokens + 轻量模型）：比整体摘要更精准，也能控制单次摘要输入规模
- **摘要用小模型**（qwen3-turbo），对话用大模型（qwen3-max）：摘要是低创造性高准确性任务，成本降低 60-70%
- **以 user 消息边界切割**：`user_indices[-fresh_keep_turns]` 定位，保证 tool_call + tool_result 成对保留
- **摘要用 `role: "system"`**：语义上是"背景信息"而不是"用户发言"

```python
def maybe_compress(messages, context, fresh_keep_turns=10, compress_threshold=0.45):
    """超过 45% 上下文使用率时：保留 system + 最近 10 轮，旧消息分块摘要。"""
    approx_tokens = sum(len(str(m.get("content", ""))) // 2
                        for m in messages if m.get("role") != "system")
    if approx_tokens / model_limit < compress_threshold:
        return  # 未超阈值

    # 旧消息分块，用 qwen3-turbo 摘要后以 <context_summary> system 消息插回
    old_msgs   = non_system[:cutoff]
    fresh_msgs = non_system[cutoff:]
    summary_msgs = [
        {"role": "system",
         "content": f"<context_summary>\n{_summarize_chunk(chunk)}\n</context_summary>"}
        for chunk in chunk_by_tokens(old_msgs)
    ]
    messages.clear()
    messages.extend(system_msgs + summary_msgs + fresh_msgs)
```

**结构化压缩 Prompt**：必须明确"保留什么、丢弃什么"，至少包含三个保留项——用户目标、关键事实、未完成事项。明确禁止：中间过程、失败尝试、重复内容。

---

## 六、三层记忆架构的实现细节

### 6.1 上下文层（L19）：Bootstrap + 剪枝 + 压缩

**Session 持久化**（两份存储各有职责）：
```python
# ctx.json：覆盖写，跨 session 快速恢复用的压缩快照
save_session_ctx(session_id, messages, ctx_dir)

# raw.jsonl：追加写，完整审计日志，调试用
append_session_raw(session_id, new_msgs, ctx_dir)
```

**Session 恢复**：
```python
def _restore_session(self, context):
    """用历史 ctx 替换 context.messages + 追加新 user 消息。"""
    history = load_session_ctx(self.session_id)
    self._history_len = len(history)

    if not history:
        return  # 全新 session，无历史可恢复

    # 在 clear() 之前提取当前轮 user 消息，避免替换后丢失
    current_user_msg = next(
        (m for m in reversed(context.messages) if m.get("role") == "user"),
        {},
    )

    context.messages.clear()
    context.messages.extend(history)
    if current_user_msg:
        context.messages.append(current_user_msg)
```

**Session 内多轮**：append-only 模式——不是重新建 Crew kickoff，而是往已有的 message list 追加一条 user 消息。Agent 在第 N 轮时，能看到从第 1 轮开始的所有中间过程。

### 6.2 文件层（L20）：memory-save + skill-creator + memory-governance

**memory-save 五种写入目标**：

| target | 写到哪里 | 存什么 |
|---|---|---|
| `soul` | /workspace/soul.md | XiaoPaw 自身设置（名字、人设）|
| `user` | /workspace/user.md | 用户偏好、习惯、个人信息 |
| `agent` | /workspace/agent.md | Agent 行为规范的增量更新 |
| `memory_index` | /workspace/memory.md | 新增主题索引条目（只写指针）|
| `topic` | /workspace/memory_{name}.md | 某主题的详细内容 |

**每条记忆条目的标准结构**：
```markdown
- 代码注释用中文  [created: 2026-01-15, updated: 2026-03-10]
```
两个时间戳：`created`（信息产生时间）+ `updated`（最近一次确认/修改时间），是治理的基础。

**Garbage Collection（记忆治理）**：
- **触发方式**：阈值触发（memory-save 写入时检测行数）+ 定期触发（每月一次）+ 用户主动
- **8 类检查**：①行数+死链 / ②野文档 / ③路由错配 / ④表述冲突 / ⑤表述冗余 / ⑥Skills 健康度 / ⑦安全扫描 / ⑧过期条目
- **CRITICAL 约束**：扫描 → 分析 → 报告 → **等待用户确认** → 清理。不得在用户确认前执行任何删除或修改操作

**无 GC 的记忆系统生命周期**：
```
0-3 月：记忆有价值，Agent 越来越好用 ✓
3-6 月：噪音积累，偶尔表现变差
6 月+：记忆成为负担，重置比维护更省事 ✗
```

### 6.3 搜索层（L21）：RAG + 混合检索

**RAG 的本质**：检索增强生成是通用范式，**不等于向量搜索**。搜索方式可以是 grep、SQL 全文索引、向量语义搜索、知识图谱——任意的。

**四种搜索方式**：

| 方式 | 适用场景 | 典型工具 |
|---|---|---|
| **grep/全文扫描** | 小规模、精确 token 匹配（错误码、变量名、文件路径） | grep, ripgrep |
| **BM25 + 字段搜索** | 工业标准，考虑词频+逆文档频率+文档长度归一化 | Elasticsearch, PostgreSQL tsvector + GIN |
| **向量语义搜索** | 模糊查询（"上次那个航班"），让 AI 理解语义 | pgvector, Milvus, Pinecone |
| **知识图谱** | 关系推理（"A 的所有依赖"）| Neo4j, Amazon Neptune |

**生产级方案是混合检索，不是纯向量**。纯向量搜索在生产中几乎不够用——"上周"是时间约束纯向量无法过滤，"代码"是标签约束纯向量精度下降。

**核心 SQL（一条搞定混合检索）**：
```sql
SELECT id, summary, ..., 
  0.7 * (1 - (summary_vec <=> %(query_vec)s::vector))
  + 0.3 * ts_rank(search_tsv, plainto_tsquery('simple', %(tsquery)s)) AS score
FROM memories
ORDER BY score DESC
LIMIT %(limit)s
```

**关键设计原则：向量是普通列，不是独立的向量数据库**。标量+向量在同一张表，一条 SQL 搞定，架构简单、运维成本低。pgvector 把向量作为普通列，专门向量数据库需要先向量搜索再回查关系数据库，两次 I/O、还要维护数据一致性。

**Schema 设计要点**：
```sql
-- 两个向量列：摘要向量 + 原始对话向量，分别用于不同搜索场景
summary_vec     vector(1024),  -- 摘要语义搜索
message_vec     vector(1024),  -- 原始对话语义搜索

-- search_tsv 用 GENERATED ALWAYS AS 自动维护（精妙设计）
search_text     TEXT        NOT NULL DEFAULT '',
search_tsv      TSVECTOR    GENERATED ALWAYS AS (to_tsvector('simple', search_text)) STORED
```

**幂等写入**：`id` 用 `session_id + turn_ts + user_message 前32字符` 的 SHA256 生成，`ON CONFLICT DO NOTHING`。网络抖动、重启恢复、手动重跑都不会产生重复记录。

**降级重试策略写进 Skill 文档**（不写代码）：
1. 搜索结果为空 → 先去掉 `--days` 限制（时间范围可能太窄）
2. 还是空 → 再去掉 `--tags` 限制（标签可能不匹配）
3. 还是空 → 切换 `--mode vector` 纯语义搜索

---

## 七、工程落地的关键实现

### 7.1 CrewAI Hook 系统

**核心代码骨架**：
```python
from crewai.hooks import LLMCallHookContext, before_llm_call
from crewai.project import CrewBase, agent, crew, task

@CrewBase  # 💡 核心点：@CrewBase 才能绑定 @before_llm_call hook
class MemoryAwareCrew:

    @agent
    def orchestrator(self) -> Agent:
        cfg = dict(_load_yaml(_CONFIG_DIR / "agents.yaml")["orchestrator"])
        cfg["backstory"] = build_bootstrap_prompt(self._workspace_dir)  # 动态覆盖
        ...

    @before_llm_call
    def before_llm_hook(self, context: LLMCallHookContext) -> bool | None:
        if not self._session_loaded:
            self._restore_session(context)  # ① 首次：从 ctx.json 恢复历史
            self._session_loaded = True

        self._last_msgs = context.messages  # 保存引用，run_and_index 后用

        prune_tool_results(context.messages, keep_turns=self._prune_keep_turns)
        maybe_compress(context.messages, context)
        return None
```

**Hook 底层运行机制**（@CrewBase 自动处理）：
1. **标记检测**：`@before_llm_call` 在方法上打 `is_before_llm_call_hook` 标记
2. **自动注册**：`_register_crew_hooks()` 扫描类方法，把 bound method 注册到全局 Hook 列表
3. **作用域绑定**：注册的是 `bound_hook = hook_method.__get__(instance, cls)`，天然隔离
4. **每次 LLM 调用前触发**：executor 遍历 Hook 列表，传入 `LLMCallHookContext`

**关键约束：in-place 修改**：
```python
# ✅ 正确：in-place 修改
context.messages[i]["content"] = pruned_content
context.messages.clear()
context.messages.extend(new_messages)

# ❌ 错误：重新赋值会断开内部引用，修改无效
context.messages = [...]
```

### 7.2 LLMCallHookContext 提供的信息

```python
context.messages    # List[dict]，mutable，in-place 修改
context.agent       # 当前 Agent 对象
context.task        # 当前 Task
context.crew        # Crew 实例
context.llm         # LLM 实例（可读 context_window_size）
context.iterations  # 当前迭代次数
context.response    # after-hook 专用（但有副作用，实践中不用）
```

可以用 `context.iterations` 做迭代上限控制，用 `context.llm.context_window_size` 动态计算压缩阈值。

### 7.3 异步建索引（不阻塞主流程）

```python
async def run_and_index(self) -> str:
    result = await self.crew().akickoff(inputs={...})

    # ① 持久化 ctx.json + raw.jsonl（同步，必须完成）
    if self._last_msgs:
        new_msgs = list(self._last_msgs)[self._history_len:]
        append_session_raw(self.session_id, new_msgs, ctx_dir=self._ctx_dir)
        save_session_ctx(self.session_id, list(self._last_msgs), ctx_dir=self._ctx_dir)

    assistant_reply = result.pydantic.reply

    # ② 异步建索引，不阻塞主流程返回
    if self._db_dsn:
        _task = asyncio.create_task(   # 💡 必须持有引用，防止 Python GC 取消任务
            async_index_turn(
                session_id      = self.session_id,
                user_message    = self.user_message,
                assistant_reply = assistant_reply,
                turn_ts         = self._turn_start_ts,
                db_dsn          = self._db_dsn,
            )
        )

    return assistant_reply
```

### 7.4 索引 pipeline

```python
def _index_single_turn(...):
    # 生成幂等 id（SHA256 前 16 位）
    turn_id = hashlib.sha256(f"{session_id}_{turn_ts}_{user_message[:32]}".encode()).hexdigest()[:16]

    conn = _connect_db(db_dsn)
    summary, tags = extract_summary_and_tags(user_message, assistant_reply)  # 调 LLM 提取摘要
    vecs = embed_texts([summary, f"用户：{user_message}\n 助手：{assistant_reply}"])  # 双向量化

    upsert_memory(conn, {
        "id":          turn_id,
        "summary":     summary,
        "tags":        tags,
        "summary_vec": vecs[0],   # 摘要向量
        "message_vec": vecs[1],   # 原始对话向量
        "search_text": user_message + " " + " ".join(tags),  # 供 GIN 全文索引
    })  # ON CONFLICT DO NOTHING — 幂等写入
```

### 7.5 完整集成架构（L22 实战）

**原骨架一行代码没动**，改动集中在四个地方：

| 改动 | 文件 | 内容 |
|---|---|---|
| **核心改造** | `agents/main_crew.py` | @CrewBase 类 + @before_llm_call 钩子 + run_and_index() |
| **新增记忆模块** | `xiaopaw/memory/` | bootstrap.py / context_mgmt.py / indexer.py |
| **新增 Skills** | `skills/` | memory-save / skill-creator / memory-governance / search_memory |
| **基础设施** | `docker-compose.yml` | 新增 pgvector 容器 |

**"骨架零改动、记忆长出来"**：好的工程设计留了扩展空间，后来加的东西只需要挂上去，不需要动骨架。

### 7.6 初始引导机制

把引导程序写进 `agent.md` 本身（不是代码里），内置"初始引导 SOP"：
```markdown
# 初始引导 SOP（agent.md 内置）

引导进度（每步完成后调用 memory-save 更新打勾）：
- [ ] ① 起名        — 介绍自己，问是否改名
- [ ] ② 主要用途    — 让用户用自己的话描述
- [ ] ③ 回复风格    — 根据用途推理 3 个候选，用户选择
- [ ] ④ 用户信息    — 聚焦问 1-2 个最关键背景
- [ ] ⑤ 禁忌        — 问有没有特别不想看到的
- [ ] ⑥ SOP 调教    — 提出 1 个核心 SOP 草案，确认后调用 skill-creator 沉淀
```

**触发时机完全由对话决定**——不是程序判断"第一次启动"，而是 Agent 自己在对话中发现引导节存在自然开始。引导可以跨 session 分多次完成，不会重复。

**自我清除机制**：六步全部完成后，XiaoPaw 调用 `memory-save`，用 Python regex 脚本把整个引导节从 `agent.md` 里删掉。引导完成，不留痕迹，也不再消耗后续每次 session 的 token 预算。

---

## 八、最佳实践（生产级经验）

### 8.1 Bootstrap 相关

| 实践 | 落地心法 |
|---|---|
| **Bootstrap 四件套缺一不可** | soul（身份）+ user（用户画像）+ agent（行为规范）+ memory index（记忆索引），缺一不可 |
| **agent.md 是自我进化日志** | 行为约束用 memory-save 增量追加到 agent.md，下次 Bootstrap 时自动注入，永久生效 |
| **MEMORY.md 设 200 行硬上限** | Claude Code 工程团队经过生产验证的数字，超过 200 行的索引反而让模型难以定位关键信息 |
| **结构化标签分块** | `<soul>` / `<user_profile>` / `<agent_rules>` / `<memory_index>`，XML 标签让模型定位更准确 |

### 8.2 压缩与剪枝相关

| 实践 | 落地心法 |
|---|---|
| **激进的触发阈值（30-50%）** | 把 `COMPRESS_THRESHOLD` 设为 0.45，不要等到 80%。Context Rot 数据表明上下文变长本身就导致性能下降 |
| **Raw log 保证 lossless** | `append_session_raw()` 必须每次 kickoff 后都执行，ctx 快照是压缩视图，raw 是完整审计日志 |
| **摘要用小模型，对话用大模型** | qwen3-turbo 做摘要、qwen3-max 做对话，成本降低 60-70% |
| **分块摘要（chunk_by_tokens）** | 比整体摘要更精准，也能控制单次摘要输入规模 |
| **结构化压缩 Prompt** | 必须明确"保留什么、丢弃什么"，至少包含用户目标/关键事实/未完成事项 |
| **后台静默压缩** | 异步放到后台线程执行，压缩在对话间隙悄悄完成，用户感知瞬时响应 |

### 8.3 写通道相关

| 实践 | 落地心法 |
|---|---|
| **准入四门控：在调用前过滤** | Utility → Confidence → Novelty → Type，写入成本极低，治理成本极高，在源头控制省 10 倍力气 |
| **双时间戳** | `created`（信息产生时间）+ `updated`（最近一次确认/修改时间），治理有据可查 |
| **更新优于追加** | 找到相关旧记忆用 `str_replace` 精准更新，完全找不到才追加。维基百科词条效果 |
| **闭环治理** | memory-save 写入超 150 行时返回信号，主 Agent 主动触发 memory-governance |
| **禁止写来源不明的内容** | 外部工具原始输出必须先人工提炼再写——防范 Prompt Injection 写入持久化记忆的第一道防线 |
| **Skill vs Memory 一句话规则** | 记是什么 → memory-save；记怎么做 + 标准化执行 → skill-creator |

### 8.4 搜索层相关

| 实践 | 落地心法 |
|---|---|
| **chunk 设计优先于模型选型** | 召回效果 80% 取决于 chunk 质量，20% 取决于 embedding 模型。问答配对为最小语义单元 |
| **幂等写入，增量更新** | id 用内容哈希（SHA256 前 16 位），`ON CONFLICT DO NOTHING` |
| **降级重试写进 Skill 文档** | 不写代码实现，写在 SKILL.md 里 Agent 自主执行 |
| **向量是普通列** | 标量+向量在同一张表，混合检索一条 SQL 搞定 |

### 8.5 工程结构相关

| 实践 | 落地心法 |
|---|---|
| **骨架零改动、记忆长出来** | 飞书路由/session 隔离/Skills 路由三个核心模块不动，记忆系统只挂上去不侵入 |
| **引导写在 agent.md 而非代码** | 引导逻辑是 Agent 行为规范不是程序流程，用户可调整，逻辑不固化 |
| **同步持久化 + 异步建索引** | ctx.json 同步（启动恢复）+ raw.jsonl 同步（审计）+ pgvector 异步（不阻塞） |
| **必须持有 asyncio.create_task 引用** | 防止 Python GC 取消任务 |
| **渐进式披露** | memory.md 是索引（很薄），topic 文件是内容（按需读），与 SkillLoaderTool 同一哲学 |

### 8.6 Session 管理相关

| 实践 | 落地心法 |
|---|---|
| **持久化在 kickoff 后由 main() 统一完成** | @after_llm_call 因框架 bug 不可用（强制 str() 破坏 tool 调用） |
| **ctx 快照 + raw 日志双写** | ctx 用于快速恢复，raw 用于 lossless 审计/调试 |
| **session 内多轮 append-only** | 不重建 Crew kickoff，往已有 message list 追加 user 消息 |
| **恢复时先提取当前 user 消息** | 在 clear() 之前提取，避免替换后丢失 |
| **Bootstrap 文件定期治理** | 每周让 Agent 自我复盘，合并可合并条目，主题文件化 |

---

## 九、反模式与避坑指南

### 9.1 上下文管理类反模式

| 反模式 | 致命后果 |
|---|---|
| **`memory=True` 就算上下文管理** | CrewAI 的 `memory=True` 是 Task 级 RAG 检索，不是对话历史压缩/Bootstrap 加载 |
| **等 token 超限再处理** | Context Rot 在超限之前就已经让模型变笨了，API 返回 `context_length_exceeded` 时已晚 |
| **重新赋值 `context.messages`** | 重新赋值让变量名指向新列表，executor 内部引用没变，修改对框架完全不可见 |
| **压缩时不保护 tool message pair** | 框架处理 tool result 时找不到对应 tool_call_id 会报错，必须以 user 边界切割 |
| **压缩粒度过细** | 单条消息为单位压缩会完全丢失语义，chunk 必须包含上下文关联的若干条消息 |
| **压缩写磁盘不加并发保护** | 多进程并发 `json.dump()` 会损坏文件，应用 `os.replace()` 原子替换或 `fcntl.flock()` 文件锁 |

### 9.2 文件记忆类反模式

| 反模式 | 致命后果 |
|---|---|
| **记忆污染链** | Prompt Injection 写入持久化记忆，每次 Bootstrap 都作为事实重新注入，复利式放大。Palo Alto Unit42 试验成功率 >95% |
| **记忆贪婪症（无差别写入）** | 2025 年 MemSkill 论文量化：无差别写入记忆的 Agent 基准测试得分比空记忆还低 |
| **记忆腐化（只写不治理）** | 真实案例：记忆文件超 6000 token 后模型随心所欲，API 开销从 $127/周飙到 $47,000/月 |
| **跨用户记忆泄露** | Echoleak 事件：GDPR/HIPAA 场景直接合规风险，扩展多用户时 workspace/{user_id}/ 命名空间隔离 |

### 9.3 搜索层类反模式

| 反模式 | 致命后果 |
|---|---|
| **把 RAG 等同于向量搜索** | 函数名/错误码/文件路径等精确 token 匹配，向量搜索召回率不如 BM25 |
| **冷启动就上 RAG** | 记忆量几十条用 grep，几百条用文件系统，上千条才需要搜索。过早引入是过度工程 |
| **向量和标量数据分离存储** | 两套系统数据一致性是噩梦，分布式事务复杂度远超收益。pgvector 把向量当普通列才是正确架构 |

### 9.4 工程实现类反模式

| 反模式 | 致命后果 |
|---|---|
| **直接给主 Agent 挂 FileWriterTool** | 主 Agent 能写到任何路径，提示可被忽略，挂载不能。正确做法是 Sub-Crew + 沙盒隔离 |
| **用 @after_llm_call 做 session 持久化** | CrewAI 强制 str() 导致 executor isinstance(answer, list) 失败，工具不执行 |
| **不区分"是什么"和"怎么做"** | SOP 写进 memory 文件得到的是语义记忆不是程序记忆，执行不可靠 |

### 9.5 通用避坑提醒

- **sub-agent 也要遵循 file 隔离**：多用户场景必须用 `workspace/{user_id}/` 命名空间
- **description 写得太被动会 undertriggering**：Claude 有不触发 Skill 的倾向，description 要 "a little bit pushy"
- **CRITICAL 规则必须 explain the why**：没有原因的 NEVER，模型在 edge case 会自行决定要不要遵守
- **Bootstrap 不是越多越好**：四件套够用，agent.md 写得越多启动越慢
- **memory.md 超过 150 行就开始预警**：等到 180 行才停才有 20 行缓冲，非常危险

---

## 十、关键经验总结

### 10.1 核心公式

> **记忆系统 = 三层架构 + 双向闭环 + 主动治理**
> - 三层架构 = 上下文层（实时）+ 文件层（持久化）+ 搜索层（按需）
> - 双向闭环 = Bootstrap 读通道 ↔ memory-save 写通道
> - 主动治理 = 30-45% 触发压缩 + 150 行触发 GC + 180 天过期候选

### 10.2 设计哲学

1. **没有魔法，只有信息流设计**——记忆的本质是构建 message list
2. **控制权从"写内容"转向"造环境"**——Harness 时代，预加载索引 + 模型按需探索
3. **目录永远很薄，书可以很厚**——索引 > 全文，200 行硬上限
4. **三种加法 + 三种减法**——Bootstrap/工具返回/记忆检索/Skill 按需 + 压缩/选择性遗忘/Sub-agent 隔离
5. **异步不阻塞、同步要保证**——建索引异步、持久化同步

### 10.3 关键工程决策

| 决策点 | 推荐选择 | 理由 |
|---|---|---|
| 记忆存储 | pgvector（向量当普通列）| 标量+向量同表，混合检索一条 SQL |
| Bootstrap 触发 | backstory 注入 | CrewAI 官方支持，无需 monkey-patch |
| 剪枝/压缩触发 | @before_llm_call Hook | 唯一可在 LLM 调用前干预的位置 |
| Session 持久化 | kickoff 后由 main() 统一 | @after_llm_call 不可用（框架 bug）|
| 写入通道 | Sub-Crew + 沙盒 | 主 Agent 不持有 FileWriterTool，挂载保证隔离 |
| 摘要模型 | 小模型（qwen3-turbo）| 低创造性高准确性任务，省 60-70% 成本 |
| 压缩阈值 | 30-45% 主动触发 | Context Rot 物理规律，不要等 80% |
| memory.md 上限 | 200 行硬上限 + 150 行预警 | Claude Code 工程团队生产验证 |
| 混合检索权重 | 向量 0.7 + 全文 0.3 | 经验值，语义相似度权重更高 |
| chunk 单元 | 问答配对为最小语义单元 | 用户问题+助手回答合在一起才能完整表达 |

### 10.4 完整闭环图

```
┌─────────────────────────────────────────────────────────────┐
│                    上 下 文 管 理 闭 环                       │
└─────────────────────────────────────────────────────────────┘

        ┌──────────── 启动（Bootstrap）────────────┐
        │                                          │
        │  soul.md + user.md + agent.md            │
        │  + memory.md（≤200 行索引）              │
        │              ↓                           │
        │  build_bootstrap_prompt()                │
        │              ↓                           │
        │  backstory 注入 system prompt            │
        └──────────────────┬───────────────────────┘
                           ↓
        ┌──────────── 运行（ReAct 循环）────────────┐
        │                                           │
        │  每次 LLM 调用前：@before_llm_call        │
        │  ├── 首次：load ctx.json 恢复             │
        │  ├── 剪枝：tool result → [已剪枝]        │
        │  └── 压缩：超 45% 时摘要旧消息            │
        │                                           │
        │  工具调用后：自动注入结果                  │
        │                                           │
        │  按需加载：SkillLoaderTool 渐进式披露     │
        └──────────────────┬────────────────────────┘
                           ↓
        ┌──────────── 写通道（运行时）─────────────┐
        │                                          │
        │  偏好/事实 → memory-save → user.md       │
        │                  → memory.md（指针）      │
        │                  → topic 文件            │
        │                                          │
        │  SOP/流程 → skill-creator                │
        │            → skills/{name}/SKILL.md      │
        │            → load_skills.yaml（注册）    │
        │                                          │
        │  搜索召回 → search_memory                │
        │           → pgvector + BM25 混合检索     │
        └──────────────────┬───────────────────────┘
                           ↓
        ┌──────────── 持久化（kickoff 后）─────────┐
        │                                          │
        │  append_session_raw() → raw.jsonl        │
        │  save_session_ctx()   → ctx.json         │
        │  asyncio.create_task() → pgvector 异步   │
        └──────────────────┬───────────────────────┘
                           ↓
        ┌──────────── 治理（定期/阈值/主动）────────┐
        │                                          │
        │  memory-governance 8 类检查              │
        │  扫描 → 报告 → 用户确认 → 清理           │
        │  归档优于删除                            │
        └──────────────────┬───────────────────────┘
                           ↓
                  ┌───── 下次 session ─────┐
                  │  Bootstrap 重新加载     │
                  │  闭环完成 ✓            │
                  └────────────────────────┘
```

### 10.5 三句话总结

1. **记忆的本质是构建 message list**——你往里塞什么，模型就知道什么；context 不只是能力问题，更是经济问题
2. **三层记忆分工协作**——上下文层管实时（剪枝+压缩）、文件层管持久化（写+治理）、搜索层管海量（混合检索）
3. **写入成本极低，治理成本极高**——准入四门控+定期 GC+确认前不删除，把治理做在源头

---

> **参考代码**：https://github.com/kid0317/xiaopaw-with-memory
> **覆盖范围**：L18-L22 全部核心概念、问题、处理方式、注意事项
> **适用场景**：企业级多智能体系统的上下文工程设计与实现
