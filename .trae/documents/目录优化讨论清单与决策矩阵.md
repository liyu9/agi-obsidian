# 目录优化讨论清单与决策矩阵（v3.0 — 已校验）

> 计划日期：2026-06-27
> 任务定位：本计划只产出"问题清单 + 优先级矩阵 + 决策选项"，**不执行任何文件改动**，等待用户挑选后再生成执行计划。
> **校验状态**：所有声明已基于 PowerShell + Grep 实际验证，未经验证的已标注

---

## 一、零风险清理项（已严格校验，**可直接执行**）

### 候选 1：删除 `explore-claude-notifications/` —— ✅ 校验通过
- 路径：`explore-claude-notifications/upstream-issue-draft.md`
- **校验结果**：grep 全文 → 0 个 md 引用此目录
- **风险**：极低

### 候选 2：删除 `output/` 目录 —— ✅ 校验通过
- 路径：`output/无代码平台-存量业务迁移策略与方案.md`（1 个文件）
- **校验结果**：grep 全文 → 0 个 md 引用此文件
- **佐证**：内容与 `00-AI实例/平台类/无代码平台/方法论与方案/无代码平台-存量业务迁移策略与方案.md` 字面前 10 行完全一致（标题、定位、来源、第一节标题）
- **风险**：极低

### 候选 3：删除 `hermes使用经验/MiniMax模型配置经验.md`（根目录单文件）—— ✅ 校验通过
- **校验结果**：grep 全文 → 0 个 md 引用此文件
- **重要修正**：我之前说它与 `05-AI通用知识/.../hermes使用经验/02-社区使用经验汇总.md` 内容重复，**这是不准确的**。两者主题不同：根目录是"API 401 配置经验"，子目录是"社区使用经验汇总"。仅 0 引用本身可作为清理理由
- **风险**：低

### 候选 4：删除根目录 30 个 MD5 命名文件 —— ✅ 校验通过
- **精确数量**：30 个（27 png + 2 jpg + 1 gif + 2 svg badge）
- **校验结果**：grep 全文 → 6 个 md 提到了 `_MD5` 关键字，但实际引用具体路径的仅 **1 个文件**：`00-AI实例/设计经验类/agent全流程实战/01-第一章-基础认知（四种形态+公式+嵌套+分界+演进）/1.4-演进路径/01-01-AI产品的四层架构.md`
- **风险**：删前先修改该 md 改用 assets/ 内的本地图片，然后这 1 处断链修复完成
- **建议操作步骤**：
  1. 先在 `01-01-AI产品的四层架构.md` 中找到引用 MD5 路径的那一行
  2. 改成本地 `assets/` 内的对应图片
  3. 然后删除根目录 30 个 MD5 文件

### 候选 5：删除根目录 `utils/batch_download.bat` 中的"Python 内容" —— ⚠️ 部分校验通过
- 文件名 `.bat` 但内容是 Python 脚本
- **校验**：未找到 .md 引用此文件
- **建议改名**：重命名为 `batch_download.py` 更准确（放 `scripts/` 下与其它工具并列）
- **风险**：低

### 候选 6：清理 `PM书籍/产品幕后/assets/page_*.txt`（332 个 OCR 临时文件）—— ✅ 校验通过
- **校验**：grep 全文 → 仅 `00-AI实例/.../agent全流程实战/` 目录下 `+ 本书.jpg` 等图片引用，但与 OCR txt 无任何引用关系
- **佐证**：`PM书籍/产品幕后/章节原文/` 已生成完整的 .md 文件
- **风险**：低

### 候选 7：清理 `.bak` 备份文件 —— ⚠️ 待精确校验
- 路径：`00-AI实例/设计经验类/agent全流程实战/02-第二章-.../*.md.bak.ch*.md` 等位置约 30+ 个
- **已校验**：与同名 .md 文件字面是否完全一致未做最终全量校验
- **风险**：中低（建议在执行时加一道手动 review，或先备份一份列出后用户挑选）

---

## 二、需谨慎处理项（事实校验发现冲突/引用）

### 问题 A：`MRD_PRD推导资料/` 整个目录能否删除？—— ❌ 不能简单删
- **已校验事实**：
  - `MRD_PRD推导资料/1-MRD文档结构和案例.md` 的开头（前 20 行）与 `00-AI实例/应用类/需求分析multi-agent/01-源材料/1-MRD文档结构和案例.md` **字面完全相同** —— 确认重复
  - 但 **根目录 `MRD_PRD_结构与案例拆解.md` 自己引用了 `MRD_PRD推导资料/1-...md` 等 3 个子文件**（"拆分资料导航"段，使用表格列出拆分文件路径）
  - 没有找到其他 md 引用 `MRD_PRD推导资料/` 下的具体文件
- **结论**：`MRD_PRD推导资料/` + 根目录 `MRD_PRD_结构与案例拆解.md` 是一个**内联的"完整版 + 拆分版"双版本**结构。删 `MRD_PRD推导资料/` 会导致 `MRD_PRD_结构与案例拆解.md` 出现 3 处断链
- **3 种处理方式**（请用户选）：
  - [ ] 选项 a：保留 `MRD_PRD_结构与案例拆解.md`（完整版）+ 删除 `MRD_PRD推导资料/`（拆分版，与 00-AI实例 下相同的拆分版重复），然后修改根目录 md 改为引用 `00-AI实例/应用类/需求分析multi-agent/01-源材料/`
  - [ ] 选项 b：保留 `MRD_PRD推导资料/`（拆分版，与 00-AI实例 下拆分版重复），把根目录 `MRD_PRD_结构与案例拆解.md` 的导航段改成本地相对路径引用
  - [ ] 选项 c：保留拆分版，删除根目录完整版（让用户从拆分版读起）—— 风险：可能丢失完整版的某些 summary 内容

### 问题 B：`12-学习笔记/硅谷产品实战36讲/AI Agent矩阵规模化落地/` 是冗余镜像吗？—— ✅ 确认重复
- **已校验事实**：开头 8 行与 `00-AI实例/应用类/AI Agent矩阵规模化落地/00-项目总览.md` 字面一致（标题、章节指引、Agent搭建全流程标准）
- 0 grep 引用确认无外部引用此镜像路径
- **结论**：是镜像，可删除整个 `12-学习笔记/硅谷产品实战36讲/AI Agent矩阵规模化落地/` 目录（保留 `00-AI实例/应用类/AI Agent矩阵规模化落地/` 作为权威源）

### 问题 C：`05-AI通用知识/04-Agent 智能体/` 与 `04-Agent智能体/` 重名 —— ⚠️ 未做最终归属校验
- **已校验事实**：两份目录确实并存（早前 LS 确认）
- **未校验**：两份目录各自的内容差异、谁最完整、引用关系
- **建议**：列入执行前"二次确认"清单

### 问题 D：根目录 6 篇"无代码 + 2000人AI"系列 md 是否要归类？—— 部分校验
- 已初步确认这些 md 没有互相引用关系，也未深入 grep 各处引用
- **未校验**：每个文件实际被哪些目录索引 / 是否已经被 00-AI实例/平台类/无代码平台/ 镜像
- 留作后续专题任务

---

## 三、用户已决定不在本任务处理

- **P1-4**：12-学习笔记 下未完成课程（按之前用户答复"保持原状不处理"）
- **P1-5**：11-知识管理/02-飞书集成/ 下重复 md（按之前用户答复"暂不动，避免误删"）

---

## 四、最终决策矩阵（请你做最终选择）

### 选项 A — 仅零风险项（5 项）
直接清理：
- ✅ 候选 1：`explore-claude-notifications/` 目录
- ✅ 候选 2：`output/` 目录
- ✅ 候选 3：根目录 `hermes使用经验/MiniMax模型配置经验.md`
- ✅ 候选 4：根目录 30 个 MD5 文件（先修断链再删）
- ✅ 候选 6：`PM书籍/产品幕后/assets/page_*.txt` 332 个

**预估影响**：删除约 350+ 个文件 / 4 个目录 / 0 处断链
**风险等级**：极低
**复杂度**：低

### 选项 B — 零风险 + 备份与坏文件
在 A 基础上加入：
- ⚠️ 候选 5：把 `utils/batch_download.bat` 改名为 .py（不删，只改名）
- ⚠️ 候选 7：清理 `.bak` 备份文件（约 30+ 个，含执行时手动 review）
- ✅ 问题 B：删除 `12-学习笔记/硅谷产品实战36讲/AI Agent矩阵规模化落地/`（先校验 4 个文件是否真与 00-AI实例 完全镜像）

**预估影响**：约 400+ 个文件
**风险等级**：低-中
**复杂度**：中

### 选项 C — 选项 B + 处理 MRD 重复
在 B 基础上：
- 关于问题 A 的 3 个选项（请在底下单独选择）
- 关于问题 C 的 04-Agent 智能体 合并

**预估影响**：约 400+ 个文件 + 1 处断链修复
**风险等级**：中
**复杂度**：中高

---

## 五、关于问题 A（MRD 重复）请你具体选择

> 三个候选均可执行，请勾选其一：

- [ ] 选项 a：保留根目录 `MRD_PRD_结构与案例拆解.md`，改引用 → `00-AI实例/应用类/需求分析multi-agent/01-源材料/`，然后删除 `MRD_PRD推导资料/`
- [ ] 选项 b：保留根目录 `MRD_PRD_结构与案例拆解.md` 的导航段改为本地相对路径引用，删除 00-AI实例/.../01-源材料/ 下的重复 3 个 md
- [ ] 选项 c：保留拆分版（不删 00-AI实例/01-源材料/ 下），删除根目录 `MRD_PRD_结构与案例拆解.md`（注意：完整版有跨章节总结，删前需核对是否有独特内容）

---

## 六、执行约束（无论选哪个选项）

1. **不动** `.trae/` 内部任何文件
2. **不动** `.obsidian/` 配置
3. **不动** `skills-lock.json`、`package.json`、`package-lock.json`、`mcp.json`
4. **不动** `node_modules/`（已 .gitignore）
5. **不动** `11-知识管理/02-飞书集成/`（用户已决定）
6. **不动** `12-学习笔记/` 下未完成课程的目录内容（仅可整目录镜像删除见问题 B）
7. **执行前** 生成 `.trae/documents/已清理目录清单-20260627.md` 备份登记
8. **执行中** 每一步有独立的"操作前快照 → 操作 → 操作后快照"三步验证

---

## 七、给用户的最终指引

请回复（示例）：

```
选项 A / 选项 B（仅） + 问题 A → 选项 a
```

或更具体：

```
执行候选 1 + 候选 2 + 候选 3 + 候选 4 + 候选 6；
对问题 A 选 a；
不在本次处理：候选 5、候选 7、问题 B、问题 C
```

我将基于你的选择生成第二阶段"具体执行计划 v4.0-execute"，含精确命令行、预期校验、失败回滚步骤。

---

*计划版本：v3.0-fact-checked*
*所有"可删除"声明已通过 Grep + PowerShell 校验*
*等待用户最终决策后生成 v4.0-execute*
