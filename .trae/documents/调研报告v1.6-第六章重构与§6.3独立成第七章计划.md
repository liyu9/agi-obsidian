# 第六章重构计划：§6.3 独立成第七章 + 表格迁移 + 业务背景叙述化

**目标文件**：`d:\360MoveData\Users\admin\Desktop\AgiP\AGI-obsidian\云端Agent记忆系统调研（从多agent调研版）\云端Agent记忆系统调研报告-v1.6.md`

**基线版本**：v1.6（当前行号以已读完内容为准；执行时按实际为准，行号会随改动漂移）

---

## 一、用户需求与决策汇总

| # | 用户原始需求 | 决策（用户确认） |
|---|---|---|
| 需求1 | §6.2.3 的「知识库归属表+授权矩阵示例」（L768-785）看起来像二期内容，位置/叙述方式需调整 | **移到 §6.1.3 二期蓝图**；同时软化"承载内容"列的绝对语气 |
| 需求2 | 知识库归属与承载内容列的描述"太经典、太绝对"，2000 人公司知识库类型多样 | 与需求1合并处理：表格迁移时，"承载内容"列改为"典型承载内容（示例）"等措辞；正文加范围限定语 |
| 需求3 | §6.1.3 的「需求详解（业务价值展开）」4段（L544-570）与业务背景同模块，需"自然列出业务需求、价值"，不要"业务价值展开"这种标签 | **全部改为正文叙述**：去掉「**需求详解**（业务价值展开）：」小标题与每个需求的粗体小标题，改为正文中自然列出"业务需求 → 价值" |
| 需求4 | §6.2.0 的「企业需求 vs 官方支持」表（L578-583）这类内容"用户想看才需要看，不删减内容" | **改为正文叙述**：表格转为正文段落叙述（保留全部信息），与需求3统一处理 |
| 需求5 | §6.2.4 是否应跟接入与记忆模型放一起？第六章太多，把接入与记忆模型单独成第七章，原第七章后延 | **§6.3 + §6.2.4 合并提升为第 7 章**；原 ## 7 产品框架 → ## 8；原 ## 8 关键风险与建议 → ## 9。同步更新所有章节交叉引用 |

---

## 二、当前状态分析（基于已读内容）

### 2.1 当前章节骨架（L1, L1166, L1209 等顶层标题）

```
## 0. 阅读说明            (L3, 含目录 L7-15)
## 1. 业务背景            (L19)
## 2. 典型用户            (L57)
## 3. 要解决的问题        (L77)
## 4. Agent 记忆系统：定义、边界与能力框架 (L93)
## 5. 候选产品调研        (L277)
## 6. 业务侧方案设计      (L456)
  ### 6.0 方案概览
    #### 6.0.1 核心概念速查 (L462)
  ### 6.1 一期/二期蓝图
    #### 6.1.1 业务场景与分期满足度 (L477)
    #### 6.1.2 一期蓝图 (L507)
    #### 6.1.3 二期蓝图 (L529) ← 含 L544-570 需求详解、L570 综述
  ### 6.2 方案架构
    #### 6.2.0 设计理念 (L574) ← 含 L578-583 企业需求表
    #### 6.2.1 产品架构图 (L616)
    #### 6.2.2 层职责说明 (L748)
    #### 6.2.3 知识库归属 × 授权矩阵 (L763) ← 含 L768-785 表格
    #### 6.2.4 关键数据流（4 条路径）(L787)
  ### 6.3 接入与记忆模型  (L931)
    #### 6.3.1 接入通道 (L935)
    #### 6.3.2 记忆类型与分类 (L971)
    #### 6.3.3 记忆记入模型 (L987)
    #### 6.3.4 记忆存储模型 (L1042)
    #### 6.3.5 记忆召回模型 (L1071)
  ### 6.4 方案级风险（4 条）(L1154)
## 7. 产品框架：功能架构  (L1166)
  ### 7.0 功能形态定义 (L1170)
  ### 7.1 一期功能架构 (L1180)
## 8. 关键风险与建议      (L1209)
  ### 8.1 关键风险 (L1211)
  ### 8.1.1 文档使用注意 (L1223)
  ### 8.2 后续建议 (L1227)
```

### 2.2 需求 5 的连带改动点（章节交叉引用扫描结果）

以下位置提到 `§6.3` / `§7` / `§8` 等需要在重编号后同步更新：

| 行号 | 当前内容 | 重编号后需改为 |
|---|---|---|
| L12 | `- 第 6 章：...§6.3 接入与记忆模型 + §6.4 方案级风险）` | 拆出"第 7 章：接入与记忆模型" |
| L13 | `- 第 7 章：**产品框架**（§7.0 ... + §7.1 ...）` | 改为"第 8 章：产品框架（§8.0 + §8.1）" |
| L14 | `- 第 8 章：关键风险与建议` | 改为"第 9 章：关键风险与建议" |
| L460 | "§6.3 给出方案级风险" | §6.3 已迁出，本句删除/改写 |
| L475 | "接入与记忆模型见 §6.3，方案级风险见 §6.4" | "接入与记忆模型见 §7，方案级风险见 §6.4"（§6.4 编号在第六章内不变） |
| L485 | "§6.2.1 / §6.2.4 / §6.3" | "§6.2.1 / §7.4 / §7"（§6.2.4 → §7.4，§6.3 → §7） |
| L595 | "与 §6.3 风险 2 一致" | "与 §6.4 风险 2 一致"（原 §6.3 风险已迁到 §6.4，此引用本身需校正——见验证清单） |
| L746 | "见 §6.3.1 接入通道" | "见 §7.1 接入通道" |
| L750 | "详见 §6.3.1 接入通道" | "详见 §7.1 接入通道" |
| L761 | "见 §6.3.1 接入通道" | "见 §7.1 接入通道" |
| L1156 | "记忆系统通用风险见 §8.1" | "记忆系统通用风险见 §9.1" |
| L1213 | "方案级风险...见 §6.4" | §6.4 编号不变，无需改 |
| L1218 | "对齐 §6.2.3" | §6.2.3 仍存在（表格被迁走但章节保留并改写），引用不变 |
| L516 | "命中率基线见 §8.2 第1条" | "见 §9.2 第1条" |
| L1159 | "重评估触发条件见 §8.2 第3条" | "见 §9.2 第3条" |
| L1234 | "关联 §6.4 风险1" | §6.4 不变 |

**§6.3 内部子章节重编号**：
- §6.3.1 接入通道 → §7.1
- §6.3.2 记忆类型与分类 → §7.2
- §6.3.3 记忆记入模型 → §7.3
- §6.3.4 记忆存储模型 → §7.4
- §6.3.5 记忆召回模型 → §7.5
- §6.2.4 关键数据流 → §7.0 或 §7.6（**决策：放 §7.0 数据流总览**，因为它是 §7.1-§7.5 的端到端实证基础，先看总览再看分模块更顺）

### 2.3 §6.3 内部交叉引用扫描（迁到第7章后需更新）

| 行号 | 当前内容 | 重编号后 |
|---|---|---|
| L933 | "§6.3.1 接入通道" + "§6.3.2-6.3.5 记忆模型" + "§6.3.3/6.3.5" | "§7.1" + "§7.2-7.5" + "§7.3/7.5" + "§6.2.4"→"§7.0" |
| L985 | "见 §6.2.3 知识库归属表" | §6.2.3 章节仍存在（表格迁走，章节保留），引用保留但措辞需调（不再叫"归属表"） |
| L989, L993, L1022, L1037, L1044, L1073, L1077, L1082, L1123 | "§6.3.1" / "§6.3.4" / "§6.2.4 路径X" | "§7.1" / "§7.4" / "§7.0 路径X" |

---

## 三、改动方案（按执行顺序）

### 改动 A：§6.2.3 表格迁移 + 软化（需求 1+2）

**目标**：把 L768-785 的「知识库归属表 + Agent×知识库 授权矩阵示例」从 §6.2.3（架构章）移到 §6.1.3（二期蓝图章），同时软化"承载内容"列的绝对语气。

#### A.1 §6.2.3 原文（L763-785）改写

原文结构：
```
#### 6.2.3 知识库归属 × 授权矩阵
**知识库独立于 Agent 存在**：知识库独立于 Agent 存在...（一段叙述）
**知识库归属与承载内容**：              ← 删除该小标题及下方表格
| 知识库类型 | 归属方 | ... |（L769-776 表格）
**Agent × 知识库 授权矩阵示例**：       ← 删除该小标题及下方表格
| Agent \ 知识库 | ... |（L780-785 表格）
```

**改后**：
```
#### 6.2.3 知识库归属与授权模型
**知识库独立于 Agent 存在**：知识库独立于 Agent 存在...（保留原叙述段）
**知识库类型与授权关系**：知识库按归属分为个人/项目组/部门/公司四类，类型多样、
  归属与敏感度各异（个人偏好低敏、部门 SOP 中敏、公司政策高敏只读）。② 层按
  Agent × 知识库 做授权矩阵（RBAC + ACL），同一知识库可被多个 Agent 授权访问、
  同一 Agent 可访问多个知识库。具体的归属类型、命名空间、典型承载内容示例与
  授权矩阵样例见 §6.1.3 二期蓝图。
```

#### A.2 §6.1.3 接收表格（在 L535 二期需求表后、L544 需求详解前插入）

表格列名软化：
- "承载内容" → "典型承载内容（示例）"
- "实例形态（多实例物理隔离）" → "实例形态"
- "数量级（N 人公司）" → "数量级（参考）"

新增过渡段 + 表格 + 矩阵样例：
```
**知识库归属类型与授权矩阵样例**

二期多 Agent 场景下，知识库按归属分为以下几类（典型分类，非穷举；2000 人公司
实际类型更杂，按业务需要增列）：

[表格：6 行 × 7 列，列名软化如上]

**Agent × 知识库 授权矩阵示例**（② 层知识库授权中心强制）：

[原 L780-785 矩阵表格原样保留]
```

---

### 改动 B：§6.1.3 需求详解改为正文叙述（需求 3）

**目标**：去掉「**需求详解**（业务价值展开）：」小标题（L544）和 4 个粗体小标题，把 4 段内容改为正文段落自然承接。

#### B.1 删除 L544「**需求详解**（业务价值展开）：」整行

#### B.2 4 段改写（L546-568）

| 段 | 原文（保留信息） | 改后形式 |
|---|---|---|
| 需求一（L546-550） | **需求一 · 一个 Agent 需要多个知识库源**\n\n[正文 2 段] | 删除粗体小标题，正文改为："当一个 Agent 需要同时调取多类知识时——例如张三用 Claude Code 做研发，既要个人偏好库（编码习惯），也要研发部 SOP 库（规范流程），还要公司政策库（HR/合规底线）。一期一个 Agent 只绑一个库，只能看到个人记忆，部门 SOP 与公司政策无处调取，Agent 输出频繁偏离规范。② 层授权矩阵让一个 Agent 被授权访问 N 个库，跨库联邦 fan-out 让一次请求并行检索个人库+部门库+公司库并合并排序——Agent 像查一个文件系统一样调取多源知识，告别手工粘贴，输出对齐规范（对应 Q1 跨 Agent 共享、Q2 长期闭环）。" |
| 需求二（L552-556） | 同上模式 | 同样删除粗体小标题，改为一段连贯叙述 |
| 需求三（L558-562） | 同上 | 同样 |
| 需求四（L564-568） | 同上 | 同样 |

每段之间用空行分隔即可，不加任何小标题。

#### B.3 L570 综述段

原：
```
> 综上：一期验证记忆链路可行性（单 Agent 跑通 capture/recall/归档），
> 二期面向 2000 人多 Agent 公司的真实场景——一个 Agent 用多库、多个
> Agent 用一库、多类型知识库统一治理与分享，是知识库独立化、跨 Agent
> 协作记忆复用的二期目标。
```

保留为引用块（这是自然的段落小结，符合"引用展示"语境）。

---

### 改动 C：§6.2.0 企业需求表改为正文叙述（需求 4）

**目标**：L578-583 的「企业需求 vs 官方支持」表格转为正文段落叙述，保留全部信息。

#### C.1 原文（L574-585）

```
#### 6.2.0 设计理念：以知识库为中心 + 企业侧扩展
**为什么需要"企业侧扩展"？** OpenViking 是面向个人开发者 / 小团队的通用开源
产品，官方只提供必要且通用的能力（4 个顶级命名空间、7 Hook、9 MCP 工具、
LLM+Embedding+VLM 三分工）。业务上有 OpenViking 官方不解决的 4 类需求：

| 企业需求 | OpenViking 官方是否原生支持 | 是否需企业侧扩展补齐 |
|---|---|---|
| 部门级隔离（...）| ⚠️ 有 account 级逻辑隔离...| ✅ 企业侧扩展（多实例物理隔离）|
| Agent×知识库 授权矩阵 | ❌ 官方...| ✅ 企业侧扩展（② 层授权中心）|
| 跨知识库联邦查询 | ❌ 官方...| ✅ 企业侧扩展（② 层 fan-out）|
| 归档冷存 180 天迁 TOS | ⚠️ 官方...| ✅ 企业侧策略 |

> 以上 4 类企业需求对应的核心概念...见 §6.0.1 核心概念速查。
```

#### C.2 改后

把表格内容融入正文段落（4 类需求逐条用文字叙述）：
```
#### 6.2.0 设计理念：以知识库为中心 + 企业侧扩展
**为什么需要"企业侧扩展"？** OpenViking 是面向个人开发者 / 小团队的通用开源
产品，官方只提供必要且通用的能力（4 个顶级命名空间、7 Hook、9 MCP 工具、
LLM+Embedding+VLM 三分工）。业务上有 4 类需求是 OpenViking 官方不解决或解决
不足的：

一是部门级隔离（设计部/研发部 SOP 不串数据）——OpenViking 有 account 级逻辑
隔离，但本方案用独立实例做多实例物理隔离，更强于官方逻辑隔离。

二是 Agent × 知识库 授权矩阵（哪个 Agent 能访问哪个知识库）——OpenViking 官方
account/user 模型表达不了跨租户 Agent 授权关系，需企业侧扩展 ② 层授权中心。

三是跨知识库联邦查询（一次请求触达多个知识库）——OpenViking 官方是单实例单
account 视角，无联邦能力，需企业侧扩展 ② 层 fan-out。

四是归档冷存 180 天迁 TOS——OpenViking 官方支持 resources 挂 S3，但没有
"180 天规则"这一企业侧策略。

> 以上 4 类企业需求对应的核心概念（官方原生 / 企业侧扩展 / ②层知识库授权中心 /
> 多实例物理隔离 / 跨知识库联邦查询 / 知识库 KB）定义见 §6.0.1 核心概念速查。
```

---

### 改动 D：§6.3 + §6.2.4 提升为第 7 章（需求 5）

这是改动量最大的一项，必须放在 A/B/C 之后执行（避免行号漂移影响前面改动定位）。

#### D.1 第 7 章新结构

```
## 7. 接入与记忆模型

本章把 §6.2 架构里的"接入通道"与"记忆模型"单独成章——§7.0 给出 4 条关键
数据流端到端实证（来自原 §6.2.4），§7.1 回答"Agent 用什么协议通信"，
§7.2-7.5 回答"记忆如何记入、存储、召回"。通道机制只在 §7.1 定义一次，
§7.3/7.5 直接引用。

### 7.0 关键数据流（4 条路径）         ← 原 §6.2.4（L787-929）
### 7.1 接入通道                       ← 原 §6.3.1（L935-969）
### 7.2 记忆类型与分类                 ← 原 §6.3.2（L971-985）
### 7.3 记忆记入模型                   ← 原 §6.3.3（L987-1040）
### 7.4 记忆存储模型                   ← 原 §6.3.4（L1042-1069）
### 7.5 记忆召回模型                   ← 原 §6.3.5（L1071-1152）
```

**注意**：把 §6.2.4 放在 §7.0 的理由——它是端到端总览，先看总览再看 §7.1-7.5 分模块更顺。但这点用户未明确，属于执行时的小决策（若用户更倾向"按原顺序 §6.3.1→§7.1 在前，§6.2.4→§7.6 在后"，可调整）。**默认按本计划执行**。

#### D.2 具体操作步骤

**步骤 1**：剪切原 §6.2.4（L787-929，约 143 行）和原 §6.3（L931-1152，约 222 行）的内容。

**步骤 2**：在原 §6.4 之前（即 L1154 `### 6.4 方案级风险` 之前）插入新章节标题：

```markdown
## 7. 接入与记忆模型

[本章引言段——基于原 L933 §6.3 引子改写]

### 7.0 关键数据流（4 条路径）

[原 §6.2.4 内容，包含 L789-929]

### 7.1 接入通道

[原 §6.3.1 内容，L935-969]

### 7.2 记忆类型与分类

[原 §6.3.2 内容，L971-985]

### 7.3 记忆记入模型

[原 §6.3.3 内容，L987-1040]

### 7.4 记忆存储模型

[原 §6.3.4 内容，L1042-1069]

### 7.5 记忆召回模型

[原 §6.3.5 内容，L1071-1152]
```

**步骤 3**：删除原 §6.2.4 与原 §6.3 的旧位置内容（因为已剪到第7章）。

**步骤 4**：原 §6.4 编号不变（仍在第6章），但章节标题前的空行/衔接段需调整。

#### D.3 章节标题重编号

| 原标题 | 新标题 |
|---|---|
| `### 6.3 接入与记忆模型` | 删除（提升为 `## 7. 接入与记忆模型`）|
| `#### 6.2.4 关键数据流（4 条路径）` | `### 7.0 关键数据流（4 条路径）` |
| `#### 6.3.1 接入通道` | `### 7.1 接入通道` |
| `#### 6.3.2 记忆类型与分类` | `### 7.2 记忆类型与分类` |
| `#### 6.3.3 记忆记入模型` | `### 7.3 记忆记入模型` |
| `#### 6.3.4 记忆存储模型` | `### 7.4 记忆存储模型` |
| `#### 6.3.5 记忆召回模型` | `### 7.5 记忆召回模型` |
| `## 7. 产品框架：功能架构` | `## 8. 产品框架：功能架构` |
| `### 7.0 功能形态定义` | `### 8.0 功能形态定义` |
| `### 7.1 一期功能架构` | `### 8.1 一期功能架构` |
| `## 8. 关键风险与建议` | `## 9. 关键风险与建议` |
| `### 8.1 关键风险` | `### 9.1 关键风险` |
| `### 8.1.1 文档使用注意` | `### 9.1.1 文档使用注意` |
| `### 8.2 后续建议` | `### 9.2 后续建议` |

#### D.4 章节交叉引用更新清单（基于第二章扫描结果）

| 行号 | 原文 | 改为 |
|---|---|---|
| L12 | `- 第 6 章：...（§6.0 ... + §6.1 ... + §6.2 ... + §6.3 接入与记忆模型 + §6.4 方案级风险）` | `- 第 6 章：**业务侧方案设计**（§6.0 方案概览 + §6.1 一/二期蓝图 + §6.2 方案架构 + §6.4 方案级风险）` |
| L13 | `- 第 7 章：**产品框架**（§7.0 ... + §7.1 ...）` | `- 第 8 章：**产品框架**（§8.0 功能形态定义 + §8.1 一期功能架构）` |
| L14 | `- 第 8 章：关键风险与建议` | `- 第 9 章：关键风险与建议` |
| **L12 后新增** | — | `- 第 7 章：**接入与记忆模型**（§7.0 关键数据流 + §7.1 接入通道 + §7.2-7.5 记忆模型）` |
| L460 | "§6.0 给出核心概念速查，§6.1 给出一/二期业务需求与开发功能蓝图，§6.2 给出完整架构设计（三层架构图见 §6.2.1、分期规划见 §6.2.0），§6.3 给出方案级风险。" | "§6.0 给出核心概念速查，§6.1 给出一/二期业务需求与开发功能蓝图，§6.2 给出完整架构设计（三层架构图见 §6.2.1、分期规划见 §6.2.0），§6.4 给出方案级风险。接入与记忆模型见第 7 章。" |
| L475 | "完整技术架构见 §6.2，接入与记忆模型见 §6.3，方案级风险见 §6.4。" | "完整技术架构见 §6.2，接入与记忆模型见第 7 章，方案级风险见 §6.4。" |
| L485 | "§6.2.1 / §6.2.4 / §6.3" | "§6.2.1 / §7.0 / 第 7 章" |
| L595 | "与 §6.3 风险 2 一致" | **此引用疑似历史错误**：原 §6.3 是接入与记忆模型，并不含风险；真正的风险 2"企业合规责任"在 §6.4。改为"与 §6.4 风险 2 一致" |
| L746 | "见 §6.3.1 接入通道" | "见 §7.1 接入通道" |
| L750 | "详见 §6.3.1 接入通道" | "详见 §7.1 接入通道" |
| L761 | "见 §6.3.1 接入通道" | "见 §7.1 接入通道" |
| L933（迁到第7章引言） | "§6.3.1 接入通道" + "§6.3.2-6.3.5 记忆模型" + "§6.3.3/6.3.5" + "图 §6.2.4" | "§7.1 接入通道" + "§7.2-7.5 记忆模型" + "§7.3/7.5" + "图 §7.0" |
| L985 | "见 §6.2.3 知识库归属表" | "见 §6.2.3 知识库归属与授权模型"（章节标题已改） |
| L989, L993, L1073, L1077 | "引用 §6.3.1" | "引用 §7.1" |
| L998, L1025, L1082, L1123 | "对应 §6.2.4 路径 X" | "对应 §7.0 路径 X" |
| L1022, L1037 | "见 §6.3.4 存储模型" | "见 §7.4 存储模型" |
| L1044 | "对应 §6.2.4 路径 1/2 中" | "对应 §7.0 路径 1/2 中" |
| L1156 | "记忆系统通用风险见 §8.1" | "记忆系统通用风险见 §9.1" |
| L516 | "命中率基线见 §8.2 第1条 PoC 验证" | "命中率基线见 §9.2 第1条 PoC 验证" |
| L1159 | "重评估触发条件见 §8.2 第3条" | "重评估触发条件见 §9.2 第3条" |

**§7.x 内部交叉引用**（迁移内容自带）：
- 原 L925 末尾"路径 1/3 走 Hooks 通道...路径 2/4 走 MCP 通道"——保留
- 原 L929"见 [附件3] / [附件4]"——保留
- 原 L985 注释"映射见 §6.2.3"——保留（§6.2.3 仍存在）

#### D.5 §6.2.4 迁出后，§6.2 章节内引用更新

原 §6.2 章末（§6.2.4 之后紧跟 `### 6.3`）的衔接需调整。§6.2.4 迁走后，§6.2.3 直接接 §6.4。需要在 §6.2.3 末尾加一句过渡："接入通道与 4 条数据流的端到端实证见第 7 章。"

#### D.6 §6.2.2 中提到"数据流主干为 ①→②→③...详见 §6.3.1 接入通道"（L750）

改为："数据流主干为 ①→②→③。接入方式（Hooks/MCP/SDK）是 Agent 出站时的协议选择，Agent 按能力择一、非依次经过，不单独成层（详见 §7.1 接入通道）。"

---

## 四、执行顺序与行号漂移应对

由于改动 D（章节迁移）会导致大规模行号漂移，**必须按以下顺序执行**：

1. **改动 A**（§6.2.3 表格迁移到 §6.1.3）—— 用 SearchReplace 定位表格内容，剪切+粘贴
2. **改动 B**（§6.1.3 需求详解改写）—— 此时 §6.1.3 已含新表格，定位需求详解段落
3. **改动 C**（§6.2.0 企业需求表改叙述）—— 独立改动，不影响其他
4. **改动 D**（§6.3 + §6.2.4 提升为第7章 + 全文章节重编号 + 交叉引用更新）
   - 4.1 先做章节标题重编号（顶层 ## 7→## 8, ## 8→## 9）
   - 4.2 再做内容迁移（剪 §6.2.4 + §6.3 → 新 ## 7）
   - 4.3 最后做交叉引用全文扫描替换

**每次 SearchReplace 后用 Read 确认实际内容**（前序对话中多次遇到缓存不一致问题）。

---

## 五、验证清单

执行完成后逐项验证：

### 5.1 结构验证
- [ ] `## 6.` `## 7.` `## 8.` `## 9.` 四个顶层章节存在且顺序正确
- [ ] `## 7. 接入与记忆模型` 下含 `### 7.0` ~ `### 7.5` 六个子章节
- [ ] `## 8. 产品框架` 下含 `### 8.0` `### 8.1`
- [ ] `## 9. 关键风险与建议` 下含 `### 9.1` `### 9.1.1` `### 9.2`
- [ ] 原 §6.2.4 与 §6.3 在第六章中已不存在（被剪走）

### 5.2 表格迁移验证
- [ ] §6.2.3 不再含「知识库归属表」与「Agent×知识库 授权矩阵示例」表格
- [ ] §6.1.3 含上述两个表格，且"承载内容"列已软化为"典型承载内容（示例）"
- [ ] §6.2.3 章节标题改为"知识库归属与授权模型"（去掉"× 授权矩阵"）

### 5.3 叙述化验证
- [ ] §6.1.3 不再含「**需求详解**（业务价值展开）：」小标题
- [ ] §6.1.3 不再含 4 个粗体小标题（"**需求一 · ...**" 等），改为正文段落
- [ ] §6.2.0 不再含「企业需求 vs 官方支持」表格，改为 4 段正文叙述

### 5.4 交叉引用验证（全文 Grep 扫描）
- [ ] `grep "§6\.3"` 应只剩历史引用（无）—— 全部更新为 §7.x
- [ ] `grep "§6\.2\.4"` 应全部更新为 §7.0
- [ ] `grep "§7\."` 出现的位置都是新第7章内部引用或对第7章的引用
- [ ] `grep "§8\."` 应为 §9.x（原 §8.1/§8.2 已变 §9.1/§9.2）
- [ ] `grep "第 [78] 章"` 验证阅读说明 L7-14 已更新
- [ ] L595"与 §6.3 风险 2 一致"已校正为"与 §6.4 风险 2 一致"（顺便修历史错误）

### 5.5 缓存与一致性验证
- [ ] 每次 SearchReplace 后用 Read 二次确认（前序对话中遇到缓存不一致问题）
- [ ] 若 SearchReplace 报成功但 Read 显示未改，用最小匹配片段重试
- [ ] 最后全文 Grep `^## ` `^### ` `^#### ` 验证标题层级一致性

---

## 六、假设与风险

1. **§7.0 数据流放最前的小决策**：用户未明确 §6.2.4 迁到第7章后是放最前（§7.0）还是最后（§7.6）。本计划默认放 §7.0（端到端总览先看），如用户反对可调整。
2. **§6.2.3 章节标题**：表格迁走后，原标题"知识库归属 × 授权矩阵"已不准确，本计划改为"知识库归属与授权模型"。如用户希望保留原标题可回滚。
3. **L595 历史错误校正**：原"与 §6.3 风险 2 一致"是历史遗留错误（§6.3 从来不是风险章），本次顺便修为"§6.4 风险 2"。如不希望改动此处可保留。
4. **章节交叉引用可能有遗漏**：第二章扫描的是已知模式（§6.3/§7./§8.），执行时需用 Grep 全文再扫一遍 `§` 字符，避免遗漏。
5. **行号漂移**：本计划给出的行号基于改动前的 v1.6 基线，执行时需按实际为准。改动 A/B/C 必须在 D 之前完成。

---

## 七、不做什么（明确边界）

- **不删除任何实质内容**：所有表格、流程图、段落只做位置迁移或形式转换（表格↔叙述），信息量不减少
- **不动 §6.3.3 / §6.3.5 的流程图**：前序对话中用户已确认"有差异则不删除"
- **不动 §6.4 风险章 / §8.x 风险表的内容**：只做编号顺移
- **不做其他抽象描述替换**：前序对话已完成的"一等公民/命脉/独立产品/闭环/核心是"替换不回滚，也不新增替换
- **不创建新文件**：所有改动在 v1.6.md 单文件内完成
