# 文档逐篇校验与修复计划

## Summary

对 `d:\360MoveData\Users\admin\Desktop\AgiP\AGI-obsidian\亚慧AI产品经理知识星球\` 下的 17 篇 markdown 文档，逐篇与飞书源文档对比，按"文本完整性 + 格式 + 图片插入位置"三个维度校验，自动生成报告并批量修复，最终每篇都通过校验。

源文档：https://my.feishu.cn/docx/YQVcdz0mgoqNWtxy9msc58uwnje（飞书云文档）

工作方式：每篇自动比对 → 报告 → 批量修复 → 再次校验。自动闭环。

## Current State Analysis

### 已生成文档
17 篇 + 1 份完整内容（`课程完整内容.md`），存放在 `亚慧AI产品经理知识星球/`。每篇结构包含标题层级、段落、图片引用、代码块、列表、表格。

### 已知问题（来自之前 `check_one.py` 检测）
- **格式问题**：HTML 标签残留（`</em>`, `</tag>`, `</abbr>` 等）、空标题、heading 缺空格、number list 缺空格
- **代码块**：5、6、7 章存在 fence 数量不平衡（部分为检测器误报）
- **图片路径**：相对路径 `images/img_xxxx.png`，已通过之前的重命名保持有效
- **内容缺失**：之前的 12_12、行业报告.md 仅 10 字符，可能内容空

### 源对照物
1. **首选源**：`https://my.feishu.cn/docx/YQVcdz0mgoqNWtxy9msc58uwnje`（飞书云文档原始内容）
2. **备用源**：`c:\Users\admin\.trae-cn\work\6a1d77e673840ff2b692bab2\doc_content.xml`（之前导出的 XML，含 17 H1）

## Proposed Changes

### 工作目录
- 临时脚本：`c:\Users\admin\.trae-cn\work\6a1d77e673840ff2b692bab2\`
- 输出报告：`d:\360MoveData\Users\admin\Desktop\AgiP\AGI-obsidian\.trae\documents\`
- 目标文档：`d:\360MoveData\Users\admin\Desktop\AgiP\AGI-obsidian\亚慧AI产品经理知识星球\`

### 实施步骤

#### Step 1: 抓取飞书源文档
- 使用 `lark-cli docs +fetch --api-version v2 --doc "https://my.feishu.cn/docx/YQVcdz0mgoqNWtxy9msc58uwnje" --detail with-ids`
- 存为 `feishu_source.xml` 到工作目录
- 备用：如飞书 API 失败，使用本地 `doc_content.xml`

#### Step 2: 解析源为 17 章标准数据
编写 `parse_source.py`：
- 按 H1 切分
- 对每章提取结构化数据：
  - `title`: 章节标题
  - `paragraphs`: 段落列表（每段纯文本 + 类型：heading/paragraph/list/quote/code/image/table）
  - `images`: 图片 token 列表（含前后 50 字符上下文）
  - `code_blocks`: 代码块内容（含语言标识）
  - `headings`: H2/H3/H4 标题及层级
  - `word_count`: 字符数统计
- 输出 `chapter_standards.json`

#### Step 3: 解析本地 17 篇 markdown
编写 `parse_local.py`：
- 复用之前的解析逻辑（更稳健版本）
- 对每篇提取同样结构
- 输出 `chapter_local.json`

#### Step 4: 编写对比器 `compare_chapters.py`
对每章输出对比报告：
- **文本完整性**：
  - 段落数对比（源 vs 本地）
  - 段落级 LCS 差异（找出本地缺失/多出的段）
- **图片位置**：
  - 源每张图的前后 50 字符 → 在本地查找位置
  - 若位置偏离 > 200 字符，标记为"图片位置错位"
- **代码块**：
  - 源代码块数 vs 本地
  - 内容相似度
- **列表项**：源列表项数 vs 本地
- **格式问题**：HTML 标签、空标题、heading 格式、code fence 平衡等
- 输出 `validation_report.md`（每篇单独章节）+ `validation_report.json`

#### Step 5: 自动修复 `apply_fixes.py`
按优先级修复：
1. **内容缺失**：从源补回缺失的段落
2. **图片位置错位**：根据上下文定位，纠正位置
3. **HTML 残留**：清理剩余 HTML 标签
4. **格式问题**：规范化 heading、number list、code fence
5. **空文件**（如 12 章）：从源补回内容
- 记录每处修复到 `fixes_applied.log`

#### Step 6: 再次校验
- 重跑 `compare_chapters.py`
- 输出 `final_report.md`，标明每篇的"已通过/剩余问题数"
- 若仍有未通过项目，列出供人工决策

#### Step 7: 输出最终汇总
- 每篇：✅ 通过 / ⚠️ 剩余 N 项 / ❌ 严重问题
- 总体：17/17 通过 / X 项剩余

### 关键技术决策

1. **图片位置对比算法**：
   - 用图前后 50 字符作锚点
   - 在本地查 50 字符子串
   - 偏差阈值 200 字符视为"错位"
2. **段落匹配**：
   - 优先用 LCS（最长公共子序列）找差异段
   - 阈值：本地少 30% 段视为"内容缺失"
3. **代码块平衡修复**：
   - 用栈式追踪，遇到不配对就追加 closing
4. **HTML 清理白名单**：
   - `<code>`、`<pre>` 保护，其他全部移除
   - 已知特殊标签：`<em>`、`<strong>`、`<u>`、`<i>`、`<b>`、`<a>`、`<span>`、`<div>`、`<p>`、`<abbr>`、`<topic>`、`<summary>`、`<instructions>`、`<example>`、`<context>`、`<task>`、`<role>`、`<output>`、`<constraint>`、`<skill>`、`<workflow>`、`<goal>`、`<profile>`、`<tag>`

### 风险与回退

| 风险 | 缓解 |
|------|------|
| 飞书 API 鉴权失败 | 回退到 `doc_content.xml` 作为对比源 |
| 图片下载 360 张失败 | 跳过下载，保留现有本地图片 |
| 段落 LCS 慢（长文档） | 对 RAG、实操案例等大章节按 H2 子节分块对比 |
| 自动修复引入新问题 | 修复前备份原文件到 `*.bak`，修复后自动回滚若不通过 |

## Verification Steps

1. 抓取飞书源 → 检查 `feishu_source.xml` 包含 17 H1
2. 解析标准 → 抽查 03 章的代码块数和源一致
3. 对比报告 → `validation_report.md` 每篇有 3 个维度评分
4. 自动修复 → `fixes_applied.log` 列出所有改动
5. 最终校验 → `final_report.md` 17/17 通过
6. 抽样人工核查：03 章 prompt 代码块、04 RAG 图片位置、05 Agent 多代码块

## Assumptions & Decisions

- **源对照基准**：优先飞书在线源，XML 作为 fallback
- **校验粒度**：段级 + 图片级（不深入句级）
- **修复策略**：保守优先（不改变文本原意，只补缺失、修格式）
- **图片位置修复**：基于上下文字符串匹配，不重排段落顺序
- **不重做整章**：仅在缺失超过 30% 时考虑整章从源重生成

## 输出物清单

| 文件 | 位置 | 说明 |
|------|------|------|
| `feishu_source.xml` | 工作目录 | 飞书源完整内容（XML） |
| `chapter_standards.json` | 工作目录 | 17 章标准结构化数据 |
| `chapter_local.json` | 工作目录 | 17 篇本地结构化数据 |
| `validation_report.md` | `.trae/documents/` | 校验报告（每篇一节） |
| `validation_report.json` | `.trae/documents/` | 校验数据（程序用） |
| `fixes_applied.log` | `.trae/documents/` | 修复记录 |
| `final_report.md` | `.trae/documents/` | 最终状态汇总 |
| `*.bak` | 目标目录 | 每篇原文件备份 |
| 修复后的 `*.md` | 目标目录 | 最终交付的 17 篇 |
