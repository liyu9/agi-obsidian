# 云端 Agent 记忆系统调研报告 v1.6 编辑计划

## 一、任务概要

对单文件 [云端Agent记忆系统调研报告-v1.6.md](file:///d:/360MoveData/Users/admin/Desktop/AgiP/AGI-obsidian/云端Agent记忆系统调研（从多agent调研版）/云端Agent记忆系统调研报告-v1.6.md) 做 5 处定向调整，不触动其他内容。

## 二、当前状态分析

- 文档开篇 L3-20 为两段引用块：候选池说明 + 术语约定。
- §0 阅读说明 L23 含核心结论但未加粗，且"②层知识库授权中心"未明确层级。
- §4（L94-106）的定义与边界采用 `>` 引用块包裹，重点内容视觉权重被压低。
- §5（L278-384）为候选调研，末尾 L384 段衔接 §6 选型。
- §6.1（L390-403）业务约束复述 + §6.2（L405-445，含 6.2.1 五维度综合判断 + 落选理由 + 选型结论）实质上是基于 §5 调研得出的选型结论，与 §6.3 起的方案设计章不同性质。
- L284 末缺句号。

## 三、修改清单（按文件位置自上而下）

### 改动 1 · 移除开篇术语引用块（L3-20）

删除开篇两段引用块：候选池说明（L3）+ 术语约定（L5-17）+ 分隔线（L19）。

**术语归宿（迁移到正文）**：
- §0「阅读说明」（L21-32）保持不动，不承载术语。
- 在 §1「业务背景」L36 标题下方新增一段「术语约定」二级小节或正文段落（不加引用块），把原 L5-17 三组术语以普通正文/列表形式承载。术语中的「知识库」概念在 §6.4.3 已经再次完整定义，迁移到 §1 不会与后文冲突。
- 候选池说明（L3）单独并入 §0「阅读说明」末尾一句话，或直接合并到 §5.0 的"调研范围与打分口径"开头（更贴语义）。**选 §5.0**，因为该处本就讲候选池范围。

**做法**：删除 L3-20 整块；在 §5.0 开头"以下四张表分别从..."之前补一段候选池说明（不加引用块）；在 §1 标题下方插入术语段（不加引用块）。

### 改动 2 · 核心结论加粗并明确"2 层"（L23）

原文 L23：

```
本报告面向 2000 人规模互联网公司**统一部署多类 AI Agent**...核心结论：以「知识库」为中心抽象、**OpenViking** 为底座、叠加多实例物理隔离与②层知识库授权中心，实现跨 Agent 记忆复用、部门/个人/会话三级隔离与企业级合规治理。
```

改为：

```
本报告面向 2000 人规模互联网公司**统一部署多类 AI Agent**（Claude Code / OpenClaw / Hermes 等）的记忆共享与治理需求。**核心结论**：以「知识库」为中心抽象、**OpenViking** 为底座，叠加**多实例物理隔离**与**2 层知识库授权中心**，实现跨 Agent 记忆复用、部门/个人/会话三级隔离与企业级合规治理。
```

要点：把"核心结论："前置加粗为"**核心结论**"；"②层"统一为"2 层"（阿拉伯数字 + 空格），避免"②层"这种符号混杂；保留原文其他用词。

### 改动 3 · §4 定义去引用块（L96-106）

把 L96-106 整块 `>` 引用块改为正文段落 + 列表。具体：
- L96"定义"行：去掉 `>`，改为正文段。
- L98-104"组成部分简介"：去掉 `>`，列表保持原样。
- L106"与 RAG / 向量数据库的边界"：去掉 `>`，改为正文段（或保留为段落式提示，但不再用引用块）。

不动列表内的措辞，仅删除每行行首的 `>` 和 `>` 空行。

### 改动 4 · L284 补结尾句号

L284：

```
- **调研对象**：4 款开源候选（OpenViking / Mem0 / Letta / Cognee）参与选型打分；3 款竞品（Coze / VikingDB / Dify）仅作功能借鉴，见[附件2 B](附件2-引用来源与竞品参考.md#附录-b竞品功能参考coze--vikingdb--dify)
```

末尾补一个中文句号 `。`（位于 `)` 之后，链接之前不补，统一在行末）。

### 改动 5 · 合并 §6.1 + §6.2 为新的 §5.6 选型结论

把 L388-445 整段（§6 标题 + 6.1 + 6.2 + 6.2.1 + 落选理由 + 选型结论 + 竞品不入选说明）整体上移，作为 §5 末尾的新小节 **§5.6 业务侧选型结论**；原 §6.3/6.4/6.5 顺次前移为新的 §6.1/6.2/6.3。

**新结构**：

```
## 5. 候选产品调研
  ... 5.0-5.5 不变
### 5.6 业务侧选型结论   ← 由原 6.1+6.2 合并
  - 5.6.1 业务约束与选型范围（原 6.1 业务约束复述 + 6.2 候选池范围说明）
  - 5.6.2 四款开源候选综合判断（原 6.2.1）
  - 5.6.3 选型结论与竞品不入选说明（原"选型结论"段 + "竞品功能参考不入选"段）
## 6. 业务侧方案设计   ← 标题去掉"选型与"
  ### 6.1 业务场景与分期价值（原 6.3，仅编号顺移）
  ### 6.2 方案架构（原 6.4，仅编号顺移）
  ### 6.3 方案级风险（原 6.5，仅编号顺移）
## 7. 关键风险与建议（不变）
```

**实施要点**：
1. 删除原 L388 行 `## 6. 业务侧选型与方案设计`。
2. L384 后衔接句"下文 §6 基于此调研结论..."改指向新 §5.6。
3. 原 L390 `### 6.1 业务约束复述` → `### 5.6.1 业务约束与选型范围`；将原 6.2 候选池范围说明 L405-411 并入 5.6.1。
4. 原 L412 `#### 6.2.1` → `### 5.6.2`（升一级，从 4 级标题到 3 级标题）。
5. 原 L438-445 的选型结论段 + 竞品不入选段 → 新增 `### 5.6.3 选型结论`。
6. 原 L447 `### 6.3 业务场景与分期价值` → `### 6.1 业务场景与分期价值`。
7. 原 L498 `### 6.4 方案架构` → `### 6.2 方案架构`；其下属 `#### 6.4.0/6.4.1/6.4.2/6.4.3/6.4.4` → `#### 6.2.0/6.2.1/6.2.2/6.2.3/6.2.4`。
8. 原 L916 `### 6.5 方案级风险` → `### 6.3 方案级风险`。
9. 全文检索 `§6.4` / `§6.3` / `§6.5` 等章节交叉引用，按新编号更新（包括 §0 L30-31、§1 L54-56、§6.1-6.2 正文内的"见 §6.4.x"等）。

**交叉引用更新清单**（须 grep 校验）：
- `§6.4.0` → `§6.2.0`
- `§6.4.1` → `§6.2.1`
- `§6.4.2` → `§6.2.2`
- `§6.4.3` → `§6.2.3`
- `§6.4.4` → `§6.2.4`
- `§6.5` → `§6.3`
- `§6.3` → `§6.1`（原业务场景）
- 段落内"§6"指代选型的措辞 → 视语境调整

## 四、改动 1-4 之间的独立性

改动 1（删 L3-20）、改动 2（L23）、改动 3（L96-106）、改动 4（L284）四项互不影响，可顺序执行；改动 5 涉及编号大调整和交叉引用，单独执行并最后做全文 grep 校验。

## 五、验证步骤

每项改动完成后，依次执行：

1. **改动 1 验证**：`grep -n "^>" ` 检查文档开篇 L1-35 范围内不再有引用块；§1 标题下方确实出现"术语约定"段；§5.0 起始处出现候选池说明句。
2. **改动 2 验证**：L23 含 `**核心结论**` 加粗标记；含 `2 层知识库授权中心`；不含 `②层`。
3. **改动 3 验证**：§4 定义段 L94-110 区间内无 `>` 行首符号；定义段落为正文段。
4. **改动 4 验证**：L284 行末为 `)` 后紧跟 `。`。
5. **改动 5 验证**：
   - `grep -n "^## \|^### \|^#### "` 核对章节编号连续无跳号；
   - `grep -n "§6\.\|§5\.6"` 核对所有交叉引用已更新到新编号；
   - §5 末尾为 5.6，§6 起始为 6.1 业务场景与分期价值；
   - 原"选型与方案设计"标题已改为"方案设计"。

## 六、假设与决策

- 假设：术语迁移到 §1（业务背景）符合"先定义后用"的阅读顺序；候选池说明迁移到 §5.0 更贴近其语义场景。
- 决策：改动 5 不改动原 6.1/6.2 的正文措辞，仅做结构重组和编号更新，避免引入语义偏差。
- 决策："②层"在全文统一替换为"2 层" 仅在改动 2 范围内执行（核心结论句），不扩散到全文，避免误改架构图等处对"②"符号的硬依赖（架构图里 `②` 作为层编号使用）。如用户希望全文统一，需另行确认。
