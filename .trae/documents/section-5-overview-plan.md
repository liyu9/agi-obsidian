# §5 产品能力对比总览新增计划

## Summary

在 `云端Agent记忆系统调研报告.md` 的 §5 章节开头新增「§5.0 产品能力对比总览」，先给出横向对比、再放各产品的详细节。纵轴使用 §4.4 记忆系统核心能力（8 项）与 §4.5 RAG / 向量数据库底座能力（3 个维度）；横轴使用 OpenViking、Mem0、Letta、Cognee、Coze、VikingDB、Dify 七款产品。每格采用 `符号 + 一句摘要` 的紧凑写法，与 §6 矩阵图例保持一致。

成功标准：读者进入 §5 时能先在两张总览表里获得整体印象，再按需下钻 §5.1-§5.7；总览与 §5.x 详情、§6 矩阵在方向上一致，不引入新事实、不改动 §5.1-§5.7 与 §6-§9 原文。

## Current State Analysis

- §5 目前直接从 `### 5.1 OpenViking（详尽）` 开始，没有先给整体对比。
- 各产品能力散落在 §5.1-§5.7 的能力映射小节（如 §5.5.3、§5.6.3、§5.7.3）中，读者需要逐节阅读才能拼出整体印象。
- §6 已经有细粒度矩阵（沿用旧 44 项细项编号），但过细，缺一层「按新 §4.4 / §4.5 维度」的粗粒度视图。
- §4.4 定义了 8 项核心能力（作用域与隔离、记忆写入、记忆类型、记忆召回、生命周期、跨 Agent 共享、治理与安全、可观测性）。
- §4.5 定义了 3 个维度（数据处理、数据存储、数据获取），并含子维度（数据类型/处理方式、存储方式/索引/权限成本、检索/重排/置信度/检索测试）。

## Proposed Changes

### 1. 新增小节 §5.0 产品能力对比总览

**文件**：`.trae/documents/云端Agent记忆系统调研报告.md`
**位置**：`## 5. 候选产品调研`（约 155 行）与 `### 5.1 OpenViking（详尽）`（约 157 行）之间。

**改什么**：新增一个 `### 5.0 产品能力对比总览` 小节，结构为：

1. 一段短导语，说明总览与详情、§6 矩阵的关系，声明图例和列顺序。
2. 表 A：`§4.4 Agent 记忆系统核心能力 × 产品`（8 行 × 7 列）。
3. 表 B：`§4.5 RAG / 向量数据库底座能力 × 产品`（3 行 × 7 列，行标题带子维度提示）。
4. 一句读法说明：结论口径以 §5.x 详情与 §6 矩阵为准。

**为什么**：用户明确要求「先列出整体的对比、再介绍详细的」；纵轴按 §4.4 / §4.5 能力框架，横轴按候选产品，可让读者先得到全局判断，再按需下钻。

**怎么做**：

- 图例复用 §6 现有约定：✅ 原生支持 / ⚠️ 需自建或部分支持 / ❌ 不支持或弱。
- 每格采用「单个符号 + 中文摘要」的紧凑格式，摘要控制在约 25 字以内。
- 产品列顺序与 §6 保持一致：OpenViking / Mem0 / Letta / Cognee / Coze / VikingDB / Dify。
- 判断口径全部来自 §5.1-§5.7 已有能力映射与 §6 已回填的方向，不引入新事实。

**验证**：

- §5 之下新增且仅新增 `### 5.0 产品能力对比总览`。
- 表 A 行数 = 8，表 B 行数 = 3，两表列数均 = 7。
- 每格非空、只使用 ✅/⚠️/❌ 三种符号。
- 与 §6 矩阵方向抽样一致：
  - Coze 部门级隔离在 §6 为 ❌，总览「作用域与隔离」不得给出 ✅。
  - VikingDB 跨 Agent 共享语义在 §6 为 ❌，总览对应列必须为 ❌。
  - Dify 跨 Agent 共享语义在 §6 为 ⚠️，总览对应列不得升为 ✅。
  - Mem0 作用域细项在 §6 全 ✅，总览「作用域与隔离」应为 ✅。
  - Letta 可观测性在 §6 为 ✅，总览对应列应为 ✅。
  - Cognee 记忆类型（实体-关系）为最大特色，总览「记忆类型」应为 ✅。
  - OpenViking 生命周期在 §6 为 ⚠️，总览「生命周期」应为 ⚠️。

### 2. 不改动 §5.1-§5.7、§6、§7、§8、§9、附录

**文件**：同上。
**位置**：§5.1 起以下所有原有章节。

**改什么**：不做任何行内容修改。

**为什么**：用户要求先总览后详情，不涉及重写详情节；§6 已经承担细项矩阵功能，§5.0 与 §6 是粗/细两层，需要共存。用户「精准执行规则」也要求不改动没有问题的原有内容。

**怎么做**：只在插入 §5.0 后做一次全文 diff 复核，确保变化范围严格落在 §5.0 新增段落内。

**验证**：diff 显示除新增段落外，无其他行修改。

### 3. 内容草案（可直接用于落地）

**导语与图例（草案）**：

> 本节先给出 §4.4 与 §4.5 两个能力框架对 7 款候选/对照产品的横向总览，每格采用「符号 + 一句摘要」。总览在前，§5.1-§5.7 为逐产品详情，§6 为按细项编号（4.1.x / 4.2.x / …）的完整矩阵。
>
> 图例：✅ 原生支持 / ⚠️ 需自建或部分支持 / ❌ 不支持或弱。产品列顺序：OpenViking / Mem0 / Letta / Cognee / Coze / VikingDB / Dify。

**表 A 草案：§4.4 核心能力 × 产品**

| §4.4 能力 | OpenViking | Mem0 | Letta | Cognee | Coze | VikingDB | Dify |
|---|---|---|---|---|---|---|---|
| 作用域与隔离 | ⚠️ 目录级天然分层，企业 ACL 需自建 | ✅ user_id/agent_id/run_id 三级原生 | ⚠️ 框架层 user/agent 隔离，企业 ACL 自建 | ⚠️ 自托管可控，无企业级 ACL | ⚠️ workspace + uuid + 渠道隔离，无部门/细粒度 ACL | ⚠️ 靠 sub-index/namespace 自建 | ⚠️ workspace + 知识库三档权限，无部门树 |
| 记忆写入 | ✅ 文件式写入，任意文本承载 | ✅ add API + LLM 自动抽取事实/偏好 | ✅ 显式工具调用写入 core/archival | ✅ 自动抽取实体-关系图 | ✅ 变量/DB/LTM/知识库多入口 | ✅ upsert_data 原子写入 | ✅ 知识库写入 + 会话变量赋值 |
| 记忆类型 | ✅ 事实/偏好/文档；关系弱 | ✅ 事实/偏好强；文档/关系弱 | ✅ 事实/偏好/过程/文档；关系弱 | ✅ 实体-关系图为最大特色 | ✅ 事实/偏好/文档；无关系、过程无 schema | ⚠️ 仅向量+元数据+文本，语义结构靠上层 | ✅ 事实/偏好/文档；无实体-关系 |
| 记忆召回 | ✅ 向量 + 关键词 + 元数据过滤 | ✅ 向量 + 元数据过滤 | ✅ 向量 + 全文（archival） | ✅ 向量 + 图混合 | ⚠️ 语义/全文/混合，内部混合实现未公开 | ✅ 5 索引 + 混合 + 标量过滤 | ✅ Top-K + Score Threshold + 元数据过滤 |
| 生命周期 | ⚠️ 读写完备；去重/合并/版本化自建 | ✅ add/update/delete + 内置去重合并 | ✅ 显式 update/delete + archival 自动压缩 | ⚠️ 图去重需上层维护 | ⚠️ 仅删除；无压缩/版本/衰减 | ⚠️ 仅 upsert/delete；无去重/压缩/衰减 | ⚠️ 有删除；去重/压缩靠工作流 |
| 跨 Agent 共享 | ✅ 文件系统协议天然跨语言，可 Sidecar | ✅ 跨语言 SDK + HTTP API | ✅ 多 Agent 原生 + 共享上下文 | ⚠️ SDK 接入，多 Agent 成熟度弱 | ⚠️ 记忆库绑定多智能体，平台级拼装 | ❌ 协议层无 Agent 上下文 | ⚠️「多 Agent」实为工作流，非跨 runtime |
| 治理与安全 | ⚠️ 开源不内置企业合规，需部署侧补 | ⚠️ 开源版基础；Cloud 有 SOC2 | ⚠️ 自托管可控；Cloud 有合规 | ⚠️ 自托管，无内置企业合规 | ⚠️ 传输加密+基础日志，无脱敏/合规审计 | ✅ 传输+静态加密 + 审计日志 + 数据驻留 | ⚠️ 企业版审计；社区版依部署侧 |
| 可观测性 | ⚠️ 基础访问日志，指标自建 | ✅ Platform 提供命中/延迟/成本看板 | ✅ trace 可解释记忆操作 | ⚠️ 基础 | ⚠️ 消息日志/数据分析，无命中率/污染率 | ✅ 控制台可查 QPS/命中率/P99 | ⚠️ 企业版监控；社区靠 Langfuse |

**表 B 草案：§4.5 RAG/向量底座能力 × 产品**

| §4.5 维度（子维度） | OpenViking | Mem0 | Letta | Cognee | Coze | VikingDB | Dify |
|---|---|---|---|---|---|---|---|
| 数据处理（数据类型 / 处理方式） | ⚠️ 承载任意文本，无内置解析/OCR | ⚠️ 以对话文本为主，非文档解析定位 | ⚠️ 以对话为主，文档能力弱 | ✅ 文档解析 + 实体-关系抽取 | ✅ 本地/在线/Notion/飞书 + OCR + 自定义分片 | ✅ TOS/URL/本地 + custom_balance 分片 + OCR + 多模态 Pipeline | ✅ 11 数据源 + Dify/Unstructured ETL + 分块参数 |
| 数据存储（存储方式 / 索引 / 权限与成本） | ⚠️ 文件系统语义，索引能力有限，无企业配额 | ✅ 可接 Qdrant/Pinecone/Weaviate/pgvector 多后端 | ⚠️ 依赖底层向量库，索引选项少 | ✅ 向量 + 图（Neo4j/NetworkX） | ✅ 火山知识库 300 万文档 / 1 万 QPS / 10 万切片 | ✅ 5 索引 + 4 量化 + 3 距离 + 单数据集无上限 | ✅ 11 向量库 + PG/Redis + 5 种对象存储 |
| 数据获取（检索 / 重排 / 阈值 / 检索测试） | ⚠️ 向量+关键词，无内置 rerank/评估 | ⚠️ 向量+元数据，无内置 rerank | ⚠️ 向量+全文，无内置 rerank | ⚠️ 向量+图，无标准 rerank | ✅ 语义/全文/混合 + Top-K/阈值 + 火山 rerank | ✅ 混合检索 + rerank + Pipeline 实验对比 | ✅ Top-K + Score Threshold + 元数据过滤 + Retrieval Testing |

**读法说明（草案）**：

> 表格结论为「横向对比口径」下的摘要。总览标 ⚠️ 不代表全场景不适用；具体是否满足业务约束需回到 §5.x 详情与 §6 细项矩阵。§7 选型不直接从本节推导，而是结合业务约束在 §7.1-§7.3 展开。

## Assumptions & Decisions

1. §5.0 只覆盖 §4.4 的 8 项能力 + §4.5 的 3 个维度，不逐项展开子维度；子维度作为「行标题括号提示」承载。若需要更细列，需再确认，避免与 §6 重复。
2. 图例、列顺序、判断方向均沿用 §6 现有约定，保证读者不需要在两套约定间切换。
3. 判断依据全部来自本报告已有事实（§5.1-§5.7 能力映射 + §5.5.2.B / §5.6.2.B / §5.7.2.B + §6 已回填矩阵），不引入外部新事实。
4. 「产品」列表按现有 §6 完整列出 7 款；对照参考（Coze / VikingDB / Dify）仍标注为对照，但在总览中不做特殊符号区分，避免格子拥挤。选型属性在 §7 说明。
5. §5.1-§5.7 详情节、§6 细项矩阵、§7-§9 均保持不变；本次仅新增 §5.0 一节。

## Verification Steps

1. **结构验证**：§5 章节树变为 `5.0 / 5.1 / 5.2 / … / 5.7`，其他章节无标题层级或编号变化。
2. **表结构验证**：表 A 严格为 8 行 × 7 列，表 B 严格为 3 行 × 7 列；表头列顺序与 §6 一致。
3. **每格非空验证**：每格必须包含一个 ✅/⚠️/❌ 与一段中文摘要，无「-」「TBD」占位。
4. **方向一致性验证**：对 Assumptions 5 与 Proposed Changes 1 的验证抽样清单逐一比对 §6 与 §5.0，方向必须一致；发现冲突时优先调整 §5.0 摘要而不是改动 §6。
5. **只增不改验证**：对目标文件做一次全文 diff，确认变化范围严格落在 §5.0 新增段落内，§5.1-§5.7 / §6 / §7 / §8 / §9 / 附录零行修改。
6. **可读性验证**：连续阅读 §5.0 → §5.1，语义衔接自然，§5.0 不夹带选型结论。